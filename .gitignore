# 依赖
node_modules/

# 构建产物（小程序 / Web 均为编译产物，不入库）
dist/
.vite/

# 测试与覆盖率
coverage/
*.tsbuildinfo

# 日志与系统文件
*.log
.DS_Store

# 环境变量（含 AppID 等敏感信息）
.env
.env.*
!.env.example
.proteus/

# 本地 Agent 会话状态（含 plan 文件——非项目源码）
.zcode/

# local npm auth (never commit)
.npmrc
scripts/commit-msg.txt
popover-open.png

# 本地工具缓存（scripts/check-publish-drift.mjs 的 pack/判定缓存）
.cache/
