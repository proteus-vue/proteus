# Proteus 项目记忆（PROJECT_MEMORY.md）

> **项目本地记忆文件**：记录当前进度、已落地文件、关键决策与偏差、验证状态。
> 新会话的 LLM **必须先读本文件**，再读 `LLM_IMPLEMENTATION_GUIDE.md` 与 `proteus.config.ts`。
> 每次阶段完成或产生关键决策后，由执行 LLM 更新本文件（保持与仓库一致）。

---

## 项目概览

- **名称**：Proteus（普罗透斯）—— Vue 跨端编译框架（小程序 Skyline + Web）
- **核心理念**：一份标准 Vue 源码，编译器化作 Web + 小程序两种形态（命名来源见指南"框架命名"章节）
- **技术栈**：Vue 3.4+ / Vite 5 / TypeScript 5.4+ / 微信基础库 2.29.2+（Skyline + wx.router）
- **文档**：`LLM_IMPLEMENTATION_GUIDE.md`（v2.2，6 阶段任务拆解；§0 痛点对照是设计决策依据）

## 当前进度（更新于 2026-08-28）

| 阶段 | 状态 | 说明 |
|---|---|---|
| P1 工程脚手架 | ✅ 完成 | 6 文件 + 配套脚手架，`build:web` 通过 |
| P2 编译期路由表 | ✅ 完成 | `gen-routes.ts` 生成 auto-routes / app.json / page.json，含分包 |
| P3 运行期路由 API | ✅ 完成 | 双端 adapter + Router + RouterView，15 个单测通过 |
| P4 编译转换插件 | ✅ 完成 | `vite-plugin-mp-transform.ts`：三转换 + buildStart 产物输出，四件套结构成型 |
| P5 运行时桥接 | ✅ 完成 | setDataBridge（路径合并+去重）/ pageLifecycle（createPage/createComponent）/ main.mp.ts 接入 |
| P6 调试与验证 | ✅ 完成 | golden fixtures 快照 + 反黑盒机制 + Web E2E 实测（6 用例），**全阶段完成** |
| v0.1 MVP | ✅ 完成 | Web + Skyline 双端编译、路由/分包/转场、setData 桥接、反黑盒、79 单测 + 8 e2e |
| v0.2 工程化基线 | ✅ 完成（决策 #72-#75） | @proteus-vue/compiler 独立包 + 注册表随包、@proteus-vue/cli（build/explain/rules）、create-proteus、CI + 贡献设施、changesets 发布流水线（npm 发布待启用） |
| 欠账清理 A/B 类 | ✅ 完成（决策 #87-#97） | 文档一致性 + B 类①-⑦ + 类型提示全链路①-⑤（198 测试） |
| 欠账清理 A/B 类 | ✅ 完成（决策 #87-#97） | 文档一致性 + B 类①-⑦ + 类型提示全链路①-⑤（198 测试） |
| **C 类拆包（packages.md 8 步）** | ✅ 全部完成（决策 #98-#105） | ①config 解耦 ②shared ③runtime ④router 工厂化+M1/M2 ⑤plugin-vite ⑥别名切换 ⑦模板重构 ⑧CI/文档——7 个 @proteus-vue/* 包 + 模板 npm 化（222 测试） |
| **pinia-plan M1-M6** | ✅ 全部完成（决策 #106） | Storage 抽象层 + 持久化双方案 + 四端工厂 + SSR 隔离 + DevTools + 测试/迁移（275 测试）；M7/M8 超级应用按需 |
| **pinia-plan M7-M8** | ✅ 全部完成（决策 #107） | 分片/调度器/配额/迁移/生命周期/敏感字段 + 协同引擎/快照/埋点/注册表（310 测试，8 个 @proteus-vue/* 包） |
| **底线审计整改** | ✅ 完成（决策 #108） | 分派层（AI 覆盖规则即生效）+ 反向漂移门禁 + 路由规则注册表/--trace-router + 文档同步（323 测试） |
| **路由规划 M3/M4** | ✅ 完成（决策 #109） | 三端共享转场映射 + Web/MP codegen（vue-router 生成 + app.json 平铺/合并）（329 测试） |
| **路由规划 B6/B7** | ✅ 完成（决策 #110/#111） | 守卫实例 API + tabBar/redirect + 集成快照/跨端矩阵/纯净性门禁/router:check（349 测试，企业级路径收官） |
| **双管线深度统一** | ✅ 完成（决策 #112） | gen-routes 走 scan/tree 管线 + derivePath 零样板 + meta 单一真相源（349 测试） |
| **路由零样板** | ✅ 完成（决策 #113） | <route> 块完全可选 + 集中式 meta（config router.meta 精确/目录前缀）（350 测试） |
| **示例工程完整工程化** | ✅ 完成（决策 #114） | examples 自包含（proteus.config/vite.config/tsconfig/package.json/index.html），根配置移交（350 测试） |
| **示例工程 npm 包方式** | ✅ 完成（决策 #115） | @proteus-vue/* 走 workspace 链接 dist（真实使用方式），仅 components 保留 alias（350 测试） |
| **Vue 能力兼容修复** | ✅ 完成（决策 #116） | 反黑盒警告清单（无对等/import）+ 事件内联表达式 + :class 简写（364 测试，规则 57→64） |
| **14-mp-first-semantics 批次 1-4 视觉对齐** | ✅ 批次 1-3 + 批次 4 交互层/能力矩阵完成（2026-08-30） | @proteus-vue/web 小程序语义 Web 模拟层：12 组件 + wx API 15 项 + **weui.io 官方像素级对齐**（toast/modal/actionSheet/picker/switch/slider/button + 暗黑模式 + 动画 + 禁用态）；规划 17（weui-io-alignment 方法论）+ 18（picker-swiper） |

## 已落地文件

**P1 工程脚手架**
- `package.json`（name: `proteus-vue`）/ `tsconfig.json` / `proteus.config.ts` / `vite.config.ts`
- `src/shims/mp.d.ts`（wx / Page / Component / RouteBuilder 全局类型）
- `src/platform/adapter.ts`（PlatformAdapter 接口）
- 配套：`index.html` / `src/main.ts` / `src/App.vue` / `src/main.mp.ts`（占位）

**P2 编译期路由表**
- `src/router/types.ts`（RouteRecord / RouteMeta / RouteParams / NavigateOptions）
- `scripts/gen-routes.ts`（扫描 → auto-routes.ts + app.json + page.json，含分包与硬边界校验）
- 示例页：`src/pages/index.vue`（Tab）/ `src/pages/user/index.vue` / `src/pages/user/profile.vue`（halfScreen）/ `src/subpackages/order/pages/list.vue`（分包）

**P3 运行期路由 API**
- `src/router/index.ts`（Router：push/back/replace + 守卫 + 栈深保护）
- `src/router/guards.ts` / `src/router/skyline.ts` / `src/router/builders/`（halfScreen / slideUp / 注册表）
- `src/platform/index.ts` + `mp-adapter.ts` + `web-adapter.ts`
- `src/router/RouterView.vue`
- `tests/router.test.ts`（15 用例，P6-3 提前落地）

**P5 运行时桥接**
- `src/runtime/setDataBridge.ts`（批量 + 数组/点号路径合并 + 值去重 + flushSync）
- `src/runtime/pageLifecycle.ts`（onReady/onUnload 钩子 + createPage/createComponent + 参数 decode）
- `src/runtime/renderer.ts`（可选扩展骨架，MVP 不做）
- `src/main.mp.ts`（App() + registerRouteBuilders，产物 app.js）
- `src/runtime/appSkeleton.ts`（★ 决策 #70：app.js 骨架模板，极简入口自动拼装）
- `tests/runtime.test.ts`（11 用例）

**P6 调试与验证**
- `tests/fixtures/pages/`（basic / input / rich / tab 四个 golden fixture）
- `tests/golden.test.ts` + `__snapshots__/golden.test.ts.snap`（快照锁定产物）
- `tests/e2e-web.test.ts`（Playwright Web E2E：首页 / SPA 跳转 / 前进后退 / 刷新恢复 / 分包页 / 404）
- `vitest.config.ts`（独立测试配置，避免加载 vite.config 触发 mp 构建副作用）
- package.json 新增 `preview:web` / `verify` / `test:e2e:web` 脚本

**P4 编译转换插件**（★v0.2 已移入 `packages/compiler/` monorepo 独立包，决策 #72）
- `packages/compiler/`（★ @proteus-vue/compiler 编译引擎独立包）：`src/index.ts`（compileVueSfc 入口）/ `template.ts` / `script.ts` / `style.ts` / `validate.ts`（产物自校验）/ `types.ts` / `overrides.ts`（规则覆盖）/ `explain.ts` + `trace.ts`（决策 trace）/ `README.md`（模块边界 + 构建发布）
- `packages/compiler/src/transforms/`（★ AI-native 透明定位落地，决策 #65）：编译规则注册表——`types.ts`（TransformRule AI 说明书接口）/ `template.ts`（30 规则）/ `script.ts`（12 规则）/ `style.ts`（5 规则）/ `validate.ts`（3 规则）/ `registry.ts`（list/get/format 查询 API）/ `README.md`（模块契约 + 阶段三演进）；`tests/transforms.test.ts`（10 用例：ID 唯一、字段齐全、映射表覆盖防漂移、API 行为）
- 决策 trace（★ 决策 #66）：`src/compiler/trace.ts`（收集器 + lineAt）+ `src/compiler/explain.ts`（explainTransform / formatTransformTrace）+ template.ts 的 `TAG_RULE_BY_TAG` 追踪键；`tests/explain.test.ts`（8 用例：ruleId 可解析防漂移 + 典型 SFC 触发预期规则）
- 底线三循环（★ 决策 #67）：`src/compiler/overrides.ts`（resolveOverrides 生效配置）+ `TransformRuleOverrides`（disabled/mapping/customTags）贯通三转换函数 + proteus.config.ts rules 段（demo-box 演示）+ `.transform-debug/` 转储携带 trace（决策链落盘）；`tests/overrides.test.ts`（17 用例）
- `vite-plugin-mp-transform.ts`（薄适配层：扫描页面 → 传选项 → emitFile）
- `tests/mp-transform.test.ts`（18 用例，P6-2 提前落地）
- 产物：`dist/mp-weixin/pages/*.wxml|.js|.wxss`（与 gen-routes 的 .json 共同构成四件套）

## 关键决策与文档偏差（新会话必读）

1. **框架命名 Proteus**（指南 v2.1）：神话"变形 + 先知"对应"编译转换 + 编译期为主"；配置文件更名 `proteus.config.ts`
2. **skyline.ts 弃用动态 `require`**：文档原方案 `require(modulePath)` 在 ESM + Vite 下不存在；改为静态 builders 注册表（`builders/index.ts`），类型安全可摇树
3. **平台选择用 `import.meta.env.MODE` 优先**：文档原方案 `config.platform` 判断会导致 Web 构建误用 mp adapter；现按 vite mode（web / mp-weixin）选择，回退 config.platform
4. **`routeBlocksPlugin`**（vite.config 内联）：plugin-vue v5.2.4 会把 `<route>` 自定义块提取为 `?vue&type=route` 虚拟模块且内容是 JSON，需包装为 `export default {...}` 才能构建；`<route>` 的真正消费者是编译期 gen-routes
5. **`tsconfig` 的 `types` 相对路径**：`["vite/client", "./src/shims/mp.d.ts"]` 实测 vue-tsc 通过
6. **新增 `@types/node`**：gen-routes（Node fs/path）类型检查需要
7. **RouterView 用 `import.meta.glob('../**/pages/**/*.vue')`**：必须覆盖分包页（`subpackages/.../pages/`）
8. **分包示例已启用**：`subPackages: [{ root: 'src/subpackages/order', name: 'order' }]`（文档默认是空数组）
9. **`vite.config.ts` mp 模式 `emptyOutDir: false`**：保留 gen-routes 生成的 app.json / page.json
10. **P5-2 renderer.ts 降级为可选扩展**：MVP 不做（glass-easel 运行时动态建节点能力有限，避免 Taro 3 式运行时 DOM 模拟）
11. **mp 模式独占编译管线**：mp-weixin mode 不用 plugin-vue，由 mpTransform 独占处理 .vue（Web 零转换原则的对称实现）；routeBlocksPlugin 仅 web 用
12. **buildStart 直接 emitFile**：小程序页面不在模块图中（main.mp.ts 未引用页面），transform 钩子不会触发；改为 buildStart 扫描 pages 目录逐个输出 wxml/js/wxss
13. **compiler-dom 运行时与类型不一致**：`@click.stop` 的 modifiers 运行时是 `{content}[]`（声明是 string[]）；`ExpressionNode` 是联合类型无统一 `.content` → 统一用 `exprContent()` 宽类型递归提取
14. **产物方法用对象简写**：`handleTap() {...}`（不能输出裸 `function handleTap(){}`，对象字面量内是语法错误）
15. **纯文本子节点紧凑单行**：`<text>hi</text>` 而非多行（产物可读性）
16. **编译引擎模块化（★重要基建决策）**：编译逻辑从插件中拆出为 `src/compiler/`——零 Vite 依赖、零 proteus.config 依赖、纯函数、选项全入参；`vite-plugin-mp-transform.ts` 降为薄适配层；`src/compiler/README.md` 记录模块边界 + 独立开源提取路径（后期移动至 monorepo packages/compiler 发布 `@proteus-vue/compiler`，附赠 CLI）
17. **反编译黑盒机制（★规避 uni-app 核心痛点）**：产物自校验（`validate.ts`：js 语法 + wxml 标签配对，坏产物抛 `CompilerError` 指明文件，绝不静默输出）；`PROTEUS_DEBUG=1` 时 WXML 注入 `<!-- @行号 标签 -->` 注释 + 输出 `.transform-debug/<file>.json` 中间产物；buildEnd 打印 warnings 汇总摘要；默认产物无调试注释；mp 构建关闭压缩
18. **路径合并支持数组下标**：`isChildPath` 用 `parent + '.'` 与 `parent + '['` 双前缀判断（`list[0].name` 是 `list` 的后代）
19. **路由参数 decode 策略**：仅对 `{`/`[` 开头的结构化值做 JSON.parse；普通标量保持字符串（对齐 P3 契约 `options.id === '1'`）——runtime createPage 与编译产物默认 onLoad 行为一致
20. **RouterView 两处缺陷修复（Web E2E 实测发现）**：空路由归一化（后退到 `/` 时 web-adapter emit 空路由 → 回退首页防 404）；routeMap 以 name 为键，RouterView 收到的是 path → 增加按 path 回退查找（同 guards.getCurrentFrom）
21. **vitest 独立配置**：vitest 默认加载 vite.config.ts（mode=test 回退到 mp-weixin 平台）会触发 mpTransform 构建副作用；新增 `vitest.config.ts` 隔离，e2e 由 `test:e2e:web` 单独运行
22. **方法内 ref 写入重写为 setData**：`name.value++`/`--`/`name.value = expr` → `this.setData(...)`，`name.value` 读取 → `this.data.name`；作用于方法体与生命周期体；复合赋值（`+=`）降级为 `this.data.name` 读写（不触发 setData）。demo 页 tap 按钮据此改为可见计数（用户实测反馈驱动）
23. **示例项目独立化（★架构决策）**：示例应用（pages/subpackages/App/main 入口/RouterView）移入 `examples/`，`src/` 仅留框架本体（compiler/platform/router/runtime/shims）；gen-routes 与 mp 插件从 `pagesDir` 推导应用根目录（dirname）；`auto-routes.ts` 生成位置保持在 `src/router/`——因框架单例 router（index.ts/guards.ts）直接 import 它，后续若改工厂化 API（createRouter(routes)）再随应用移动；`RouterView` 是应用壳（glob 需指向应用页面目录），随示例存放；新增 `@proteus-vue/*` 别名指向 src，示例代码以此访问框架（与未来 npm 包导入路径一致）
24. **页面导航链接（<a href> / <router-link>）**：模板转换 → `view bindtap="__navigateTo" + data-url`（route-type 支持），script 转换按 `usesNavigate` 标志注入 `__navigateTo`（wx.navigateTo，仅 MP 产物；Web 端 <a href> 走浏览器导航）；⚠ 编译生成的正则转义在字符串传递中丢失（`/^\//` 变 `/^//`）——产物自校验当场捕获，改用字符类 `/^[/]/` 规避；跳转 Tab 页需 switchTab，导航链接仅指向非 Tab 页（注释已标注）
25. **Web 端 SPA 导航**：web-adapter 全局拦截站内 `<a>` 点击（`preventDefault` + `pushState` + emit），外部链接（非 `/` 开头）/ `target=_blank` / 修饰键点击（新标签页）不拦截；E2E 用 window 标记验证点击后无整页刷新
26. **tabBar ≥2 项（真机校验修复）**：微信要求 tabBar.list 至少 2 项，demo 原只有首页 1 个 Tab → 开发者工具报错；新增 `examples/pages/mine.vue`（我的）作为第二个 Tab；gen-routes 加守卫：tabRoutes < 2 时告警并忽略 tabBar（不再产出非法 app.json）
27. **Skyline lazyCodeLoading（真机校验修复）**：页面 renderer=skyline 时微信要求 app.json 声明 `lazyCodeLoading: requiredComponents`；gen-routes 按 config.skyline 开关自动补齐
28. **真机校验两处无效配置修复**：① `app.json window.renderer` 被判无效——移除，Skyline 改由页面级 `renderer: skyline` 声明（指南 v1.0 臆想的字段被真机否掉）；② `page.json customRouteKeyName` 被判无效——移除，自定义路由仅靠 `wx.navigateTo({ routeType })` + 注册的 builder 生效；demo 个人资料链接加 `route-type="halfScreen"`（handler 带 fail 降级到普通跳转）
29. **小程序导航无效修复（__ 前缀方法名）**：微信保留 `_` 前缀（框架内部字段如 `__route__`），`bindtap="__navigateTo"` 绑定可能失效 → 自动 handler 改名 `proteusNavigateTo` / `proteusOnXxxInput`（避开下划线）；`PROTEUS_DEBUG=1` 时输出 `[proteus] navigate dataset/url` 调试日志（编译选项 `debug` 透传）；demo 链接 `/pages/user` → `/pages/user/index`（微信页面路径必须完整匹配 app.json pages 声明，不能省略 index）
30. **小程序导航最终根因：微信 navigateTo 相对路径解析（★真机调试经验）**：日志 `navigateTo:fail page "pages/pages/user/index" is not found`——微信 navigateTo **不带前导 `/` 的 url 按相对当前页目录解析**（当前页 pages/index → 前缀 pages/ + url），导致双重前缀；修复：handler **保留前导 `/`**（`/pages/user/index` 为绝对路径）；调试日志无条件输出为临时手段，验证通过后需回收门控（PROTEUS_DEBUG）
31. **Skyline 转场不生效（worklet 必须 function 关键字，★真机调试经验）**：真机日志 `routeType(halfScreen) handlePrimaryAnimation should be a worklet function`——注册成功、导航成功，但微信 **不识别箭头函数为 worklet**；修复：builder 的 worklet 方法（handlePrimaryAnimation/handleSecondaryAnimation）改用 `function () { 'worklet' ... }`；待验证后回收 registerRouteBuilders 的临时调试日志；demo 首页已移除不存在的 /logo.png（控制台 500）
32. **微信 ES5 转译 babel helper 报错（★真机调试经验）**：`module '@babel/runtime/helpers/arrayWithHoles.js' is not defined`——微信开发者工具将页面 JS 转 ES5 时，数组解构（`for (const [k,v] of Object.entries())`）需要 `_slicedToArray`→`_arrayWithHoles` helper，但 helper 模块不在小程序包内；同类风险：对象展开 `{...}`、对象解构 `const {a} = ctx`。修复：生成代码全部改为无需 helper 的写法（索引循环 Object.keys、直接属性赋值、ctx.primaryAnimation）；golden 快照已更新
33. **微信 worklet 静态分析（★真机调试经验）**：`addRouteBuilder` 必须直接传函数引用（`wx.router.addRouteBuilder('halfScreen', halfScreenBuilder)`），不能通过注册表动态取值（`builtinBuilders[name]`）——微信编译期静态分析无法穿透动态查值，报 should be a worklet function；builder 模块直接用 import 引用
34. **全链路调试机制（★反"猜问题"）**：`npm run debug:mp`（PROTEUS_DEBUG=1）一键构建，统一 `[proteus][环节]` 日志 + Date.now 时间戳：App 启动 / builder 注册 / 页面 onLoad(参数)/onReady / 导航 tap/navigateTo 成功失败 / wx.onError 全局错误；页面日志由编译器注入（script.ts debug 门控），app 层走 `runtime/debug.ts` trace；vite define 注入 `__PROTEUS_DEBUG__`（mp.d.ts 声明），正式构建 rollup 常量折叠把调试代码连字符串全部摇掉（grep 验证零残留）；此前多轮真机排查靠手动加日志逐个猜，此机制一次构建看全链路
35. **Skyline 自定义路由真机排查终局（★长线经验，最终定案环境问题）**：经历 8+ 轮真机调试：① worklet 必须局部具名 const 箭头函数+简写返回（内联对象方法报 should be a worklet function）② screenHeight 渲染线程全局逻辑层引用 ReferenceError → wx.getWindowInfo 逻辑层取值 + 闭包捕获 px（真机验证闭包捕获生效）③ rollup 打包的 app.js 中 worklet 响应式重执行失效（t 卡 0）→ **app.js 改插件直出纯文本**（esbuild 转译 + charset utf8 + 还原单引号 worklet + __PROTEUS_DEBUG__ 替换 + mp-entry-stub 占位）④ 移除页面透明配置后**微信官方预设 wx://bottom-sheet 仍报 `applyAnimatedStyle can not find corresponding nodes`（UI 线程）** → 定案：devtools Skyline 模拟器 + 灰度基础库 3.17.2 环境问题，非框架代码；建议：切稳定基础库 + 真机预览验证；半屏视觉列为待真机确认项（框架侧代码已按官方形态验证完备）
36. **真机预览不支持 ?? 运算符（★真机经验）**：真机预览报 `invalid file: pages/index.js, 46:42 SyntaxError: Unexpected token ?`——`??`（ES2020）在真机/预览管线直接语法错误（开发者工具模拟器可容忍）；编译产物（ref 重写）统一改为显式 null 检查三元；MP 产物需保持 ES5 安全：无 `??` / 无 `?.` / 无数组解构 / 无对象展开（已全量 grep 扫描验证）
37. **自定义路由根因确诊：不能从 tabBar 页面发起（★长线真机排查收官）**：8+ 轮排查终局——半屏/自定义路由从 tab 页发起报 `applyAnimatedStyle can not find corresponding nodes`（UI 线程，官方预设 wx://bottom-sheet 同样失败）；**从非 tab 页发起即正常**（真机对照确诊）。期间沉淀：worklet 局部具名 const 箭头函数+简写返回 / screenHeight 渲染线程全局→wx.getWindowInfo+闭包捕获 / rollup 打包对 worklet 不友好→app.js 插件直出纯文本 / glass-easel 为 Skyline 强制字段（真机校验）/ `??` 真机不支持；框架代码始终正确，demo 测试链接此前全部在 tab 页（首页）导致全失败；已归档平台硬边界：**自定义路由跳转必须从非 tab 页发起**
38. **halfScreen 半屏视觉修复（canTransitionFrom: false）**：真机确认半屏打开后前页被挤开一部分 + 右侧黑屏——根因：builder 未声明 `canTransitionFrom: false`，微信默认给前页套压出动画（往左推开露出黑底）；修复后前页保持不动（与官方预设一致）；圆角改为仅上角（16px 16px 0 0，弹窗样式）；`RouteBuilderResult` 按官方 CustomRouteConfig 补全 14 字段（opaque/maintainState/transitionDuration/barrier*/canTransition*/handle*Animation/snapshot/fullscreenDrag/popGestureDirection）
39. **自定义路由 builder 开放为框架能力（★架构决策）**：builder（动画曲线/遮罩/联动/时长）由**应用开发者在 main.mp.ts 编写注册**（该文件即应用入口，插件直出 app.js）；平台约束：必须与 addRouteBuilder 同文件静态可分析（官方形态 const fn + 直接引用）；已删除框架内配置驱动注册表（src/router/builders/ + registerRouteBuilders——真机验证不可行，保留会误导开发者）；指南 P3-3/P3-4 重写为开放能力文档（14 个可自定义项列表）
40. **综合能力演示 builder（scaleDown 缩放转场）**：`examples/main.mp.ts` 新增 scaleDownBuilder + `examples/pages/showcase.vue` 演示页，一次覆盖全部开放能力：Easing 进出曲线（cubicBezier + `wx.worklet.derived`，按 primaryAnimationStatus 切换进入缓出/退出缓入）、遮罩 barrierColor、前后页联动（handleSecondaryAnimation 官方 Step-3 下沉缩放，px 由逻辑层 wx.getWindowInfo 计算闭包捕获）、时长 transitionDuration/reverseTransitionDuration、手势 fullscreenDrag + popGestureDirection vertical；`RouteContext` 按官方 CustomRouteContext 补全 primaryAnimationStatus/secondaryAnimationStatus/userGestureInProgress/startUserGesture/stopUserGesture/didPop（真机待验证）
41. **转场能力验证终局（★长线）**：B 页动画/曲线/遮罩(0.8)/时长(400/360ms)/手势全部真机通过；**前后页联动**：① handlePreviousPageAnimation（≥3.0.0）生效但**掉帧且不跟踪手势**（慢慢拉看不到层叠）② 经典架构（A 页自带 builder 的 handleSecondaryAnimation）**完全不生效**（A 永远默认压出）③ 分包页不能作 A 页；最终采用 handlePreviousPageAnimation 作演示；**掉帧规避经验**：borderRadius 逐帧动画不可合成→静态化、Easing/derived 链提取不稳定→worklet 内联多项式（easeOutCubic/easeInCubic）、整页 scale 昂贵→避免、**物理引擎**（wx.worklet 的 spring/timing）用于手势驱动场景而非自定义路由进度（微信自驱 transitionDuration）
42. **内置预设 builders（★架构决策）**：真机验证过的 halfScreen/slideUp/scaleDown 沉淀为 `src/router/presets/*.ts`（带全部真机经验注释）；开发者在 `proteus.config.ts` 的 `customRoute.builders` 声明即可——**vite-plugin-mp-transform 直出 app.js 时读取预设源码（esbuild 转译）+ 内联函数定义 + 生成注册块**（`if (wx.router) addRouteBuilder(name, fn)`，模块顶层执行），绕开"同文件静态可分析"平台约束，开发者零手写；main.mp.ts 精简为纯入口（自写 builder 仍支持，见指南 P3-4 ②）；`extractBuilderFnName`/`assembleAppJs` 纯函数独立可测
43. **手写覆盖内置预设（filterOverriddenPresets）**：开发者可在 main.mp.ts 同名 `addRouteBuilder('<name>', fn)` 覆盖内置预设——插件检测 main 源码中已注册的同名预设并**跳过自动注册**（开发者优先）；demo 演示覆盖 halfScreen（改遮罩 0.6/高度 92%）；优先级：main 手写 > 配置预设
44. **Web 端平台边界：自定义路由转场为 Skyline 专属（★架构澄清）**：routeType/worklet/barrier/路由容器均无 Web 对等机制——`router.push({ routeType })` 在 Web 由 `isSkyline()=false` 优雅忽略、导航一致；按"Web 原生"原则，Web 转场用 Vue `<Transition>`（Web 一等公民），API 层统一（routeType 双端同 API）、视觉机制各平台原生；已文档化进平台硬边界
45. **Web 原生转场实现（routeType → Vue Transition，★架构落地）**：同一套 routeType 双端生效：链路 router.push 透传 routeType → web-adapter（navigateTo 携带 + `<a route-type>` 点击拦截读属性）→ RouterView `:name` 动态 Transition（out-in + :key 按路由重挂载）；映射：halfScreen/slideUp→slide-up（上滑 cubic-bezier 0.35,0.91,0.33,0.97）、scaleDown→scale（缩放+淡入）、默认 fade；mp-adapter.navigateTo 不再转发 routeType（自定义路由跳转专属 skyline.ts 的 navigateWithCustomRoute）
46. **Web 层叠缩放（scaleDown 同屏层叠）**：CSS 层叠关键 = Transition `default` 模式（新旧同屏）+ 绝对定位重叠（.router-view 相对定位 + .page absolute inset 0 + overflow-y auto）；scaleDown：新页 translateY(100%)→0 滑入覆盖（z:2）、旧页 scale(0.92)+translateY(4%)+圆角+opacity 0.8 下沉（z:1）；`:mode="isLayered ? undefined : 'out-in'"` 动态切换；其余转场保持 out-in 避免布局堆叠
47. **Web 页面纸片化（层叠转场基础）**：demo 页面根元素透明 → 层叠时只见内容不见页面；RouterView `.page` 基础样式 `background: #fff + min-height: 100vh`（全局样式 + class 透传到页面根）——每页为独立白色纸片，层叠缩放才有"两页叠动"的层次感
48. **Web 转场全面层叠化 + 无障碍（★打磨）**：所有 routeType 转场层叠（Transition default 模式 + .router-view.layered .page 绝对定位重叠）：slideUp→新页 translateY(100%)推入(z:2)+旧页 translateY(-20%)淡出(z:1，对应 MP secondaryAnimation)；halfScreen→新页滑入(z:2)+旧页保持原位淡出（对应 MP 半屏前页保持）；scale→旧页下沉缩放；fade（普通导航/回退）out-in；`prefers-reduced-motion: reduce` 关闭全部转场；⚠ 教训：替换 CSS 块时 .router-view 基础样式被误删，需自检完整性（加 node 断言检查）
49. **Web 遮罩层对齐 MP barrierColor（★打磨）**：层叠转场新增 `.route-barrier`（absolute inset 0 + z-index 1 + `--barrier-opacity` 变量 + barrier-in 淡入动画 0.3s + pointer-events none）——halfScreen 0.4 / scaleDown 0.8 与 MP 预设 rgba(0,0,0,0.4/0.8) 对齐；旧页 leave z-index 1→0 使遮罩位于旧页之上、新页(z:2)之下；slideUp（opaque）无遮罩
50. **修复遮罩残留（★真机/浏览器调试）**：转场结束后新页 enter-active(z:2) 类被移除恢复 auto z-index，而遮罩(z:1) 常驻 → 遮罩盖在停留页上（"切换过去遮罩停留在当前页"）；v2.35 用 Transition 事件（after-enter 移除）修复但引入新问题：before-enter 在新元素入场才触发，遮罩重置不可靠（"进入又没遮罩"）
51. **遮罩最终方案：纯 CSS z-index（★打磨终局）**：`.page` 基类恒 `z-index: 2`（停留页永远盖住遮罩 z:1）；旧页仅 leave-active 时降 z:0 暴露在遮罩下；遮罩常驻挂载（showBarrier 按 routeType），转场期间天然可见、结束后被停留页盖住——**零事件时序依赖**，最稳；halfScreen 0.4 / scaleDown 0.8
52. **Web 反向转场（返回动画，★打磨）**：adapter 用 `history.state.proteusIndex` 记录栈深，popstate 时比较判断 back/forward（外部跳入无索引视为 forward）；RouterView 记录 `lastForwardName`（当前页进入时的转场名），后退时用 `${lastForwardName}-back` 反向转场：scale-back（B 滑出底部 z:2、A 从 scale(0.92) 恢复 z:1）、slide-up-back（B 滑出、A 从 translateY(-20%) 恢复）、halfscreen-back（B 滑出、A 轻微 8% 恢复）；replace（redirectTo/reLaunch/switchTab）视为前进；prefers-reduced-motion 覆盖全部 -back 类
53. **Web 反向转场补遮罩（back 分层，★打磨）**：back 转场 A 页 enter z-index 1→0（低于遮罩 z:1），遮罩在 back 时由 barrier-in 切换为 **barrier-out 淡出**（from var(--barrier-opacity) → 0）——前页恢复过程中被压暗、B 滑出时分层清晰，对应 MP reverse 时遮罩随弹窗退出消失；isBack = transitionName endsWith('-back')；reduced-motion 下 barrier-out 直接 opacity 0
54. **首次转场无动画（异步组件预热，★浏览器调试）**：现象——第一次进入/退出无下沉转场，第二次正常；根因：首次导航目标页为 defineAsyncComponent（chunk 加载中），Vue Transition 在异步首次挂载时跳过动画，第二次 chunk 缓存同步挂载才正常；修复：RouterView 启动时预热全部页面（import.meta.glob 逐个 load）+ `pageCache` Map 缓存已解析组件，view computed 优先返回缓存（同步挂载保证转场），未预热完才回退 defineAsyncComponent
55. **刷新后首次后退无动画（historyIndex 恢复，★浏览器调试）**：现象——刷新后在转场页首次后退无反向动画，第二次正常；根因：web-adapter 的 historyIndex 初始化为 0，但刷新后浏览器 history 保留旧条目（state.proteusIndex 仍存在）→ 首次后退 stateIndex < historyIndex(0) 不成立 → 误判 forward → 走 fade；修复：初始化 `historyIndex = history.state?.proteusIndex ?? 0`（从当前条目恢复栈深）；redirectTo/reLaunch/switchTab 的 replaceState 也携带 proteusIndex 保持栈深一致（否则 replace 后索引丢失）
56. **导航类型全转场（replace/reset/tab，★打磨）**：adapter onPageLoad 的 direction 扩展为导航类型 `'forward' | 'back' | 'replace' | 'reLaunch' | 'switchTab'`（web-adapter 各方法分别 emit）；RouterView 按类型选转场：forward+routeType→base 层叠、back→`-back` 反向、redirectTo→'replace'（旧页 scale(0.98) 淡出 + 新页淡入，out-in）、reLaunch→'reset'（淡入）、switchTab→'tab'（淡入淡出）；isLayered 从 `!== 'fade'` 改为显式名单（layeredNames）；replace/reLaunch/switchTab 置 lastForwardName='fade'（无堆叠语义）；reduced-motion 覆盖全部新类
57. **样式标签选择器映射（★bug 修复）**：现象——小程序编译后 `<h1>/<p>/<a>` 样式丢失；根因——template 已把 h1/p/a → text/view 映射，但 WXSS 里的 `h1 {}` / `.links a {}` 选择器未重写，匹配不到元素；修复——新建 `src/compiler/tags.ts` 抽出共享 `TAG_MAP`+`EVENT_MAP`（template.ts 与 style.ts 共用），style.ts 的 `transformStyleToWxss` 增加选择器标签重写：仅重写每条规则选择器部分，命中条件=标签名位于选择器起始或组合器之后（空格 / `> + ~ , (` 之后，前面不能是 `\w - . # : [ *`），类名 `.a`/`#input`/`tag-a`/`data-input` 不误伤；属性选择器 `[..]` 先掩码后还原（引号内容不误伤），占位符 `\u0000` 加入 lookahead（`p[data-x]` 不漏）；`@media`/`@keyframes` 骨架原样保留；纯函数签名不变，Web 端不受影响（Vite 原生处理）；产物验证 `.links a → .links view`、`h1 → text` 对齐 wxml
58. **标签语义基础样式注入（★h1/p/a 视觉还原）**：现象——小程序端 h1 映射为 text 后是普通小字号，而 Web 有浏览器 UA 默认样式（大标题/加粗/链接色），两端视觉不一致；根因——h1-h6/p/a 全映射为 text/view 后**无默认样式可注入**（text 标签选择器会命中所有 text 互相冲突，必须用类区分）；修复——`tags.ts` 新增 `SEMANTIC_CLASS`（h1-h6/p/a → proteus-h1~h6/proteus-p/proteus-a），template.ts 映射时自动附加基础类（与静态 class 合并前缀、与 `:class` 插值拼接 `class="proteus-p {{expr}}"`，rich-text/v-html 不附加），style.ts 注入 `BASE_SEMANTIC_WXSS`（对齐 Web UA：h1 64rpx/700 等，rpx 直接书写不过 px2rpx，注入在用户样式之前）；用户样式（`.home h1 → .home text` (0,2,0)）特异性高于基础类 (0,1,0) 正常覆盖；产物验证 `<text class="proteus-h1">` + `.proteus-h1 { font-size: 64rpx; font-weight: 700 }`
59. **基础样式 margin 改单边 em（★p 换行高度对齐 Web 标准）**：现象——小程序段落间距明显小于 Web（.proteus-p 原 margin 8rpx=4px，Web UA 是 1em=16px，差 4 倍）；标准——HTML 附录 D：p 为 display:block + margin-block 1em + line-height normal，h1-h6 为 0.67/0.83/1/1.33/1.67/2.33em（相对自身字号），Web 相邻段落 margin 折叠取 max；关键约束——Skyline 是自研引擎类 flex 布局**不折叠 margin**（WebView 模式才折叠），双边 em 在 Skyline 下间距翻倍（2em）仍偏离 Web；修复——margin 全部改为**单边 bottom + em 相对自身字号**：Skyline 不折叠与 Web 折叠在主流组合（段落连续 p→p、标题后接段落 h1→p）下视觉间距恰好一致（p→p 均 1em、h1→p 均 0.67emₕ₁=21.4px）；p 不设 font-size（继承与 Web 一致），h1-h6 font-size 保持 rpx 固定（对齐 16px 基准的 2em/1.5em/…）；已知微差——p→h1（段落后接标题）16px vs Web 折叠 21.4px，可接受
60. **多行字面量提取（★showcase v-for 内容丢失修复）**：现象——showcase 页小程序端 for 循环内容完全不渲染；根因——`extractData` 的 const 正则 `(.*)$` 无 dotAll 不跨行，`const cards = ref([\n {...},\n])` 只抓到首行 `ref([` → evalLiteral 失败 → data.cards=undefined → wx:for 遍历 undefined 无内容（首页 items 单行数组不受影响）；修复——`extractInitializer` 括号平衡扫描（追踪 ()[]{} 深度 + 跳过字符串/块注释/行注释，字符串内括号如 `rgba(0,0,0,0.8)` 不干扰深度，深度归零遇 ;/行尾结束）提取多行初始值；连带修复——① 只提取**零缩进行首**顶层 const（函数体/生命周期体/块内局部 const 不再误提取为 data，`trim()` 会吃掉缩进空格导致判断失效，须用严格空串判断）② ref 内层正则加 `s` 标志 + 可选尾分号（`ref(0);` 也支持）；验证——showcase.js data.cards 完整输出 5 项对象数组，77/77 单测
61. **语义标签选择器重写为类选择器（★卡片 h3 被染灰修复）**：现象——showcase 卡片 h3 文字小程序端灰色、Web 端黑色；根因——`h3` 与 `p` 都映射为 text，`.card h3` 与 `.card p` 重写后都是 `.card text`（同特异性 (0,2,0)），后写的 `.card p`（含 color:#666）覆盖前一条 → h3 被染灰；`div/a → view` 同隐患；修复——选择器重写时**语义标签（h1-h6/p/a）映射为基础类选择器**（`.card h3 → .card .proteus-h3`、`.links a → .links .proteus-a`），模板侧已自动附加 proteus-* 类故精确匹配，多对一映射不再撞选择器；非语义标签仍映射为标签（div → view、img → image、input → input…）；验证——showcase.wxss 两条规则独立（.card .proteus-h3 无 color、.card .proteus-p color:#666），79/79 单测
62. **文档体系落地（★README + 中文文档）**：根 `README.md`（门面：命名来源/痛点对照/核心特性/快速开始/目录/测试/路线图/已知限制）+ `docs/` 四篇中文文档——getting-started（环境/命令/首个页面/开发者工具导入/全链路调试/常见问题）、configuration（proteus.config.ts 全量字段）、compiler（编译管线/TAG_MAP/EVENT_MAP/指令映射/语义基础样式/选择器重写/反黑盒/MVP 限制/独立开源计划）、routing（路由生成/Router API/守卫/routeType 转场双端对照/平台硬边界）；文档内容全部基于真实代码与已归档决策（测试数 79、预设 builders、平台硬边界等）；后续文档更新随代码变更同步（版本号 62 起与决策同步）
63. **开源协议 + 对标大厂路线图（★收尾）**：① LICENSE 选 **Apache-2.0**——宽松可商用（与 MIT 同等核心自由）+ 专利授权条款（对采用者/贡献者都友好），适合被嵌入商业项目的基建类框架；LICENSE 文件含版权行（Copyright 2026 Proteus Contributors）+ 官方全文，package.json 加 `license: "Apache-2.0"`，README 加协议章节；② docs/roadmap.md 对标 uni-app/Taro 3 的完整规划：九域能力矩阵（开发体验/编译能力/路由/状态管理/组件生态/原生能力/工程化/性能/多端覆盖，✅现状🟡部分❌缺失）+ 版本里程碑（v0.1 MVP✅ → v0.2 工程化基线=独立包@proteus-vue/compiler+CLI+create-proteus+CI → v0.3 编译能力补全=computed/watch/组件系统/scoped CSS/sourcemap → v0.4 运行时性能=setData 深度/虚拟列表/Pinia/性能基准 → v0.5 多端=支付宝/抖音/鸿蒙 → v1.0 生产可用 → v2.0 生态）+ monorepo 拆分（packages/compiler·runtime·router·plugin-vite·cli·create-proteus·shared，单向依赖）+ 量化性能目标（主包≤1.2MB/setData≤60次每秒/冷启动≤1.5s/万条虚拟列表 60fps）+ 非目标（SSR/App 原生/初期组件库）+ 每里程碑验收清单
64. **git 仓库关联（★首次提交推送）**：仓库 https://github.com/proteus-vue/proteus（组织 proteus-vue）；`git init -b main` + `remote add origin`；新增 `.gitignore`（node_modules/dist/.vite/覆盖率/日志/.env——AppID 敏感信息不入库，git check-ignore 验证生效）；package.json 补 repository/homepage/bugs 字段；getting-started clone 命令改真实地址；待提交 18 项全为源码与文档（node_modules/dist 已排除）；首次提交后按 docs/roadmap.md v0.2 推进（@proteus-vue/compiler 独立包优先）
65. **项目定位升级：AI-native 透明跨端编译框架（★宪章级决策）**：定位从"跨端框架"升级为 "AI-native 透明"——差异点不再是"跨端"（uni-app/Taro 都在做），而是**编译器对 AI 代理与人类开发者完全透明**。落地：新增 `src/compiler/transforms/` 编译规则注册表——把散落在 template/script/style/validate 的所有转换规则抽为自描述规则（49 条），每条 = 一份 AI 说明书（id/phase/title/what/why/when/example/verify/status/source/决策号）；API：`listTransformRules(phase?)` / `getTransformRule(id)` / `formatTransformRule(rule)` / `formatTransformCatalog()`（经 compiler/index.ts 导出）；防漂移：规则 `mapping` 与 tags.ts 常量（TAG_MAP/EVENT_MAP/SEMANTIC_CLASS）同源引用，`tests/transforms.test.ts`（10 用例）校验键全覆盖 + 值一致 + ID 唯一；README/GUIDE §0.5/compiler.md/roadmap.md 同步定位；阶段二（随 @proteus-vue/compiler 独立包）：规则增加 apply() → 注册表升级为分派层 → explainTransform(source) 决策 trace（详见 src/compiler/transforms/README.md）；本阶段注册表只描述不执行，不动摇 79 单测锁定的编译管线；测试 79 → 89
66. **决策 trace 落地（★阶段二提前实现）**：explainTransform(source) 输出一份 Vue SFC 实际触发的全部转换规则（决策链路）——透明定位从"静态注册表"升级为"转换过程可观察"。落地：`src/compiler/trace.ts`（TransformTrace 收集器 + lineAt）注入 template/script/style 三个转换函数（`options.trace` 可选，默认零开销，产物与既有单测行为完全一致）；`src/compiler/explain.ts`（explainTransform + formatTransformTrace 按阶段分组渲染）；template 侧 trace 键引用 `TAG_RULE_BY_TAG`（由规则 mapping 反推，只取 tag/ 前缀规则——semantic/base-class 与 event/click-to-tap 的 mapping 键是另一维度，混入会污染标签追踪）；`tests/explain.test.ts`（8 用例：所有 trace ruleId 可解析防漂移 + 典型 SFC 触发预期规则 + v-model 链路成对 + Component/Page 模式 + 渲染）；示例：真实 index.vue 输出 27 template + 10 script + 3 style 决策；README/compiler.md/roadmap v0.3 同步；阶段三：规则 apply() 分派层（随 @proteus-vue/compiler 独立包）；测试 89 → 97
67. **底线三循环落地（★框架立身之本，宪章级）**：框架的底线 = 三条闭环真实可跑——① AI 能力涨 → AI 写更复杂的 transform → 框架获得新能力；② 编译出问题 → AI 看 dist/ → 定位到哪条 transform → 改规则 → 重编译；③ 用户需求变 → proteus.config.ts 开关 → 行为即时变化。落地：`src/compiler/overrides.ts`（resolveOverrides：TAG_MAP/EVENT_MAP/SEMANTIC_CLASS 常量 + 覆盖补丁合并 + 禁用集；未知规则 ID 编译期警告防笔误）；`TransformRuleOverrides`（disabled / mapping / customTags）加入 CompileOptions + 三转换函数全部改走生效配置（template 的 tagMap/eventMap/semanticClass/disabled 进 SerializeContext；style 的选择器重写正则按生效映射生成；script 的 data/methods/lifecycle/ref/自动 handler/onload 均支持禁用）；`proteus.config.ts` 新增 rules 段（演示 customTags: demo-box → view，showcase.vue 已加 <demo-box> 演示元素）；循环 ② 闭环：`.transform-debug/<file>.json` 转储新增 trace 字段（compileVueSfc 三阶段 trace 合并入 CompileResult.trace），debug 构建 dist 产物 + 决策链一处定位；`tests/overrides.test.ts`（17 用例：customTags/mapping/disabled 各阶段 + trace 反映生效行为 + 未知 ID 警告）；测试 97 → 114
68. **路线图新增 v0.6：App 原生（Vue 自定义渲染器）+ Vapor 兼容（★标准 Vue 原则的长期红利）**：两条新主线入 roadmap——① **App 原生端**：MVP 原"非目标"改为规划 v0.6，用 `@vue/runtime-core` 的 `createRenderer` 定义原生 host config（view→原生视图/text→原生文本/事件桥接），标准 Vue SFC 直接运行；架构要点：App 端 = **运行时渲染通道**（Vue 官方渲染器，非自研 diff），与 Web/MP 编译期通道并列，两条通道共享源码/Router API/规则注册表心智，"编译期为主"原则只约束 Web/MP（App 端属平台适配层扩展，不构成原则妥协）；落地位置 `packages/renderer-app/`（@proteus-vue/renderer-app）+ router app adapter + 原生能力桥；② **Vapor 兼容**：v0.4 研究 Vapor reset/effect 命令式 codegen 借鉴进 setData（路径合并→依赖追踪精确化，前置研究）；v0.6 Web 端 Vapor 模式（@vue/vapor 跑 Web，无虚拟 DOM 更小包体）+ MP 编译管线验证特性子集兼容（**同一份源码双模式可编译**）；Vapor 与 Proteus 哲学同构（都拒绝虚拟 DOM）互为镜像；能力矩阵 #9 多端覆盖、里程碑总览、非目标（App/Vapor 改"MVP 非目标、规划 v0.6"）、monorepo 结构（renderer-app 包）、验收清单（v0.6 行）同步；roadmap v2.48 → v2.50；GUIDE §0.4 / README 规划行同步
69. **Skyline iOS 真机白屏 bug 入路线图（★平台可靠性关注）**：微信平台已知问题（iOS 真机偶发白屏，与基础库版本/特定能力相关，非框架代码——决策 #35 已定案过类似环境问题）。框架对策三层入 roadmap v0.5：① **能力兼容清单**——编译期警告扩充（float/fixed 已有先例），白屏高发场景（动效/worklet/特定组件）编译期预警；② **页面级降级通道**——renderer 可配置 WebView 兜底（仅白屏风险页启用，不全局降级）；③ **v1.0 真机验收**必须覆盖 iOS Skyline 白屏复现与降级切换（v1.0 目标 + 验收清单均加此条）；README 已知限制同步（含 roadmap 链接）；roadmap v2.50 → v2.51
70. **多入口优化：main.mp.ts 极简模式（★开发体验）**：原多入口设计要求开发者手写重复样板（App() 包装 / onLaunch 调试日志 / wx.onError 错误捕获 / 预设注册），每应用一份。优化：**极简模式**——`assembleAppJs` 检测入口不含 `App(` 时，自动拼装 `src/runtime/appSkeleton.ts` 骨架（App 包装 + onLaunch 调试日志 + 全局错误捕获 + 内置预设注册），开发者只写自定义 builder（覆盖/新增预设）；**全量模式**（入口含 App()）向后兼容（尊重开发者完整自定义 app 生命周期）；examples/main.mp.ts 从 54 行降到 38 行（仅保留自定义 myHalfScreenVariant + 注册）；插件测试 +3（极简骨架生成 / 纯骨架 / 无自定义有预设）；产物验证：开发者覆盖优先（halfScreen 不重复注册）+ 预设定义在前注册在后（静态可分析）+ __PROTEUS_DEBUG__ 正式/调试双模式替换正确；文档同步（configuration/routing/GUIDE P3-4/README）；测试 114 → 117
71. **examples 丰富：按能力矩阵铺开（★活文档）**：新增 `examples/pages/forms.vue`（表单与指令能力演示：v-model input/textarea → value+bindinput、:class 对象语法 → 三元拼接、:style 对象语法 → prop:{{expr}}、v-if-v-else-if-v-else → wx:if/wx:elif/wx:else、@click.stop → catchtap、v-html → rich-text nodes）与 `examples/pages/config-demo.vue`（proteus.config rules 演示页：customTags 已启用 demo-box→view，页面内含 disabled/mapping 试玩说明）；config 的 customTags 从"注释演示"改为"启用演示"（产物直观可见），showcase.vue 的 demo-box 元素挪至 config-demo；首页 links 增加两个入口；实测 forms.wxml 六项能力全部正确编译；踩坑记录：`ref<'a'|'b'|'c'>('a')` 泛型标注触发编译器静态求值限制（data 变 undefined）——示例改用无泛型 ref；getting-started 新增"示例应用覆盖的能力"表格；测试 117 不变（router/golden/e2e 对页面数无依赖，e2e 用精确 href 选择器不受新增链接影响）
72. **v0.2 工程化基线落地（★monorepo 拆分：@proteus-vue/compiler 独立包 + CI + 贡献设施）**：按 roadmap v0.2 推进——① **独立包**：`git mv src/compiler → packages/compiler/src`（目录自包含：仅依赖 @vue/* + 内部相对路径），新增 `packages/compiler/package.json`（@proteus-vue/compiler，peer 依赖 @vue/compiler-sfc/dom）+ `tsconfig.build.json`；构建 = tsc --emitDeclarationOnly（.d.ts）+ esbuild 单文件 bundle（external @vue/*，规避 ESM 相对导入无扩展名问题）；根 package.json 加 workspaces，`node_modules/@proteus-vue/compiler` 链接验证：dist 包消费 compileVueSfc/listTransformRules(49)/explainTransform 全通；适配层（vite-plugin/proteus.config/tests×5）import 改 `./packages/compiler/src`；vite alias 与 tsconfig paths 加 `@proteus-vue/compiler`（先于 @proteus-vue/*，数组/对象保序）；② **CI**：`.github/workflows/ci.yml` 双 job（verify: vue-tsc/test/build:mp/build:web/独立包构建；e2e-web: playwright chromium）；③ **贡献设施**：CONTRIBUTING.md（规则改动四处同步约定）+ Issue 模板（bug 要求附决策链 trace / feature 要求标规则 ID）；perl 批量替换踩坑：替换串自带引号导致 `src''` 双引号（TS1005），二次修正；测试 117 不变 + 双端构建 + 独立包构建全绿
73. **@proteus-vue/cli 落地（★v0.2：独立编译 + 底线循环 ② 命令行化）**：`packages/cli/`——① `proteus build <dir> --out <dir> [--debug] [--no-px2rpx] [--rpx-ratio <n>] [--rules <json>]`：扫描目录 .vue → wxml/js/wxss（编译引擎独立可用、脱离 Vite；.json 由框架 gen-routes 负责）；② `proteus explain <vue-file | rule-id>`：文件 → 决策 trace（formatTransformTrace）、规则 ID → AI 说明书（formatTransformRule），智能识别（fs.existsSync 优先）；③ `proteus rules [phase]`：能力目录；核心逻辑纯函数（args.ts parse*/explain.ts explainTarget/rules.ts）可单测（tests/cli.test.ts 9 用例，含 --rules JSON 读取）；构建 = esbuild 单文件（bundle 内包含 @proteus-vue/compiler，external @vue/*）+ --banner shebang；踩坑：源码 shebang + banner 双 shebang 导致 SyntaxError（line 2）——源码去掉 shebang 由 banner 注入；根 package.json 加 `proteus`/`cli:build` 脚本，node_modules/.bin/proteus 链接验证（重装 npm install 生效）；实测：explain forms.vue（30 决策）/ explain script/ref-incdec（说明书）/ build 7 页 + trace 落盘全通；测试 117 → 126；roadmap v0.2 CLI 行勾选
74. **create-proteus 脚手架落地（★v0.2：npm create proteus 一键双端工程）**：`packages/create-proteus/`——① **模板 = 主仓库快照 + 手写模板**：`scripts/snapshot-template.ts` 自动快照 src/platform/runtime/shims/router（除 auto-routes）/scripts（除自身）/vite-plugin（import 替换 @proteus-vue/compiler）/tsconfig/index.html（入口替换 src/main.ts），手写模板（package.json 含 {{name}} 占位 / proteus.config / vite.config / src/main* / App.vue / pages/index.vue / auto-routes.ts 仅首页占位）；② **核心逻辑 copyTemplate 纯函数**（tests/create-proteus.test.ts 4 用例：骨架完整性 / {{name}} 替换 / 插件 import npm 包 / 首页 SFC）；③ **端到端验证**：生成 31 文件 → npm install（@proteus-vue/compiler 未发布，临时 file: 指向仓库包）→ build:web ✓ + build:mp ✓（四件套 + app.json + 自动 app.js 骨架 + WXML 正确编译）；④ 发布形态验证：dist + templates 同目录运行生成成功；踩坑一串：快照脚本 rmSync 全清模板目录删掉手写模板（改 MANAGED 白名单保护）、auto-routes 暂存文件放 src/router 内被 rm 连带删除（backup 移到 TPL 外）、模板 auto-routes 接口与主仓生成版不一致（缺 routes/tabRoutes，补全）、perl 替换 package.json 未生效（改 node 直接改）、模板缺 mp-entry-stub.ts（scripts 整体复制）；模板接口对齐主仓后 Web 类型检查通过；roadmap v0.2 脚手架行勾选；测试 126 → 130
75. **发布流水线就绪（★v0.2 收官项：changesets，用户指示暂不真实发布 npm）**：引入 @changesets/cli（2.31.1）——`.changeset/config.json`（access public / baseBranch main / updateInternalDependencies patch / 根 private 包不参与版本）；脚本 `changeset` / `changeset:version` / `changeset:publish`；初始 changeset（v02-engineering-baseline，三包 minor）→ 实测 `changeset version`：三包 0.1.0→0.2.0（与 v0.2 里程碑语义对齐）+ 各包 CHANGELOG.md 生成 + @proteus-vue/cli 对 @proteus-vue/compiler 内部依赖自动更新 0.2.0 + changeset 文件自动消费；v0.2 里程碑全部六项完成（独立包/注册表随包/CLI/脚手架/CI+贡献设施/发布流水线），npm 发布待启用（无真实 publish、无 CI 发布 job）
76. **v0.3 第一棒：computed 读路径 + v-show（★编译能力补全）**：① **computed 读路径**——`const x = computed(() => 表达式)` 编译为 data 派生字段：`extractData` 改造返回 `{ data, computed }`（computed 分支收集 rawComputed 二次处理，依赖可定义在其后）；`extractComputedFromInit`（仅支持箭头简写+表达式体，`{` 开头块体拦截——否则生成 `{ return ... }` 坏表达式，产物自校验会兜底但需编译期拦下；依赖静态提取 `x.value`、缺失警告）；生成三处：data 不含派生字段、onLoad 初始化 `this.setData({ double: this.data.count * 2 })`（默认/显式 onLoad 均注入）、依赖写入 setData 合并重算；**关键 bug 修复**：同一 setData 对象里派生表达式读 `this.data` 是**旧值**（setData 异步批量、对象内求值用当前 this.data）→ `writeSetData`：有派生补丁时先 `this.data.x = 新值` 再 `setData({ x: this.data.x, double: ... })`；trace 规则 `script/computed-to-data` + 注册表 AI 说明书（50 条规则）；② **v-show**——规则从 limitation 升级 implemented：`v-show="x"` → `hidden="{{!x}}"`（小程序 hidden = display:none，元素始终渲染，语义对齐）；模板/注册表/文档同步（compiler.md 指令表 + MVP 限制、README 已知限制、configuration.md 示例 v-show-limit → directive/v-for、packages/compiler/README）；demo：forms.vue 加 double 派生 + v-show 行；测试：+2 v-show +7 computed（含 onLoad 初始化/写入合并/只读依赖/缺失警告/块体警告/模板插值）→ 130 → 139；golden 不受影响
77. **v0.3 第二棒：scoped CSS + :class 数组语法（★编译能力补全）**：① **scoped CSS**——小程序无 scoped 原生机制，编译期属性选择器等价：compileVueSfc 检测任一 `<style scoped>` → `scopedIdFrom(filename)`（djb2 哈希 → `data-v-xxxxxx`，同文件稳定）注入 template + style；template 侧元素附加作用域属性（`template/scope-attr` 规则）+ style 侧 `:deep(X)` 去包装 + 每条规则选择器末尾追加 `[scopeId]`（@media 骨架保留，`style/scoped-css` 规则）；MVP 简化：任一 scoped 全量作用域化、:deep 部分同样作用域化（组件边界后续完善）、可 rules.disabled 关闭；② **`:class` 数组语法**——`[a, { b: on }, 'c', cond ? 'x' : 'y']` → 逐项拼接 `{{((a)?(a)+' ':'')+(on?'b ':'')+'c '+((cond ? 'x' : 'y')?(cond ? 'x' : 'y')+' ':'')}}`（splitTopLevel 顶层逗号分割跳过字符串/括号；字符串字面量/对象/简单变量/三元支持，其余警告；三元 `?` 周围空格正则坑 + 重复求值括号包裹）；踩坑：style.ts 注册表 skyline-unsupported 规则缺 status 字段（补全，transforms.test 字段完整性兜底）；demo：forms.vue style 改 scoped + :class 数组；测试 +6（scoped 4：属性附加/scopeId 稳定/:deep/非 scoped 无副作用；:class 数组 2）→ 139 → 145；golden 不受影响
78. **v0.3 第三棒：watch 支持（★computed/watch 主线补全）**：`watch(ref, (newVal, oldVal) => { body })` 编译为 `proteusWatchX` 方法——① extractWatch 正则提取（单 ref + 箭头函数回调；`=>` 与 `{` 间空格正则坑；同一 ref 多 watch 警告后者覆盖；依赖缺失警告）+ 回调体 ref 读写照常重写（可再触发 computed/watch 联动）；② 写入点联动：rewriteRefAccess 传入 watches，写入 ref 时 `const oldX = this.data.x; <writeSetData>; this.proteusWatchX(this.data.x, oldX)`（旧值在写入前保存；watchTail 分号分隔——缺分号同行语句会语法错误）；③ immediate: true → onLoad 初始化调用一次（oldVal = undefined，默认/显式 onLoad 均注入）；④ 前置 ++/-- 统一走 writeSetData（forceWrite 保证表达式值 = 新值）；⑤ 注册表 `script/watch-to-methods` 规则（52 → 53 条）；demo：forms.vue watchLog 显示 count 变化；测试 +8（方法生成/写入联动/与 computed 同链/immediate/回调体重写/依赖缺失/多 watch 警告/禁用）→ 145 → 153；golden 不受影响
79. **v0.3 第四棒：组件系统（★v0.3 验收核心，props/emits/slots 全落地）**：① **子组件编译**——`defineProps` 对象形式 → Component `properties`（type 映射 String/Number/Boolean/Object/Array + default→value，无 default 按类型默认；`props.xxx` → `this.data.xxx` 重写）；`defineEmits` + `emit('x', payload)` → `this.triggerEvent('x', payload)`；`<slot>` 原样透传；**组件模式无 onLoad**（微信组件无此钩子）→ computed 初始化 / immediate watch 走 `attached()`；defineProps/defineEmits 宏不提取 data；② **父页面**——自定义事件（非 EVENT_MAP）→ `bind:` 冒号形式（`@updated` → `bind:updated`；EVENT_MAP 内保持 bindxxx 兼容既有产物）；③ **usingComponents 自动注入**——gen-routes 扫描页面模板自定义组件标签（原生/HTML/customTags 白名单排除）→ 约定 `components/<name>/index` → page.json 注入（缺失警告）；④ **插件扫描 components 目录**（`<appRoot>/components/`，isComponent 判定自动命中）；⑤ **TS 参数标注剥离**（`stripParamTypes`：e: { detail?: number } → e，产物纯 JS——vue-tsc JSDoc 类型未生效的替代方案）；踩坑：defineProps 被当普通 const 提取（组件宏跳过）、组件误生成 onLoad（attached 替代）、demo-box 被 gen-routes 当组件（customTags 白名单）、m.index 类型（?? 0）；demo：`examples/components/counter/index.vue` + `pages/components-demo.vue`（props/事件/usingComponents 全链路实测产物正确）；注册表 +2 规则（script/define-props、script/define-emits，53 → 55 条）；测试 +10 → 153 → 163；golden 不受影响
80. **v0.3 收官：方法级 sourcemap（★v0.3 全部验收完成）**：产物 JS ↔ 源码映射——`buildSourceMap`（sourcemap v3：sources/sourcesContent 内联源码 + mappings VLQ 编码；segment = [genCol=0, srcIdx=0, srcLine(delta), srcCol=0]，无映射行空 segment，sourceLine 跨行累计 delta）+ `vlqEncode/vlqDecode`（base64 VLQ，编解码往返单测）；extractMethods 返回 MethodInfo{src,line}（源码起始行）、WatchInfo 补 line；transformScriptToPage 维护 lineMappings（产物行 → 源码行：方法体/watch 回调体）——`pushMapped` 按 src 分行映射；debug 构建：方法前 `// @行号 方法名` 注释 + `<rel>.js.map` 落盘 + js 尾部 `//# sourceMappingURL`（微信开发者工具定位源码；正式构建零 map 残留，grep/ls 验证）；踩坑：测试 sourceLine 是 delta 需累加（非绝对行）；注册表不加规则（产物级能力，非转换决策）；测试 +2 → 163 → 165；roadmap v0.3 全部六项 + 验收清单 ✅；compiler.md 反黑盒表 + roadmap/PROJECT_MEMORY 同步
81. **v0.4 第一棒：setData 深度优化（深层 diff）+ 性能基准套件（★运行时性能）**：① **setDataBridge 深层 diff**——markDirty 收到对象/数组时递归 diff（`diffPaths`：对象逐键 / 数组逐下标 / 标量整体替换）出**叶路径补丁**，只推送变化子路径（对象更新从整对象 → 变化字段，直击 uni-app 全量大对象痛点）；首次推送无旧值基准 → `flattenPaths` 展开叶路径；标量值去重不变；路径合并语义从"整对象原子"变为"叶路径补丁"（同叶路径覆盖，最终值正确）；`reset()` 方法（测试隔离——单例 lastValues 跨测试泄漏坑）；② **性能基准 `tests/perf/`**——setData 桥接（1000 次变更 → 1 次批量 setData / 对象 diff 全量 4 条 vs 变化 1 条 ×4 减少 / 同值高频零推送）+ 编译引擎（典型业务页含 template 指令+ref+computed+watch+scoped style，平均 0.5ms/次报告 + 宽松阈值 500ms 防 flaky），随 npm test 运行（CI 门禁）；测试 +8（runtime +4 diff + perf 4）→ 165 → 173；roadmap v0.4 勾选 setData 深度优化 + 性能基准；剩余：虚拟列表 / Pinia / 包体积 / Vapor codegen 借鉴
82. **v0.4 第二棒：虚拟列表（框架内置组件雏形，★验收项）**：`examples/components/virtual-list/index.vue` + `pages/virtual-list-demo.vue`——**数据切片 + 占位**方案：props（items/itemHeight/height）+ calc 计算可视区（start = scrollTop/itemHeight，count = ceil(height/itemHeight)+2 缓冲）→ visible 切片（setData）；顶部占位高度 start*itemHeight；**双端同源码**：`<scroll-view>` 模板 → MP 原生滚动 + bindscroll（EVENT_MAP 新增 scroll）/ Web 自定义元素 + scoped CSS `scroll-view { overflow-y: auto }`；onMounted 首屏计算；**踩坑一串**：① ref([]) 推断 never[] 与编译器正则冲突（JSDoc @type 在 vue-tsc 不生效）→ 非空初始值推导类型 + defineProps 对象里 `as any`（不进产物）；② `props.items` 在 computed 里不重写（deps 提取只认 .value）→ 用方法 + ref 存切片；③ 模板字符串样式表达式 WXML 不支持 → :style 对象语法；④ :key 需简单标识符（start+i 不支持 → 用 i）；⑤ props default 函数（() => []）产物 value 无效 → 忽略 + 警告；⑥ 组件 import 仅 Web 端注册（MP 编译器忽略 import，标签走 usingComponents）；测试 +2（bindscroll / VirtualList 编译产物）→ 173 → 175；roadmap v0.4 勾选虚拟列表；剩余：Pinia / 包体积 / Vapor codegen 借鉴
83. **虚拟列表框架内置化（★定位修正，决策 #82 后续）**：用户指出 VirtualList 实际在 examples/components/（应用组件）而非 roadmap 所述框架内置——修正为真内置：`git mv examples/components/virtual-list → src/components/virtual-list`；**插件 buildStart 改造**：files 从 string[] 改为 { file, rel } 结构，框架组件（src/components/）rel 规范化为 `proteus/<name>/index`（产物 dist/mp-weixin/proteus/virtual-list/index.*，与应用组件 `/components/...` 路径空间隔离避免同名冲突）；**gen-routes collectComponents 回退链**：应用组件（appRoot/components/）优先 → 框架组件（src/components/）→ usingComponents `/proteus/<tag>/index`；demo import 改 `@proteus-vue/components/virtual-list/index.vue`（Web 注册，MP 忽略）；测试路径同步；实测产物：proteus/virtual-list 四件套 + 使用页 page.json `/proteus/virtual-list/index`；测试 175 不变；roadmap 虚拟列表行改为「框架内置 src/components/」；README 目录结构 src/components 标注
84. **框架组件导入体验优化（★用户反馈：冗长导入路径）**：`import VirtualList from '@proteus-vue/components/virtual-list/index.vue'` 暴露实现路径太啰嗦 → **聚合入口 + 命名导入**：新增 `src/components/index.ts`（`import VirtualList from './virtual-list/index.vue'; export { VirtualList }`）——Web 端 `import { VirtualList } from '@proteus-vue/components'`（@proteus-vue → src/ 别名解析到聚合入口）；MP 端无需 import（标签走 usingComponents 自动）；新增 `src/shims/vue.d.ts`（`declare module '*.vue'`——.ts 文件 import .vue 需要模块声明，vue-tsc 对 .vue 内部 import 原生支持但 .ts 聚合文件需要）；新增内置组件流程：组件放 src/components/<name>/ + 聚合导出 + 插件/路由自动支持；实测 vue-tsc + Web（virtual-list chunk）+ MP（proteus/ 产物不变）+ 175 测试全绿
85. **v0.4 第三棒：Pinia 双端适配（Web 完整 + MP 运行时桥）**：① **Web 端原生 Pinia**——npm i pinia（4.0.3）、examples/main.ts `createApp(App).use(createPinia())`、`examples/stores/counter.ts`（setup 风格 defineStore + computed）、`pages/pinia-demo.vue`（count/double + inc/reset 按钮，首页链接）；Web 构建 chunk 验证 ✓；② **MP 端框架级 store 桥**——`src/runtime/store/index.ts`：`createStore(id, initial)`（单例 Map + state/set/get/subscribe 广播）+ `connectPageStore(page, store, map?)`（初始同步 + 订阅变化 → page.setData，返回取消函数）；定位：MP 端 Pinia 依赖跨模块编译（MVP 限制：产物单文件无模块系统），store 桥为过渡方案（Web/后续基础）；`tests/store.test.ts`（4 用例：单例共享/广播与取消/页面连接初始同步+setData/带 map 映射）；pinia-demo 页 MP 端构建有预期警告（store.inc() 非简单方法引用 / useCounterStore() 无法求值——页面已标注 Web 演示）；测试 +4 → 175 → 179；roadmap v0.4 Pinia 行标 🟡（Web 完整 + MP 桥，MP 编译待跨模块）；剩余：包体积控制 / Vapor codegen 借鉴
86. **v0.4 第四棒：包体积预算仪表（★v0.4 收官）**：`scripts/bundle-report.ts`（纯函数可单测：`scanMainPackage` 扫描 dist/mp-weixin 排除分包 root 前缀 + 隐藏调试目录 → 主包体积 + Top N 大文件；`formatBundleReport` 报告渲染）+ `proteus.config.ts` 新增 `budget`（mainPackageKB 默认 1200KB roadmap 目标、strict 超限构建失败）+ build:mp 脚本链尾挂载；踩坑：分包排除前缀推导（源码 root examples/subpackages/order → 产物 subpackages/order 需去掉 appDir 前缀）；实测主包 32KB / 预算 1200KB + Top 5 报告；Vapor codegen 借鉴在 roadmap 行补对照分析（依赖追踪精确化 = 编译器路径化写入 + 运行时深层 diff，与 Vapor reset/effect 同构，v0.6 做基准）；测试 +2 → 179 → 181；roadmap v0.4 全部六项完成；剩余：v0.4 里程碑验收（除 MP Pinia 编译待跨模块外全 ✅）
87. **欠账清理开始（★用户审视 roadmap「已标完成仍有待办」）**：盘点出三类欠账——A 文档一致性（README 139→181 单测/版本 v2.47/规则数 50→55、roadmap 能力矩阵滞后）✅ 已清；B 里程碑内待做；C 结构性（框架本体拆包/真机实测）。**CSS 预处理器（scss）**：编译器新增 `CompileOptions.preprocessStyle(lang, content)` 钩子（@proteus-vue/compiler 保持零依赖，适配层注入）；插件 `import * as sass from 'sass'`（1.103.1 devDep）+ `preprocessStyle` 实现（scss/sass → css，失败警告原样输出；less 警告未内置）；compileVueSfc 对 lang 的 style 块先过钩子再进 transformStyleToWxss；demo：forms.vue style 改 `lang="scss"`（变量 $brand + 嵌套 .watch-log，产物 wxss 验证 $brand→#1a7af8、嵌套展开）；测试 +2（钩子被调用 + 结果进 wxss / 无 lang 不触发）→ 181 → 183；Web 端 vite 原生处理 scss 验证 ✓；roadmap 预处理器行 ✅（less 待内置）、能力矩阵 #2 更新、compiler.md 样式章节、README 规划行更新
88. **欠账清理 B 类②：事件修饰符 .self/.once + v-on 键位（★v0.3 指令补全收尾）**：`.self` → 编译期生成包装 `proteusSelfXxx(e) { if (e.target === e.currentTarget) this.xxx(e) }`（事件源判断）；`.once` → `proteusOnceXxx(e) { if (!this.data.__onceXxx) { this.data.__onceXxx = true; this.xxx(e) } }`（data 标记，ES5 安全）；仅对简单方法名 handler 包装；与 .stop/.prevent 组合时 catch 优先不包装；template 收集 selfHandlers/onceHandlers（SerializeContext Set + TemplateTransformResult 数组）→ script 生成包装方法；键位 `@keyup.enter` 等 → 编译期警告（小程序无对等键盘事件，input 请用 @confirm，原样输出）；注册表新规则 `event/modifier-self-once`（implemented，55 → 56 条）；demo：forms.vue 加 .self 按钮；测试 +4（self 包装/once 标记/键位警告/stop+self 组合）→ 183 → 187；compiler.md 事件映射表 self/once/键位行、roadmap 指令补全行 ✅；剩余 B 类：computed 写路径、watch 多源、defineExpose/泛型 defineProps/组件嵌套、类型提示全链路；C 类：框架本体拆包、真机实测
89. **欠账清理 B 类③：watch 多源/函数源（★roadmap computed/watch 行补全）**：extractWatch 重构——源解析：单 ref（count）/ 数组 `[a, b]`（正则提取 ref 名）/ 函数 `() => expr`（依赖从 expr 的 x.value 提取 + getter 转写 this.data 形式）；WatchInfo 改 `{ id, deps[], params, body, immediate, line, expr? }`（id = deps 组合 AAndB）；生成：方法名 proteusWatch<id>；**多源回调数组**（`proteusWatchAAndB([this.data.a, this.data.b], [oldA, oldB])`）、**单源回调标量**（Vue 语义）；旧值逐个保存 `const oldA = this.data.a, oldB = this.data.b;`；immediate 单源标量/多源数组；踩坑：`const oldA, oldB = ...` 语法错误（oldA 无初始化）→ 逗号带初始化；单源回调误包数组 → 标量；测试 +3（数组源联动/函数源求值/函数源缺失依赖警告）→ 187 → 190；roadmap computed/watch 行更新（剩 computed 写路径）；剩余 B 类：computed 写路径、defineExpose/泛型 defineProps/组件嵌套、类型提示全链路；C 类：框架本体拆包、真机实测
90. **欠账清理 B 类④：computed 写路径（显式 setter，★roadmap computed/watch 行收官）**：extractComputedFromInit 扩展对象形式 `computed({ get: () => expr, set: (v) => { body } })`——解析 get/set（setter 提取 param + body）；ComputedInfo 加 `setter?`；生成：方法体 `x.value = v` → `this.proteusSetX(v)`（setter 存在）/ `/* computed x 只读（无 setter），赋值已忽略 */` 注释（只读赋值，产物无副作用）；proteusSetX 方法生成（setter 体内 ref 读写照常重写 + 派生/联动）；onLoad 初始化不变（getter 派生）；踩坑：TS objM.index possibly undefined（?? 0）；测试 +2（setter 方法生成 + 写入重写 / 只读赋值注释）→ 190 → 192；roadmap computed/watch 行全 ✅；剩余 B 类：defineExpose、TS 泛型 defineProps、组件嵌套 usingComponents、类型提示全链路；C 类：框架本体拆包、真机实测；注：`docs/proteus-pinia-plan/`（9 篇 Pinia 四端适配规划，非本会话创建）随 git add -A 误入提交 523a4aa，待用户决定去留
91. **欠账清理 B 类⑤：defineExpose（no-op 校验）+ TS 泛型 defineProps（★组件系统收尾）**：① **defineExpose**——小程序组件 methods 天然可被 selectComponent 访问 → 编译期 no-op；`checkDefineExpose` 解析声明成员：方法 ✓（已在 methods）、ref 值（data 字段）暴露无对等机制 → 警告（请用方法包装）；trace 规则 `script/define-expose`（57 条）；② **TS 泛型 defineProps**——`defineProps<{ label: string; count?: number }>()` → properties（`mapTsType`：string/number/boolean → String/Number/Boolean + 默认值、object → Object、xxx[]/Array → Array、联合字面量 → String、未知 → 警告按 String）；propsVar 提取正则兼容 `defineProps<`（`[<(]`）；注册表 script/define-props 规则更新（对象 + 泛型两形式）；测试 +6（defineExpose no-op/ref 值警告/不污染 data + 泛型类型映射/数组联合/未知类型警告）→ 192 → 198；roadmap 组件系统行更新（剩组件 usingComponents 嵌套）；剩余 B 类：组件嵌套 usingComponents、类型提示全链路；C 类：框架本体拆包、真机实测
92. **欠账清理 B 类⑥：组件嵌套 usingComponents（★组件系统收官）**：gen-routes 新增 `writeComponentJsons`——扫描应用组件（appRoot/components/）+ 框架组件（src/components/）模板，collectComponents → 生成各组件 component.json（usingComponents，产物路径与插件 rel 一致：应用 /components/、框架 /proteus/）；demo：`examples/components/panel/index.vue`（TS 泛型 defineProps + 嵌套使用 counter + @change 监听）+ components-demo 页使用；踩坑：defineProps< 泛型形式未过组件宏跳过（`props: undefined` 警告）→ extractData 宏正则改 `defineProps\s*[<(]`；实测：panel component.json `{ usingComponents: { counter: "/components/counter/index" } }` + components-demo page.json counter/panel 双注入 + panel 四件套（含 index.json）；剩余 B 类：类型提示全链路；C 类：框架本体拆包、真机实测
93. **类型提示全链路步骤 1：路由参数类型生成（★B 类最后一项）**：`<route>` 块支持 `params` 声明（JSON：字段名 → string/number/boolean）→ gen-routes parseRouteBlock 解析 + RouteRecord 携带 → writeAutoRoutes 生成 `RouteParamsByName`（按路由名索引，字段可选 ?:，未声明路由 {}，未知类型警告按 string）；示例 user/profile.vue 声明 { id, from, kw }；踩坑：文件注释里 `<route>` 字样被正则先匹配（改措辞）；验证 build:mp 产物含类型表 + vue-tsc；规划文档 docs/types.md（6 步拆分，防上下文撑爆）
94. **类型提示步骤 2：router.push 泛型推导**：`NavigateOptions<N extends keyof RouteParamsByName = keyof RouteParamsByName>`（N 由 name 字面量推断 → name 受限 + params 匹配；path-only 回退全部路由名；**非条件类型保证 EPC 多余属性检查**——条件类型会关闭 EPC）；push/replace 泛型化；类型断言测试 tests/types/router-params.types.ts（正例 + 负例 @ts-expect-error）；兼容：router.test.ts params id 1→'1'（profile 声明 string）、运行时负例 name:'not-exist' 加类型断言（类型层已拦截）
95. **类型提示步骤 3：PageOnLoad 页面参数 + runtime onLoad**：types 加 `PageOnLoad<N>`（N=本页路由名，自动匹配 RouteParamsByName）；runtime 加 onLoad 导出（**Web 端 no-op 兼容**——源码 onLoad 在 Web 端不报错，签名 (options?: any) 宽松）+ `src/runtime/index.ts` 聚合入口；示例 user/profile.vue onLoad((options: PageOnLoad<'user-profile'>)) → options.id 推导；踩坑：runtime onLoad 签名（hook 注册 vs 参数接收）、demo `??` 残留产物（真机不支持，显式 null 检查）；产物 onLoad 提取 + 标注剥离
96. **类型提示步骤 4：事件处理器类型（shims）**：`src/shims/events.d.ts` 全局声明（**无 export**——export 变模块非全局）`MpEvent<TDetail>`（detail/target/currentTarget.dataset）/ `MpInputEvent`（v-model handler）/ `TapEvent`；命名避开 DOM 内置（InputEvent 撞 UIEvent.detail 声明合并灾难 → MpInputEvent）；示例 components-demo.vue onChange(e: MpEvent<{ value?: number }>)；踩坑：@ts-expect-error 只覆盖紧邻行（多行函数 body 错误漏）→ 单行负例；产物标注剥离零残留（grep 验证）
97. **类型提示步骤 5：端到端 + 文档（★B 类全部收官）**：示例页三处闭环（profile PageOnLoad / components-demo MpEvent）；docs/types.md 升级正式文档（三处用法 API 参考 + 公开类型表 + 已知边界——跨模块 import MVP 限制说明：示例页 router.push 在 MP 产物为未定义引用，类型推导在开发期完整、运行时导航用 <a href>）；roadmap 类型提示行 ✅；**B 类欠账全部完成**（A 文档 + B 类①-⑦ + 类型提示①-⑤），剩 C 类：框架本体拆包、真机实测；测试 198
98. **拆包步骤 1：config 解耦（★C 类拆包前置）**：三处 config 依赖移除——① `setDataBridge` 改工厂 `createSetDataBridge({ batchWindow })`（默认 16，单例 `setDataBridge = createSetDataBridge()` 兼容导出；`SetDataBridgeOptions` 接口）② `isSkyline()` 改读 `__PROTEUS_SKYLINE__`（mp.d.ts 声明 + vite.config define 注入 `isMp && config.skyline`；**vitest 独立 config 不加载 vite define → `typeof __PROTEUS_SKYLINE__ !== 'undefined'` 守卫**，测试环境回退 false）③ `platform/index` 删 config 依赖（`import.meta.env.MODE === 'mp-weixin'` 直接选 adapter——原 config.platform 回退实际不触发，vite mode 恒为 web/mp-weixin）；验证：vue-tsc + 198 测试 + 双端构建全绿 + 产物无 define 残留（skyline.ts 运行时模块不进 MP 产物 bundle）；docs/packages.md 步骤 1 勾选；剩余步骤 2-8
99. **拆包步骤 2：shared 包（★adapter + shims 归 @proteus-vue/shared）**：`git mv src/{platform,shims} → packages/shared/src/`；包 scaffold（package.json exports → dist + tsconfig.build esbuild 单文件 + index.ts 聚合 adapter/类型）；引用面：runtime/router 4 处 `../platform` → `@proteus-vue/shared`、RouterView `@proteus-vue/platform` → `@proteus-vue/shared`；vite alias + tsconfig paths/types/include + vitest.config alias + snapshot-template 源改 packages/shared/src（模板暂复制保持可用，步骤 7 重构）；tests 3 处 mock 改 `@proteus-vue/shared`；踩坑：① perl `@proteus-vue` 数组插值吞掉（replaceAll node 修复）② vitest 独立无别名（vitest.config 补）③ mock 路径与实际 import id 不一致（re-export 链 mock 失效 → mock '@proteus-vue/shared'）④ import.meta.env 类型（shared 独立构建无 vite types → (import.meta as any)）；验证 vue-tsc + 198 测试 + 双端 + shared 包构建 + 模板快照 + workspace 链接（@proteus-vue/shared）；docs/packages.md 步骤 2 勾选；剩余步骤 3-8
100. **拆包步骤 3：runtime 包（★@proteus-vue/runtime）**：`git mv src/runtime → packages/runtime/src`（appSkeleton/debug/pageLifecycle/setDataBridge/store + index 聚合）；包 scaffold（依赖 @proteus-vue/shared，构建 external shared）；引用面：插件 appSkeleton 相对路径、tests 相对路径、vite/vitest alias + tsconfig paths/include @proteus-vue/runtime、snapshot 源改 packages/runtime/src；踩坑：runtime 独立构建缺全局类型（`__PROTEUS_DEBUG__`/PageOptions/ComponentOptions 在 shared mp.d.ts）——tsconfig.build types 引 shared shims；验证 vue-tsc + 198 测试 + 双端 + runtime 包构建 + 模板快照 + workspace 链接（@proteus-vue/runtime）；docs/packages.md 步骤 3 勾选；剩余步骤 4（router 工厂化）-8
101. **拆包步骤 4：router 包 + 工厂化 + 路由规划 M1/M2（★@proteus-vue/router + 透明化地基）**：`git mv src/router → packages/router/src`；**工厂化**：index.ts 改 `createRouter(routes)`（删全局单例，路由表调用方注入）；guards 同工厂化（runBeforeEach/runAfterEach 收 routeMap 参数，getCurrentFrom 反查）；**auto-routes 随应用**：`examples/router/auto-routes.ts`（gen-routes routesOutput 改指向；RouterView 改相对导入 `./auto-routes`，仓库/模板通用）；**类型提示兼容方案（★关键决策）**：RouteParamsByName 改空基接口 + auto-routes 用 `declare module '@proteus-vue/router/types'` 模块扩充注入（vue-router 同款）——基类空接口保证 keyof=never 时 name 受限负例成立，扩充后 push 泛型/PageOnLoad 零回归（tests/types/router-params.types.ts 全过）；**M1/M2 增量**（docs/proteus-router-plan）：schema.ts（手写校验 + RouteValidationError 含 loc：path/name/redirect-parent 互斥/transition 枚举/JSON 可序列化/checkDuplicates/validateParentRefs）+ scan.ts（@vue/compiler-sfc parse 复用，block.loc 行号定位，多块报错，无块跳过）+ tree.ts（嵌套树两遍扫描：parent 显式优先覆盖 path 前缀推导，找不到 parent/成环报错，sortByPath 稳定排序可复现）+ merge.ts（meta 深合并限深 3，页面胜）；**踩坑**：① lazy 默认值语义（scan 不强制置 true，RouteBlock.lazy 可空，树构建时 defaults 解析——否则全局 lazy 永失效）② RouteNode 保留 parent 字段（构建期使用，Omit 掉会断 tree.ts）③ 包独立构建 spread never（无扩充时 RouteParamsByName[never] → `as RouteParams` 显式收窄）④ 包 tsconfig.build types 补 @types/node（node:fs 模块声明）⑤ `@proteus-vue/router/types` 子路径解析：vite alias 用目录级 `@proteus-vue/router → packages/router/src`（前缀匹配子路径）+ tsconfig paths 双键（router/router/* 置于 @proteus-vue/* 前）；验证 vue-tsc 零错 + 216 测试（+18 M1/M2 用例）+ 双端构建 + router 包构建 + 模板快照（gen-routes 复制时 `../packages/router/src` → `../src/router` 路径替换）+ workspace 链接（@proteus-vue/router）；docs/packages.md 步骤 4 勾选；剩余步骤 5（plugin-vite 包）-8
102. **拆包步骤 5：plugin-vite 包（★@proteus-vue/plugin-vite，插件 + gen-routes 归包）**：`git mv vite-plugin-mp-transform.ts scripts/gen-routes.ts → packages/plugin-vite/src/`；**appSkeleton 从 runtime 迁入 plugin-vite**（构建期 app.js 骨架模板归构建期包，runtime 纯运行期，依赖方向保持 plugin-vite → compiler + router）；**config 解耦（★关键）**：插件不再 import 项目 config——`PluginOptions.config: ProteusConfig` 由 vite.config 注入（`mpTransform({ config })`）；ProteusConfig 类型契约迁入包内 `config.ts`（根 proteus.config.ts 改 import 类型 + 实例化，budget 改可选 + bundle-report 缺省防御）；**gen-routes 双形态**：纯函数库 `runGenRoutes({ config, root })`（可单测）+ CLI 入口 `src/cli.ts`（tsx 直跑，main 检测拆独立文件）；**踩坑**：① workspace 依赖版本必须对齐实际包版本（@proteus-vue/compiler 实为 0.2.0，声明 0.1.0 触发 npm 404 去 registry 拉）② vite.config bundle 阶段不走 resolve.alias → 插件 @proteus-vue/compiler 解析到 dist 产物（给 compiler 加 prepare 钩子保 dist 新鲜；且暴露 dist 陈旧 bug——重建解决）③ 预设 builders 路径随 router 拆包同步（config.customRoute.builders 改 packages/router/src/presets）④ tsx 动态 import 需带 .ts 扩展名 + 相对层级（src/ → 项目根为三级）⑤ 动态 import 项目根 config 拉偏 tsc rootDir 推断（d.ts 嵌套 dist/packages/...）→ CLI 拆独立 cli.ts + tsconfig.build rootDir ./src + exclude cli.ts；验证 vue-tsc 零错 + 219 测试（+3 gen-routes：主包/分包/坏 JSON 容错）+ 双端构建 + plugin-vite 包构建 + 模板 gen-routes CLI 冒烟（生成 app.json/page.json）+ workspace 链接（@proteus-vue/plugin-vite）；docs/packages.md 步骤 5 勾选；剩余步骤 6（别名切换）-8
103. **拆包步骤 6：别名与引用面全量切换（★删除泛化 @proteus-vue → src/）**：vite alias + tsconfig paths 全量精确映射 `@proteus-vue/{router,runtime,shared,compiler,plugin-vite,components}`；新增 `@proteus-vue/components` → `src/components`（框架内置组件暂留 src，组件库 v2.0 方向；vite 单键前缀匹配子路径 `@proteus-vue/components/virtual-list/index.vue` + tsconfig 双键 components/components/*）；**风险收敛**：步骤 2-5 已逐包精确化 import 面，本步只删兜底、无存量 import 受影响（grep 盘点零残留）；模板 vite.config 同步精确化（vendored 结构：@proteus-vue/shared → src/platform/index.ts——shared 的 adapter 聚合在模板由 platform 目录充当；模板 tsconfig 仍复制主仓 paths 指向 packages/——模板不跑 vue-tsc，步骤 7 重构时统一）；验证 vue-tsc 零错 + 219 测试 + 双端构建 + 模板快照；docs/packages.md 步骤 6 勾选；剩余步骤 7（模板重构）-8
104. **拆包步骤 7：create-proteus 模板重构（★npm 包形态，拆包收官前最后一步）**：**模板删除框架 vendored 副本**（src/platform|runtime|router 框架代码、vite-plugin-mp-transform.ts、scripts/gen-routes-cli|bundle-report）→ 依赖 `@proteus-vue/{router,runtime,shared,plugin-vite,compiler}` npm 包；模板应用侧 `src/`：main*/App.vue/pages + router 单例（createRouter）+ RouterView（应用壳）+ auto-routes（占位）+ **shims 应用侧全局类型**（自 shared 复制——shared 包不发布 .d.ts shims，微信宿主类型应用侧持有是标准做法）+ scripts/gen-routes.ts 薄壳（runGenRoutes 来自包）；snapshot-template 重构（只快照应用壳，手写模板列表扩展）；**router 包 exports 补子路径**：./types/./schema/./scan/./tree/./merge（纯类型）+ ./package.json（require.resolve 需要），files 加 src/presets（预设源码随包发布，插件内联 fs.readFileSync 需要源码）；**插件 resolvePkgPath**：config.builders 的 node_modules 包内路径 → createRequire 解析包根 + 子路径（scoped 包正则 `(?:@[^/]+/)?[^/]+`）；**踩坑**：① 模板 App.vue 引 `@proteus-vue/router/RouterView.vue` 被 exports 拦截（npm 包无此子路径 → 应用壳改相对导入 './router/RouterView.vue'）② @proteus-vue/plugin-vite 是命名导出（模板 default import 不匹配 TS2349 → 统一 `import { mpTransform }`，根 vite.config 同步）③ require.resolve('@proteus-vue/router/package.json') 被 exports 限制拦截（ERR_PACKAGE_PATH_NOT_EXPORTED → 补 ./package.json 导出）④ node_modules 路径正则最初 `[^/]+` 只匹配一层（@proteus-vue 被当包名 → 支持 scoped 包两层）⑤ 模板构建走 workspace 链接 dist（plugin-vite 加 prepare 钩子保新鲜）；验证：**模板目录内端到端构建通过**（gen-routes → vue-tsc → vite build web/mp-weixin，app.js 三个内置预设全部内联注册 addRouteBuilder×3）+ 主仓 222 测试（+3 resolvePkgPath）+ vue-tsc + 双端构建；docs/packages.md 步骤 7 勾选；剩余步骤 8（CI/文档）
105. **拆包步骤 8：CI/构建/文档收尾（★C 类拆包全部收官）**：CI verify job 加 `npm run build --workspaces`（7 包独立构建——npm ci 时 prepare 钩子自动建 dist）+ **模板快照一致性检查**（snapshot-template 后 git diff --exit-code -- packages/create-proteus/templates，防手写模板与主仓漂移）；根 verify 脚本加 workspace 构建；docs/roadmap.md 工程化行 ✅（222 测试 + 拆包 8 步全落地 + npm 发包待启用）+ compiler/create-proteus 行描述更新（npm 包形态）；docs/packages.md 状态 ✅ + 验收清单全勾；**C 类拆包 8 步全部完成（决策 #98-#105）**：`src/` 仅剩 components，7 个 @proteus-vue/* 包（compiler/cli/create-proteus/router/runtime/shared/plugin-vite）+ 模板 npm 化；遗留：npm 发包待启用（用户暂不发布），发布后验证 `npm create` 真实端到端
106. **pinia-plan M1-M6 完整实现（★多端持久化骨架，docs/proteus-pinia-plan 按 Batch 分 5 提交）**：落点适配拆包后架构（Storage → packages/shared/src/storage，Pinia 层 → packages/runtime/src/pinia）；**M1 Storage**：StorageAdapter 契约（统一 async——同步后端也走 Promise）+ Memory/LocalStorage/WxStorage/NativeKV（占位）+ serialize（★踩坑：JSON.stringify replacer 在 Date.toJSON() 之后调用拿不到 Date 实例 → 自写 mark walk 打标）+ trace（--trace-storage 对齐）；**M2 持久化**：createPersistedStatePlugin（社区兼容，storage 可选自动选平台、normalizeStorage 包 Web Storage、paths 嵌套 a.b.c）+ persisted()/createPersistence（防抖 50ms、零开销未标记不挂订阅、pick/omit）；**★踩坑：pinia 4.x 的 pinia.use() 在 app.use(pinia)（install）时才把插件推入 _p（toBeInstalled 机制）→ 测试必须模拟 install（createApp({}).use(pinia)）**；**M3 四端工厂**：createWebPinia/createMpPinia/createAppPinia/createSsrPinia（setPlatform 注入 + 各端持久化后端）+ examples/stores/player.ts（四端共用 + persisted 声明）改造 pinia-demo；**M4 SSR**：createSsrPinia 每请求独立 + 并发隔离测试 + hydration 顺序（先 state.value 再 use）+ examples/ssr/ 参考实现；**M5 DevTools**：pinia 原生 Vue DevTools（无需代码）+ createDevtoolsPlugin（$onAction/$subscribe → [pinia] trace，__PROTEUS_DEBUG__ 开关）+ registerStoreSnapshot（__PROTEUS_STORES__ 快照，微信开发者工具可调）；**M6 测试+迁移**：storage.contract（一条契约三端跑，★踩坑：clear(prefix) 各端 key 空间不同（Memory 无前缀）→ 契约只用无参 clear + prefix 定向由专测）+ stores-player 纯逻辑（原生 createPinia）+ cross-platform 矩阵（同一组用例 × web/mp/ssr）+ stores-purity（grep 禁平台分支）+ docs/pinia-migration.md（≤10 行接入）+ examples/migration-from-vue 对照；**其他踩坑**：测试环境缺浏览器全局（web-adapter 顶层 location）→ jsdom per-file + 不能全局 polyfill（window 会让 sass 走浏览器初始化崩）；runtime 包 peer pinia + build external pinia；原生 store 桥（src/runtime/store）保留（轻量场景，与 Pinia 层共存）；验证 vue-tsc 零错 + 275 测试（+33：storage 11 + contract 12 + persistence 9 + platforms 6 + ssr 3 + devtools 4 + stores 4 + cross-platform 3 + purity 1）+ 双端构建；docs/proteus-pinia-plan 09 进度表 M1-M6 ✅；剩余 M7/M8（超级应用：分片/配额/迁移/同步/可观测）按需追加
107. **pinia-plan M7-M8 完整实现（★超级应用可靠性 + 协同可观测，决策 #106 追加）**：**M7.1 分片**（eager 默认/lazy 惰性 $hydrated/$hydrate——★踩坑：Pinia store 是 reactive proxy，挂 ref 自动解包 → $hydrated 对外即布尔、响应性由闭包 ref 保证）+ keys 只恢复指定字段；**M7.2 调度器**（PersistScheduler：防抖/maxWait 强制落盘/高频合并/flush 串行链 + onAfterFlush 钩子——配额检查挂写盘后保证账本一致）；**M7.3 配额**（QuotaManager 账本式体积追踪 + protected 优先淘汰 + 单 key 超限直抛 + QuotaExceededError；★踩坑：淘汰若依赖 adapter.keys 实时枚举会因调度器缓冲未落盘而失效 → 自维护 ledger 账本）；**M7.4 迁移**（version + migrations 链式 1→3 = 1→2→3 + 失败丢数据走初始值 + 数据更新降级丢弃；★版本化仅在 version>0 包裹 state 兼容 M2 契约）；**M7.5 生命周期**（scope:page 注册表 + disposePageStores 批量 $dispose；★$dispose 不清 state 属性，编排以注册表为准）；**M7.6 敏感字段**（volatile 不落盘 / encrypted 加密存储——默认轻量混淆（XOR+base64）文档注明生产换平台安全存储 + Encrypted type tag）；**M8.1 协同引擎**（新包 @proteus-vue/pinia-sync：LWW store 级时间戳 + clientId 字典序兜底 + 离线缓冲重放 + excluded 字段跳过 + CRDT 占位降级；★踩坑：$subscribe 默认 'pre' 异步 flush 导致 applying 回声标志失效 → flush:'sync'；mutation.key 对直接赋值是 undefined → 协同粒度统一 store 级）；**M8.2 快照/时间旅行**（capture/restore/take/timeTravel，生产默认关 enableInProd 开）；**M8.3 状态埋点**（createStateTracer：filter 默认空不上报 + 采样 + 批量节流 + volatile/encrypted 从 trace 剔除——★整 store 变更净化敏感字段）；**M8.4 类型注册表 + AI 规范 + CI 门禁**（examples/stores/registry.ts：StoresRegistry + useStore<K> 编译期校验 useStore('typo') 报错 + docs/pinia-stores-conventions.md 铁律/禁写清单 + CI grep 门禁）；验证 vue-tsc 零错 + 310 测试（+35：m7-sharding-scheduler 7 + m7-quota-migrate 10 + m7-scope-secure 7 + sync 5 + m8-snapshot-tracer 6）+ 双端构建 + pinia-sync 包构建 + workspace 链接（8 包）；docs/proteus-pinia-plan 09 进度表全 ✅；**pinia-plan 全部完成（M1-M8）**
108. **底线审计 + 整改（★AI-native 透明框架自我检视，用户发起）**：审计结论——四支柱（规则即文档 57 条 / 防漂移 / 决策 trace / 底线三循环）骨架健康，五缺口：① 注册表无 apply() 分派层（底线 ① 半自动）② 实现↔注册表漂移无 CI 门禁（靠自律）③ 路由层转换无规则登记 + --trace-router 未闭环 ④ Web mapRouteType 隐性转换 ⑤ 文档规则数滞后（55/49 vs 57）；整改：**P0 分派层**（types 加 RuleContext/RuleApplier/TransformRule.apply + registry.executeRule(id, ctx)——未登记 apply 抛错提示 source；style/px-to-rpx 与 template/scope-attr 登记 apply 示范 + style.ts/template.ts 调用点改走 executeRule；★验证：覆盖规则 apply（px→vw、scoped 属性改名）→ 编译输出即时变化 + 恢复后原行为不变——底线循环 ① 完全形态，tests/compiler-dispatch.test.ts 7 用例）；**P1 漂移门禁 + 路由透明化**（tests/registry-drift.test.ts：grep 实现文件（template/script/style/validate/overrides）规则 ID 字面量 → 全部可解析 + 无孤儿 implemented 规则（豁免 validate/tag/semantic——表驱动由 transforms.test 映射覆盖守护）；packages/router/src/rules.ts 路由生成规则注册表 7 条 AI 说明书（scan/schema/path-derive/parent-explicit/meta-merge/unique-check/derive-name）+ buildRouteTree/runGenRoutes 加 trace 参数（--trace-router 闭环：来源登记/显式 parent/path 推导/根 决策链输出），tests/router-rules.test.ts 4 用例）；**P2 文档同步**（README 55→57 + 分派层、compiler/README 49→57 + executeRule、docs/compiler.md 57 + 漂移门禁 + 分派层已落地；Web mapRouteType 转场映射确认已在 routing.md 表格透明化 + 实现位置标注，无需改动）；验证 vue-tsc 零错 + 323 测试（+10：dispatch 7 + drift 2 + router-rules 4 - 1 并入）+ 双端构建；遗留：分派层仅示范规则 apply 化（其余规则仍描述层，AI 可逐个登记）、gen-routes 与 router 包 scan/tree 双管线并存（新旧两套路由推导，后续统一）
109. **路由规划 M3/M4 codegen（★scan/tree 透明化管线消费端落地，决策 #108 遗留推进）**：确认 M1/M2 的 scan/schema/tree/merge 此前仅被 tests 使用（未接入构建链路，透明化地基悬空）→ 落地消费端：**三端共享转场映射** packages/router/src/transforms/transform-transition.ts（WEB_TRANSITION_MAP/MP_ROUTE_TYPE_MAP/webTransitionName/mpRouteType/isTransition——RouterView.vue 的 mapRouteType 私有硬编码改为框架共享表 webTransitionName，透明化 P2 遗留闭环）；**M3 Web codegen** codegen/web.ts（generateWebRoutes：RouteNode[] → vue-router RouteRecordRaw 代码，lazy → () => import()、children 递归、meta.transition 不直映射（RouterTransition 消费）、产物头 AUTO-GENERATED 标注来源）；**M4 MP codegen** codegen/mp.ts（generateMpConfig/flattenNodes/toPageConfig：Skyline MPA 无原生嵌套 → children 平铺 + meta.__parent 降级保留父链、routeType 映射、mergeAppJson 合并策略 <route> > 用户手写 > 默认）；router 包 index 导出 codegen/transforms；rules.ts 补 3 条规则（route/web-codegen、route/mp-codegen、route/transition-map，路由规则注册表 7→10 条）；测试 tests/router-codegen.test.ts 6 用例（lazy 代码分割/嵌套递归/平铺降级/routeType 全枚举/合并保用户字段/共享映射）；验证 vue-tsc 零错 + 329 测试（+6）+ 双端构建 + router 包构建；docs/proteus-router-plan 09 批次表 B1-B4 ✅（B5 App codegen 待 v0.6）；遗留：gen-routes 与 scan/tree+codegen 双管线并存（新管线完整可用、旧管线兼容既有产物；深度统一需 <route> path 语义决策 + examples 零改动迁移策略，规划 M1 验收项，后续推进）
110. **路由规划 B6（M6 守卫 + tabBar + redirect，★决策 #109 后续批次）**：**守卫实例 API**（Router.beforeEach/afterEach delegate 到 guards 模块级注册表——三端 API 一致；guards 加 clearGuards 测试隔离 + runBeforeEach/runAfterEach 加 trace 参数）；**守卫 trace**（--trace-router：push 内 __PROTEUS_DEBUG__ 注入时输出 [guard] 放行/被拦截 决策链，对齐 runGenRoutes 的 [route] trace）；**redirect 跨端**（codegen/web 的 nodeToRecord 映射 redirect 字段（vue-router 原生）；codegen/mp 的 toPageConfig 加 redirect 标注（小程序无原生 redirect，运行时 onLoad redirectTo 模拟，M6 §5））；**tabBar config 驱动**（ProteusConfig.router.tabBar 类型（唯一声明源，M6 §3）+ generateMpConfig 加 tabBar 参数 → app.json.tabBar（pagePath 匹配路由 name）+ 超 5 项告警降级（小程序原生上限，M6 §3.3）；现有 meta.isTab 兼容保留）；**踩坑**：① 守卫是模块级注册表，测试跨用例累积（clearGuards 测试隔离）② 守卫 trace 依赖 __PROTEUS_DEBUG__（测试注入全局 + 清理）③ codegen/mp 对象展开（MP 产物安全改显式赋值）④ MP tabBar 的 icon → iconPath 字段映射；验证 vue-tsc 零错 + 336 测试（+7 guards-tabbar）+ 双端构建 + router 包构建；docs/proteus-router-plan 09 批次表 B6 ✅（B5 App codegen 待 v0.6、B7 测试/迁移待办）；路由规则注册表 10→14 条
111. **路由规划 B7（M6 测试套件 + 迁移工具，★决策 #110 后续批次，企业级路径收官）**：**L2 集成快照**（tests/router-codegen.integration.test.ts：fixtures → scanRoutes → buildRouteTree → generateWebRoutes/generateMpConfig 快照（toMatchFileSnapshot，可审计产物契约）；★踩坑：codegen/web 逗号逻辑（record 行自带尾逗号 + join 加逗号 → 双逗号 `],,`——改「每行自带尾逗号 + join('\n')，数组元素间 join(',\n')」快照干净））；**L4 跨端矩阵**（tests/router-platform-matrix.test.ts：transition 枚举全覆盖 × Web/MP/App 映射一致 + Web/MP 共享表键集一致防漂移）；**路由纯净性门禁**（tests/router-purity.test.ts：packages/router/src 下非 skyline.ts/presets//codegen/ 不含 wx./window./localStorage/document.——★门禁实战抓到 presets 平台实现 + 注释 wx. 字样，豁免/修正）；**迁移工具 router:check**（cli 包：checkRoutes(pagesDir) 纯函数（scanRoutes 严格校验 loc + buildRouteTree trace）+ parseRouterCheckArgs + CLI 子命令；★router 包改多入口构建（index/scan/tree/rules/schema/merge/transform-transition/codegen 各出 js——此前只有 d.ts，cli bundle 无法解析运行时）+ exports 补 import 字段；实测：合规 `router:check ✓ 4 个页面` + 嵌套 trace；非法 `✗ 文件:行:列 精确报错`）；验证 vue-tsc 零错 + 349 测试（+13：integration 2 + purity 1 + matrix 7 + router-check 3）+ 双端构建 + cli/router 包构建；docs/proteus-router-plan 09 批次表 B1-B7 ✅（企业级路径全部完成，B5 App 待 v0.6）；遗留：双管线深度统一（gen-routes 旧执行路径 vs scan/tree+codegen 新管线，需 <route> path 语义决策 + examples 零改动迁移）
112. **双管线深度统一（★gen-routes 走 scan/tree 管线，<route> 是路由唯一真相源；用户发起：双管线统一 + meta 冗余优化）**：**scan 扩展 derivePath**（scanRoutes(rootDir, options：verbose/derivePath)——path 缺省从文件位置推导（pages/home.vue → pages/home，index.vue 归并目录 pages/user/index → pages/user）+ 同步推导 name（kebab-case deriveNameFromFile：user-profile / index 归并目录名）；schema 在 derivePath 模式放宽：path 允许无斜杠（产物路径语义）+ name 允许 kebab-case（NAME_KEBAB_RE，对齐既有产物命名）；RouteBlock 扩展 pageJson/customRouteKeyName（gen-routes 特有扩展进统一管线，schema 校验提取）；**gen-routes 重构**：parseRouteBlock（正则）删除 → scanRoutes（schema 校验 + loc + derivePath）；buildRoutes 父子推导（同目录 index.vue 逻辑）删除 → buildRouteTree（path 前缀推导 + 显式 parent + trace）；行为变化：坏 <route> JSON 从「警告跳过」→「严格报错（含 loc）」（透明化，gen-routes badjson 测试更新）；**★踩坑**：① findParentByPath 硬编码 `/` 前缀（M1 的 /home 语义）不匹配无斜杠推导 path（pages/user/profile → prefix '/pages' ≠ 'pages'）→ 自适应匹配两种语义 ② derivePath 推导相对 rootDir（pagesDir）而 gen-routes relSrc 相对 APP_DIR——RouteBlock.path（相对 pagesDir）与产物 path（相对 appDir）两基准并存，buildRoutes 构造 tree blocks 用 relSrc（与产物一致）③ 规则表重复登记（route/derive-name 原已有，误加重复 → Set.size 校验抓到）；**meta 单一真相源**：docs/routing.md 补「单一真相源（决策 #112）」章节——路由声明只写页面 <route>（meta + 可选 path/name/parent/redirect/params），auto-routes/app.json 是生成物（AUTO-GENERATED 勿手编），不存在第二处手写 meta；规则注册表补 route/derive-path（15 条）；产物变化：user（pages/user/index）新增 parent: "index"（新管线文件层级推导更一致，运行时不受影响）；验证 vue-tsc 零错 + 349 测试 + 双端构建 + 模板快照；遗留：scan RouteBlock.path 与 gen-routes 产物 path 双基准（相对 pagesDir vs appDir），后续可统一为单一基准
113. **路由零样板（★<route> 块完全可选 + 集中式 meta；用户痛点：上百页面逐页写 <route> 是灾难）**：**scan includeNoRoute**（无 <route> 块页面也收录——derivePath 推导 path/name + meta 空 + loc 文件头；不搭配 derivePath 报错提示）；**gen-routes 集中 meta**（config.router.meta：按页面路径键（pagesDir 相对去扩展名）配置，目录前缀匹配 user → user 下全部页面；优先级：页面 <route>.meta > 精确路径 > 目录前缀（最长匹配）> 默认；★精确路径=目录级的细化（mergeMeta 精确胜，非取代）——resolveConfigMeta 先取目录基底再精确覆盖）；gen-routes 全页面收录（includeNoRoute: true）；**★踩坑：router-rules.test 的 runGenRoutes 用真实 cwd 写产物（污染 examples/router/auto-routes.ts——缺分包 config 覆盖产物丢 order-pages-list）→ 改临时目录 mkdtemp 隔离**；规则注册表补 route/config-meta（16 条）；docs/routing.md 补「<route> 完全可选 + config router.meta 集中配置」；验证 vue-tsc 零错 + 350 测试（+1 集中 meta 用例）+ 双端构建；产物修复：order-pages-list（分包）恢复
114. **示例工程完整工程化（★examples 自包含；用户诉求：examples 里要有 proteus.config/vite.config/tsconfig/package.json，不应挂在仓库根）**：**examples 自包含**（新增 examples/{proteus.config.ts（pagesDir 'pages'、集中 meta 演示 user 目录 requiresAuth/transition）、vite.config.ts（@proteus-vue/* alias → ../packages/src + mpTransform({config}) + mp-entry-stub）、tsconfig.json（独立 paths → ../packages + shims）、package.json（完整工程 deps @proteus-vue/*+vue+pinia + scripts）、index.html（入口 /main.ts）、scripts/{gen-routes.ts 薄壳、mp-entry-stub.ts、bundle-report.ts（自根移入）}、shims/（自 shared 复制））；**根瘦身**：删根 vite.config.ts/proteus.config.ts/index.html/packages/plugin-vite/src/cli.ts（读根 config 的入口，examples/模板改用薄壳）；根 package.json 脚本 npm --prefix examples 转发；根 tsconfig 移除 examples include（examples 独立 vue-tsc，CI 加步骤）；snapshot-template index.html 复制源改 examples（入口 /main.ts → /src/main.ts）；**★踩坑**：① tsconfig paths 相对层级（examples 到 proteus 一层 ../——写成 ../../ 解析到 debug/packages 失败 → 回退 node_modules dist 旧类型（meta 报错线索））② cli.ts import '../../../proteus.config.ts' 根 config 删除后挂 → 删 cli.ts 改薄壳 ③ bundle-report 随 examples（import ../proteus.config 相对 examples）④ 集中 meta 演示：user 目录页 meta 合并 requiresAuth/transition（页面 <route> title + config 目录级叠加，产物验证）；验证 vue-tsc 零错（根 + examples 独立）+ 350 测试 + 双端构建（根转发 examples）+ 模板快照；遗留：根 README/docs 目录结构描述待同步（下次文档批次）
115. **示例工程改真实 npm 包使用方式（★用户诉求：examples 完全用真实 npm 包方式，而非源码 alias）**：examples/vite.config.ts + tsconfig.json 删除 @proteus-vue/{router,runtime,shared,plugin-vite,compiler} 的源码 alias/paths → 走 workspace 链接的 npm 包 dist（与 create-proteus 生成工程一致：main/exports/types 子路径解析）；仅 @proteus-vue/components 保留 alias（组件库未拆包，v2.0 方向）；**★踩坑**：① plugin-vite 包构建依赖 @proteus-vue/router 的 dist 类型（scan.d.ts 需最新——workspaces 构建不保证拓扑序，先 build router 再 plugin-vite）② examples→proteus/src 层级是 ../src（写成 ../../src 解析到 debug/src 失败 → 回退 node_modules 找不到 @proteus-vue/components）③ tsc 无 -p 时不用 tsconfig（Node10 默认，paths 不生效——误导调试）；验证：examples vue-tsc 零错（@proteus-vue/* 从 dist types）+ build:web/build:mp（预设注册）+ 根 vue-tsc + 350 测试；遗留：npm registry 发布后 examples 可切真实 registry 包（当前 workspace 链接等价验证）
116. **vue-compat 修复（★Vue 能力兼容，用户审视后落地；docs/vue-compat-plan.md 三批）**：评估实测——主路径覆盖高但 4 类**静默失败**违反反黑盒（零警告产物坏）：动态组件 `<component :is>` 无效标签、自定义指令剥离无痕、模板 ref 永不绑定、import 剥离（单文件无模块系统，跨模块引用 undefined 且 script 可空壳）；**Batch A 反黑盒**：template.ts 加平台无对等警告（directive/custom——switch default 区分内置 v-slot/v-pre/v-cloak 与自定义指令；template/is-component、template/template-ref、template/no-peer——transition/teleport/suspense/keep-alive）+ script.ts import 剥离警告（★跳过 `from 'vue'`——Vue API 由编译器静态处理，golden fixtures 的 ref import 误报修复）+ 函数调用初始化警告文案 actionable（Batch C）；6 新规则登记；**Batch B 内联事件表达式**：tryInlineHandler（count++/--/++/-- 前后缀 → proteusInlineInc/DecXxx + setData(this.data.x±1，对齐 ref 重写；fn(1)/fn('a') 简单方法调用无 . 链 → proteusInlineFnXxx + this.fn(...)）+ script 生成包装方法（复用 .self/.once 机制 + pushMapped sourcemap）+ 规则 event/inline-expression（可 disabled）；★TDZ 踩坑：inline 块引用 isSelf 在声明前（移到声明后）；**Batch C 收尾**：:class 数组对象简写 `{ on }`（键即值）+ 跨模块初始化警告补「函数调用/跨模块引用 + store 桥提示」；★golden 回归踩坑：import 警告误报 `from 'vue'`（内置 API 不算跨模块）——跳过 vue；★registry-drift：trace.add 的 ID 与登记不一致（node/template-ref vs template/template-ref）→ 统一；验证 vue-tsc 零错 + 364 测试（+14 vue-compat）+ 双端构建；docs/vue-compat-plan.md 批次表 A/B/C ✅；规则注册表 57→64 条；遗留：provide/inject、作用域插槽、Transition 运行时等价（编译期已警告，运行期为后续）
117. **vue-compat-advance 三批（★三大运行期能力，docs/vue-compat-advance.md；基线 367 测试 26f98d2）**：**Batch 1 作用域插槽编译期警告**（8f79512）：`<slot :item>` → 显式警告「父侧拿不到子数据，请用 props+事件回调」，规则 slot/scoped-slot；**Batch 2 `<transition>` 装饰式进入动画**（5821678）：子元素注入 `class="proteus-transition-{name}"`（name 缺省 fade）+ 按需注入 keyframes（fade/slide-up/scale；`usesTransition` 标记 → 仅使用 transition 的页面注入，golden 不破坏）+ 规则 transition/component、transition/animation-wxss；transition-group/teleport/suspense 保留警告；★registry-drift 正则补 slot|transition 命名空间（`\\/` 写法）；**Batch 3 provide/inject 页面级注入桥**（3c6ead6）：运行时桥 runtime/src/provide-inject.ts 统一挂 `getApp().__proteusProvides`（registerProvide/readInject/clearProvides/provideCount，与编译产物共享同一存储，非 MP 环境回退 globalThis）；编译 script.ts：extractProvideInject（零缩进顶层单行提取，inject 支持默认值）+ buildProvideInject（页面 onLoad 单函数合并块 registry 一次 + provide + inject；组件 provide 放 created 先于子组件 attached、inject 放 attached）+ provide 值表达式重写裸 ref/ref.value → this.data（含 computed 名）+ inject 默认 ES5 三元；extractData 对 inject() 跳过「跨模块引用」误导警告；★踩坑：默认 onLoad 路径 initLines 多行元素仅首行缩进（immediateWatch/provide 块错位）→ 逐行缩进修复；产物直接读写 getApp().__proteusProvides（MP 单文件无模块系统，不 import 运行时）——手写运行时路径与编译产物共用一表；规则 script/provide-inject（67 条）；测试 +6 → 377 全绿；demo：examples/pages/provide-inject-demo.vue + components/inject-consumer（页面 provide → 组件 inject + 默认值，双端构建验证）；遗留：作用域插槽运行时等价、Transition 离开动画、provide/inject 响应式联动（ref 引用）与页面级隔离（pageId）——当前值快照 + 全局注册表
119. **vue-compat-advance Batch 4：provide/inject 响应式联动（★docs/vue-compat-advance.md §6，382 测试）**：**语义对齐 Vue**——`provide("k", refName)` 传 ref → 响应式联动；`.value`/字面量 → 静态快照（对齐 Vue：传 ref 联动、传值快照）；**运行时桥**（runtime/src/provide-inject.ts）：注册表加 `__subs` 保留键（订阅 `{k, fn}[]`）+ `subscribeProvide(key, cb)`（幂等取消）/ `notifyProvide(key)`；provideCount 过滤保留键；结构向后兼容 Batch 3（值仍直接存 provides[key]）；**编译**（script.ts）：buildProvideInject 裸 ref 提供 → provideRefs（ref→key）+ `__subs[key]` 初始化，inject 块追加订阅（`const __self = this` + 每 key 守卫块仅订阅已初始化 __subs 的 key，`__proteusSubs` 记录）；rewriteRefAccess 新参 providedRefs：ref 写入点（赋值 + 4 种自增/自减）追加 `this.proteusSyncProvide("key","ref")`；生成 proteusSyncProvide（p[key]=this.data[ref] + 遍历 __subs 通知，ES5 索引循环）/ proteusUnsubscribeProvide（indexOf/splice 移除）；挂载：组件 inject → detached 取消，页面 inject → 显式 onUnload 前置 / 无则生成承载 onUnload（★组件模式不生成 onUnload，生命周期差异）；demo 改裸 ref 联动 + 切换按钮；验证产物：onLoad __subs 初始化 + changeUser 写入点 proteusSyncProvide + 组件 attached 订阅/detached 取消；测试 +5 → 382 全绿 + 双端构建；遗留：作用域插槽运行时等价、Transition 离开动画、provide/inject 页面级隔离（pageId）
120. **vue-compat-advance Batch 5：Transition 离开动画（★docs/vue-compat-advance.md §7，386 测试）**：**模板**（template.ts）——transition 直接子元素 v-if 为裸 ref 名时启用：v-if 改写 `wx:if="{{__tv{i}}}"`（显示状态，初始 = ref 初始值）+ class 插值 `{{__tl{i} ? '...-leave' : ''}}`（离开中切换 leave 动画 class），收集 ctx.transitions（ref/tName/index）传 script；复杂表达式/多子元素保持 Batch 2 现状；trace 规则 transition/leave-state（★registry-drift 门禁抓到未登记 → 补 AI 说明书，transition/component 描述同步更新为实际装饰式实现）；**样式**（style.ts）——TRANSITION_WXSS 补 leave class + keyframes（fade-out 0.25s / slide-up-out 0.32s / scale-out 0.4s，forwards 保持末帧）；**脚本**（script.ts）——data 注入 `__tv{i}`（初始 = v-if ref 初始值）/`__tl{i}: false` + 生成 `proteusTransitionToggle{i}()`（on true → clearTimeout + 恢复 __tv=true/__tl=false；on false → __tl=true 播离开动画 + setTimeout（fade 250/slide-up 320/scale 400 对齐 keyframes）后 __tv=false 延迟移除；定时器 id 存实例属性 __tlTimer{i} 防重/可取消）+ rewriteRefAccess 新参 transitionToggle（ref 写入点追加 `; this.proteusTransitionToggle{i}()`，与 provideSync 并存）；**★gen-routes 修复**：`<transition>` 被误判为自定义组件警告（编译器已消费、产物不输出）→ NATIVE_MP_TAGS 白名单加 transition；**验证**：测试 +4 → 386 全绿 + 双端构建；demo：forms.vue 过渡卡片（toggleCard 切换，产物确认 wx:if __tv0 + class 插值 + toggle 方法 + wxss leave scoped 选择器匹配）；遗留：作用域插槽运行时等价、provide/inject 页面级隔离（pageId）
121. **vue-compat-advance Batch 6：provide/inject 页面级隔离（★docs/vue-compat-advance.md §8，390 测试）**：**注册表结构升级**（runtime/provide-inject.ts）——`__proteusProvides = { [pageId]: { key: value, __subs: {...} }, __seq: n }`；新增 `nextPageId()`（__seq 递增生成 'pN'）/ `destroyPage(pageId)`；registerProvide/readInject/subscribeProvide/notifyProvide 加可选 pageId（缺省从 getCurrentPages 栈顶 `__proteusPageId` 推导，无则回退 'global' 命名空间）；provideCount 遍历全部命名空间；**编译**（script.ts）——页面 onLoad 打开段：`__reg.__seq = (__reg.__seq || 0) + 1` → `this.__proteusPageId = 'p' + __reg.__seq` → `const provides = (__reg[pid] || (__reg[pid] = {}))`（提供/注入/订阅都落页面命名空间）；组件 created/attached 打开段：`getCurrentPages()` 栈顶页面 `__proteusPageId`（组件渲染期间栈顶 = 所属页面）→ `this.__proteusPageId = __pid || 'global'` → 同命名空间（跨页隔离）；proteusSyncProvide/proteusUnsubscribeProvide 按 `this.__proteusPageId` 定位；onUnload：有 provide 或 inject 时注入 `delete __reg[this.__proteusPageId]`（页面销毁清理防泄漏，与订阅取消同处）；★结构破坏性变更仅限编译产物（每次构建重新生成，无跨构建持久状态），Batch 3/4 测试断言同步更新（provides 顶层声明行 → 页面命名空间打开段）；验证：测试 +4 → 390 全绿 + 双端构建；demo 产物确认（页面 __seq/pageId/命名空间 + onUnload 清理；组件栈顶解析）；遗留：作用域插槽运行时等价
123. **module-plan B1：模块契约（★@proteus-vue/module 新包 + CLI module:check，405 测试）**：**包**（packages/module）——`defineModule(config)`（编译期校验，不合法当场抛错含字段+原因）+ `validateModuleConfig`（纯校验：name kebab-case 必填 / version semver 必填 / dependencies key+range+★自环报错 / exports 相对路径 / preload 未声明依赖警告 / capabilities/lifecycle 结构）+ `scanModuleConfigs`（递归扫描 proteus-module.config.ts + 校验汇总 + ★重名检测全局唯一铁律）+ `formatModuleCheck`（CLI 输出）；**CLI**——`proteus module:check [dir]`（扫描 + 校验 + 退出码；checkModuleConfigs 纯函数可测）；**★踩坑三连**：① vitest 无法解析新包 → vitest.config alias 补 @proteus-vue/module；② fixture 配置加载——esbuild bundle 后 eval 撞 ESM require（"No exports main defined"）→ 改 transform（不 bundle）+ 删 require 行 + `import_module.defineModule` 前缀替换 + defineModule 由 Function 参数注入（零外部依赖加载，与 validate 同源）；③ module 包 bundle 把 esbuild 打进 ESM 产物（CJS require 失效）→ external:esbuild + esbuild 移 dependencies，CLI external @proteus-vue/module + esbuild；**验证**：测试 +9（module-contract.test：合法/缺字段/非法值/自环/preload 警告/defineModule 抛错 + 扫描汇总/重名/跳过目录）→ 405 全绿 + verify（双端构建 + workspace 构建）；demo：examples/proteus-module.config.ts（app 模块：types/events 公共契约）+ examples/types.ts + events.ts；CLI 实测 `module:check examples` → 1/1 通过；**遗留**：B2 编排器 / B3 依赖图谱（环检测）全 ⬜
124. **module-plan B3：依赖图谱（★packages/module/src/graph.ts + CLI --graph，415 测试）**：**DependencyGraph 类**——addModule/addDependency/fromConfigs；`detectCycles`（DFS 三色标记，去重 + 按长度排序，返回完整环路径）；`topologicalSort`（Kahn，★被依赖者先 init：入度 = 依赖数 + 反向索引 dependents——初版方向反了（依赖者先出）→ 修正）；`chunkGroups`（chunk 分组，未声明归入自身名）+ `chunkOf`/`dependenciesOf`；`versionConflicts`（同一模块被不同 range 声明 → 检出，精确交集为后续）；**CycleError**（环路径 + Suggestion，透明化）；`buildModuleGraphManifest`（module-graph.json：modules + chunks + initOrder）；`moduleGraphToMermaid`（Mermaid 输出，有环 → 说明文本不崩）；**CLI**——module:check 集成：环检测（环 → 退出码 1）+ 版本冲突警告 + 拓扑序汇总行 + `--graph`（Mermaid 追加）；scan entry 保留 dependencies/chunk（图构建输入）；**验证**：测试 +10（module-graph.test：DAG 拓扑序/简单环/复杂环/自环/chunk+manifest/版本冲突/100 模块链 <100ms/Mermaid + CLI 环集成/--graph）→ 415 全绿 + verify；CLI 实测 `module:check examples --graph` → 拓扑序 app；**遗留**：B2 编排器 / B4 Web 打包 / B5 Skyline 分包（消费 chunk/initOrder）全 ⬜
125. **module-plan B5：Skyline 分包（★gen-routes 集成分包依赖 + preloadRule，417 测试）**：**gen-routes**（plugin-vite）——`GenRoutesOptions.moduleConfigs`（@proteus-vue/module 扫描产物：name/chunk/dependencies/preload）；runGenRoutes 构建 moduleChunks（模块→chunk）+ subPackageModules（分包名↔模块 chunk/name 匹配，chunk 未分包 = 主包模块）；**app.json 生成**：`subPackages[].dependencies`（模块 dependencies 中其他分包，微信非独立分包字段；主包模块不产生）+ `preloadRule`（分包模块 preload 引用其他分包 → 分包入口页 `{ network: 'all', packages: [...] }`）；★校验：模块依赖引用未知模块（不在模块契约）→ 警告（透明化反黑盒）；**examples/scripts/gen-routes.ts**——async 扫描模块契约（scanModuleConfigs）→ 传入 runGenRoutes（构建输出 `[gen-routes] 模块契约 N 个`）；scan entry 补 preload 字段；plugin-vite 加 @proteus-vue/module 依赖；**CLI module:check** 加分包映射行（模块 → chunk，提示与 config.subPackages 匹配生成 dependencies/preloadRule）；★页面归属仍靠目录（subPackages[].root），Router M7.1 的 <route> chunk 字段未实现（页面→模块映射为后续）；模块桶 module.js 骨架待 B2 编排器后；**验证**：测试 +2（gen-routes.test：分包依赖+preloadRule 生成 / 未知依赖警告——fixture 页面名踩路由命名规则改小写）→ 417 全绿 + verify（examples 构建输出模块契约扫描）；**遗留**：B2 编排器 / B4 Web 打包 / B6 App 模块 / B7-B9 全 ⬜
126. **module-plan B2：运行时编排器（★packages/module/src/orchestrator.ts，425 测试）**：**createModuleSystem({ modules })**——注册（重复注册报错）→ **resolve 先于拓扑**（★依赖完整性：未注册依赖当场报错；版本协商：`satisfies(version, range)` 实现 ^/~精确/范围语义，不匹配抛 VersionMismatchError 含冲突链）→ B3 拓扑序 init（被依赖者先；环 → CycleError）→ ready/active/inactive/destroy（destroy 逆拓扑序）；**ModuleInstance**——name/version/state/services（工厂懒创建，init 时实例化）/events（createModuleEventBus on/emit/取消）；**★踩坑**：① topologicalSort 对悬挂依赖（依赖未注册模块）静默返回空 → resolve 检查提前到拓扑前 + 图不完整防御抛错（透明化）；② 版本协商原本在拓扑后 → 未注册依赖检查轮不到，重构为 resolve 先于拓扑；**单例保证**：工厂不自动挂载（纯函数可测），调用方（main.mp.ts）挂 getApp().moduleSystem——页面切换不重建；**验证**：测试 +8（module-orchestrator.test：拓扑序 init/ready 顺序、services 懒创建+getModule、active/inactive、destroy 逆序、重复注册/未注册依赖/环抛错、satisfies ^/~/精确/范围矩阵、版本冲突、事件总线）→ 425 全绿 + verify；**遗留**：B4 Web 打包 / B6 App 模块 / B7-B9 全 ⬜（模块桶 module.js 骨架可在 B2 后落地——分包根生成 createModuleSystem 挂载产物）
127. **module-plan B4：Web 打包（★packages/module/src/web-codegen.ts，428 测试）**：**generateRollupOptions(graph, opts)**——模块图谱（B3 chunkGroups）→ Rollup manualChunks：id 匹配 `/<modulesDir>/<moduleName>/` 或 `/<modulesDir>/<moduleName>.<ext>` → 所属 chunk（默认 modulesDir 'modules'，可配）；输出文件名模板 `assets/js/[name]-[hash].js`；不匹配 → undefined（Rollup 默认：vue/pinia 共享依赖自动提取 vendor）；**examples/vite.config.ts**——defineConfig 改 async（扫描模块契约 → DependencyGraph → web 模式合并 generateRollupOptions；MP 模式不受影响）；★无副作用：examples app 模块无 modules/ 目录 → manualChunks 不匹配任何 id → 零影响（未来有模块目录自动生效）；**CLI module:check** 输出 Web chunk 行（chunk ← 模块）；**★动态加载转换**（`ms.getModule('trade')` → `import('./modules/trade/module')`）与 modulepreload 生成为后续（真实模块化业务落地时）；**验证**：测试 +3（module-web-codegen.test：模块目录/单文件 → chunk、非模块/共享依赖 → undefined、自定义 modulesDir+文件名模板）→ 428 全绿 + verify（双端构建，web 构建含 webRollup 合并）；**遗留**：B6 App 模块 / B7-B9 全 ⬜
129. **module-plan B7a：分包体积监控（★M7.6，432 测试）**：**examples/scripts/bundle-report.ts 扩展**——`scanSubPackages(outDir, roots)`（各分包体积，纯函数）+ `SUBPACKAGE_LIMITS`（单分包 >1.5MB warn / >2MB error（微信硬限）/ 总分包 >16MB error）+ `checkSubPackageLimits`（超硬限 → error + 退出码 1；超阈值 → warn）+ `formatBundleReport` 增分包体积行（阈值标记 ❌超限/⚠超阈值）；main() 集成（构建链尾输出分包报告 + 门禁）；**验证**：测试 +4（bundle-report.test：分包扫描/报告阈值标记/单分包硬限阻断/总分包 16MB 阻断）→ 432 全绿 + verify（build:mp 输出分包体积行）；**B7 剩余子项**（M7.1 编译期环硬卡已在 B3 CLI 退出码覆盖 + gen-routes 未知依赖警告；M7.2 共享依赖去重检测 / M7.3 懒加载 loadModule / M7.4 沙箱 / M7.5 内存守护）拆批待续；**遗留**：B6 App 模块 / B7b-d / B8-B9 全 ⬜
130. **module-plan B7b：共享依赖去重检测（★M7.2，436 测试）**：**CLI `module:duplicates [distDir]`**（packages/cli/src/module-duplicates.ts）——`readSubPackageRoots`（读 dist/mp-weixin/app.json 的 subPackages[].root，产物自描述零配置）+ `scanDuplicateModules`（分包目录四件套 hash（md5）去重，同 hash ≥2 分包 → 检出）+ `formatDuplicateReport`（重复清单 + 建议移入主包；零重复输出 B0 天然去重说明）；退出码 1（有重复）；★架构确认：B0 共享模块编译到主包 + require 同路径天然去重 → 分包间重复风险已被架构规避（examples 实测零重复）；**验证**：测试 +4（module-duplicates.test：app.json roots 读取 / 相同内容 ≥2 分包检出 / 不同内容单分包零重复 / 报告渲染）→ 436 全绿 + verify + CLI 实测 examples 零重复；**B7 剩余**：M7.3 懒加载 loadModule / M7.4 沙箱 / M7.5 内存守护拆批待续；**遗留**：B6 App 模块 / B7c-d / B8-B9 全 ⬜
131. **module-plan B7c：懒加载 loadModule（★M7.3 简化版，439 测试）**：**编排器**（orchestrator.ts）——重构 `createInstance(def)`（初始注册 + 懒加载共用）；`loadModule(def)`：已注册返回已有实例（幂等）→ 依赖须已注册（否则报错）+ 版本协商（VersionMismatchError）→ 注册 + init（钩子执行 + state ready）；★`modules()` 改 defs.keys()（含懒加载模块）、`destroy()` 改逆拓扑序（初始）+ 逆注册序（懒加载追加）——初版 destroy 用纯逆注册序破坏逆拓扑语义（destroy:trade 应在 user 前）→ 修正；**验证**：测试 +3（module-orchestrator.test：懒加载 init + getModule/modules 可用、幂等不二次 init、依赖未注册/版本不匹配报错）→ 439 全绿 + verify；**B7 剩余**：M7.4 沙箱 / M7.5 内存守护（安全模型 + DevTools 记录，优先级低）拆批待续；模块桶 module.js 生成（分包根 createModuleSystem 挂载）待真实模块化业务（B9 整合）；**遗留**：B6 App 模块 / B7d / B8-B9 全 ⬜
132. **module-plan B9：整合（★CLI init module + 迁移指南 + 文档，447 测试）**：**CLI `proteus init module [dir]`**（packages/cli/src/module-init.ts）——生成 proteus-module.config.ts 骨架（defineModule 模板含全字段注释：name/version/dependencies/exports/chunk/preload/capabilities；已存在 → 不覆盖抛错），新工程零门槛接入模块化；**迁移指南**（10-migration.md）——按实际 API 重写：Step 1 `proteus init module` / Step 3 共享逻辑跨模块引用（B0 import→require 实际用法）/ Step 4 运行时编排（createModuleSystem/getModule/loadModule 实际签名 + 小程序端 getApp().moduleSystem 挂载）/ Step 5 `module:check --graph` + `audit module` 验证；`migrate module` 自动转换标注待实现；**module-plan README**——进度表（B0-B8 ✅ + 验证文件映射）+ 快速上手（init → check → audit）；**09 批次表**——B8/B9 ✅；**验证**：测试 +3（module-init.test：骨架内容/不覆盖/args）→ 447 全绿 + verify + CLI 实测（/tmp 生成骨架确认）；★module-plan 核心批次（B0-B5/B7a-c/B8/B9）全部收尾；**遗留**：B6 App Native（依赖 v0.6 自定义渲染器）、B7d 沙箱/内存守护（低优先）、模块桶 module.js 生成（待真实模块化业务）、`migrate module` 自动转换、eslint-plugin-proteus（B8 ESLint 规则）
133. **roadmap 现状校订 + 结构性项确认（★docs/roadmap.md + README，447 测试）**：**确认**——用户点名的三项结构性项已在路线图合适位置：① App 端 Vue 自定义渲染器（v0.6 详细规划：packages/renderer-app + createRenderer 运行时通道 + 与编译期通道并列的架构要点）；② Vapor 兼容（v0.6 Web 端 Vapor 模式双模式可编译 + v0.4 codegen 借鉴已落地）；③ Skyline iOS 真机白屏 bug（v0.5 三层对策：能力兼容清单编译期预警 / 页面级 WebView 降级通道 / v1.0 真机验收覆盖）；platform-plan（v0.5 多端扩展 + Capability 体系）全 ⏳ 未启动；**校订**——roadmap 能力矩阵现状列反映最新成果：编译能力补 vue-compat-advance 全批、状态管理补跨模块引用已通（B0，Pinia 待放行）、工程化 222→447 单测 + 8 包 + module-plan B0-B9 ✅、性能补分包体积监控；架构演进 packages 补 @proteus-vue/module；§8 验收 v0.4/v0.5 补前置基建提前完成标注；**README**——规则数 67→69、单测 377→447、目录结构补 packages/module、核心特性新增「Vue 能力渐进兼容」与「模块化」两条；packages/compiler/README 规则数同步；**验证**：文档改动零代码影响，447 测试基线不变；**遗留**：v0.5 多端扩展（platform-plan B1-B9 ⏳）、v0.6 App/Vapor、npm 发布、eslint-plugin-proteus
134. **platform-plan B1：Capability 契约（★@proteus-vue/capabilities 新包 + CLI capabilities:manifest，457 测试）**：**包**（packages/capabilities）——`CapabilityMeta/CapabilityAPI/CapabilityAdapter/CapabilityDefinition/Capability` 类型（tier 1-4：L1 通用/L2 映射/L3 独占/L4 实验）+ `defineCapability`（编译期校验：id kebab-case/tier/adapters 非空/fallback）+ `validateCapabilityDefinition`（纯校验）+ 注册中心（registerCapability 重复报错/registerCapabilities/clearCapabilities/hasCapability）+ `useCapability/getCapability`（平台 adapter 探测实例化 + fallback 解析；未注册/无 adapter → 显式失败）+ `detectPlatform`（feature detection：wx → skyline / window → web）；**scan**（src/scan.ts，node 工具走子路径 '@proteus-vue/capabilities/scan' 不进运行时入口）——walkCapabilityFiles + loadCapabilityFile（esbuild transform + defineCapability Function 参数注入 + fileRequire）+ scanCapabilities（幂等注册 → capability-manifest.json：id/tier/platforms/fallback）；**CLI `capabilities:manifest [dir]`**（扫描 + 落盘 .proteus/capability-manifest.json）；**★踩坑三连**：① index re-export scan 把 node:fs/esbuild 带进浏览器产物（Web 构建失败）→ scan 走子路径；② 方法体内 TS 类型断言/泛型不剥离（`as unknown as {...}` 产物语法错误）→ 编译器 rewriteRefAccess 加简单断言剥离（as unknown/any/never/标识符/单层泛型；★正则  在 > 后不成立导致泛型残留 → 去 ）；③ extractMethods 丢 async（async function/箭头 → 产物非 async 方法里 await 非法）→ 正则捕获 async 前缀 + 方法简写带 `async ` 修饰；**demo**：examples/capabilities/clipboard.capability.ts（web: navigator.clipboard / skyline: wx.setClipboardData，export ClipboardAPI 接口跨平台统一类型）+ config-demo.vue 复制按钮（useCapability + isSupported 探测；★MP 端接入待含第三方依赖共享模块放行——剥离警告已标注）；**验证**：测试 +10（capabilities.test 8：defineCapability 校验/重复注册/useCapability 平台解析/getCapability fallback/detectPlatform/scan manifest/产物目录跳过/落盘；mp-transform 2：async 方法保留/断言剥离）→ 457 全绿 + verify（双端构建）+ CLI 实测（clipboard L2 web/skyline）；**遗留**：B2 Adapter Registry / B3 编译期分叉 / B4 运行时降级 / B5-B9 全 ⏳；capability 包在 MP 的打包接入（含第三方依赖共享模块放行）
135. **platform-plan B2：Adapter Registry（★packages/capabilities/src/adapter.ts，466 测试）**：**CapabilityRegistry 类**（多实例隔离工厂，SSR/Worker 独立）——`register`（同 capability+platform 重复 → 报错编译期约束 §7；priority 降序排序）/ `registerIdempotent`（scan 场景跳过）/ `has` / `clear` / `registerFallback` / `entries`（capability → priority 降序 → platform 排序）/ `validate`（fallback 未注册 → 问题清单 §7）；**选择策略**（§4）——`resolve`（async 完整版：platform 过滤 → priority 降序 → isSupported 探测（抛错视为不支持）→ 命中 wrap；无命中 → fallback 递归返回 fallback 能力实例；命中 → cap.fallback 属性引用降级）/ `resolveSync`（仅同步探测）；**defineAdapter**（独立 adapter 文件：capability/platform/priority/isSupported/create + runsInWorklet 标注；校验缺失字段报错）；**index 重构**——B1 描述文件的 adapters 展开为 adapter 注册（isSupported 延迟到调用——§6 adapter 不得模块顶层执行平台 API）+ registerAdapter（幂等）/ resolveCapability（async）；**★设计澄清**：同 capability+platform 唯一（编译期约束）与 priority 排序（同平台多实现预留）并存——priority 影响 entries 排序 + 未来多实现；fallback 语义：resolve 无命中返回 fallback 能力实例（id = fallback），命中时 cap.fallback 属性引用；**验证**：测试 +9（adapter-registry.test：defineAdapter 校验/重复注册报错+幂等/platform 过滤+priority entries/异步探测/fallback 递归与属性引用/validate/多实例隔离/registerAdapter+useCapability/resolveCapability）→ 466 全绿 + verify（双端构建）；**遗留**：B3 编译期分叉 / B4 运行时降级 / B5-B9 全 ⏳
136. **platform-plan B3：编译期分叉（★能力缺失编译期可见，470 测试）**：**manifest 增强**（scan.ts）——entry 加 `source`（描述文件相对路径，产物可追溯铁律）；**check.ts**（node 工具走 '@proteus-vue/capabilities/check' 子路径）——`scanCapabilityUsage`（业务 .vue/.ts 的 useCapability/resolveCapability 引用扫描，含泛型形态 `resolveCapability<BioAPI>`——★正则补 `>?` 支持）+/ `checkCapabilityUsage`（业务引用 vs manifest 平台覆盖：缺失（引用但当前平台无 adapter）含使用方 + gaps（平台覆盖缺口））；**CLI**——`capabilities:manifest [dir] [--platform web|skyline|app]`：能力清单（含 source）+ 平台缺失报告（§7：gaps 清单 + 业务引用警告 + 全覆盖确认）；**★架构约束确认**：index.ts 运行时入口不 re-export node 工具（scan/check）——浏览器/MP 产物零 node 依赖（B1 踩坑教训，B3 重申）；**验证**：测试 +4（capability-check.test：业务引用扫描（含泛型）/缺失检出含使用方/全覆盖空/manifest 含 source + --platform 报告）→ 470 全绿 + verify（双端构建）+ CLI 实测（examples --platform skyline → clipboard 全覆盖 ✅）；**遗留**：B4 运行时降级 / B5 平台原生模块 / B6-B9 全 ⏳；编译期 useCapability→静态 adapter import 替换（产物裁剪）为后续（当前运行时 registry 平台探测已覆盖选择）
137. **platform-plan B4：运行时降级（★M4，476 测试）**：**UnsupportedAPI**（§2）——`unsupported(capability, platform, reason)`：isSupported false + **thenable 抛 CapabilityError**（防 await 误用）；`unsupportedCapability`（降级 Capability：meta + isSupported false）；**错误模型**（§6）——`CapabilityError` 类（code: UNSUPPORTED/PERMISSION_DENIED/UNAVAILABLE + capability + platform + reason）；**required 标记**（§4）——CapabilityMeta.required（缺失阻断流程）+ CapabilityRegistry.registerRequired/isRequired；**useCapability 降级语义**（§7 缺失不崩溃）——无命中时：required → 抛 CapabilityError（阻断）；非 required → 返回 unsupported 包装（isSupported false，业务走 UI 分支）；fallback 自动生效（B2 resolve 无命中 → fallback 能力实例）；**验证**：测试 +6（capability-fallback.test：unsupported thenable 抛错/unsupportedCapability/非 required 不崩溃/required 阻断/fallback 自动切换（meta.id 变 fallback）/CapabilityError 结构）→ 476 全绿 + verify（双端构建）；★旧测试断言更新（useCapability 缺失从抛错 → unsupported 降级，B4 语义）；**遗留**：B5 平台原生模块 / B6-B9 全 ⏳；Skyline 降级 UI 模式（不自动切换渲染）为业务侧模式约定
138. **platform-plan B5：平台原生模块规范（★§6 禁止清单 + §7 静态检查，481 测试）**：**scanPlatformViolations**（check.ts，走 '@proteus-vue/capabilities/check' 子路径）——平台文件判定（`.web./.skyline./.app./.mp.` 后缀或 adapters/platforms 目录）+ 平台 API 裸调用正则（`wx.|window.` + 调用形态，非 declare 声明）；**规则**：业务目录（pages/components 等，排除 capabilities/adapters/platforms/shims）禁 wx.*/window.* 裸调用（走 capability）；平台文件防泄漏（skyline/app/mp 文件禁 window.，web 文件禁 wx.）；**CLI `capabilities:check [dir]`**——违规清单 + 修正指引 + 退出码 1（§7 CLI 静态检查验收）；**★踩坑**：① matchAll 需要全局正则（非 g 报错）→ 统一加 g；② 块注释里 `wx.*/window.*` 的 `*/` 提前闭合注释（TS 语法错）→ 改写；③ examples main.mp.ts 是 MP 平台入口（含 wx.getWindowInfo/wx.router）→ `.mp.` 后缀纳入平台文件判定；**验证**：测试 +5（platform-violation.test：业务 wx./window. 违规、平台文件泄漏双向、合规零违规、shims 豁免、CLI 输出）→ 481 全绿 + verify（双端构建）+ CLI 实测（examples `capabilities:check` → ✅ 通过：业务零平台 API，wx 仅在 main.mp.ts）；**遗留**：B6-B9 全 ⏳；platforms/ 目录完整形态（entry/runtime/globals）待新平台接入时落地；新平台接入流程（§5）文档化
139. **MP 端 capability 接入（★@proteus-vue/* 框架包放行，platform-plan B5 尾 + module-plan B0 边界收窄，485 测试）**：**plugin**（plugin-vite）——`resolveSharedModule` 抽为导出纯函数（可测）：相对路径本地共享模块 + **@proteus-vue/* 框架包白名单放行**（node_modules 解析包 dist → 产物 `_proteus/<name>.js`）；第三方裸模块（pinia/vue/lodash）仍 null（B0 边界保持）；第三方检测改 `!startsWith('.') && !startsWith('@proteus-vue/')`；共享模块 esbuild bundle 加 `external: ['@proteus-vue/*']` + **esbuild 插件 onResolve 映射产物路径**（`_proteus/<name>.js` 相对本产物目录——微信 require 缓存同路径同实例，避免描述文件内联包与页面 require 包双 registry）；BFS 收集含 @proteus-vue/* 依赖；**产物验证**：config-demo.js `const { registerCapability, useCapability } = require('../_proteus/capabilities.js')` + clipboard.capability.js 内 `require("../_proteus/capabilities.js")`（external 映射）+ _proteus/capabilities.js 6KB 完整运行时——**MP 端能力体系真正可用**（config-demo 复制按钮：useCapability('clipboard') → skyline adapter → wx.setClipboardData）；pinia（stores/player）仍跳过（第三方保持）；**验证**：测试 +4（plugin.test：@proteus-vue 包放行/相对路径共享/第三方 null/未构建包 null）→ 485 全绿 + verify（双端构建）；**遗留**：Pinia 等第三方共享模块接入（届时配合分包/去重）、B6-B9 全 ⏳
142. **Pinia MP 编译 P1+P3（★模板 store 绑定桥 + pinia 放行，490 测试）**：**P1 模板绑定**——template.ts `rewriteStoreRefs`（`{{ store.<field> }}` / `:prop="store.x"` → 剥离前缀 + storeBindings 收集；嵌套 store.current.title → current.title）+ ctx.storeBindings → TemplateTransformResult；script.ts `storeBindingLine`（onLoad：`const __self = this` + 初始 `setData({ field: __self.store.field })` + `store.$subscribe(function () { __self.setData(...) })`——Pinia 原生订阅，store 变量 = useXxxStore() runtimeInit 实例属性）；规则 script/store-binding（★registry-drift 门禁抓到 → 登记）；**P3 放行**——plugin 共享模块第三方白名单加 pinia/vue-demi/@vue/reactivity/@vue/shared/@vue/runtime-core；★体积惊喜：`stores/player.js` 仅 **29.2KB**（esbuild tree-shaking 生效；此前 B0 评估 906KB 为未 tree-shake 口径），主包 45→83KB（预算 1200KB 内）；pinia-demo MP 端 `const { usePlayerStore } = require('../stores/player.js')` + wxml `{{ current.title }}` 剥离 + onLoad $subscribe 绑定——**状态显示可用**；**验证**：测试 +5（pinia-mp-compile.test：wxml 剥离/onLoad 注入/无引用不注入/pinia 解析/其他第三方跳过——★踩坑：测试 SFC script 内容带缩进 → extractData 只提取零缩进顶层 → 修正）→ 490 全绿 + verify（双端构建）+ build:mp 实测（主包 83KB）；**遗留**：P2 store 方法事件包装（@click="store.play()" → proteusStoreXxx，inline handler 扩展）、platform-plan B6-B9 全 ⏳
143. **Pinia MP 编译 P2（★store 方法事件包装收尾，492 测试）**：**tryInlineHandler 扩展**（template.ts）——`store.method(args)` 事件表达式（无参 `store.toggle()` / 对象字面量 `store.play({...})` / 含 store. 表达式 `store.setVolume(store.volume - 0.1)`）→ `proteusStore<Method><Key>` 包装方法（`this.store.method(args)` + 事件内 `store.` → `this.store.` 改写——store 是 useXxxStore() 编译的实例属性）；**★踩坑**：音量 +/- 参数 `store.volume - 0.1` vs `+ 0.1` 的 key（args 去 \W）同为 'storevolume01' → 同名方法冲突覆盖（两个按钮调同一方法）→ key 保留符号映射 Minus/Plus 区分；**验证**：pinia-demo MP 端按钮产物（bindtap=proteusStoreToggleNoArgs / proteusStorePlaytitle... / proteusStoreSetVolumestorevolumeMinus01 / Plus01；包装体 `this.store.setVolume(this.store.volume - 0.1)`）；测试 +2（pinia-mp-compile P2：wxml bindtap + js 包装体 + Minus/Plus 区分 + 非 store 链式仍警告——★旧 vue-compat 断言 store.toggle() 警告 → 改 a.b() 保留警告语义）→ 492 全绿 + verify（双端构建）；★**Pinia MP 编译 P1-P3 全部完成**：pinia-demo MP 端状态显示 + 交互完整可用（主包 83KB）；**遗留**：platform-plan B6-B9 全 ⏳
144. **platform-plan B6-B9 收尾（★CI 门禁接入 + 文档整合，492 测试基线）**：**B9 CI 接入**——.github/workflows/ci.yml 加「能力体系门禁」步骤（capabilities:check examples（B5 平台规范硬卡，违规退出码 1）+ capabilities:manifest examples --platform skyline/web（B3 编译期分叉缺失报告））；本地实测三命令全过（skyline/web 全覆盖）；**09-migration-ci.md 更新**——§3.1 静态检查改为实际命令（capabilities:check/manifest）+ 失败条件对齐 B5 禁止清单；§4 audit 命令表补 Module 层 + 标注各层实际接入状态（Pinia stores 门禁 ✅ / Platform capabilities ✅）；**10-execution-batches 状态表**——B8 矩阵 ✅（manifest + capabilities/check 测试覆盖）、B9 ✅（CI 接入）、B6/B7 延后（能力级可靠性由 B4 降级覆盖；可观测待 DevTools 基建）；**验证**：文档 + CI 配置改动零代码影响，492 测试基线不变；★platform-plan 主体（B1-B5 + B8/B9）全部收尾；**遗留**：B6/B7（超级应用场景延后）、v0.6 App/Vapor、npm 发布
145. **Router M7.1：<route> 块 chunk 字段（★module-plan 05 页面→模块映射闭环，493 测试）**：**router**（schema.ts/types.ts）——RouteBlock 加 `chunk?: string`（kebab-case 校验，非法报错含 loc）；**gen-routes**——PageInfo.chunk + 分包页面 chunk 校验（与分包名不一致 → 透明化警告「页面 chunk 应对齐模块分包名」）；页面→模块映射缺口闭合（B5 标注的「Router M7.1 未实现」落地：分包页面声明 chunk 与分包名/模块 chunk 对齐）；**验证**：测试 +1（gen-routes.test：chunk 对齐不警告/不一致警告/非法 kebab-case 报错）→ 493 全绿 + verify（双端构建）；**遗留**：v0.6 App/Vapor（唯一未落地 roadmap 结构性项，待规划细化）、npm 发布、eslint-plugin-proteus
146. **v0.6 规划细化（★docs/proteus-app-plan/：App 渲染器 + Vapor 批次规划，roadmap 结构性项收尾）**：**00-overview**——三端复用主张（Web/MP 编译期通道已交付 + App 运行时通道 createRenderer）+ 架构分层 + Vapor 兼容主张 + 依赖（module-plan/platform-plan/pinia-plan + 前置 npm 发布）+ 批次总览 B1-B6；**01-app-renderer**——packages/renderer-app 结构（host view/text/props/events + router-adapter + bridge）+ createRenderer host config 核心（createElement/patchProp/insert/remove 原生节点）+ 样式系统三端对齐表（px→dp/rpxRatio/语义类）+ 路由/状态桥（router app adapter + Pinia 原生）+ 能力桥（capabilities adapters.app 复用 platform-plan）；**02-vapor**——Vapor 与 Proteus 哲学同构对照表（都拒绝虚拟 DOM）+ 双模式构建（Vapor Web + Proteus MP 同一源码）+ 特性子集兼容矩阵 + 基准（v0.4 setData 借鉴回顾）；**09-execution-batches**——B1 骨架/B2 host 完整/B3 路由状态桥/B4 capabilities app adapter/B5 demo/B6 Vapor（依赖链 + 前置标注）；roadmap v0.6 段补规划引用；**验证**：纯规划文档零代码影响，493 测试基线不变；★roadmap 结构性项全部落地规划（App/Vapor/白屏兜底）；**遗留**：v0.6 实现（待 v0.5 稳定 + npm 发布）、npm 发布、eslint-plugin-proteus
147. **router-plan 盘点 + B11 自动守卫（★文档一致性修复 + requiresAuth 拦截，495 测试）**：**盘点**——router-plan 企业级 B1-B7 全部 ✅（scan/tree/codegen/guards/tabBar/测试迁移），B5（App codegen）⬜ 待 v0.6（proteus-app-plan 对齐），超级应用 B8-B12 部分被其他批次覆盖（B8 chunk 字段 + B9 preloadRule + B10 转场 MVP ✅）；**★文档不一致修复**——09 §5 进度追踪旧式 [ ] 全未勾（与总览表 B1-B7 ✅ 矛盾）→ 同步勾选 + 标注 B8-B12 实际覆盖；**B11 补做**——createRouter(routes, options) 加 `auth` 检查器 + `onAuthFail`：requiresAuth 页面自动拦截（先于用户守卫，trace 可观察 `[guard] requiresAuth → xxx 被拦截`）；无 auth 检查器 → 放行（默认行为不变）；测试 +2（router-guards-tabbar：未登录拦截 + onAuthFail / 已登录放行 / 无检查器与非 requiresAuth 放行——★踩坑：index 是 tabBar 页面走 switchTab 非 navigateTo → 改用 forms）→ 495 全绿 + verify；**遗留**：B12 DevTools 面板/CI 审计、v0.6、npm 发布
149. **lifecycle-plan 盘点（★用户问询 + 全板块盘点，495 测试基线）**：**lifecycle-plan 未实现**——B1-B9 全 ⬜（无 defineApp/LifecycleOrchestrator 代码）；现有覆盖：页面/组件级生命周期（编译 onLoad/onReady/onUnload 映射 + runtime createPage/createComponent ✅）+ App onLaunch 生成（appSkeleton ✅ 非阶段化）；**缺口**：App 级五阶段编排（bootstrap→coreReady→navigationReady→beforeFirstPaint→interactive）+ 超时降级 + --trace-lifecycle + 冷热分离；**★全板块盘点**——docs/ 共 17 个 proteus-* 规划：已收尾 module/pinia/platform/router（+app-plan v0.6 规划）；**未实现**：lifecycle、api（网络/设备层无代码）、component（组件库 P0 Draft）、devtools（面板）、i18n、security（useAuth 无）；**已实现但规划文档未同步状态**：compiler/cli/build/testing/types（代码有，规划进度表未勾）；blueprint（总纲）；06 批次表更新（验收清单改 ⬜ 全未勾 + 缺口/可独立先行标注）；**建议**：lifecycle B1+B2（defineApp + Orchestrator）可独立先行（不依赖 api/component）；**遗留**：各未实现板块、v0.6、npm 发布
150. **lifecycle-plan B1+B2（★defineApp + LifecycleOrchestrator，505 测试）**：**runtime/src/lifecycle.ts**——`PHASE_ORDER`（bootstrap→coreReady→navigationReady→beforeFirstPaint→interactive）+ `DEFAULT_PHASE_TIMEOUT`（3000/5000/2000/3000/5000ms）+ `DEFAULT_PHASE_FALLBACK`（warn/minimal/home/skeleton/lazy）+ `PhaseTimeoutError` + `withTimeout`（Promise.race）+ `LifecycleOrchestrator`（register 重复报错/run 顺序执行/超时降级（applyFallback：warn console/minimal isMinimalMode/home·skeleton·lazy trace）/错误隔离（coreReady 失败 → minimal 标记，后续继续）/onPhase* 回调 + trace（success/timeout/error + fallback）/getStatus（idle/running/completed/degraded）+ getTrace）+ `defineApp`（五阶段 + 运行时钩子 onShow/onHide/onMemoryWarning/onNetworkChange/onDestroy/onRecover/onError + phases 配置覆盖 + ★校验：未知配置键/未知阶段报错透明化 + run(launchType) + emit(运行时钩子) + ProteusApp）；index 导出；**demo**：main.ts Web 入口改 defineApp（bootstrap 日志 + interactive mount——Web 零转换直跑）；**验证**：测试 +10（lifecycle.test：五阶段顺序/超时 fallback+继续/coreReady 失败 minimal+错误隔离/PhaseTimeoutError/重复注册+getTrace/defineApp 顺序+async/phases 覆盖/未知键校验/emit 运行时钩子/launchType 透传）→ 505 全绿 + verify（双端构建）；**遗留**：B3-B9（Web/Skyline 编译期映射、页面 store 清理、App 原生、可观测、迁移工具）、api/component/security 等板块
151. **lifecycle-plan B3/B4（★Web demo + Skyline App 级钩子，506 测试）**：**B3（Web 映射）✅**——main.ts Web 入口 defineApp（bootstrap 日志 + interactive mount）已覆盖（B1+B2 同批）；**B4（Skyline App 级）✅ 部分**——appSkeleton 增强：App() 生成 onShow/onHide 调试钩子（三端一致：Web visibilitychange）；★MP 端阶段化边界：app.js 直出无模块解析（不得 import）→ defineApp 在 MP app.js 不可用（worklet 内联约束，bundle 风险）——阶段化在入口全量模式（自定义 App() 手动编排）或 Web/运行时路径；**验证**：测试 +1（plugin.test：骨架含 onShow/onHide）+ build:mp 产物确认（app.js onShow/onHide 5 处）→ 506 全绿 + verify（双端构建）；**遗留**：B6（页面 onUnload 自动 $dispose store——页面级生命周期编译已映射，store 自动清理待）、B7-B9、api/component/security 板块
152. **lifecycle-plan B6（★页面 onUnload 自动 $dispose store，508 测试）**：**编译器**（script.ts）——检测 useXxxStore() runtimeInit（storeVar）且非组件 → `storeDisposeLine`（`if (this.store && this.store.$dispose) { this.store.$dispose(); this.store = null }`）注入 onUnload（显式存在前置 / 无则生成承载 onUnload；与 provide-inject 清理 unsubLine/pageCleanupLine 共存）；needsPageCleanup 条件扩展；**验证**：build:mp 产物（pinia-demo onUnload 含 $dispose + 命名空间清理）测试 +2（pinia-mp-compile B6：$dispose 注入/无 store 不注入）→ 508 全绿 + verify（双端构建）；**遗留**：B7-B9（App 原生/可观测/迁移工具）、retainState（keep-alive 语义）、api/component/security 板块
153. **板块状态标注同步（★compiler/cli/build/testing/types 五个已实现板块，508 测试基线）**：**盘点结论**——五板块代码已完整实现（compiler：@proteus-vue/compiler 69 规则；cli：10+ 命令；build：plugin-vite + CI；testing：508 测试 + 门禁；types：shims/PageOnLoad/RouteParamsByName/配置 schema 部分）但规划 README 无状态标注（与 lifecycle 等未实现板块混淆）；**落地**——各 README 标题后加 ★实现状态（compiler ✅ 已实现 / cli ✅ / build ✅ / testing ✅ / types ✅ 部分实现（B1-B2 落地，B3-B7 待））；types 首行超长块引用（edit_file JSON 解析问题）→ 大锚点替换；**验证**：纯文档零代码影响，508 测试基线不变；★全板块最终状态：已收尾 module/pinia/platform/router/lifecycle（B1-B4+B6）+ 已实现标注 compiler/cli/build/testing/types + 未实现 api/component/devtools/i18n/security + 规划 app（v0.6）；**遗留**：api/component/security/i18n 板块、v0.6、npm 发布
154. **api-plan B1+B4 部分（★@proteus-vue/api 新包：request 抽象 + 设备信息，518 测试）**：**包**（packages/api）——`RequestConfig/RequestResponse/ApiError`（code/status/config 可定位）+ `IRequestAdapter`（wx/web/app）+ `createRequestAdapter`（平台探测）+ `buildUrl`（baseURL+params 拼接）+ `getDeviceInfo`（MP wx.getWindowInfo / Web window）+ `createApi`（request + get/post/put/delete/patch 快捷方法 + beforeRequest 拦截器（skipAuth 跳过）/afterResponse + 重试（指数退避简化线性 + 耗尽包装 RETRY_FAILED）+ 超时（AbortController → TIMEOUT）/网络错误（NETWORK_ERROR））；**★踩坑**：client.ts 缺 IRequestAdapter import（TS 报错）+ adapters 未 re-export ApiError（index 解析失败）+ 测试超时 mock 的 AbortError name 未设置（adapter 的 name 判断失败）→ 三处修复；**验证**：测试 +10（api.test：web GET/post/错误/超时/重试/重试耗尽/拦截器+skipAuth + wx 包装/fail + 设备信息/buildUrl）→ 518 全绿 + verify（双端构建）；**api-plan 状态**：B1 ✅（Request）、B2 ✅（Storage 由 pinia M1）、B4 ✅ 部分（设备信息；导航由 PlatformAdapter）、B3（Auth 依赖 security）、B5-B9 ⬜；**遗留**：Auth/Payment/UI/Media 等能力域、api 包接入 examples/生命周期 coreReady 阶段、component/security/i18n 板块、v0.6、npm 发布
155. **api 包接入生命周期 + demo（★@proteus-vue/api 落地 MP 产物，518 测试）**：**main.ts**——Web 入口 coreReady 阶段初始化 createApi（lifecycle 阶段集成）；**config-demo.vue**——「设备信息」按钮（getDeviceInfo：platform/screenWidth/Height/isSkyline，Web/MP 双端）；★踩坑：编辑覆盖了 config-demo 的 type import（Capability/ClipboardAPI 丢失 → TS 报错）→ 补回；**验证**：build:mp 产物 `_proteus/api.js`（3.8KB bundle）+ config-demo.js `const { getDeviceInfo } = require('../_proteus/api.js')`（@proteus-vue/api 框架包 MP 放行）→ 518 全绿 + verify（双端构建）；**api-plan 接入闭环**：@proteus-vue/api（B1 Request + B4 设备信息）→ lifecycle coreReady 阶段 → examples demo → MP 产物；**遗留**：Auth（依赖 security）、Payment/UI/Media 能力域、component/security/i18n 板块、v0.6、npm 发布
156. **api-plan B3 + security M2 凭证托管（★@proteus-vue/api createAuth + 自动 Authorization，522 测试）**：**auth.ts**——`AuthManager`（getToken/setToken/isAuthenticated/subscribe 登录态变化）+ `createAuth(storage?)`（默认内存 storage；持久化可接入 StorageAdapter；token 键 proteus.auth.token）+ 与 createApi 集成（beforeRequest 后自动加 `Authorization: Bearer <token>`，skipAuth 跳过；拦截器可先 refresh token 再自动加头）；types.ts ApiOptions 加 auth；index 导出；★踩坑：vi.stubGlobal('wx') 在 afterEach 只 restoreAllMocks 未恢复（wx adapter 残留 → B3 测试走 wx 适配器报 request:fail）→ 补 unstubAllGlobals；**验证**：测试 +4（api.test B3：set/get/isAuthenticated/subscribe/清除、自动加头+skipAuth+无 token 不加、拦截器 refresh 后加新 token、持久化恢复）→ 522 全绿 + verify（双端构建）；**api-plan 状态**：B1 ✅ / B2 ✅ / B3 ✅ 部分 / B4 ✅ 部分 / B5-B9 ⬜；**security-plan 状态**：M2 凭证托管 ✅（骨架）；M1/M3-M7（架构/编译期规则/注入防护/网络安全/供应链/运行时隔离）⬜；**遗留**：完整登录态/权限守卫（withPermission）、加密存储、component/devtools/i18n 板块、v0.6、npm 发布
148. **pinia-plan 盘点（★用户问询，M1-M8 + MP 编译 P1-P3 全部完成确认，495 测试基线）**：**代码核实**——M1 Storage（packages/shared/src/storage + storage.contract.test）✅；M2 Persistence（runtime/pinia/persistence plugin+lightweight + pinia-persistence.test）✅；M3 Platforms（createWebPinia/createMpPinia/createAppPinia + pinia-platforms.test）✅；M4 SSR（ssr.ts + ssr-isolation.test）✅；M5 DevTools（devtools.ts + pinia-devtools.test）✅；M6 Testing+Migration（tests + pinia-migration.md）✅；M7 可靠性（scope.ts + pinia-m7-* 三测试）✅；M8 协同/可观测（@proteus-vue/pinia-sync + snapshot/tracer + pinia-m8/pinia-sync.test）✅；**9.x MP 编译 P1-P3 全部完成**（模板绑定/事件包装/放行——3f51a59）；**★文档滞后修复**——09 批次表 9.x 行 ⬜ 规划就绪 → ✅（P1+P2+P3 完成）；12-mp-compile.md 验收 4 项全勾（495 测试）；**结论**：pinia-plan 全部批次完成（M1-M8 + MP 编译接入），无遗留；**遗留**：v0.6、npm 发布、eslint-plugin-proteus
141. **Pinia MP 编译规划（★docs/proteus-pinia-plan/12-mp-compile.md + 目录尾随空格 bug 修复）**：**★bug 修复**——`docs/proteus-pinia-plan ` 目录名**尾随空格**（`ls docs/proteus-pinia-plan/` 路径解析失败，工具/脚本都会踩）→ 重命名去空格；**盘点确认**——pinia-plan 09 状态表正确（更新后进度追踪 M1-M8 ✅，旧表为历史残留）；**规划落地**——Pinia MP 编译三缺口（① 共享模块放行：stores/player.ts import pinia → B0 跳过（体积 906KB）；② 模板绑定：`{{ store.current.title }}` 读 this.data.store = undefined（store 实例属性）；③ 事件调用：`@click="store.play()"` 复杂表达式警告原样）+ 三批方案：P1 模板 store 绑定桥（template 收集 store.<field> 引用 → connectPageStore 订阅 → data 同步，store 前缀剥离）；P2 store 方法事件包装（proteusStoreXxx 包装方法）；P3 放行 + 体积（stores 目录专用放行或分包，体积门禁 B7a）；依赖 module-plan B0/B7a + pinia-plan connectPageStore（runtime/src/store.ts）；验收：pinia-demo MP 端可运行（播放/暂停/音量 + 状态显示）；批次表 09 加 9.x ⬜ 规划就绪；**验证**：文档改动零代码影响，485 测试基线不变
140. **全仓盘点收尾（★docs 一致性，485 测试基线）**：**README**——单测 181→485（两处：npm test 行 + tests/ 目录行）、MVP 列表补 platform-plan 能力体系、核心特性新增「能力体系（platform-plan B1-B5）」条目、目录结构补 packages/capabilities；**roadmap**——能力矩阵 #6 原生能力补 capability 体系（MP 端可用）、#7 工程化 447→485 单测 + 9 包 + platform-plan B1-B5、§5 架构树补 capabilities 包；**packages.md**——目标包结构补 module/capabilities（9 包）+ runtime 描述补 provide-inject；**compiler.md 无数字滞后**；**验证**：文档改动零代码影响，485 测试基线不变；**当前全貌**：vue-compat-advance 全批 ✅ + module-plan B0-B9 ✅ + platform-plan B1-B5 ✅（MP capability 可用）+ 拆包 9 包 ✅；**遗留**：Pinia 第三方接入、platform-plan B6-B9（超级应用/可靠性/可观测/迁移 CI）、v0.6 App/Vapor、npm 发布、eslint-plugin-proteus
128. **vue-compat-advance Batch 7：作用域插槽平台限制确认（★收尾，428 测试）**：**平台调研结论**——作用域插槽运行时等价在 MP **无通用解**：微信 webview 的 template import 无法动态选择多父模板（List 被多页用不同插槽内容 → import 冲突）+ 插槽内容引用父 data 时子侧无访问通道；**Skyline（主渲染引擎）不支持跨文件模板**；uni-app/Taro MP 端同样不完整；**落地**——警告增强（template.ts：补「MP/Skyline 平台限制」定性 + 替代模式具体写法 `<MyList :items @item-tap />` + triggerEvent 回传）；规则 slot/scoped-slot 说明书更新（平台限制 + 替代模式，状态 implemented）；测试断言更新（平台限制/triggerEvent）；docs/vue-compat-advance.md 新增 §9 Batch 7 小节 + §5 遗留关闭作用域插槽项（替代模式 props+事件已由组件系统 v0.3 完整支持）；**验证**：428 全绿 + verify；★vue-compat-advance 全部批次收尾（Batch 1-7 ✅：作用域插槽警告/平台限制、Transition 进入+离开动画、provide/inject 值传递+响应式联动+页面级隔离）
122. **module-plan B0：跨模块引用最小闭环（★docs/proteus-module-plan/09-execution-batches.md，396 测试）**：背景——module-plan 9 批全 ⬜，跨模块引用是后续基建（Pinia/API/Component）地基；旧行为 import 剥离 + 警告、共享模块不在编译范围、函数调用初始化静态求值失败整条丢失；**compiler**（script.ts）——`extractImports`（named/default/namespace/side/type 五形态解析）+ import 处理：`moduleImports`（插件预计算 source→requirePath）命中 → 产物顶部 `const { a } = require('...')`（default 用 `.default !== undefined ? .default : 模块` 兼容 ESM/CJS），vue/@proteus-vue/*/type/.vue 跳过，未收录 → 剥离 + 警告（文案更新指向 B0）；`extractData` 函数调用初始化（静态求值失败）→ runtimeInits 收集 + `runtimeInitLine`（onLoad/attached 注入 `this.x = fn()` 实例属性，不再丢调用，data 不含该字段）；新规则 script/runtime-init（★registry-drift 门禁抓到未登记 → 补登记）；**plugin**（plugin-vite）——扫描页面/组件 import 的相对路径共享模块（.ts/.js）→ BFS 收集依赖链 → esbuild bundle（全内联 + minify）→ CJS 单文件产物（stores/player.js 2.1MB → minify 906KB 仍超 → **★B0 边界：含第三方裸依赖的共享模块树跳过编译**（pinia 全量进主包浪费且 MP 端模板/事件不支持），纯逻辑共享模块（utils）走 require）；requirePath 回填（页面产物相对路径）；★踩坑：scanImports 对 .ts 文件只提取 <script> 块返回空 → 第三方依赖检测失效（stores/player 未被跳过），改 .vue 提取 script / .ts 用全文；**验证**：测试 +6（module-import.test.ts：named/default/namespace 三形态 + 未收录剥离 + 运行时初始化 + 规则登记）→ 396 全绿 + 双端构建（Web 端 Vite ESM 原生不变）；demo：examples/utils/format.ts + forms.vue（refreshTime → require + 运行时调用，产物确认 `const { formatTime } = require('../utils/format.js')` + 共享模块 0.6KB）；主包回落 45KB；**遗留**：Pinia/第三方依赖共享模块接入（后续批次，配合分包/去重）、B1-B9 全 ⬜
118. **框架内置组件路径漂移修复（★virtual-list 等，决策 #115 拆包后遗症）**：现象——build:mp 时 gen-routes 警告 `<virtual-list>` 未找到（既非应用组件也非框架组件）；根因——monorepo 拆包后 examples 独立成工程，plugin-vite（编译侧）与 gen-routes（page.json 侧）仍按「工程根 src/components」找框架组件，而组件库未拆包实际在仓库根 src/components（决策 #115 仅修了 Web 侧 alias `@proteus-vue/components → ../src/components`）；修复——两包新增显式选项：PluginOptions.frameworkComponentsDir + GenRoutesOptions.frameworkComponentsDir（缺省相对工程根 src/components，create-proteus 模板行为不变），examples/vite.config.ts 与 examples/scripts/gen-routes.ts 显式传入仓库根 src/components（★路径基准注意：gen-routes.ts 在 scripts/ 下需 `../../src/components`，vite.config.ts 在工程根用 `../`）；验证——virtual-list-demo.json `usingComponents: { "virtual-list": "/proteus/virtual-list/index" }` + dist/mp-weixin/proteus/virtual-list/{index.js,wxml,wxss} 输出 + 警告消失；377 测试 + 双端构建全绿
157. **新规划文档批次入库前一致性修复（★用户发起：10 个新 docs 目录未跟踪 + 规约层三文件 G 表互相矛盾）**：新文档 = architecture-facade（规约）/ css-compat / app-renderer / safe-area / memory-plan / memorial-skeleton / app-capabilities / test-framework / types-plus / design-principle（原则补充），全部未提交。**G 表统一（★核心）**：00-architecture.md 自称唯一规约，其 G-01~G-20 为基线**不动**；ARCHITECTURE.md 与 01-optimization-log.md 的旧表（G-10 起与规约冲突的 Family B）重写对齐；新 8 plan **追加** G-21~G-28（遵守「禁止本地重新编号」铁律，不做插入式重编号）——css-compat G-21 / app-renderer G-22 / safe-area G-23 / memory-plan G-24 / memorial G-25 / skeleton G-26 / theme+fontscale G-27 / cache G-28（test-framework 已并 G-07、types-plus 已并 G-01）；新 plan 声称的旧编号全部重指向（css G-04→G-21、renderer G-05→G-22、safe-area G-04/G-05/G-08→G-21/G-22/G-23、memorial G-11/G-12→G-25/G-26、app-capabilities G-13~G-15→G-27/G-28，含 README/10-batches/11-batches/architecture-update/各 01-09 文件头）。**校验脚本落地**：`scripts/check-consistency.js`（纯 Node ESM 内置模块：scope 残留扫描 + 三文件 G 表集合一致 + 序列完整 + 包注册表对照（仅报告）+ contracts + `--layering`）；`.github/workflows/consistency.yml` 从 plan 目录移入真实 CI（原 tsc -b packages/*/tsconfig.json job 无效——包只有 tsconfig.build.json，改跑 check-consistency.js --layering）。**scope 清零**：types-plus/test-framework 19 处遗留 `@proteus/*` → `@proteus-vue/*`。**缺文件/机器路径**：memorial-skeleton 与 app-capabilities 补 pack.sh + CHECKSUM.md（safe-area pack.sh 修复：开头 rm CHECKSUM.md 防自哈希 + 去掉 /data/workspace 硬编码；design-principle validate.py OUT 改脚本相对路径、config-update.md 误标 G-10: → 铁律 #10）；**glass-plan/performance-plan 已补齐**（docs/proteus-glass-plan 追加 G-29 + docs/proteus-performance-plan 追加 G-30 入规约表；glass 里程碑标号 G-04~G-18 统一为 M1-M6、performance 声称的 G-10/G-05 → G-30/G-22，safe-area 09「外部规划」标注改 G-30）。验证：`node scripts/check-consistency.js` 全绿（G-01~G-30 三文件一致）+ 三 pack.sh sha256sum -c 通过；纯文档+工具改动，749 测试基线不变；**遗留**：未提交（待用户确认后入库）
158. **types-plus-plan B8 落地（★小程序官方类型整合 + MpComponentSchema，用户发起：组件/API 依赖的类型地基）**：盘点结论——B1-B7 已由旧 types-plan + 类型收口落地（@proteus-vue/types 包），真正缺的是 B8/B9。**B8 落地**：① `packages/types/src/mp/component-schema.ts`——MpComponentProp/MpComponentSchema/MpComponentRegistry（§9 规格）+ 20 标签 starter 注册表（覆盖 Compiler TAG_MAP 发射标签 + 示例在用原生标签，含 p-* 别名）+ getComponentSchema/registerComponentSchema（§10 用户扩展）+ REQUIRED_TAGS 编译期完整性守卫；② `sdk-version.ts`——MpSdkVersion + 维护表 MP_SDK_VERSION_MAP + validateMpSdkVersion + resolveTypingsVersion（未收录回退默认并标注 mapped=false 透明化）；③ `official-typings.ts`——官方类型桥（Wx/WxRequestOption/WxShowToastOption 等别名，`/// <reference types="miniprogram-api-typings" />` opt-in）；④ `@proteus-vue/types` 新增 `./mp` 系列子路径 exports + 构建入口 + devDep miniprogram-api-typings@5.2.3；⑤ 根 index 导出纯类型（component-schema/sdk-version），official-typings 走子路径（避免全局命名空间意外激活）；⑥ 测试 tests/types-mp.test.ts（8 用例）；★环境发现：749 基线在 Node 22 跑（require(ESM)），Node 18/20 下 web/jsdom 测试加载失败为既有环境差异；verify 757/86 全绿 + 双端构建 + 17 包构建。**遗留**：shims `wx: any` 替换官方类型（需解 App/Page/Component/console 全局冲突 + Page.Option 严格性涟漪，独立批次）、MpSdkVersion 维护表补录、B9 跨端 PlatformAPI（依赖 B8，下一批）
159. **types-plus-plan B9 落地（★跨端 PlatformAPI 统一契约，API 实现的类型地基）**：`packages/types/src/platform-api.ts`——StorageAPI（get/set/remove/clear）/ RouterAPI（push/replace/back）/ UIAPI（showToast/showLoading/hideLoading）/ PlatformAPI（request + 三域，request 复用 api-types 的 RequestConfig/RequestResponse **单一来源**，不造第二套——规划 §2 的 header/statusCode 命名收敛为 headers/status）；根 index 导出 + `./platform-api` 子路径 + 构建入口；`@proteus-vue/api` types.ts re-export（运行时实现方，消费方一处取用）；类型断言 tests/types/platform-api.types.ts（正例 + @ts-expect-error 负例，build:web vue-tsc 校验，防漂移）；★踩坑：mock 泛型 `data: undefined as T`（非泛型箭头返回 undefined 不满足 T）、RequestConfig 需从 api-types 导入（platform-api 未 re-export）。验证：types/api 包构建 + verify 757/86 全绿（Node 22）+ 根 vue-tsc 零新增。**规划进度**：types-plus B1-B9 全部 ✅（B9 验收项里 ESLint no-restricted-globals 由 capabilities:check B5 平台规范扫描覆盖）；**遗留**：shims wx:any 替换（独立批次）、MpSdkVersion 维护表补录；下一候选：shims 替换 / component-plan / api 包接入 PlatformAPI 运行时
160. **PlatformAPI 运行时落地（★@proteus-vue/api createPlatformAPI 四域实现，B9 契约消费端闭环）**：`packages/api/src/platform.ts`——createPlatformAPI(adapter?) 返回完整 PlatformAPI：**request**（复用 createRequestAdapter 平台探测 wx.request/fetch，adapter 可注入）；**storage**（wx.setStorageSync 系 / localStorage JSON / 内存 Map 兜底三级）；**router**（wx.navigateTo/redirectTo/navigateBack / web pushState+replaceState+手动 dispatchEvent(popstate)——框架 web-adapter/RouterView 监听 popstate 驱动路由）；**ui**（wx.showToast/showLoading/hideLoading / web DOM toast 带 .proteus-toast 类 + console 降级 SSR 安全）；平台探测 globalThis 窄类型（api 包不直接 import 官方包保持轻量）；★MP 产物 ES5 安全（无 ?./??/展开/解构，显式 null 检查）；index.ts 导出 createPlatformAPI + PlatformAPI 类型 re-export。**★契约自包含修复**：platform-api.ts re-export RequestConfig/RequestResponse（消费方从契约文件一处取用，踩坑：createPlatformAPI 构建报 TS2459「declares locally but not exported」→ 补 re-export）。测试 tests/platform-api.test.ts 7 用例（jsdom：wx 分支 stubGlobal 三域转发 + web 分支 localStorage/history/popstate/DOM toast + request 注入 adapter 转发；afterEach vi.unstubAllGlobals——决策 #156 踩坑教训）。验证：verify 764/87 全绿（Node 22）+ api 包构建；**遗留**：web ui 为轻量 DOM toast（后续可接 @proteus-vue/web toast 组件）、shims wx:any 替换、MpSdkVersion 维护表补录；下一候选：shims 替换（B8 验收闭环）或 component-plan（p-* 组件，依赖 MpComponentSchema）
161. **shims wx:any → 官方类型替换（★B8 验收闭环，消灭 wx: any 硬伤）**：`packages/shared/src/shims/mp.d.ts` + `examples/shims/mp.d.ts`（模板副本，决策 #104）两处同步：① 顶部 `/// <reference types="miniprogram-api-typings" />` ② `declare const wx: any` → `declare const wx: WechatMiniprogram.Wx` ③ 删自建 `declare function App/Page/Component`（官方全局 `declare let Page: WechatMiniprogram.Page.Constructor` 等已有）；保留自建：RouteContext/RouteBuilder/RouteBuilderResult（官方不覆盖的 Skyline 自定义路由形态）、PageOptions/ComponentOptions、screenHeight/windowWidth（官方 skyline.d.ts 无）、__PROTEUS_DEBUG__/__PROTEUS_SKYLINE__。**★官方全局激活的涟漪修复三处**：① `mp-adapter.ts` getCurrentPages 是**全局函数**不在 wx.*（官方类型 TS2339）→ 改 `typeof getCurrentPages !== 'function'` + 全局调用；② `platform-guards.test.ts` require.resolve 被官方 `Require` 类型覆盖（无 resolve）→ `createRequire(import.meta.url)`；③ `examples/main.mp.ts` CustomRouteBuilder：官方 `CustomRouteConfig` 字段**全必选** vs 自建 RouteBuilderResult 可选 → `as unknown as WechatMiniprogram.CustomRouteBuilder` 显式边界适配（产物校验保留可选形态）。官方 Console（debug/error/group/groupEnd/info/log/warn）覆盖代码全部用法（log/warn/error/info），无 console.trace/table 使用。验证：verify 764/87 全绿（Node 22）+ shared/router/runtime 包构建 + examples vue-tsc 0 错误 + 根 vue-tsc 仅剩 2 个既有 web-button 错误；grep 确认两处 shims 零 `wx: any`。**types-plus B8 全部验收完成**；**遗留**：MpSdkVersion 维护表补录、web ui 接 @proteus-vue/web toast；下一候选：component-plan 消费 MpComponentSchema / MpSdkVersion 补录 / css-compat
162. **内置组件拆包 @proteus-vue/built-in-components（★架构决策：内置组件 = 微信小程序内置组件为基准，与扩展组件区分；用户发起）**：背景——`src/components` 不再是内置组件；新基准是微信内置组件（view/text/button/…），Web 端用 `@proteus-vue/web` 的 Vue 模拟（proteus-*）。**拆包（用户核心理由：区分框架内置组件 vs 扩展组件，扩展走 @proteus-vue/components）**：`git mv packages/web/src/{components,open-type.ts,global-components.ts,style.css} → packages/built-in-components/src/`；新包 scaffold（deps @proteus-vue/shared+types、peer vue、exports ./style.css）+ `installBuiltInComponents`（13 个 proteus-* 全局注册）+ `schemas.ts`（BUILT_IN_TAGS ↔ MpComponentSchema 对照，编译期 Record 断言防漂移）+ index（WebView/WebText/… + OPEN_TYPE_* + BUILT_IN_TAGS）+ README（与扩展组件区分表）；**web 包瘦身**：installWebPlatform = installBuiltInComponents + installWxApi（聚合，依赖方向 web → built-in-components），index re-export 内置组件（向后兼容）；**B8 注册表补 icon**（Web 已实现 icon 但 MpComponentRegistry 漏登记 → 补 schema + REQUIRED_TAGS）。**★踩坑五连**：① workspace 依赖版本（@proteus-vue/shared 实为 **0.2.0-beta.0** 声明 0.1.0 → npm 404 拉 registry，决策 #102 复现）② **GlobalComponents 双声明冲突 TS2717**（web/src import 包名 @proteus-vue/built-in-components → 根程序经 node_modules 拉 dist 声明，与 tests 相对路径拉 src 声明双份合并）→ 根 tsconfig paths 加 built-in-components 源码 alias（与 @proteus-vue/components 同模式，决策 #115）③ vitest 缺 `@proteus-vue/types/mp/component-schema` 子路径 alias → vitest.config 补（resolve 不了 exports 子路径）④ web/dist 旧构建残留 global-components.d.ts（新构建不再生成，esbuild 不清理）→ rm 重建 ⑤ examples 走 dist（决策 #115）需重建 web 包 dist。测试 tests/built-in-components.test.ts 4 用例（BUILT_IN_TAGS ⊆ 注册表 / 注册表形状 / icon 补登记 / install 注册 13 组件）。验证：verify 768/88 全绿（Node 22）+ examples vue-tsc 0 错误 + 根 vue-tsc 仅剩 2 既有 + check-consistency 通过；**遗留**：src/components 旧 p-* 体系保留不动（builtin-components-demo 在用，后续迁移）、Skyline 端内置组件 usingComponents 自动解析链路、App 端 v0.6
163. **PlatformAPI 收口演示页（★业务代码零 wx.* 的落地形态，用户发起）**：`examples/pages/platform-api-demo.vue`——四域演示：① request（api.request GET，默认 httpbin.org/get CORS 开放，URL 可编辑）② storage（api.storage.set/get/remove JSON 往返）③ router（api.router.push→showcase / back）④ ui（api.ui.showToast / showLoading 2s 自动关 / hideLoading）+ 对照说明（wx.* 直写 → platformAPI.* 收口映射）；页面顶部 `const api = createPlatformAPI()`（演示每页实例，注释标注生产建议模块级单例/DI）；首页链接 + `<route>` meta；★分层澄清（用户问询）：业务层 wx.* 由 capabilities:check B5 平台规范扫描门禁禁止（决策 #138/#144），平台实现层（adapters/presets/main.mp.ts wx.router）保留官方 wx 全局类型；不建框架自己的 wx 命名空间（铁律 #6 第三方复用 + 运行时 wx 即全局）；demo 页 wx.* 为模拟层验证性质。验证：examples vue-tsc 0 + verify 768/88 全绿（Node 22）+ **MP 产物确认**（app.json 收录 + 四件套 + `require('../_proteus/api.js')` 共享模块放行 + createPlatformAPI 引用）；**遗留**：examples 全局 wx 双声明隐患（mp.d.ts 官方 Wx vs proteus-web.d.ts WxApi，Web 端类型边界，待修）
164. **遗留事项清理（★用户发起「继续修遗留事项」，四连）**：① **examples 全局 wx 双声明——实为死文件**：排查发现 examples/tsconfig include **不含 src/shims/**（只有 shims/），`examples/src/shims/proteus-web.d.ts`（declare global wx: WxApi）**从未参与编译**，双声明冲突不存在，demo 页 wx 类型一直是官方 Wx；删除文件 + 空目录（examples vue-tsc 0 错误 + build:web 通过验证）；② **MpSdkVersion 维护表修正**：原 '2.29.2': '3.0.0'/'3.0.0': '3.12.0' 为占位且语义混淆（**typings 3.x 线 ≠ 基础库 3.x**——typings 3.0 是 2020 老线，3.12.3 才对应 2024 基础库 3.x）；按 npm 发布窗口对齐修正 '2.29.2'→'3.6.0'（2022-09 稳定线）、'3.0.0'→'3.12.3'（2024-08 稳定线），标注人工维护 + 非官方权威映射 + 版本号不同步提醒；③ **根 vue-tsc 清零**：web-button.test.ts multiSelector events 推断 never[]（TS2345 既有积压）→ `reactive<{ changes: unknown[][]; columnchanges: number[][] }>` 显式类型——**根 vue-tsc 零错误**（PROJECT_MEMORY 长期「36/2 个预先存在错误」积压项正式关闭）；④ **router-plan 机器路径**：docs/proteus-router-plan/09 的 `/data/workspace/...` 改为仓库相对路径。验证：verify 768/88 全绿（Node 22）+ examples vue-tsc 0 + 根 vue-tsc 0 + check-consistency 通过 + 全仓 /data/workspace 零残留
165. **PlatformAPI 高频 API 补齐（★用户问询「showModal 是不是没做」驱动）**：差距盘点——@proteus-vue/web 模拟层 WxApi 已有完整 showModal（WeUI DOM）/showActionSheet，但 PlatformAPI 契约（B9）只覆盖 showToast/showLoading/router 三方法/request/storage。**补齐**：① types/platform-api.ts 扩展 `ui.showModal`（ModalOptions/ModalResult：title/content/showCancel/confirmText/cancelText）+ `ui.showActionSheet`（ActionSheetOptions/Result：itemList/cancelText，取消 tapIndex=-1）+ `router.switchTab/reLaunch`（Web 端映射 replace 语义，注释说明）；形状自测 mock 补全；② api/platform.ts：WxGlobal 窄类型补 showModal/showActionSheet/switchTab/reLaunch；wx 分支回调式 success 转发（Promise 包装）；web 分支 DOM 实现（createDomOverlay 内联样式 .proteus-modal/.proteus-actionsheet + 确认/取消/遮罩点击 + item/cancel 行带类名，SSR console 降级）；③ tests：wx 分支转发（switchTab/reLaunch/modal/sheet 断言）+ Web DOM modal（确认/取消/遮罩）+ actionSheet（点项/取消）→ 9 用例（770 全绿）；④ demo 页 platform-api-demo.vue 补 showModal/showActionSheet 按钮 + 对照表更新（wx.showModal → api.ui.showModal 等）；★pageScrollTo 不补（跨端差异大，走 capability）。**★踩坑**：createDomOverlay 的 doc 参数化（TS18048 判空）、actionSheet 选择器需类名（mask 后代含 box 本身，querySelectorAll div 偏移错位）。验证：verify 770/88 全绿（Node 22）+ 根 vue-tsc 0 + api 包构建；**遗留**：pageScrollTo 走 capability、src/components 旧体系迁移、Skyline usingComponents 链路
166. **types-plus 全量对照核查 + B1/B5 收口补齐（★用户发起「对比 types-plus-plan 看没做完的」）**：逐份对照 10 份文档验收项 → gap 清单（P0：条件类型工具 / lifecycle 类型收口 / IR 守卫；P1：CONFIG_LAYER_VIOLATION / contracts 包；P2：千级 registry 类型性能；备注：__PLATFORM__ 编译期注入为已定案差异——现状运行时 detectPlatform）。**P0 三项落地**：① `packages/types/src/utils.ts`——IfPlatform/ExtractByPlatform/RequiredBy（01 §2/§6 验收「调用点正确收窄」）+ 类型断言 tests/types/conditionals.types.ts（正例 + @ts-expect-error 负例；★踩坑：interface 是类型非值，void [adapterWeb,...] 报 TS2304 → 删）；② `packages/types/src/lifecycle-types.ts`——AppPhase/LaunchType/LifecycleContext/PhaseHook/FallbackStrategy 收口（08 契约表「types 提供」，以 runtime 实现字段为准：launchOptions/network/platform/isMinimalMode），runtime/lifecycle.ts 改 re-export（LifecyclePhase = AppPhase 别名 + PHASE_ORDER Record<AppPhase,true> 编译期断言防漂移）；③ `packages/types/src/ir-guards.ts`——isRouteIR/isStoreIR/isSFCIR + assertRouteIR/assertStoreIR/assertSFCIR（05 §2）+ IR_GUARD_ERROR_CODES（IR_INVALID_ROUTE/STORE/SFC，05 §3）+ source 定位；tests/ir-guards.test.ts 9 用例；types 包 exports 补 ./lifecycle-types ./utils ./ir-guards 子路径 + 构建入口 + 根 index 导出。验证：verify 779/89 全绿（Node 22）+ 根 vue-tsc 0 + types/runtime 包构建；**剩余**：P1 CONFIG_LAYER_VIOLATION（config 字段归属标注 → 跨层依赖检测，需设计）、P1 contracts 包（架构规约 L0 新包，大决策）、P2 千级 registry 类型性能验证、audit 规则自动生成（03 产物 3）
167. **CONFIG_LAYER_VIOLATION 落地（★types-plus B2 §4 + B5 §3，跨层隐式依赖检测）**：`packages/types/src/config-layers.ts`——① `ConfigLayer`（compiler/router/pinia/api/platform/module/component/lifecycle/build 九层）+ `CONFIG_FIELD_LAYERS`（12 个顶层字段归属表，对齐 02 §4 + validateConfig KNOWN_FIELDS）；② `CROSS_LAYER_PATTERNS` 跨层反模式清单（router 禁 pinia 语义键 stores/storeKey/hydrate/persist、customRoute 禁 compiler 语义 mapping/customTags、rules 禁 router 语义 navigateTo/switchTab/reLaunch——05 §3「router 字段不得影响 pinia 行为」落地实例）；③ `checkConfigLayerViolations(config)` 纯函数：归属表完整性（顶层字段漏标 → 违规，新增字段防漂移）+ 反模式越层检测（path 定位）；④ `getFieldLayer` 查询。**CLI 集成**：config-validate.ts validateConfig 末尾合并 layer 错误，未知字段跳过（避免与 CONFIG_UNKNOWN_FIELD 重复报——按顶层字段过滤）。测试：tests/config-layers.test.ts 8 用例（归属表覆盖/getFieldLayer/清单字段有归属/合法零违规/三个反模式/漏标）+ config-validate.test.ts 补 2（越层集成 + 未知字段不重复）。验证：verify 788/90 全绿（Node 22）+ 根 vue-tsc 0 + examples/proteus.config.ts 校验仍通过（CLI 集成零回归）；**剩余**：P1 contracts 包（08 + 架构规约 L0）、P2 千级 registry 类型性能、audit 规则自动生成（03 产物 3）
168. **@proteus-vue/contracts 包落地（★架构规约 L0 跨层 DTO 契约，types-plan §07，P1 收官）**：新包 scaffold（零依赖纯类型）：`route.ts`（RouteTransition/RouteMeta/RouteRecord 收口自 types）+ `api.ts`（ApiResponse<T> 跨层响应 DTO，运行时 RequestResponse 含 config 留 api-types）+ `store.ts`（StoreSnapshot 结构化快照契约，与 runtime __PROTEUS_STORES__ 互补）+ `capability.ts`（CapabilityDescriptor 对齐 capabilities scan ManifestCapabilityEntry 形状）+ exports ./route ./api ./store ./capability 子路径。**收口（★避免循环依赖：contracts 零依赖 types，types → contracts 单向）**：types/index-shared.ts RouteTransition → re-export contracts；types/router-types.ts RouteRecord/RouteMeta → re-export contracts（RouteBlock/RouteNode.meta 改 import contracts）；types package.json 加 @proteus-vue/contracts 依赖；**★顺手消除重复定义（铁律 #9）**：router/transforms/transform-transition.ts 的本地 `type RouteTransition`（与 types/index-shared 重复）→ import @proteus-vue/types——grep 验证全仓仅 contracts 一处定义。规约注册表补 built-in-components 行（00-architecture L1 + ARCHITECTURE.md monorepo，文档与代码 1:1）。测试 tests/types/contracts.types.ts 类型断言（正例 + @ts-expect-error 负例 + types re-export 一致性）。验证：verify 788/90 全绿（Node 22，零回归）+ 根 vue-tsc 0 + check-consistency：contracts 从「规划中」清单消除 + packages/contracts 存在 ✅；**P1 全部收官（CONFIG_LAYER_VIOLATION + contracts）**；**剩余**：P2 千级 registry 类型性能、audit 规则自动生成（03 产物 3）、MpSdkVersion 维护表补录
169. **types-plus P2 收尾（★03 §4 产物 3 audit 规则 + 06 §5 千级 Registry 性能，用户发起「继续收尾」）**：① **CONFIG_AUDIT_RULES**（config-layers.ts）——数据驱动 audit 规则注册表（03 §4「字段新增自动扩展规则无需手写」）：config:layer-violation（check = checkConfigLayerViolations）+ config:layer-unassigned（归属表完整性）；新增字段只需补录 CONFIG_FIELD_LAYERS，规则自动覆盖；types index 导出；测试 +3（注册表 id/两条规则 check 行为）；★踩坑：edit_file 误替换 describe 头破坏测试文件结构（旧 describe 残留无头）→ 补回头修复；② **registry-perf.test.ts**（tests/perf/，06 §5）——生成 1000 条 StoresRegistry 模块扩充声明 → 独立 tsc --noEmit 计时：**1000 级 379ms**（阈值 30s，对齐 07 §7「千级 store CI < 3min」）；★踩坑：declare module 是 ambient 扩充非文件模块，main.ts 须 `import type {} from 'proteus-registry-perf'`（import './registry' 报 TS2306 is not a module）；验证：verify 792/91 全绿（Node 22）+ 根 vue-tsc 0。**★types-plus-plan 全部完成（B1-B9 + §07 contracts + P0/P1/P2）**；**遗留**：MpSdkVersion 维护表持续补录（维护性质）；下一候选：css-compat / safe-area / glass 等 G 序批次
170. **style-safety 批次入仓 + G-31 执行位登记（★新 plan 与规约整合）**：docs/proteus-style-safety/（12 文档 + README，全端样式运行时安全：三层防线编译期+运行时+原生闸门，原则 #10.1 补充）随 P2 提交入仓，但声称「执行位 G-16」与规约 G-16（blueprint B1-B5）**撞号**——check-consistency 只查规约层三份文件 G 表，扫不出 plan 声称编号冲突。**按 v3.2 追加先例收口**：① style-safety 全部 G-16 声称 → **G-31**（7 文件 16 处，perl 批量替换）；② 00-architecture.md：版本 v3.2→v3.3、文档数 10→11 份新增、G 表加 G-31 行（style-safety B1-B4，依赖 G-21 CSS 矩阵 ✅/G-22 App Renderer/ G-10 Compiler IR）、追加说明 v3.3（G-16 = blueprint、style-safety = G-31，撞号一律以本表为准）；③ ARCHITECTURE.md 标题 G-01~G-30→G-31 + 表行；④ 01-optimization-log.md 加 v3.3 改动记录 + G-31 行。验证：check-consistency 通过（三文件 G 表一致 + G-01~G-31 连续）+ 全仓 G-16 零残留（style-safety 文档）；★经验：新 plan 入仓需同步规约 G 表，plan 声称编号与规约冲突以规约表为准；pack.sh 缺失与其他批次一致（生成时填充约定）
171. **css-compat G-21 B1 落地（★CSS 跨端兼容：--strict-css 校验 + 编译期重写 + 报告）**：新包 **@proteus-vue/css-compat**（★拆包决策：规划文档说「Compiler 包内新增 CSS 处理管线」，但 style-safety G-31 要消费其白名单（ALLOWED_STYLE_PROPS），独立包避免循环依赖 + 与 built-in-components 拆包模式一致；check-consistency 包注册表 L1 登记，css-compat 从「未落地」消除）。**B1 交付**：① `rules.ts`——CSS001-012 数据驱动规则注册表（02 表全量：float/display:inline/`*`/`[attr]`/元素选择器/深层后代>2级/z-index/calc·vh·vw/backdrop-filter/:nth-child/box-shadow rgba/@media 白名单），strict error / 降级 warn + fixable 标注；② `rewrite.ts`——calc 数值折叠（同单位二元运算）/ vh·vw→% / rgba→#RRGGBBAA（03 §一.3 内部表示）；③ `report.ts`——buildCssCompatReport（03 §三 结构：rewritten/semanticComponents/forbidden/bundleCssBytes + violations），AST 遍历（★避开 TS 对字符类内 `{`/`(` 的量词解析坑，正则计数改 postcss AST）；④ `extract.ts`——SFC `<style>` 提取（@vue/compiler-sfc，多块/scoped/lang）；⑤ **CLI `proteus css:check [dir|file] [--no-strict] [--fix] [--report <path>]`**（扫描 .vue/.css → 校验 + 重写统计 + 报告落盘，error 级 exit 1）。测试 24 用例（12 报错码各一 + 重写 4 + 报告 2 + SFC 2 + CLI 3）。**★顺手修既有 bug**：capabilities 包 esbuild bundle 未 external 内置模块（fs/path/…）+ esbuild 自身 → ESM 下 dynamic require 崩溃（CLI dist 真实运行全挂，vitest alias 到源码掩盖）→ build 补 `--external:esbuild --external:fs ... --external:constants`；★踩坑：① TS 正则字面量字符类内 `{` 触发量词解析错乱（TS1005 ')' expected）→ 正则计数改 AST 遍历 ② `foldCalcExpr` 捕获组编号错误（m[4] 是第二个数字非单位，单位在 m[5]）→ calc 折叠失效 ③ `.match()` 带 g 标志不返回捕获组（m[2] undefined）→ 改 exec。验证：verify 816/93 全绿（Node 22）+ 根 vue-tsc 0 + check-consistency 通过 + CLI dist 真实运行闭环（css:check examples 出 CSS005/CSS007/CSS012 命中）；**剩余**：B2 五端 Renderer 映射（Style IR）、B3 预算门禁；下一候选：style-safety G-31 B1（消费本包白名单）或 css-compat B2
172. **style-safety G-31 B1 落地（★全端样式运行时安全：validateStyle + 白名单 + CLI style:check）**：**runtime 包 `src/style-safety/`**（@proteus-vue/runtime/style-safety 子路径）：① `whitelist.ts`——ALLOWED_STYLE_PROPS（03 §1 全量：✅直映射 32 项 Length/Color/Opacity/Integer/FlexNumber/FlexAlign/FlexJustify/Transform/TransformOrigin + 🔶 SEMANTIC_ONLY backdropFilter/filter + ❌ FORBIDDEN display/float/clear/verticalAlign）+ PROP_TYPES 类型守卫 + FALLBACK_DEFAULTS 降级默认值（06 §5：width/height→0、opacity→1、color→inherit、borderRadius→0）；② `platform-narrowing.ts`——narrowValue 逐平台收窄（B1 基础：Length 字符串→数值归一、Opacity/Integer 校验，五端真机映射 B2/B4，平台参数预留）；③ `validator.ts`——validateStyle（06 §2 核心：①白名单 ②类型守卫 ③平台收窄 + 降级 + silent/onReject 上报钩子）+ validateProp/isValidStyleProp；**CLI `proteus style:check [dir|file] [--platform web|skyline|ios|android|harmony]`**——模板 `:style` 绑定静态分析（08 §2 STS001-006：白名单/静态值类型/语义组件/禁用属性 + STS006 动态源提示非阻断）+ 静态推导覆盖率报告（08 §3 stats）。**★语义澄清**：动态值只查属性名（值运行时 validateStyle 兜底），静态字面量才查类型——避免把标识符当字符串误判；**★踩坑四连**：① vitest alias 前缀匹配（@proteus-vue/runtime 会吞 /style-safety 子路径 → 子路径 alias 必须在父路径前）② `:style` 提取正则 `[^"']*` 遇表达式内引号提前截断 → 反向引用配对，但 `\1` 引用的是组1 (style) 而非引号组 → `\2` ③ CSS 变量 `--barrier-opacity` 被 kebab→camel 归一化破坏（→ `-BarrierOpacity`）→ `--` 前缀保持原样放行（Theme token 层级，03 §2）④ ALLOWED_STYLE_PROPS key 用 camelCase 而模板常写 kebab（'backdrop-filter'）→ 归一化映射。验证：verify 833/95 全绿（Node 22，816→833 +17）+ 根 vue-tsc 0 + check-consistency 通过 + CLI dist 真实运行（style:check examples 零违规，静态覆盖率 0.50）+ patchStyle 接入点预留（App Renderer G-22 落地时挂 validateStyle）；**B1 完成**；**剩余**：B2 值类型系统逐平台收窄细化、B3 :style AST 编译期推导（Compiler transform）、B4 五端原生闸门；下一候选：css-compat B2 / style-safety B2 / 并行变更提交
173. **style-safety G-31 B3 落地（★编译期 :style 推导：可达值集 + 常量折叠 + 覆盖率；★白名单下沉 contracts）**：**架构决策（铁律 #9 同源）**：runtime 与 compiler 都要消费样式白名单，但 compiler（L0）依赖 runtime（L1）违反分层铁律 → **白名单数据下沉 contracts 包 `src/style.ts`**（STYLE_PROP_LEVELS 纯数据契约，L0 零依赖），runtime whitelist.ts 改为从 contracts 导入（API 不变，测试零改动），compiler 从 contracts 校验——三处同源。**compiler 包 `src/style-safety/`**（@proteus-vue/compiler/style-safety 子路径）：① `constants.ts`——extractScriptConstants 顶层 const 字面量常量表（05 §5：`const baseWidth = 100` → 折叠数据源；跳过 ref/computed/箭头函数/缩进/import）；② `reachability.ts`——deriveReachableValues 可达值集推导（05 §2：Literal 静态 / Identifier 常量折叠或动态 / Conditional union 两侧 / Binary 静态操作数折叠 / 成员·调用·未知动态保留来源名）；③ `index.ts`——analyzeStyleBindings(template, constants)：:style 绑定提取 + 对象字面量解析（从 CLI style-check 迁移，消除 cli/compiler 重复）+ 白名单校验（STS001-006）+ 静态/动态分类 + 覆盖率统计（05 §4 目标 > 80%）。**CLI style-check 重构**：薄壳化（扫描 .vue → extractScriptConstants + analyzeStyleBindings → formatStyleCheck），B1 解析逻辑全部归 compiler。测试 tests/style-safety-b3.test.ts 12 用例（reachability 5 + constants 2 + analyze 5）。★踩坑：① 成员表达式（apiData.width）非合法标识符 → default 分支保留原始表达式为 dynamicSources 名（原返回 'unknown' 断言失败）② evalLiteral('() => 1') 返回函数值 → 常量表排除 typeof function ③ CSS 变量动态值（--x: computed 变量）计入 dynamic 不跳过（覆盖率准确：RouterView barrierOpacity 动态 → static 0/dynamic 1）。验证：verify 845/96 全绿（Node 22，833→845 +12）+ 根 vue-tsc 0 + check-consistency 通过 + CLI dist 真实运行（style:check examples 覆盖率 1.00；RouterView 单文件 0.00——computed 动态值正确计入）；**B3 编译期推导落地**；**剩余**：B2 逐平台收窄细化、B4 五端原生闸门 + DevTools、_validated/_runtimeValidate 代码生成接入（B3 后续）；下一候选：css-compat B2 / style-safety B2 / 并行变更提交
174. **style-safety G-31 B2 落地（★值类型系统 + 逐平台收窄矩阵）**：**runtime `src/style-safety/types.ts`**（04 §1-§2）——命名类型守卫（类型谓词）：isLength（数值/px/rem/%/auto）/isColor（hex/rgba/hsla/var(--token)/transparent/inherit，**命名色 'red' 不入白名单**——矩阵约束）/isOpacity（0-1 有限）/isInteger/isFlexNumber/isFiniteNumber/isEnum（FlexAlign/FlexJustify 枚举工厂）。**platform-narrowing.ts 增强（04 §3）**：`PLATFORM_LENGTH_RULES` 五端规则注册表（iOS CGFloat 拦负数禁 auto / Android TypedValue 拦 NaN·auto→wrap-content / 鸿蒙 Length 拦负数超限 / Skyline Number 拦非有限 / Web CSSLength 最宽松）+ `narrowLength` 按平台拦截/归一（字符串→数值仅非 web；web 保留原值 CSSLength 原生语义）+ `narrowTransform`（**Skyline 同层渲染不支持 rotate/skew → 拦截走原生动画 API**，CSS 矩阵联动）。**★重构**：whitelist PROP_TYPES 改复用 types.ts 命名守卫（isLength/isColor/...），消除重复逻辑（B1 内联守卫 → B2 谓词复用）；★行为变更：web 端 Length 字符串不再归一为数值（04 §3 原样返回）——B1 测试断言同步更新。测试 tests/style-safety-b2.test.ts 12 用例（守卫 3 + 逐平台矩阵 6 + validateProp 联动 2 + 注册表 1）。验证：verify 856/97 全绿（Node 22，845→856 +11）+ 根 vue-tsc 0 + check-consistency 通过；**style-safety B1/B2/B3 三层防线闭环（白名单 + 类型系统 + 逐平台收窄 + 编译期推导 + 运行时 Validator）**；**剩余**：B4 五端原生闸门 + DevTools（依赖 App Renderer/真机）、_validated 代码生成接入；下一候选：css-compat B2 / style-safety B4 / 并行变更提交
175. **css-compat G-21 B3 落地（★预算门禁 + CI 脚本 + 全局聚合报告）**：**css-compat 包 `budget.ts`**——CSS_BUDGETS 六项预算表（10 §一：bundleCssBytes ≤60KB / criticalCssBytes ≤14KB / styleIRObjects ≤1500 / selectors ≤800 / semanticRatio ≥0.7 / forbiddenCount =0）+ checkCssBudget 门禁校验（max/min 方向）+ formatBudgetCheck。**report.ts 扩展**：CssCompatReport 加 forbiddenCount（float+universalSelector 总数）+ selectors + **classSelectors**（semanticRatio 数据源）；parse 错误分支补齐字段。**CLI css:check 聚合（10 §四）**：global 全局报告（bundle 汇总 + critical 取单文件最大·最坏首屏 B3 近似 + styleIRObjects≈selectors 代理指标 + semanticRatio=classSelectors/selectors + forbiddenCount=error 级 CSS001-007 数）+ budgetChecks；`--report` 落盘含 global/budgetChecks；ok 判定含预算门禁（超限 exit 1）。**scripts/check-css-report.mjs**：CI 门禁脚本（读报告 JSON → 六项断言 → 超限 exit 1 阻断 PR；--budgets 打印预算表）。**★语义修正**：semanticRatio 从「语义组件建议命中占比」（CSS 侧无法统计 p-* 实际使用）改为「类选择器占比」（.class 范式 = 语义化，06 选择器级联哲学——CSS 侧可准确计算）；forbiddenCount 只统计 error 级禁止项（loose 降级后 warn 不阻断，测试语义对齐）；★踩坑：checkFile 手动构造 report 漏 B3 新字段（selectors/forbiddenCount/classSelectors null）→ 补齐累加；预算门禁对 examples 真实生效（24 个 CSS001-007 违规：元素选择器/深层后代/z-index——examples 样式待迁移）。验证：verify 861/98 全绿（Node 22，856→861 +5）+ 根 vue-tsc 0 + check-consistency 通过 + **CLI→check-css-report.mjs 全链路闭环**（css:check examples --report → 脚本断言：字节 14426≤60K / 选择器 146≤800 / 语义占比 96.6%≥0.7 全过，违规 24 → exit 1 门禁阻断）；**css-compat B1/B3 落地（B2 五端 Renderer 依赖 G-22 App Renderer）**；**剩余**：B2 Style IR 五端映射、examples 样式迁移（24 处禁止项）；下一候选：css-compat B2（待 G-22）/ style-safety B4 / examples 样式治理 / 并行变更提交
176. **examples 样式治理 + 门禁规则三修（★dogfooding：自己落地的门禁用在自己 demo 上）**：css:check examples 24 处 CSS001-007 违规治理到 **0**。**① 规则层三修（css-compat rules.ts）**：a) **CSS007 z-index error→warn**——B1「出现即报」无法判定跨父级 stacking（14 处全误伤：RouterView 层叠转场合法用法），降级提示待 B2 精确判定后升 error（02 表目标态保持 error）；b) **CSS008 vh/vw 收窄**——仅 height/width 报（键盘弹出遮挡输入框）；`min-height: 100vh` 弹性安全用法豁免（RouterView 2 处因此免改）；c) **CSS006 多行选择器误计 bug**——`.fade-enter-active,\n .fade-leave-active` 行首缩进空格被 countCascadeDepth 计为组合符（报 19 层）→ 折叠换行+行首空白后计数。**② examples 治理（6 文件，元素选择器 → 类选择器 + 模板加类）**：`.links a` → `.links .link`（index.vue 12 个 a / user / order list 模板加 class="link"）；`.card h3/p` → `.card .card-title/.card-desc`（showcase）；`button` → `.btn`（pinia-demo 4 按钮）；`.msd-slider` inline-block → block（mp-semantics，flex 子项无差别）。**③ CLI 行号修复**：postcss loc 相对 style 块 → 补块起始行（extractStyleBlocks baseLine）→ 文件绝对行号（此前 :12 实为 style 块内偏移）。**★踩坑**：perl 里 `@click` 数组插值（`@` 在 perl 是数组符号）→ 替换失效，转义 `\@click`；edit_file 误删 for-of 闭合括号 → 补回。验证：verify 861/98 全绿（Node 22）+ 根 vue-tsc 0 + check-consistency 通过 + **examples 预算门禁全绿**（CSS001-007 违规 0 / 字节 14451≤60K / 语义占比 97.3%≥70% / 剩余仅 CSS007 warn 提示 + CSS012 @media 提示，不阻断）+ 测试 +3（CSS007 warn 级别 / CSS008 min-height 豁免 / CSS006 多行回归）；**门禁真实闭环（demo 全绿）**；下一候选：css-compat B2（待 G-22）/ style-safety B4 / 并行变更提交
177. **docs 目录规范化 + plus 系列执行位收口（★工作区并行变更收口 + G 表撞号处理）**：① **目录重命名**（git rename 识别 81 文件，6 目录统一 -plan 后缀：blueprint/css-compat/design-principle/safe-area/style-safety/test-framework）+ 新增 cli-plus/devtools-plus/router-plus 三份 P0 plan（第 33-35 份）；② **★补漏提交**：css-check.ts 行号补偿 + index.ts --report 聚合 + style-safety.test.ts B2 断言（3 个文件前批漏 add）；③ **★G 表撞号检查**（style-safety G-16 先例）：plus 系列声称 G-17/18/19 全部与规约 blueprint/website 撞号 → 规约 G 表追加 **G-32（router-plus）/ G-33（cli-plus）/ G-34（devtools-plus）** + v3.4 追加说明（声称编号重指向 + 旧编号引用 G-05/G-06/G-09/G-11/G-12/G-13/G-16 以本表为准）+ plan 文档 46 处编号重指向（router-plus G-17→G-32、cli-plus G-18→G-33、devtools-plus G-19→G-34；交叉引用：Style Safety G-16→G-31、CLI G-18→G-33、Router G-17→G-32；cli-plus「G-07~G-16 横切区间」保留原编号——区间语义非单点引用）；三份规约文件 G 表同步 G-01~G-34。验证：check-consistency 通过（三文件 G 表一致 + G-01~G-34 连续）+ 全仓 plus 文档 G-15~G-19 零残留（保留区间除外）；★经验：新 plan 入仓必须同步规约 G 表 + 检查声称编号；**工作区并行变更全部收口**
178. **cli-plus G-33 M1 落地（★proteus check 一键全量门禁）**：**新命令 `proteus check [dir] [--no-strict-css|--no-strict-style|--no-strict-router|--no-strict-cli]`**（03-strict-cli.md §1 聚合四域，默认全开）：css（css:check 校验+预算门禁）/ style（style:check :style 白名单+覆盖率）/ router（router:check 路由合规）/ cli（config:check proteus.config.ts 校验，缺文件跳过提示——独立编译模式）+ 聚合文本报告 + 任一域失败 exit 1。parseCheckArgs 纯函数（--no-* 关闭对应域）。**★顺手修**：css:check 跳过预处理器 style 块（`lang="scss"` 等——forms.vue scss 被 postcss 解析报 CSS-PARSE 的既有 bug，门禁针对产物 CSS 子集）；**examples 路由治理**：router:check 检出 12 个页面 `<route>` 缺 path（builtin-components-demo/components-demo/config-demo/forms + 脚本批量 11 个）→ 按文件路径推导补全（scripts/backfill-route-path.mjs 一次性工具，推导规则：/pages/xxx 与 /subpackages/order/pages/list）。测试 tests/check-cli.test.ts 4 用例（全绿四域/样式违规 css 域失败/--no-* 关闭/parseCheckArgs）。★踩坑：validateConfig 必填字段（skyline/pagesDir/routesOutput/customRoute/setDataBridge/style）——测试 config 需完整形态（参照 examples/proteus.config.ts）。验证：verify 865/99 全绿（Node 22，861→865 +4）+ 根 vue-tsc 0 + check-consistency 通过 + **examples 上 `proteus check` 四域全绿 EXIT=0**（css 21 文件零违规 / style 覆盖率 / router 15 页 15 路由 / cli 配置通过）+ 路由补全 build:mp 无回归；**G-33 M1 CLI Core 骨架（check 聚合）落地**；**剩余**：M2 dev/build 骨架、defineProteus 类型；下一候选：cli-plus M1 剩余 / router-plus（G-32）/ css-compat B2（待 G-22）
179. **defineProteus 配置入口落地（★cli-plus G-33 M1 收尾，Vite defineConfig 模式）**：types 包 `src/define-proteus.ts`（@proteus-vue/types/define-proteus 子路径 + index 导出 + build 入口 + vitest/tsconfig alias）：`DefineProteusConfig`（01-cli.md §3 五端统一入口：entry + targets{web/skyline/ios/android/harmony} + features{glass/safeArea/memorial/skeleton/styleSafety/strictRouter 能力开关} + theme/fontScale/cache + router.deepLink）+ `defineProteus(config)` identity 函数。**★设计决策**：**identity 零运行时逻辑**（借鉴 Vite defineConfig）——types 包保持「纯类型 L0」铁律（不引入 Zod 运行时依赖），「配置错误 IDE 即时报错」靠类型推导，深度校验走 CLI（config-validate/check）。**★配置形态区分**：defineProteus 是 cli-plus 的**多端 targets 新形态**（entry/targets/features），与既有 ProteusConfig（platform/skyline/pagesDir 单端形态）并存——前者 v2 五端统一入口（G-33 M1 收尾），后者现有编译管线；文档 `@proteus-vue/config` 导入路径暂以 types 子路径承载（拆包后续按需）。测试 tests/define-proteus.test.ts 3 用例（identity 原样返回/全字段/必填字段 @ts-expect-error 负例）。验证：verify 868/100 全绿（Node 22，865→868 +3）+ 根 vue-tsc 0 + check-consistency 通过 + types 包构建正常；**G-33 M1 完成（check 聚合 + defineProteus）**；下一候选：cli-plus M2（dev/build 骨架）/ router-plus（G-32）/ css-compat B2（待 G-22）
180. **cli-plus G-33 strict-cli 完整规则 + dev 骨架（★CLI001-004 全落地 + 生成文件指纹机制）**：**strict-cli.ts**——① **CLI002 缺失必要 target 配置**（error，★形态识别：旧 ProteusConfig 有 platform 无 targets → 跳过，避免对单端编译管线误报）；② **CLI003 能力开关冲突**（warn，FEATURE_PLATFORM_MATRIX 六能力→平台矩阵：glass/safeArea/skeleton/styleSafety/strictRouter 五端、memorial 仅 web/skyline；feature 开启但 targets 含不支持端 → warn）；③ **CLI004 .proteus/ 生成文件一致性**（warn，**★指纹基线机制**：`registerGeneratedFile` 登记 sha256 到 `.proteus/.generated.json`，checkProteusDirConsistency 对比基线——**设计演进：JSON 头部注释标记破坏 JSON.parse（capabilities.test 崩）→ 改 hash 基线**；非 JSON 原生文件标记 AUTO_GENERATED_MARK 保留未来用）；集成进 check.ts cli 域（CLI001 config:check + CLI002/003 defineProteus 形态 + CLI004 一致性）。**生成器规范**：capability-manifest.ts / module-audit.ts 写文件后 registerGeneratedFile（纯 JSON 落盘保 JSON.parse 兼容）。**dev 骨架**（dev.ts）：parseDevArgs（--target web|skyline 默认 web，非法 target 报错）+ runDev 纯函数返回 spawn 计划（web → vite --mode web；skyline → npx tsx scripts/dev-mp.ts；app 端待 M3 原生同步），index.ts spawn + stdio 继承 + SIGINT/SIGTERM 转发。★踩坑：parseDevArgs 从 ./dev 而非 ./args 导入（index.ts import 列表加错导致 TS2300 Duplicate identifier）。测试 tests/strict-cli.test.ts 9 用例（CLI002 三态/CLI003 三态/CLI004 四态/dev 三态）。验证：verify 879/101 全绿（Node 22，868→879 +11）+ 根 vue-tsc 0 + check-consistency 通过 + **examples `proteus check` 四域全绿 EXIT=0 且 CLI004 零提示**（capability-manifest/module-graph 重新生成 + 指纹登记，.proteus/ gitignore 忽略）；**G-33 M1 全量完成（check + defineProteus + strict-cli 规则 + dev 骨架）**；**剩余**：M2 Web+Skyline 构建管线（复用 Vite，build 已部分有）、M3 原生工程同步（待 App Renderer）、M4 CI/CD 模板；下一候选：cli-plus M2 / router-plus（G-32）/ css-compat B2（待 G-22）
181. **cli-plus G-33 M2 落地（★proteus build --target 工程构建）**：**parseBuildArgs 加 `--target web|skyline|all`**（非法/缺参报错；缺省 = 独立编译不变）；**build.ts `planTargetedBuild`**（纯函数）：TARGET_BUILD_SCRIPTS 模板约定（web → build:web、skyline → build:mp——mp-weixin = Skyline 小程序包），读项目 package.json scripts 校验脚本存在（缺脚本报错提示 create-proteus 模板生成）→ spawn 计划 [{npm run build:web}]；**index.ts build 分支分流**：args.target 存在 → spawnSync 串行执行计划（stdio inherit，任一失败 exit 传递）；否则保留独立编译（buildDir）。测试 tests/build-target.test.ts 5 用例（脚本映射/单 target 计划/all 顺序/缺脚本报错/parseBuildArgs）。验证：verify 884/102 全绿（Node 22，879→884 +5）+ check-consistency 通过 + **examples 上 `proteus build --target web` 真实闭环**（spawn build:web → vue-tsc + vite build 跑通）；**G-33 M1+M2 完成（check/defineProteus/strict-cli/dev/build --target）**；**剩余**：M3 原生工程同步（待 G-22 App Renderer）、M4 CI/CD 模板；下一候选：router-plus（G-32）/ css-compat B2（待 G-22）/ M4 CI/CD 模板
182. **约定式路由收口（★用户发起「每个页面都要 route 块太麻烦，可以去掉吗」）**：**发现体系早已支持**（决策 #112/#113）：scanRoutes 的 `derivePath`（path 从文件路径推导）+ `includeNoRoute`（无块页面收录，meta 由 config 集中注入）——scan.ts 注释明确「页面 <route> 块完全可选」，gen-routes 已用（derivePath:true + includeNoRoute:true）。**落地**：① **router-check 对齐**（router-check.ts）：scanRoutes 从严格模式（true）改 `{ verbose:true, derivePath:true, includeNoRoute:true }`——与生成器双管线统一，`<route>` 块可选（仅覆盖 path/meta/params/pageJson 时写）；② **examples 全量清理**：14 个页面 `<route>` 块删除（strip-route-blocks.mjs 工具；**保留 user/profile**——params 类型声明无其他来源）+ title/isTab/requiresAuth 全部移入 proteus.config.ts 的 router.meta 集中注入（index/mine 的 isTab、分包 list、目录级 user 前缀）；③ **行为变更**：无块页面从「私有页跳过」→「推导收录」（private.vue 测试 4→5）。**验证**：gen-routes 重新生成后 auto-routes.ts **零 diff**（meta 注入结果与块声明等价——title/isTab/requiresAuth/params 全保留）+ build:mp 正常 + `proteus check` 四域全绿 + verify 884/102 全绿 + check-consistency 通过；**约定式路由落地：页面零样板，`<route>` 块仅剩 params/pageJson 等特殊声明场景**；下一候选：router-plus（G-32）/ css-compat B2（待 G-22）
183. **router-plus G-32 M1 落地（★路由语义层 + 五端导航映射 + 栈 diff + strict-router）**：**router 包 `src/navigation.ts`**（@proteus-vue/router/navigation 子路径 + exports + build 入口 + vitest/tsconfig alias）：`STACK_SEMANTICS`（push/present/replace/tab）+ `isStackSemantic` + `NAVIGATION_MAP`（§2.2 五端映射纯数据：四种栈语义 × web/skyline/ios/android/harmony 各端 API 名——Web/Skyline 运行时可执行指令、App 端原生 API 名，原则 #10）+ `BACK_MAP`（back 语义五端）+ `validateStackSemantic`（ROUTE004 语义校验）+ `resolveNavigation`（语义→端 API，缺省 push）。**`src/stack-diff.ts`**（@proteus-vue/router/stack-diff 子路径）：`computeRoutePatch(prev, next)` 栈 diff（公共前缀 → pop 多余 + push 新增；根切换 → tab + push）+ `applyRoutePatch`（补丁应用，round-trip 测试）；§2.3「转场事务」的纯逻辑核心。**strict-router 集成**（router-check.ts）：ROUTE003 path kebab-case（★兼容两种格式：Vue Router '/home' 前导斜杠 + 小程序 'pages/home'）+ ROUTE004 meta.stack 非法值（validateStackSemantic）。测试 tests/router-plus.test.ts 10 用例（NAVIGATION_MAP 完整性/语义校验/栈 diff 5 态/applyRoutePatch round-trip/strict-router）。★踩坑三连：① `import from '@proteus-vue/router'` 顶层触发 shared web-adapter（Node 无 location 崩）→ 子路径导入（router-check.ts 改 @proteus-vue/router/navigation）② vitest alias 前缀吞子路径（@proteus-vue/router 吞 /navigation）→ 子路径 alias 前置（navigation 在 router 前）③ **路由纯净性门禁误伤映射表**（router-purity.test 禁 wx. 直连，但 NAVIGATION_MAP 值是 API 名**字符串数据**非调用——收敛点与 codegen/ 同性质 → ALLOWED_WX 加 navigation.ts）。验证：verify 894/103 全绿（Node 22，884→894 +10）+ check-consistency 通过 + router 包构建正常；**G-32 M1（Router Core：栈 diff + 语义层 + strict-router）落地**；**剩余**：M2 iOS 手势返回+安全区（待 G-22）、M3 Android+鸿蒙 NavPathStack（待 G-22）、M4 转场事务+玻璃+Deep Link（待 Glass）；下一候选：router-plus M4 纯逻辑部分（Deep Link）/ css-compat B2（待 G-22）/ cli-plus M4 CI 模板
184. **router-plus G-32 M4 Deep Link 落地（★04-deep-link.md 纯逻辑部分）**：**router 包 `src/deep-link.ts`**（@proteus-vue/router/deep-link 子路径 + exports + build 入口）：① `parseDeepLinkUrl`——**手写 URL 解析**（小程序无全局 URL 类 + ES5 安全：禁 ?. ?? 展开 解构；scheme://host/path?query 分割 + decodeURIComponent query 对）；② `matchPattern`——pattern 匹配（'/product/:id' → pathname 段数对齐 + :param 提取，字面段不匹配 → null）；③ `isDeepLinkAllowed`——白名单安全（04 §4：custom scheme + host 精确匹配；universal link https host 通配 `*` 正则）；④ `resolveDeepLink`——白名单 → pattern 匹配 → ResolvedRoute { path, params, query, stack }；⑤ `buildColdStartStack`——冷启动初始栈（04 §3：最深层祖先链，祖先须在路由表存在，否则直达单层——配合 computeRoutePatch/applyRoutePatch 完整闭环）。★设计与 defineProteus router.deepLink 配置对齐（04 §5：scheme/host/universalLinks/routes pattern→path 映射）。测试 tests/deep-link.test.ts 10 用例（URL 解析 2/pattern 匹配 2/resolve 4 态/冷启动 2）。验证：verify 904/104 全绿（Node 22，894→904 +10）+ check-consistency 通过 + router 包构建正常；**G-32 M1+M4-DeepLink 落地**；**剩余**：M2/M3 原生端（待 G-22 App Renderer）、M4 转场事务+玻璃集成（待 Glass M1）；下一候选：cli-plus M4 CI 模板 / css-compat B2（待 G-22）/ glass（G-29）
185. **test-framework M3 落地（★@proteus-vue/test-core + proteus test CLI，可落地性确认）**：**评估**：现有测试体系已覆盖 L1（vitest 单测）/L2（golden 快照雏形）/L4 Web E2E（Playwright），缺口是 L3 标准件 + CLI 入口。**新包 @proteus-vue/test-core**（03-component-integration.md 铁律）：① `createMockContext`——**唯一 wx 来源**：storage（set/get/remove/clear 内存 Map 实现 + vi.fn 可断言）/router（navigateTo 等 5 API）/ui（showToast 等 5 API）四域 + Page/Component/App 构造器捕获（registrations 断言）+ getApp/getCurrentPages + getSystemInfoSync + `cleanup()`（vi.unstubAllGlobals 恢复）；② `mountMpComponent`——SFC → compileVueSfc 真实编译 → Function 注入全局执行逻辑层 JS（Page/Component 捕获）→ 实例化 { data 快照 + setData 追踪（calls 断言）+ 方法绑定 } + wxml——**逻辑 + WXML 双断言**（不真实渲染，L4 下沉）。**CLI `proteus test [unit|e2e:web|e2e:mp]`**（test.ts + args + index）：unit → vitest run（★排除 e2e——E2E 依赖构建产物 + Chromium，与根 test 脚本一致）；e2e:web → Playwright（提示先 build --target web）；e2e:mp → automator 提示（需 IDE）。★踩坑：vitest 1.x vi.fn 泛型约束 **any[]**（旧 API，函数签名泛型报 TS2344）→ Mock 赋值双重断言 `as unknown as`；wx 对象/返回值整体 `as MockContext` 断言。测试 tests/test-core.test.ts 7 + tests/test-cli.test.ts 4。验证：verify 915/106 全绿（Node 22，904→915 +11）+ check-consistency 通过 + `proteus test` 真实运行 915 全过（e2e 排除）；**test-framework B3（L3 标准件）+ CLI 入口落地**；**剩余**：B2 编译快照体系标准化、现有 wx mock 测试迁移 test-core、B5 automator（需 IDE）；下一候选：app-config（G-35）/ css-compat B2（待 G-22）
186. **app-config G-35 M1 落地（★应用全局配置：四层合并 + 自研轻量校验）**：新包 **@proteus-vue/app-config**（L2 横切，纯 TS 零依赖，09-batches M1「无外部阻塞可立即启动」）。**types.ts**——AppConfig Schema（01 §2.2 全量：app{id/name/version/buildNumber} + env + api{baseUrl/timeout/retry/cache} + features{玻璃/骨架/灰度/实验分组 + 扩展点} + theme/font/safeArea + platform 覆盖 + remote）+ DeepPartial + Env/Platform/ConfigLayer。**merge.ts**——`deepMerge`（深合并原语：对象递归 + **数组替换不拼接** + 不突变入参）+ `mergeAppConfig`（四层合并：**默认 < env < platform < remote**，§2.1）+ `extractPlatformOverride`。**validate.ts**——自研轻量校验器（~2KB 零依赖，不强制 zod，铁律 #5 数据驱动 RULES 表）：必填（app/api/features/theme/font/safeArea 15 项）+ 范围（version semver / timeout (0,120000] / retry 0-5 / defaultScale 0.5-2.0 / env·theme 枚举）+ `validateAppConfig`（返回 { ok, errors }）+ `validateAndApply`（**非法降级默认值 + 告警收集不抛错**——与 Style Safety G-31 同哲学「宁可降级不崩溃」）。测试 tests/app-config.test.ts 13 用例（合并 6 + 校验 5 + 降级 2）。★ES5 安全（运行时进 MP 产物：无 ?. ?? 解构，手写 getByPath）。验证：verify 928/107 全绿（Node 22，915→928 +13）+ check-consistency 通过 + app-config 包构建正常；**G-35 M1（Schema + 合并 + 校验）落地**；**剩余**：M2 defineAppConfig + useAppConfig 响应式（Vue runtime）、M3 多环境加载、M4 远端下发 + L1 缓存（Cache G-28）、M5 五端持久化 + CLI 集成；下一候选：app-config M2（响应式）/ css-compat B2（待 G-22）
187. **app-config G-35 M2 落地（★defineAppConfig + useAppConfig 响应式 + setConfig 校验 + useFeatureFlag）**：**store.ts**（02-runtime-api.md §2/§3/§4）——`configRef` 单例 `ref<AppConfig>`：① `initAppConfig(defaults)`（启动初始化，幂等覆盖）；② `getConfig()`（非响应式读）；③ `setConfig(partial | updater)`（**★先 deepMerge 再校验**——浅合并会把 features 整体替换导致误报缺失（踩坑）；非法拒绝 + console.warn 不抛错；通过后深合并 + ref.value 赋值触发响应式）；④ `useAppConfig()`（reactive 代理；**setup 外抛错同 useRoute**——getCurrentInstance 判空）；⑤ `useFeatureFlag(key)` + **`getFeatureFlag(config, key)` 纯函数**（setup 外可测，enabled = 非 false 非 undefined）。**define.ts** `defineAppConfig`（identity + `__isAppConfig` 标记，Vite 模式）+ APP_CONFIG_MARK 导出。包加 vue peerDependency + build external vue。★踩坑三连：① watch 回调默认异步队列 → 测试用 `{ flush: 'sync' }`；② vitest ESM 下 `require()` 相对路径不可用（Cannot find module）→ 顶部 import；③ `vi` 需显式 import（非 globals）。测试 tests/app-config-store.test.ts 7 用例（defineAppConfig 标记/setConfig 三态（深合并·updater·非法拒绝）/useAppConfig 抛错/getFeatureFlag 三态）。验证：verify 935/108 全绿（Node 22，928→935 +7）+ check-consistency 通过 + app-config 构建正常；**G-35 M1+M2（合并 + 校验 + 响应式 API）落地**；**剩余**：M3 多环境加载 + 平台覆盖（mergeAppConfig 已有基础）、M4 远端下发 + L1 缓存（Cache G-28）、M5 五端持久化 + CLI 集成；下一候选：app-config M3 / css-compat B2（待 G-22）
188. **app-config G-35 M3 落地（★多环境加载 + 平台覆盖 + CLI app-config:check + check 聚合）**：**load.ts**（§2.1 层级落地）——`resolveEnvConfig(envConfigs, env)`（env 层选择）+ `loadAppConfig(layers)`（默认 < env < platform < remote 五层合并，LoadAppConfigLayers 入参；loadAppConfig 的 platform 覆盖复用 extractPlatformOverride + mergeAppConfig）。**CLI `proteus app-config:check [file]`**（app-config-check.ts，06-cli-integration §1）：加载 app.config.ts → validateAppConfig → 结构化错误输出（fail-fast exit 1，CLI001 同族）；**★语义分层**：独立命令缺文件 → error（显式运行期望文件存在，提示 `proteus gen config` 生成骨架）；**check 聚合的 app-config 域**（check.ts strictCli 分支）缺文件 → **跳过不阻断**（与 cli 域缺 proteus.config.ts 的独立编译模式语义一致）；★踩坑：loadTsConfig 返回 unknown → validateAppConfig 需 `as DeepPartial<AppConfig> | undefined` 断言 + DeepPartial 入 import（TS2304/TS2345 两连）。测试 tests/app-config-load.test.ts 7 用例（env 选择/五层合并/平台覆盖/CLI 合法/非法 2 错误/缺文件 gen config 提示/check 聚合含 app-config 域）+ check-cli.test.ts 更新（全绿项目 5 域断言：app-config 域跳过不阻断）。验证：verify **942/109** 全绿（Node 22，935→942 +7）+ check-consistency 通过 + cli 包构建正常（app-config external 生效）；**G-35 M1-M3（Schema + 合并校验 + 响应式 + 多环境加载 + CLI）落地**；**剩余**：M4 远端下发 + L1 缓存（Cache G-28）、M5 五端持久化 + CLI gen；下一候选：app-config M4 / css-compat B2（待 G-22）
189. **test-framework B2 落地（★编译快照规范化：@proteus-vue/test-core/snapshot 子路径）**：02-snapshot-compile.md §快照对象四件套纯逻辑落地。**snapshot/wxml.ts**——手写轻量 tokenizer（wxml 自定义标签集，@vue/compiler-dom HTML 语义不适用）：`parseWxml`（标签/属性/文本/注释，★属性值含 `>` 如 `{{a > 1}}` 不终止标签——引号感知 findTagEnd）+ `normalizeWxml`（**结构等值基准**：注释丢弃 + 文本空白折叠 + 属性按键排序）+ `canonicalizeWxml`（稳定字符串形态进 vitest 快照）+ `diffWxml`（首个分歧点 path 定位，如 `<children>[2]` `<attrs>.class`）+ `assertWxmlEqual`。**snapshot/sourcemap.ts**——手写 VLQ 解码（sourcemap v3：`;` 分行 `,` 分段，srcLine 跨段 delta 累积，符号位最低位）：`decodeVlqSegment`/`decodeMappings` + `verifySourceMap`（每个映射段 genLine/srcLine 在产物/源码行数内——02「行列映射回 .vue 源文件」完整性门禁）。**snapshot/assert.ts**——`checkJsExports`（Page/Component/App/module.exports 配置对象顶层键存在性：平衡括号扫描提取 + key 识别）+ `normalizeJson`（键排序稳定序列化）。**接线**：golden.test.ts 升级（wxml 规范化快照 + verifySourceMap 零违规 + checkJsExports data/onLoad；快照 -u 重生成）；根 package.json 加 `test:update`（vitest run -u，02 §更新流程）。★踩坑三连：① serialize/diffNode 联合类型收窄——`a.type !== b.type` 不双向收窄（TS2339）→ 显式 as 断言（WxmlElement/WxmlText）② verifySourceMap 测试自写 mappings 'AAKA' srcLine=5 超出 2 行源码（断言 bug 非代码 bug）③ toMatchObject 只列部分 attrs 全量断言失败。测试 tests/snapshot-core.test.ts 24 用例（parse 4/normalize 3/diff 5/VLQ 3/verify 4/exports 3/json 2）。验证：verify **966/110** 全绿（Node 22，942→966 +24）+ 根 vue-tsc 0 + test-core 包构建（exports ./snapshot 子路径 + esbuild 多入口 outdir）；**test-framework B2（L2 编译快照规范化）落地**；**剩余**：B4 Playwright 关键路径、B5 automator（需 IDE）、现有 wx mock 测试迁移 test-core；下一候选：cli-plus M4（CI/CD 模板）/ app-config M4（待 Cache G-28）/ css-compat B2（待 G-22）
190. **cli-plus G-33 M4 落地（★proteus ci:init CI/CD 模板 + create-proteus 模板内置 workflow）**：02-build-pipeline.md §3 落地。**cli 包 src/ci.ts**——`CI_FILE_PATHS`（三平台路径表：github → .github/workflows/proteus.yml / gitlab → .gitlab-ci.yml / circleci → .circleci/config.yml）+ `parseCiArgs`（--platform 缺省 github + --targets 缺省 web,skyline + 位置目录；非法 platform/未知参数/多余位置参数报错）+ `generateCiWorkflow`（纯函数 YAML：GitHub check 门禁 job（actions/setup-node@v4 + npm ci + npx proteus check）+ build job（needs: check + 逐端 npx proteus build --target + upload-artifact dist/ + dist/mp-weixin/）；GitLab stages + artifacts；CircleCI jobs + workflows requires）+ `planCiInit`（mkdir -p + 落盘）。**CLI 注册 `proteus ci:init`**（index.ts + HELP_TEXT 补条目）。**create-proteus 模板加 .github/workflows/proteus.yml**（新工程开箱 CI）；★walk 放开点文件拷贝（原 `entry.name.startsWith('.')` 跳过整个 .github → 改仅排除 .git，模板含 CI 文件）。测试 tests/ci-init.test.ts 10 用例（github 默认含四要素/skyline 定制/gitlab/circleci/parseCiArgs 三态/落盘三态）。验证：verify **976/111** 全绿（Node 22，966→976 +10）+ 根 vue-tsc 0 + cli 包构建 + **CLI dist/源码真实闭环**（ci:init github + gitlab 双平台生成 + 文件内容正确）+ create-proteus 测试零回归；**G-33 M1-M4 全部落地（check/defineProteus/strict-cli/dev/build --target/CI 模板）**；**剩余**：M3 原生工程同步（待 G-22 App Renderer）；下一候选：app-config M4（待 Cache G-28）/ css-compat B2（待 G-22）/ style-safety B4（待 G-22）
191. **app-config G-35 M4 落地（★远端下发 + L1 缓存：降级链四态 + 非法字段降级）**：03-remote-config.md §1/§3/§4/§7 纯逻辑落地（plan 标注 ⚠️ mock——远端拉取注入 mock 可单测）。**remote.ts**——① `ConfigCache` L1 缓存抽象 + `createMemoryConfigCache`（get/set/clear；**G-28 Cache 落地时换磁盘实现接口不变**）；② `FetchLike` 结构化 fetch 形状（**避免 DOM lib 依赖**，Node/Web fetch 与注入 mock 均满足）+ `createHttpsFetcher`（https source：X-App-Version 头 + res.json + HTTP 非 2xx 抛错；★环境无 fetch 抛错提示注入 custom fetcher 走 wx.request——小程序端无全局 fetch）；③ `fetchAndApplyRemote`（§1 集成模型 ②③④ + §4 降级链完整四态：拉取成功 → **mergeAppConfig 深合并 + validateAndApply 非法字段降级（宁可降级不崩溃，M1 哲学）+ apply 回调（调用方接 setConfig 响应式）+ 写缓存**；拉取失败 → fallback=last-cached 有缓存 → 缓存合并生效 source=cache；无缓存/fallback=defaults → 默认值 source=defaults）；返回 `RemoteApplyResult { source/config/cached/degraded }`。**★语义决策**：§4 文档写「校验失败拒绝整个 remote」，但包既有 validateAndApply 是逐字段降级（决策 #186 哲学）→ 采用降级（合法字段生效 + degraded 报告），与全包哲学一致。★踩坑两连：① 测试选 99999 超时值**在 (0,120000] 范围内**（合法）→ 改 999999 超限 ② FetchLike 返回缺 status 字段（TS2339）+ `opts.cacheEnabled && !!opts.cache` 产出 boolean|undefined（TS2322）→ `!!` 收窄。测试 tests/app-config-remote.test.ts 9 用例（缓存往返/https 三态/降级链五态）。验证：verify **985/112** 全绿（Node 22，976→985 +9）+ 根 vue-tsc 0 + app-config 包构建；**G-35 M1-M4（Schema + 合并校验 + 响应式 + 多环境加载 + CLI + 远端下发 + L1 缓存）落地**；**剩余**：M5 五端持久化 + CLI gen；下一候选：app-config M5（需原生）/ css-compat B2（待 G-22）/ style-safety B4（待 G-22）
192. **app-config G-35 M5 JS 侧落地（★五端存储适配层 + CLI gen config 补全）**：05-five-end-storage.md 映射表 JS 侧（App 端原生持久化待 G-22，接口已定）。**storage.ts**——`ConfigStore { save/load/clear }` + ① `createWebConfigStore`（localStorage，注入 storage 可测 + 无 localStorage 环境内存降级不抛错）② `createSkylineConfigStore`（wx.setStorageSync，注入 wx 可测）③ `createVolatileConfigStore`（App 端 iOS/Android/鸿蒙**内存降级**——宁可降级不崩溃，待 G-22 原生接线）④ `createPlatformConfigStore(platform, opts)`（05 §映射总览统一入口：web/mp-weixin 真实持久化，其余 volatile）；load 端损坏 JSON → null 不抛错。**CLI `proteus gen config [file]`**（app-config-gen.ts，06 §1）——defineAppConfig 形态骨架（M3 缺文件提示引用的命令补齐）；已存在抛错不覆盖；index.ts + HELP_TEXT 注册。**★loadTsConfig 沙箱扩展**：config 文件禁运行时依赖（铁律），但 defineAppConfig 是纯 identity（Vite defineConfig 模式）→ 沙箱 fileRequire 为 `@proteus-vue/app-config` 注入 identity stub（与 plugin-vite require 剥离同性质——框架已知的配置辅助导入），app.config.ts 保持 canonical 形态且 CLI 可加载。测试 tests/app-config-storage.test.ts 8 用例（web 三态/skyline 两态/volatile/platform 分发三端）+ tests/app-config-gen.test.ts 3 用例（骨架形态/已存在不覆盖/**★闭环 gen → app-config:check 通过**）。验证：verify **996/114** 全绿（Node 22，985→996 +11）+ 根 vue-tsc 0 + cli/app-config 包构建 + **CLI dist 真实闭环**（gen config → app-config:check ✅ 通过）；**G-35 M1-M5 全部 JS 侧落地（剩余仅 App 端原生存储接线待 G-22）**；下一候选：css-compat B2（待 G-22）/ style-safety B4（待 G-22）/ test-framework B4（Playwright E2E）
193. **css-compat G-21 B2 数据层落地（★Style IR → 五端 Renderer 映射表 @proteus-vue/css-compat/layout-semantics 子路径）**：B2「五端 Renderer 映射」的**数据层先行**（映射表是纯数据契约，渲染器消费待 G-22——NAVIGATION_MAP / PLATFORM_LENGTH_RULES 同模式：先锁映射语义，G-22 即插即用）。**mappings.ts**——① `SEMANTIC_COMPONENTS`（04 §一 六个语义组件：p-glass/p-sticky/p-scroll/p-shadow/p-bg-gradient/p-safe-area + 设计语义 props（blur/elevation/preset 非平台术语）+ capability）② `LAYOUT_SEMANTICS_MAP`（05 §二 5 语义 flex-row/flex-col/stack/grid/scroll × 五端原生实现：UIStackView/FrameLayout/Row()/scroll-view 等）③ `VISUAL_MAP`（05 §四 4 属性 background-color/opacity/color/backdrop-filter × 五端：iOS alpha/UIGlassEffect、鸿蒙 fontColor/effect:blur）④ `SKYLINE_CSS_SUPPORT`（05 §五 Skyline 子集矩阵：支持/不支持/部分——float/通用选择器/属性选择器在不支持清单，对齐 CSS002/004）⑤ `CONVERGENCE_RULE`（06 端差异收敛：业务层禁平台分支）。**resolve.ts**——`resolveLayoutSemantic`/`resolveVisual`（未知语义/属性 → undefined 降级）+ `semanticComponentSpec`/`isSemanticComponent`（04 反模式检测辅助）。包 exports ./layout-semantics + esbuild 多入口（outfile → outdir 模式，test-core 同款）。测试 tests/layout-semantics.test.ts 12 用例（布局表完整性 2/视觉表 2/组件清单 2/resolve 3/Skyline 矩阵 2）。验证：verify **1008/115** 全绿（Node 22，996→1008 +12，**突破千测**）+ 根 vue-tsc 0 + css-compat 包构建；**css-compat B1/B3 + B2 数据层落地（B2 渲染器消费待 G-22）**；下一候选：style-safety B4（待真机）/ css-compat B2 渲染层（待 G-22）/ test-framework B4（Playwright E2E）
194. **test-framework B4 落地（★Web E2E 关键路径 data-testid 实测 + 顺手修 web WxApi success 回调 bug + e2e preview root 修复）**：04-e2e-web-playwright.md 关键路径落地。**demo 页补 data-testid**（platform-api-demo：pad-storage-{set,get,remove}/pad-storage-log/pad-modal/pad-ui-log；pinia-demo：pinia-now/pinia-volume/pinia-play/pinia-vol-down）+ **tests/e2e-web-keypaths.test.ts** 3 用例（① storage 收口闭环：写入→读取（JSON 往返）→删除 ② UI 模态：showModal → WeUI modal 出现 → 确认关闭 + 回调 ③ Pinia 持久化：volume 经 **page.reload 真实恢复**（浮点 0.7000000000000001 落盘 → 范围断言）+ playing/current 瞬时态重置）。**★顺手修两个真 bug**：① **web WxApi showModal/showActionSheet 从不调 opts.success**——api 包 wx 分支靠 success 回调 resolve（决策 #165「回调式 success 转发」），web 端 api.ui.showModal/actionSheet Promise 永远挂起 → wx.ts done() 补 `opts.success(result)` 调用 + WxApi 类型补 success/fail 签名（对齐微信语义）；② **e2e preview 404**——产物在 examples/dist/web（vite outDir = dist/{platform}），但 preview() 默认按 cwd（仓库根）解析 config → 服务空目录 → 所有 e2e 超时（旧 e2e-web.test.ts 同样坏）→ `preview({ root: <examples>, mode: 'web' })`。★踩坑：① vitest 无 Playwright matcher（toHaveText 非法）→ waitForFunction 轮询风格（对齐 waitText）② 两个 e2e 文件并行跑 Chromium → 5s 默认超时全炸 → `--no-file-parallelism` 串行 ③ `--exclude` 单值限制 + shell glob 展开拆参 → 脚本里引号包裹 `"tests/e2e-*.test.ts"`（vitest 自己 picomatch）④ SPA 内 pinia store 是模块单例——跨页导航 current/playing 仍在，「未播放」断言错误 → 改 reload 测真实持久化路径 ⑤ 持久化写入异步（Adapter）→ reload 前 waitForFunction 等落盘。验证：verify **1008/115** 全绿（Node 22）+ **e2e 套件 11/11**（e2e-web 8 + keypaths 3，`npm run test:e2e:web`）+ 根 vue-tsc 0；**test-framework B2/B3/B4 落地（B1/B5 既有，B5 automator 需 IDE）**；下一候选：style-safety B4（待真机）/ css-compat B2 渲染层（待 G-22）
195. **test-framework B7+B8 落地（★跨端断言 helper + CI 接入 §08 约束）**：06/08 两章从文档进代码。**B7：test-core `events.ts`**（06 §统一事件 helper）——`tap` 按端分发（Web @vue/test-utils trigger('click') / 小程序 automator tap()）+ `isWebElement`/`isMpElement` 类型守卫；06 铁律落地：跨端用例只碰逻辑/状态/语义，DOM 差异下沉 p-* 映射 + 编译快照。**B8：ci:init workflow 补 test job**（§08「决策未定前 CI 只跑 L1-L3 + 编译快照 + Web Playwright」的可执行约束）——GitHub `test` job（needs: build：npx proteus test + build --target web && test e2e:web）/ GitLab stage test（needs: [build]）/ CircleCI test job（requires: [build]）；automator MP E2E 留注释「仅 Mac runner / 云测节点条件执行」；create-proteus 模板同步。**顺手收口 `proteus test`**：unit 的 --exclude 改 `tests/e2e-*.test.ts`（引号 glob）+ e2e:web 双文件（e2e-web + keypaths）`--no-file-parallelism` 串行——与根 test:e2e:web 脚本一致。**B7 文档**：test-framework README 状态表更新（02 B2/04 B4/06 B7/08 🔶 决策待定/10 🔶 待 v0.6 组件）。★踩坑：ci-init「skyline 定制」断言 `not.toContain('npx proteus build --target web')` 误伤 test job 的 Web E2E 构建 → 改断言 `- run: ` 前缀（build job step 形态）。测试 tests/test-core-events.test.ts 4 用例（web 分发/mp 分发/统一复用/守卫）+ ci-init.test.ts 断言更新（test job/needs: build/stages test/requires）。验证：verify **1012/116** 全绿（Node 22，1008→1012 +4）+ 根 vue-tsc 0 + test-core/cli 包构建；**test-framework B1-B4 + B7-B8 落地（B5 automator 待 IDE、B6 待 150 页 Blueprint、B8 后端决策待定）**；下一候选：G-22 App Renderer（解锁 cli-plus M3/css-compat B2 渲染层/app-config M5 原生/app-plan）
196. **test-framework B6 落地（★proteus audit all 全量门禁 + 顺手修 check router 域既有回归）**：10-blueprint-integration.md「proteus audit all」可执行部分（150 页场景抽象为任意项目根）。**audit-all.ts**——`runAuditAll(root)` 六域聚合（route/module/config/i18n/capabilities/components）+ `AUDIT_ALL_BUDGET_MS = 12000`（CI 耗时预算，超预算阻断）+ `formatAuditAll` 报告；复用各检查函数（try/catch + performance.now 计时）；缺配置域跳过不阻断（独立编译模式语义）。**★顺手修既有回归：check router 域扫错目录**——约定式路由收口（#182）后 checkRoutes 扫项目根把 App.vue（path="App" 大写）/RouterView.vue 当页面推导收录 → ROUTE003 误报 → `proteus check examples` 实际 false（与 #180/#182 声称的「全绿」不符）；新增 `resolvePagesDir(root)`（轻量正则读 proteus.config.ts 的 pagesDir，默认 pages；目录存在用之否则 fallback root）→ check.ts router 域 + audit-all route 域都用它，**check examples 恢复五域全绿**。**域目录语义**：components 域扫 `root/src/components`（无目录跳过，examples 组件在仓库根 #115）；capabilities 域保持 root 扫描（B5 真实门禁——mp-semantics-demo 演示页 8 处 wx.* 被如实抓到，audit all 比 check 更严）。★踩坑：tsx 直跑 audit-all 报 ERR_PACKAGE_PATH_NOT_EXPORTED（子路径 exports 限制）→ 走 vitest（alias 已配）；examples 上 components 域传 root 误扫根目录。测试 tests/audit-all.test.ts 3 用例（examples 六域+预算+capabilities 如实 / 空项目全跳过 ok / format 报告）。验证：verify **1015/117** 全绿（Node 22，1012→1015 +3）+ 根 vue-tsc 0 + cli 包构建 + **CLI dist/源码真实闭环**（check examples 五域全绿 ✅ + audit all examples 6 域 42.4ms 预算内，仅 capabilities 报 demo 页 B5 违规）；**test-framework B1-B4 + B6-B8 落地（B5 automator 待 IDE、B8 后端决策待定）**；下一候选：G-22 App Renderer（解锁 cli-plus M3/css-compat B2 渲染层/app-config M5 原生/app-plan）
197. **test-framework B5 骨架 + 根 vue-tsc 积压清理（★IDE 就绪资产 + 长期声称的「根 vue-tsc 0」恢复）**：**B5：tests/e2e-mp-smoke.test.ts**——miniprogram-automator 冒烟骨架（05-e2e-mp-automator.md 铁律：连接 ws://localhost:9420 → reLaunch 首页 → page.$ 断言 → disconnect；★用例独立运行不依赖页面栈）；运行前置注释完整（微信开发者工具 CLI auto --auto-port 9420 + build:mp + PROTEUS_MP_E2E=1）；**★诚实标注未验证**（本机未装 IDE，PROTEUS_MP_E2E 未置位 → describe.skipIf 跳过）；★TS 处理：miniprogram-automator 未装依赖 → 动态 import 用字符串变量（`import(AUTOMATOR_MODULE)`）避免 TS2307 模块解析；文件被根 test 排除（tests/e2e-*.test.ts 通配，与 Web E2E 平级）。**★顺手清根 vue-tsc 积压**（verify 不跑根 vue-tsc——build:web 是 examples 的 vue-tsc——长期「根 vue-tsc 0」声称与实际脱节）：① test-core.test.ts TS2571（Record 读值 unknown 调用）→ 双重断言 `as unknown as { Page }` ② css-budget.test.ts TS2741 两处（CssGlobalReport 缺 classSelectors——B3 加字段后测试构造漏补）→ 补 classSelectors（全绿报告 45/60=0.75 与 semanticRatio 一致）。验证：verify **1015/117** 全绿（Node 22）+ **根 vue-tsc 0 错误恢复**（EXIT=0）+ e2e-mp 骨架 skip（IDE 就绪即启用）；**test-framework 全批次收官：B1-B4/B6-B8 落地 + B5 骨架就位（仅剩 B5 真跑待 IDE、B8 后端决策、09 App 端占位）**；下一候选：G-22 App Renderer（解锁 cli-plus M3/css-compat B2 渲染层/app-config M5 原生/app-plan）
198. **test-framework B5 升级（★用户发起「允许配置 IDE 路径」→ e2e:mp 从骨架变完整链路）**：**cli 包 src/mp-e2e.ts**——① `MP_IDE_DEFAULT_PATHS`（darwin/win32 微信开发者工具 CLI 常见路径表）② `resolveMpIdeCli`（探测顺序：**PROTEUS_IDE_CLI 环境变量 → --ide 参数 → 平台默认路径**；exists 注入可测；全缺 → null）③ `planMpE2E`（纯函数：无 IDE → **throw 含三级配置指引**（环境变量/--ide/安装默认探测）；有 IDE → 计划 { ideCli/projectDir/port/needBuild/steps }，产物 dist/mp-weixin 缺失 → needBuild 提示）④ `waitForAutomatorPort`（net.connect 轮询端口就绪，60s 超时）。**CLI `proteus test e2e:mp [--ide <cli>] [--port <n>] [root]` 完整链路**：parseTestArgs 加 --ide/--port/root 解析（--port 数字校验；仅 e2e:mp 允许第二个位置参数 = 项目根）→ planMpE2E → needBuild 中止提示 build:mp → **spec 内 automator 官方 launch()（内部 spawn IDE auto --trust-project + waitUntil 轮询 connect + checkVersion + 配置写入）**，CLI 只装配 env（PROTEUS_MP_E2E=1/PROTEUS_AUTOMATOR_PORT/PROTEUS_IDE_CLI/PROTEUS_MINI_PROGRAM_PATH）后跑 vitest；无 IDE → 指引报错 exit 1。**spec 同步**：端口从 env 读（PROTEUS_AUTOMATOR_PORT 缺省 9420）。**HELP_TEXT** 补 e2e:mp 说明。★验证策略：探测/计划/超时纯逻辑可单测（注入 exists/paths/env/短超时），真机链路留待 IDE 环境。★踩坑：测试断言笔误（默认路径不含 'real' 前缀）+ planMpE2E 步骤文案更新后断言未同步（'auto --project'/'PROTEUS_MP_E2E=1' 字样移除）。测试 tests/mp-e2e.test.ts 7 用例（探测四态/计划三态/端口超时）。验证：verify **1015→1023** 全绿（Node 22，+8）+ 根 vue-tsc 0 + cli 包构建 + **CLI 真实闭环**（无 IDE → 指引 + exit 1 ✅）；**test-framework B1-B8 全部落地（B5 链路就位仅差 IDE 真机、B8 后端决策待定、09 App 端占位）**；下一候选：G-22 App Renderer（解锁 cli-plus M3/css-compat B2 渲染层/app-config M5 原生/app-plan）
199. **★B5 真机实测（用户提供 IDE 路径 /Volumes/data1/work/office-applications/wechatwebdevtools.app）+ 修框架 gap：build:mp 产出 project.config.json**：**实测过程**（examples + 微信开发者工具 2.01.2510260 + miniprogram-automator 0.12.1 安装为 devDep）：① IDE CLI 探测 ✓（--ide 传参命中）② 产物检查 ✓ ③ automator launch 启动 IDE（auto --trust-project 解决新项目信任弹窗）④ 项目编译成功（restart appservice compile）+ IDE 已登录（cli islogin → login:true）+ automation 服务监听 9420 ✓ ⑤ **★卡在 appid**：IDE 日志持续 `gettestpublib 41002 appid missing`——touristappid（游客模式）与占位 wx0000000000 均不被新版 IDE 的 CLI automation 识别为有效 appid → automation 半启动（WS 连上不响应 → automator launch 挂起超时）。**★框架 gap 修复：build:mp 未产出 project.config.json**（产物无法直接导入微信开发者工具；之前产物里的遗留文件无 appid）→ gen-routes.ts 新增 `writeProjectConfig()`：compileType/appid（读 config.appid）/projectname（ROOT basename）/setting（minifyWXML/urlCheck），随 app.json 一起生成——**小程序包可导入性补齐（automator 前置）**。★踩坑：① examples 构建走 plugin-vite **dist**（workspace 链接）→ 改 src 需先重建包再 build:mp ② automator 0.12.1 与新版 IDE 的 --auto-port 参数已移除（auto -h 无该选项）→ 弃用手写 spawn IDE 链路，改官方 automator.launch()（内部处理端口/信任/轮询/版本）③ Connection closed 反复出现根因是 appid 而非协议。测试：gen-routes.test.ts 补 project.config.json 断言（appid/compileType/projectname）；mp-e2e.test.ts 步骤断言更新。验证：verify **1023/118** 全绿（Node 22）+ 根 vue-tsc 0 + **build:mp 产物含 project.config.json（appid=wx0000000000 占位）**；**B5 真跑边界：需真实小程序 appid（wx 开头 16 位）——examples 占位与 touristappid 均无效，用户提供真实 appid 后 `proteus test e2e:mp examples --ide <cli>` 即可全链路跑通**；下一候选：G-22 App Renderer
200. **★B5 真机实测打通三关（用户提供真实 appid wx33bc04a52024def7）**：**① appid 关**：替换 examples/proteus.config.ts 占位 → 重新 build:mp（project.config.json 带真实 appid）→ IDE 日志 `fetchAttr for wx33bc04a52024def7` + **appid missing 清零** + simulator launch success；★IDE 按项目路径缓存旧配置（占位 appid 打开过 examples 路径）→ 改 project.config.json 不重读，**复制产物到全新路径**避开缓存后才生效。**② WXML 编译关（★顺手修真 compiler gap）**：微信编译器 `Fatal: unmatched parenthesis` → appLaunch timeout——mp-semantics-demo 的 `:type="('primary' as any)"` 的 TS 类型断言原样进 WXML（`{{'primary' as any}}`）；**Web 端 Vue 编译器自动剥离 as 断言，自研 MP 编译器没做** → template.ts `exprContent` 统一剥离 TS 类型断言（stripTypeAssertions：括号包裹 (expr as Type)→expr + 尾部 as Type→expr 连续循环；字符串内 as 保留）+ 回归用例（type={{'primary'}}/disabled={{x}}，无 as any）。**③ automator 兼容关**：`Tool.getInfo` 响应在新版 IDE 缺 SDKVersion → automator 0.12.1 checkVersion 读 undefined.split 崩（0.12.1 即 npm 最新）→ **scripts/patch-automator.mjs 幂等补丁**（SDKVersion 缺失视为 "3.0.0" 满足版本校验；MARK 检测 + 目标代码存在性校验），CLI e2e:mp 分支 spawn vitest 前自动执行（import.meta.url 定位仓库根 scripts/，src/dist 同路径）。**★剩余最后一步：IDE GUI「设置 → 安全设置 → 服务端口」**——`start cli server error: [object Object]` 表明 automation 服务被 IDE 安全设置拦（CLI auto 无法代开 GUI 开关），需用户手动开一次后 e2e:mp 即全绿。★踩坑：npm install 同版本不重写已存在文件（验证补丁幂等需手动还原原始代码）；kill IDE 后 daemon 自动拉起新实例占 9420（需 pkill -f wechatwebdevtools.app 全杀 + 换端口）。验证：verify **1024/118** 全绿（Node 22，1023→1024 +1：as any 回归）+ 根 vue-tsc 0 + 补丁脚本应用/幂等双跑验证 + checkVersion 崩消除（15s 超时 → Uncaught [object Object] 服务端口错误，再非 split 崩）；**B5 链路：探测→launch→appid→编译→checkVersion 全部打通，仅剩 IDE GUI 服务端口开关**；下一候选：G-22 App Renderer
201. **★B5 框架化（★用户发起「上面的坑是不是都可以优化进自动化测试框架」→ 实测坑全部内化）**：真机实测踩的坑转成框架自动诊断/健壮性。**mp-e2e.ts 加**：① `isValidAppid`（真实 appid 校验：wx + 16 位十六进制；占位 wx0000000000/touristappid/空 → false——automator 必挂）+ `PLACEHOLDER_APPIDS` ② `diagnoseMpE2EEnv` **环境体检一次性报告**（四检查：ide-cli / build-output / appid（读 project.config.json，无效/缺失 → error 含修复指引「proteus.config.ts 配置后重新 build:mp」）/ automator-port（被占 → warn：残留 IDE daemon 自动拉起提示）；error 级 → 中止，warn 不阻断）+ `formatMpE2EDiagnosis` 报告（✅/⚠/✗ 图标）③ `prepareMpE2EProject` **产物独立副本**（dist/mp-weixin → .proteus/e2e-mp，每次重建干净状态——避 IDE 路径缓存旧 project.config.json + 不污染 dist，IDE 会写 private.config 等）。**CLI e2e:mp 集成**：体检输出 → 硬错误中止 → 副本生成（env 指向副本）→ patch → vitest；portInUseSync 同步端口探测（net 有界等待 300ms）。**spec 失败模式诊断**：`launchErrorHint` 把实测坑转可行动指引——Connection closed → 「GUI 设置 → 安全设置 → 服务端口」（CLI 无法代开）；Failed to launch → 检查 IDE 路径/登录（cli islogin）；SDKVersion/Cannot read → 补丁自动应用提示。★验证策略：体检/副本纯逻辑可单测（注入 exists/isPortBusy/真实 fs tmp 工程）；★踩坑：诊断测试注入 exists 过宽（含 dir 即存在 → project.config.json 被误判存在）→ 改真实 fs 检查；★剩余：automator 0.12.1 与新 IDE 的深层协议（JSON 消息解析 Unexpected end of input）超出框架可控范围（官方 SDK 维护责任），服务端口 GUI 开关开启后可最终确认。测试 tests/mp-e2e.test.ts 16 用例（+9：isValidAppid 2/体检 4/副本 2/诊断格式 1）。验证：verify **1032/118** 全绿（Node 22，1024→1032 +8）+ 根 vue-tsc 0 + cli 包构建 + **CLI 真实闭环**（体检报告全绿输出 + 副本生成 + patch 幂等跳过）；**B5 框架化完成：环境体检/产物副本/补丁自动执行/失败诊断 全内化**；下一候选：G-22 App Renderer
202. **★B5 automator 兼容边界确认（用户确认服务端口已开，端口 19425）**：**决定性实测**：① `--port 19425` 跑 e2e:mp → 仍 `Unexpected end of input`（automator 解析 IDE 消息崩）② `cli auto`（enable automation）**成功**：`✔ IDE server has started, listening on http://127.0.0.1:54723` + `✔ Using AppID: wx33bc04a52024def7` + `✔ auto`——**自动化启用成功 + 真实 appid 识别** ③ 但 **19425/9420 均无监听**（netstat/lsof 空；IDE server 端口 54723 为 HTTP 服务，WS 404）④ 逐个探测 IDE 全部监听端口（4447/60876/48277/32123/58352/14497/24143）+ 正确 JSON-RPC 消息（{id,method,params}）→ **无 automation WS**（14497 WS 打开无响应、其余 403/hang-up/404）⑤ 微信官方文档 auto.html 404 失效。**★结论：automator 0.12.1（2021）与新 IDE 2.01.x 自动化端口协议不兼容**——`--auto-port` 参数已移除（cli auto -h 证实）、automation WS 端点动态化 + 非公开协议；服务端口 19425 是 IDE 配置值但非 automation WS 监听端口。**框架已全部内化**（体检/副本/SDKVersion 补丁/失败诊断/端口可配）；**剩余为官方 SDK 跟进责任**（automator 需适配新 IDE 端口协议）。**收口**：05 文档加「已知限制」表（新旧 IDE 对比 + 框架内化清单 + 剩余边界）+ README 状态表更新（05 🔶 B5：链路就位，旧版 IDE 可全链路跑通）。验证：verify **1032/118** 全绿（Node 22）+ 根 vue-tsc 0；**B5 框架化收官：框架侧全部就绪，automator 端口协议兼容留待官方 SDK 更新或 IDE 版本适配**；下一候选：G-22 App Renderer
203. **★B5 全链路真跑通（★决定性修复：patch 脚本 bug 根因 + 端口复用 + 冒烟断言形态）**：**★根因确认：所有「Unexpected end of input」是我的 patch 脚本 bug**——MiniProgram.js 是**单行压缩文件**，patch 里插 `// proteus-automator-patch` 注释把整行剩余代码全吞掉 → 类定义残缺 → automator 从 checkVersion 修复后全崩。修复：幂等检测改用**特征串**（`const e=(await this.send("Tool.getInfo"))||{}` 出现即已 patch），禁 // 注释 MARK。**★automator 与新 IDE 实际兼容**：修复后 9420 automation 正常监听 + `Tool.getInfo` 返回 `{version, SDKVersion:"3.16.2"}`（项目就绪后 SDKVersion 正常；之前为空是项目未就绪）——消息格式 {id,method,params} 完全正确。**★页面激活态**：Page.getData/Page.getElement 报 `page is not on top of page stack`（模拟器页面未激活，GUI 态）→ 冒烟断言改用全链路最稳通道：`connect → reLaunch('/pages/index') → currentPage().path === 'pages/index' → systemInfo().platform === 'devtools'`（2026-08-31 实测通过）。**★端口复用（死循环修复）**：已运行 IDE 的 automation 端口被占 → automator launch 报 `Port 9420 is in use` → CLI 体检用**异步 probePort**（net.connect 短超时；busy-wait 同步探测不可靠——事件回调晚于循环退出）→ 被占 → **connect 复用模式**（env PROTEUS_MP_E2E_CONNECT=1，spec 直接 connect 不 launch）。**★副本重建竞态**：prepare 副本每次重建 → IDE 重编译 → 首次 connect 后 reLaunch 挂住（120s 超时），IDE 稳定后 6s 通过（记录为已知时序，重跑即稳）。验证：**`proteus test e2e:mp examples --port 9420 --ide <cli>` EXIT=0**（体检 ✅ 副本 补丁幂等 connect 复用 reLaunch currentPage systemInfo 全通）+ verify **1032/118** 全绿 + 根 vue-tsc 0；**B5 正式收官：小程序 E2E 在 examples 真机全链路跑通**（05 文档已知限制表改为「实际兼容」+ README 05 ✅）；下一候选：G-22 App Renderer
204. **统一测试 API 落地（★用户发起「自动化测试框架没统一测试 API」→ mountComponent 双端挂载 + stateOf/textOf 统一断言）**：**test-core `mount.ts`（新）**——① `sfcToComponent`（SFC 源码 → Web 组件对象：compileScript + compileTemplate 标准双段编译 + esbuild 剥离 TS（format **cjs**——esm 的 `export { x as default }` 字符串替换易残留）→ `require("vue")` 替换 __VUE__ 注入 new Function 执行；★双段分开 transform/执行（`const __returned__` 是 setup 内部局部，模块顶层引用不到）+ JS 侧组装（组件对象 .render = render））；② `mountWebComponent`（@vue/test-utils mount）；③ `mountComponent`（统一入口：web 真实渲染 / **mp 归一化 host**——mountMpComponent 的 instance 摊平 + wxml 顶层暴露，stateOf/textOf 直接可用）。**★关键环境/行为决策**：① 必须 happy-dom（esbuild TextEncoder instanceof 检查 jsdom 跨 realm 崩——plan §3 兑现）② ★不启用 inlineTemplate：setup 返回 __returned__ 绑定对象 → setupState 代理 → vm 可读状态（inlineTemplate 时 setup 返回 render 函数，绑定不暴露 vm.count=undefined）③ stateOf web 读 **vm.$.setupState**（Object.keys(vm) 为空——公开代理无 own keys 走 has/get trap）+ $data 合并（options API）；④ ★web 渲染更新在微任务队列——increment 后同步读 ref 已变，文本断言需先 await $nextTick。**★mount-mp 两处顺手修复（真实缺口）**：① 实例只扫顶层函数，未摊平 config.methods（组件模式方法在 methods:{}——编译器强制）→ 方法暴露补 methods 合并（★hooks: 之前测试只断言 data/setData 从未覆盖方法）② setData 只记录不应用 → 合并进 data（真实语义，统一状态断言依赖）。**tap 统一分发**（06 §统一事件 helper 已有）+ stateOf/textOf 增强（06 §分层断言：状态完全共用、DOM 各自断言）。测试 tests/test-core-unified.test.ts 4 用例（web 渲染+状态+事件 / mp 逻辑+WXML / 双端复用同一断言 / tap 分发）+ 全 test-core 15 用例。★踩坑三连：① esbuild 产物名 stdin_default 不确定 → 不依赖命名 ② compileTemplate 需 bindingMetadata（{{ count }} → $setup.count 否则 _ctx.count undefined）③ mount.ts 运行时依赖 esbuild/@vue/compiler-sfc/@vue/test-utils/vue → test-core build 全 external（防 esbuild 自 bundle 崩）+ peerDeps + devDeps 声明。验证：verify **1036/119** 全绿（Node 22，1032→1036 +4）+ 根 vue-tsc 0 + test-core 包构建（dist 含 mountComponent/stateOf/UnifiedMpHost d.ts）+ 双端构建；**统一测试 API 落地：同一份 SFC → web/mp 两端挂载 → stateOf/textOf/tap 跨端复用断言**；下一候选：G-22 App Renderer
205. **统一测试 API（E2E 层）TestDriver 落地（★用户澄清：统一 API = 测试能力抽象，一套能力接口多端自动化；参考 docs/wechatide-skill automator「意图→工具」表）**：**`packages/test-core/src/driver/`（新子路径 @proteus-vue/test-core/driver）**——① **types.ts**：`TestDriver`（navigate/reLaunch/back · currentPage/systemInfo · element · evaluate · screenshot · waitFor · close）+ `TestElement`（tap/input/longPress/text/value/attribute/waitFor/exists）+ PageSnapshot/SystemSnapshot + **结构类型**（PlaywrightPageLike/PlaywrightLocatorLike/AutomatorMiniLike/AutomatorElementLike 最小形状——**零硬依赖**：不 import playwright/automator，句柄用户/CLI 装配后传入）；能力域对照表写入注释（automation_navigate→navigate/reLaunch/back、automation_runtime_info→currentPage/systemInfo、automation_element_action→TestElement、automation_evaluate→evaluate、simulator_screenshot→screenshot、waitForSelector/wait→waitFor）。② **web.ts** `createWebDriver(page)`（navigate=page.goto / element=page.locator 惰性重查 / longPress=mousedown+延时+mouseup 近似模拟——web 无原生长按）。③ **mp.ts** `createMpDriver(mini)`（★05 经验内化：navigate/reLaunch=mini.reLaunch 全链路最稳、断言走 currentPage/systemInfo/evaluate、元素每次操作重新解析 currentPage().$() 不缓存——导航后自然失效重查；back=wx.navigateBack 降级）。④ **index.ts** `createDriver({ platform:'web', page } | { platform:'mp', mini })` 统一入口（对齐 mountComponent 模式）。**构建/配置**：package.json exports ./driver + build 多入口 + vitest/tsconfig alias（driver 在父路径前）。测试 tests/test-driver.test.ts 7 用例（fake 句柄 vi.fn 验证统一接口双端分发：navigate 分发/currentPage 形状/element 操作/evaluate·screenshot·waitFor/**★同一份跨端业务用例代码双端跑**/close）。★踩坑：ElementWaitState 无 hidden（对齐 automator waitForSelector 语义）→ web waitFor state 直接透传映射。验证：verify **1043/120** 全绿（Node 22，1036→1043 +7）+ 根 vue-tsc 0 + test-core 包构建（dist/driver 5.6kb + d.ts）+ 双端构建；**统一测试 API 两层就位：组件层 mountComponent/stateOf/textOf（L3）+ E2E 层 TestDriver（L4）——同一份用例代码多端跑**；剩余：真实 e2e 文件（e2e-web*/e2e-mp-smoke）迁移到 driver 示范（后续批次）、console/network 取证能力（wechatide debugger 域）标扩展；下一候选：G-22 App Renderer
206. **TestDriver 实战验证跑通（★用户要求验证实现；Web 真机 2/2 + MP 真机 CLI EXIT=0）**：新增 tests/e2e-driver-shared.ts（runSharedSmoke：同一份跨端用例——reLaunch → element.waitFor → 文本断言 → tap → currentPage/systemInfo → screenshot → close，只碰 TestDriver 能力接口）+ tests/e2e-driver-web.test.ts（Playwright 真实浏览器 2/2：runSharedSmoke 全链路 + web 专属 tap 后响应式更新 'tapped 0→1 times'）+ e2e-mp-smoke.test.ts 改造走 createDriver({platform:'mp'})。**★MP 真机验证（旧版 IDE 2.01.2510260 重新登录后）CLI e2e:mp EXIT=0**：connect 复用 → runSharedSmoke 稳通道（reLaunch/currentPage/systemInfo）+ evaluate 页面 data 断言（count=0）。**★真机踩坑四连（全部内化）**：① **automator evaluate 必须传函数**——内部 `fn.toString()` 序列化，传字符串原样下发 → 运行时无响应**挂起**（mp.ts resolve/back/waitFor 三处 + spec 改函数）；② **page.$/$$/screenshot 受模拟器激活态/协议限制挂起**（B5 冒烟避开元素 API 的原因）→ runSharedSmoke 加 elementOps/screenshotOps 可跳过（web 完整、MP 走稳通道）+ 05 文档记录；③ **driver.close()（disconnect）后调用 evaluate 挂起** → closeAtEnd 选项（调用方专属断言后再 close）；④ **端口探测双栈**（前轮已修，本轮真机确认 IDE automation 只监听 IPv6 `*:9420`——单 IPv4 探测误判空闲 → 误走 launch 端口冲突）。★顺手修复：web systemInfo evaluate 改裸表达式（箭头函数源码被 playwright 当表达式求值返回函数对象）；AutomatorMiniLike.evaluate 参数 any[]（unknown 逆变）+ back 用 globalThis.wx（test-core build 无 miniprogram 类型）；根 vue-tsc 存量类型清理（WebHostLike.vm 放宽 + fake 句柄断言）。★环境边界实测：新版 IDE（/Volumes/data1/applications/wechatwebdevtools.app）automation WS 端口不可发现（决策 #202 边界再确认——cli auto ✔ auto 但逐端口探测无 WS）；旧版 IDE 登录过期（code 10）需 GUI 重登。验证：verify **1043/120** 全绿（Node 22，exit=0）+ 根 vue-tsc 0 + Web E2E 全套 13/13 + **MP E2E 1/1 真机 EXIT=0** + test-core/cli 包构建；**TestDriver 两层实战闭环：Web 全能力 + MP 稳通道（元素/截图标注激活态边界）**；下一候选：G-22 App Renderer
207. **TestDriver debug 能力域 + 小程序独有能力收口（★用户发起「登录凭据/工具日志/控制台/网络/清缓存/刷新模拟器收进能力抽象」）**：TestDriver 扩展两族——**① debug 能力域（wechatide debugger 域）**：`consoleLogs(filter?)`/`networkRequests(filter?)`/`clearCache()`/`refresh()` 扁平方法（ConsoleEntry/NetworkEntry 类型：web 真实级别+status / mp grep 行猜测）；web = page.on('console'/'pageerror'/'request'/'response') 事件缓冲收集（PlaywrightPageLike 加 on?/reload）+ localStorage 清理 + reload；**mp = 注入式 MpDebuggerLike 句柄**（consoleGrep/networkGrep/clearCache/refresh 可选——automator 无这些 API，对应 wechatide get_simulator_console/get_simulator_network/debug_clear_cache/simulator_refresh；未注入抛错提示）；**② 小程序独有能力（automation_wx_api / automation_testaccount）**：`driver.wxApi`（call/mock/restore → automator callWxMethod/mockWxMethod/restoreWxMethod）+ `driver.ticket`（set/get/refresh/testAccounts → automator 原生）；**web 端降级抛错**（业务已收口 platformAPI，web 无 wx 运行时/登录凭据对等）。★automator 原生 API 盘点（MiniProgram）：导航/运行时/evaluate/exposeFunction/scrollTo/remote/审计 + ticket 四件套 + wx 三件套（call/mock/restore）——console/network/clearCache/refresh 确无 → 注入句柄设计。★类型细节：MpDebuggerLike/结构类型全可选（TS2722 需双非空断言 `debuggerHandle!.clearCache!()`——可选链不缩小对象本身）；AutomatorMiniLike 扩展 wx/ticket 可选方法。测试 tests/test-driver.test.ts 12 用例（+5：web 事件收集+filter / mp debugger 句柄分发+未注入报错 / wxApi 分发 / ticket 分发 / web 降级）。验证：verify **1048/120** 全绿（Node 22，1043→1048 +5）+ 根 vue-tsc 0 + test-core 包构建 + Web E2E 13/13；**TestDriver 能力面收口：导航/元素/运行时/执行/截图/等待 + debug 四件 + 小程序独有两族**；剩余：MpDebuggerLike 真实 wechatide 工具装配（CLI 侧候选）、console/network 断言 helper（§06 分层）；下一候选：G-22 App Renderer
208. **TestDriver CDP debug 能力（★用户问「web 端 CDP debug 能力收进去了吗」——此前仅 Playwright 高层事件，无 CDP 底层会话）**：`driver.cdp` 子域（`send<T>(method, params)` / `on(event, handler)`）——**web 注入式**：`createDriver({ platform:'web', page, cdp })`（`createWebDriver(page, cdpSession?)` 第二参数，Playwright `context.newCDPSession(page)` 透传）；覆盖 Performance/Trace 性能追踪、Network 节流与拦截（emulateNetworkConditions/Fetch.enable）、DOM/CSS 样式检查、Profiler 覆盖率等 CDP 域命令——**driver 只做透传不预置封装**（灵活 + 零依赖）；**mp 端降级抛错**（无 CDP 概念——渲染是原生 WXML 非 Chromium，用 evaluate/wxApi 替代）；web 未注入 CDP 会话 → 抛错提示（createWebDriver 第二参数）。★能力分层说明：consoleLogs/networkRequests（Playwright 高层事件，开箱即用）vs cdp（底层域命令，注入式）——前者覆盖常规断言，后者覆盖性能/节流/覆盖率等深度 debug。类型：CdpHandle/CdpSessionLike（结构类型，on 可选）。测试 +3（注入透传 send/on / 未注入报错 / mp 降级）→ test-driver 15 用例。验证：verify **1051/120** 全绿（Node 22，1048→1051 +3）+ 根 vue-tsc 0 + test-core 构建；**TestDriver web debug 能力补全：事件层（console/network）+ CDP 域命令层（透传）**；下一候选：G-22 App Renderer
209. **剩余能力收尾（★用户发起「继续收尾元素级交互、MpDebuggerLike 等」）**：**① MP 元素查询超时诊断**（test-core mp.ts）——automator `page.$` 在模拟器未激活/协议异常时**挂起无响应**（实测 8s+），此前会静默拖死用例（120s vitest 超时）→ MpElement 所有查询（resolve/waitFor-detached/exists）包 `withBound`（3s Promise.race 有界）+ **ELEMENT_QUERY_HINT 诊断**（激活模拟器/改稳通道 currentPage·systemInfo·evaluate/05 文档）；$ 挂起 → 快速失败明确提示，$ 返回 null → 正常轮询。**② MpDebuggerLike 真实装配通道**（cli + spec）——CLI `proteus test e2e:mp --debugger <module>`（parseTestArgs 新增；仅 e2e:mp 支持，缺值/其他 scope 报错）→ env `PROTEUS_MP_DEBUGGER_MODULE`（绝对路径）→ spec（e2e-mp-smoke）`loadDebuggerHandle()` 动态 import 装配（模块导出 default MpDebuggerLike 或 createMpDebugger()；加载失败抛「导出 default/createMpDebugger」可行动提示）→ `createDriver({ platform:'mp', mini, debugger })`；05 文档给适配器示例（consoleGrep/networkGrep → wechatide 工具、clearCache → wx.clearStorageSync）。★语义：console/network/clearCache/refresh 是 wechatide 工具能力（automator 无 API）→ 框架提供**可插拔注入通道**（接口 + CLI 装配 + 示例），真实工具由用户环境提供。测试：test-cli +2（--debugger 解析三态）；test-driver/mp-e2e 回归。验证：verify **1052/120** 全绿（Node 22，1051→1052 +1）+ 根 vue-tsc 0 + cli/test-core 构建；**剩余能力收尾：元素层挂起 → 快速失败诊断；debugger 句柄可插拔装配（CLI 通道）**；剩余：App 端（v0.6）/ 小游戏画布（wechatide 独有）/ B8 后端决策——均为平台边界非框架缺失；下一候选：G-22 App Renderer
210. **随包 Agent Skill（★用户发起「自动化测试框架也提供一个 skills 放到包里面」）**：`packages/test-core/skills/proteus-test/`（同 docs/wechatide-skill 形态：SKILL.md + skill.yaml + references/ 五篇）——AI agent 直接使用测试框架。**SKILL.md**：frontmatter（name/version/description——Proteus 跨端测试框架，提写测试/跑测试/TestDriver/mock wx 时使用）+ 运行前检查（Node ≥22.12 PATH / 工程根 / MP 真机门禁：真实 appid·build:mp·IDE·服务端口）+ 意图→命令表（npm test / test:e2e:web / proteus test e2e:mp --debugger / audit all / check）+ 写用例工作流 + 失败快表 + 参考索引。**references/**：① cli-commands.md（命令/参数/env 变量/文件命名）② testdriver-api.md（TestDriver 全能力索引 + 组件层统一挂载）③ cross-platform-cases.md（06 铁律 + runSharedSmoke 共享用例模式 + MP 稳通道）④ mp-e2e-guide.md（真机前置门禁/CLI 全链路/诊断表/边界/debugger 适配示例）⑤ pitfalls.md（环境坑合集：Node/happy-dom/evaluate 函数/双栈端口/时序）。**★随包分发**：test-core package.json files 加 "skills"（npm 发布后随 @proteus-vue/test-core 分发，装包即得 skill）；test-core README 标注随包 Skill；plan README 文档清单加 14-agent-skill 行。验证：verify **1052/120** 全绿（Node 22，纯文档 + package.json files 改动）+ 根 vue-tsc 0；**自动化测试框架 Agent Skill 就位：意图→命令 → 能力索引 → 用例模式 → 真机指南 → 坑位，AI 可直接驱动框架写/跑/诊多端测试**；下一候选：G-22 App Renderer
211. **配置职责边界梳理（★用户发起「有些属于 app-config 的也在 proteus.config 里面，重新梳理应用配置、框架配置职责」）**：**梳理结论**——职责划分文档有原则（01 §1.2 构建期 vs 运行时）但**工程无形态**（examples 无 app.config.ts，应用配置无处安放 → 塞 proteus.config）+ 三处语义易混（appid vs app.id / platform×2 / style 编译换算 vs theme·font 运行时）。**产出四件**：① **职责边界文档**（01 §1.2b 决策 #211）：唯一判断准则「字段被构建期消费（Compiler/CLI/产物）→ proteus.config；被运行时消费（业务/useAppConfig）→ app.config」+ 全字段对照表（platform=构建目标 vs 运行时平台覆盖 / appid=平台编译标识 vs app.id=应用运行时标识 / style·budget·rules 构建期 vs theme·font·features·api·env 运行时 / router.tabBar·meta 属框架）+ 易混项澄清表 + 铁律（构建期字段必须 proteus.config——放 app.config 编译期读不到；运行时字段必须 app.config——放 proteus.config 无法热更新）。② **examples 落地 app.config.ts**（真实应用配置：app/env/api/features/theme/font/safeArea）+ config-demo 页应用配置演示区（initAppConfig + useAppConfig + useFeatureFlag + setConfig 运行时更新 glassEffect 开关——页面级 init 注释说明生产应在启动期一次；config-demo import 双端可用）。③ **★共享模块白名单加 vue**（plugin-vite THIRD_PARTY_ALLOW）——app-config store 运行时依赖 vue，原白名单只有 pinia/@vue/*，app-config 共享模块会被跳过 → MP 端不可用；加 vue（框架包运行时依赖 vue 是生态常态，pinia 先例，bundle 体积 bundle-report 监控）→ 实测 `_proteus/app-config.js` 18.8KB 产出 + config-demo 页面 require 引用 + WXML 含应用配置文本。④ **类型注释澄清**（ProteusConfig.appid「平台编译标识，构建期消费」/ AppConfig.app.id「应用运行时标识」）。★顺手：config-demo 编辑时误删 clipboard Capability import 已恢复。验证：verify **1052/120** 全绿（Node 22）+ build:web + build:mp（_proteus/app-config.js 共享模块）+ examples vue-tsc；**配置职责边界落地：判断准则 + 全字段对照 + 易混澄清 + 工程形态（app.config.ts 双端可用）**；下一候选：G-22 App Renderer
212. **应用标题归属梳理（★用户问「应用标题在哪里设置」）**：**排查现状**——① Web 浏览器标签：`examples/index.html` 硬编码 `<title>Proteus</title>`（与 app.config.app.name 脱节）；② 小程序导航栏：app.json `navigationStyle: 'custom'`（自定义导航，无 navigationBarTitleText）；③ `router.meta.title`（proteus.config 集中 meta）**无任何消费方**（runtime/examples 均未读 meta.title——无导航组件展示）；④ `app.config.ts app.name`（'Proteus Demo'）仅 config-demo 展示。**★结论 + 落地（决策 #212）**：应用标题唯一事实源 = **`app.config.ts` 的 `app.name`**（运行时消费——浏览器标签/关于页/分享）；`router.meta.title` = 页面级导航标题声明（框架配置，编译期元数据，小程序 custom 导航组件消费待补）；Web 端 App.vue onMounted 运行时设置 `document.title = appConfig.app.name`（index.html 静态 title 仅首屏兜底，注释标明）+ 职责边界表补「应用标题」行（app.name 属应用配置；页面导航标题声明属框架 router.meta）。**剩余标注**：页面级标题展示需导航组件（小程序 custom 导航消费 meta.title——后续组件批次）；分享/关于页标题用 app.name（已就绪）。验证：verify **1052/120** 全绿 + build:web（App.vue 编译通过）；**应用标题落点：运行时唯一事实源 app.name + 页面级 meta.title 声明（展示待导航组件）**；下一候选：G-22 App Renderer
213. **proteus health 工程健康检查 + help 美化（★用户发起「cli 是不是缺健康检查」「help 输出好看点」）**：**① `proteus health [dir]`**（cli health.ts + index.ts 注册 + HELP_TEXT）——工程/环境健康一次性诊断（与 check 领域门禁正交）：node-version（≥22.12 require(ESM)）/ project-root / node_modules / scripts（build:web·build:mp·test）/ app-config（决策 #211 职责）/ appid（isValidAppid 复用，占位 error）/ pages-dir / build-output / workspace-links（@proteus-vue/* dist 完整性，#102 坑）/ ide-cli（resolveMpIdeCli 复用）；✅/⚠/✗ 报告，error → exit 1（warn 不阻断）；测试 +4。**② help 美化**——HELP_TEXT 平铺 → HELP_GROUPS 分组（构建与开发/检查与门禁/测试/生成与迁移/诊断与工具）+ formatHelpText(useColor=isTTY)：命令名（proteus <子命令> 前两 token）cyan、分组标题 bold + 分隔线、<必选> 黄、[可选] 灰、头部框线 + 尾部提示块（--help/文档/GitHub）+ 版本；非 TTY/CI 纯文本（无 ANSI 裸码）；desc 首行 6 空格后续行保留内缩进（防叠加）；测试 +3。验证：verify **1058/121** 全绿（Node 22）+ CLI 实测（health examples：真实 appid ✅ + Node 18 告警 + IDE 缺失提示；help 分组/缩进/着色）；**CLI 健康门禁 + 帮助体验就位**；下一候选：G-22 App Renderer
214. **发布前收口：去掉 packages 相对路径包引入 + 包健康检查（★用户发起「去掉 packages 相对路径的包引入，做完整 npm 发布前包健康」）**：**① 相对引入收口**——全仓 60 处主入口相对引入（`../packages/X/src`/`src/index`）→ **包名 `@proteus-vue/X`**（57 文件，node 脚本可控替换——perl `\$3` 转义把捕获组输出空的教训，用 node replace 回调）；tests/types 类型测试（types 内部子路径 utils/index-shared/router-types/api-types/platform-api）→ `@proteus-vue/types` 主入口（主入口已全 re-export）；create-proteus 测试用**真实包名** `create-proteus`（裸名非 @proteus-vue/ 前缀）；**保留 160 处内部模块单测**（`src/args`/`src/check` 等——CLI/types 内部实现非公开 API，包名不暴露，属合理开发态）；**补全 vitest alias 5 个（api/built-in-components/pinia-sync/renderer-app + create-proteus 真实名）+ tsconfig paths 8 个**（缺 alias 的包测试改包名后解析崩——先补后改）。**② 包健康检查脚本 `scripts/check-package-health.js`**（npm run check:pkg，接入 verify 末尾）：22 包逐包——必填字段（name @proteus-vue/ 或 create- 前缀/version/license/type:module）/ main+types+exports 子路径→dist 文件存在 / dist/index.js 已构建 / files 含 dist（+README/skills）/ **依赖版本对齐**（deps/peerDeps/devDeps 中 @proteus-vue/* 声明去 ^/~ 必须精确等于 workspace 实际——决策 #102 npm 404 坑）/ 源码无跨包相对引入（packages/*/src 禁 `../packages`——发布后相对路径断）；**bin 工具包豁免**（cli/create-proteus：main/types/exports 非必填，改查 bin 目标存在）。★顺手修复：perl 批量替换翻车（59 处 `@proteus-vue/` 空后缀）→ git checkout 恢复 + node 脚本重做。验证：verify **1058/120** 全绿（exit=0，57 文件改包名后 alias/paths 补齐全量通过）+ **check:pkg 22 包 0 error/0 warn** + 全仓主入口相对引入 0；**发布前收口完成：测试统一走包名（真实消费形态）+ 包健康门禁入 verify**；剩余：npm 实际发布（用户暂不发布）、docs/packages.md 发布清单更新（后续）；下一候选：G-22 App Renderer
215. **npm 首次发布（★beta 预览 22 包全部上线，决策 #213/#214 后收官）**：**发布流程**——`npm run changeset:publish`（pre 模式不允许自定义 `--tag`，直接跑即用 pre.json 的 beta tag）；**★凭据机制**：账号开 2FA + npm 下线 classic token/身份验证器后，唯一可行 = **granular Automation token**（绕过 2FA），但 granular token 只能选**已发布**的包 → 未发布包形成死锁；**★决策 #215a：create-proteus 收口改名 `@proteus-vue/create-proteus`**（命令 `npm create @proteus-vue/proteus my-app`；从未发布过、改名零注册表负担；模板/文档/测试/alias/check-pkg/pre.json/CI 全同步，1058/121 全绿）；**发布结果**——21 包首发一次成功 + create-proteus 改名后补发，**22/22 全部上线**（dist-tag beta，首发布 npm 自动置 latest）；发布后 `npm view` 一度 404 是 CDN 传播延迟（dist-tags 端点先行、主文档滞后，几分钟收敛）；**顺带补齐 9 个缺 README 的包**（api/capabilities/module/pinia-sync/plugin-vite/router/runtime/shared/web，npm 页面随下次版本发布展示）；**验证**：check:pkg 22 包 0 error/0 warn + 全量 1058 单测 + build + PUT 200 确认 + dist-tags 端点确认；**剩余**：docs/packages.md 发布记录已更新、正式版流程（pre exit → version → 同步 → publish）、真实 npm create 端到端验证；下一候选：G-22 App Renderer
216. **G-34 M1 HMR 运行时落地（★devtools-plus 首块：@proteus-vue/hmr 包，Web 端优先纯逻辑）**：工具链未落地盘点后用户选 G-34 M1 优先（不依赖 G-22、开发体验最大增量）。**新包 `@proteus-vue/hmr`**（23 包，零依赖注入式）——① **HmrPayload 协议**（id 单调递增防乱序/file/type vue|js|css|asset|native-binding/code/action update|reload）+ **HmrEvent 可观测事件**（原则 #3 编译透明 → DevTools 数据源）；② **createHmrRuntime**：分派（vue/js → applyModule 热替换，失败/缺 code 降级安全 reload；native-binding → HMR002 自动安全 reload；css/asset → 资源热替换）+ **状态保留**（snapshotState/restoreState，restore 消费快照——Flutter Hot Reload 体验）+ **严格规则**（HMR001 副作用未清理 warn、HMR002 原生 binding 自动 reload、HMR003 状态丢失检测）+ 按 id 幂等；③ **createVueHotAdapter**：import.meta.hot 语义面（accept/dispose/invalidate）+ applyWithState 状态往返，无 hot 环境 no-op 降级；④ **createHmrClient**：WS 客户端（createSocket 注入可单测）+ 指数退避重连（maxAttempts 上限 + reconnecting 事件）+ 断线重连成功后 attempt 归零；⑤ **createSafeReload**：save/restore/reload（collect 注入，Web 实现 sessionStorage + location.reload；M3 原生侧接 Router G-32 栈序列化）。验证：**24 用例全绿**（分派/顺序幂等/状态保留/HMR001-003/重连/适配/安全 reload）+ 包构建 7.3kb + **check:pkg 23 包 0 error/0 warn**（新包自动纳入）+ vitest alias/tsconfig paths/include 注册；docs/proteus-devtools-plus-plan/04-batches.md M1 ✅；**M1 落地：HMR 运行时就位，M2 CDP 桥接 / M3 原生 reload / M4 视图检查器待后续**；★**性能补丁（同日）**：批量 applyBatch（同文件合并只保留最终状态 + 乱序按 id 全局排序）+ client 数组消息自动走批量 + **性能基准用例（1000 payload 批量应用 < 100ms，G-34 §6 推送→渲染预算实达标）**——1000 条 50 文件循环变更 → merged 950 / applied 50 / 毫秒级；27 用例全绿；**遗留性能项**：编译侧增量（<50ms 预算未落地，dev-mp 全量重建 + M8 缓存）、安全 reload 实测、DevTools 开销（M2）；下一候选：G-34 M2（DevTools 桥接）或 benchmark 基线工具
217. **G-34 收尾：编译侧增量 + dev server 端到端闭环（★dev-mp 接入，M1 完整落地）**：用户要求「把 HMR 这条线全部收尾再继续 M2」。**① `@proteus-vue/hmr/dev-server` 子路径**（Node-only，依赖 ws ^8.18）：`createHmrDevServer`——WS 服务端（★host 缺省 127.0.0.1 显式 IPv4，避免 `::` 双栈绑定下 IPv4 连接不达）+ 文件 watch 防抖管线 + **增量编译回调注入**（compile(files) → HmrPayload[]）+ 广播（单 payload 发对象 / 多 payload 发数组）+ 可观测事件（listening/client-connect/files-changed/compiled/broadcast/error）+ port 0 随机端口实际值 getter + close 超时兜底；★flush 过滤目录级事件（递归 watch 附带父目录条目——只保留 isFile，否则 node_modules 等目录事件误触发编译）。**② examples/scripts/dev-mp.ts 接入**：内置 HMR dev server（PROTEUS_HMR_PORT 缺省 5174）+ 变更 .vue → compileVueSfc 单文件增量编译 → WS 广播（产物重建与 HMR 广播双通道并行：前者给微信开发者工具、后者给 HMR Runtime/DevTools）；dev-mp 在 examples-only（不入 create-proteus 模板，CI 快照不受影响）。**③ E2E 真实验证**：dev-mp 启动 → WS 客户端连接 → 修改 examples/pages/index.vue → 收到 `{file: pages/index.vue, type: vue, action: update, codeLen: 1072}` payload——**端到端闭环跑通**（验证脚本用完即删）。★踩坑：ws 双栈 `::` 绑定下 127.0.0.1 连接挂起（显式 host 修复）；macOS 递归 fs.watch 父目录条目误报（isFile 过滤）；测试批量广播收到 1 条数组消息（received.length=2 非 3）。验证：**dev-server 6 用例**（WS 连接/广播/close、防抖合并、ignore、compile 异常、真实 compileVueSfc 增量）+ **全量 1091/122 全绿** + check:pkg 23 包 0 error；**G-34 M1 完整落地（运行时 + 协议 + 编译侧增量 + dev server + 端到端）**；下一候选：G-34 M2（DevTools 桥接 CDP + Style Safety 可视化）
218. **G-34 M2 DevTools 桥接落地（★CDP 桥 + Style Safety 可视化数据源）**：**① `@proteus-vue/hmr/cdp` 子路径**——`createCdpBridge`：CDP 命令子集（Runtime.enable/disable/evaluate + **Proteus.* 自定义域** enable/disable/getStyleGates + 未知方法 -32601/缺 method -32600）+ **Proteus 事件 → CDP 消息转译**（Runtime.consoleAPICalled 调试台可见 + 结构化 Proteus.event；style-gate → Proteus.styleGate）+ StyleGateRecord 环形缓冲（500，getStyleGates 查询）；注入式（transport/evaluate）纯逻辑可单测。**② `@proteus-vue/hmr/style-gate` 子路径**——`collectStyleGateRecords(style, {platform, allPlatforms})`：**三道闸门链可视化**（白名单 STYLE_PROP_LEVELS 等级 STS001/003/004 → 校验 validateProp STS002 → 平台收窄 narrowValue 五端原生值映射），输出 StyleGateRecord { gates 轨迹, decision pass/narrow/drop, nativeValues, rejectReason }——「编译透明」原则（#3）直接体验，DevTools 面板数据源；★依赖 @proteus-vue/runtime/style-safety（hmr 包首个包间依赖，esbuild external）。★踩坑：runtime style-safety 真实 API 与假设不同（isValidStyleProp 需带值、validateStyle 返回清洗后 style 而非拒绝数组、narrowValue(prop,value,platform) 三参、rpx 运行时非法——编译期处理，改用 px 验证收窄）。验证：**cdp 6 + style-gate 5 + bridge 事件 3 = 14 用例全绿** + 全量 + 包构建 4 bundle + check:pkg 23 包 0 error；**G-34 M1+M2 落地（M3 原生 reload / M4 视图检查器待 G-22）**；下一候选：G-34 M3（待 G-22）/ benchmark 基线 / devtools B3 面板
219. **benchmark 性能基准 + 回归门禁落地（★performance-plan G-30 工具链缺口 #6 收口）**：`scripts/benchmark.ts`（tsx 跑，`npm run bench` / `bench:update` / `--json`）——**5 项功能基准**（中位数抗抖动：预热 3 + N 轮排序取中）：compile-vue-sfc（30 轮，~0.38ms）/ setdata-deep-diff-1000（50 轮，**~332ms——深层 diff 潜在优化点，先留基线**）/ hmr-apply-batch-1000（~0.07ms，G-34 <100ms 预算达标）/ style-gate-collect-100（~0.03ms）/ trace-redact-1000（~0.3ms）；**回归门禁**：median > 基线 ×1.5 → error（exit 1）/ >1.2 → warn，基线存 `benchmarks/baseline.json`（`--update` 写回），**CI 接入 verify job**（bench 步）；★踩坑：@proteus-vue/runtime 模块初始化需浏览器全局（shared web-adapter window/document/location/history + vue runtime-dom）→ **happy-dom Window 设全局后再动态 import runtime**（手工 stub 逐个补 location→history→window→document 越补越多，直接换 happy-dom 一步到位）；verify **1105/122** 全绿 + bench 基线首次写入 + 二次运行 5/5 ✓ 无回归；**G-30 工具链侧落地（功能级回归防护；真机矩阵待原生）**；下一候选：devtools B3 时间轴面板（TraceBus + Proteus.event 通道现成）或 G-22 App Renderer
220. **setData 深层 diff 性能优化（★benchmark 驱动：332ms → 109ms，-67%）**：benchmark 首次跑出 setdata-deep-diff-1000 ≈ 332ms（1000 项全量变更 → 2000 叶补丁）暴露热点——**① diffPaths 中间数组分配**（`out.push(...diffPaths(...))` 每层 spread 拷贝 + `new Set([...Object.keys(a), ...Object.keys(b)])` 每个对象一次 Set+双数组分配）→ **收集器模式**（共享 out 数组直接 push，零中间数组）+ 键集合改为「新值键 + 旧值独有键」两次遍历（hasOwnProperty 判删除；补丁顺序变化不影响路径级结果）——此步实测**未显著提速**（瓶颈不在这）；**② 真凶 addDirty O(n²)**（每补丁 `[...map.keys()]` 全量拷贝 + 遍历全部已有脏路径 isChildPath 前缀判断——2000 兄弟叶子补丁 × 递增 map = 200 万次比较 + 2000 次全量数组拷贝）→ 去掉 spread（Map 迭代中删除安全）+ **isChildPath 索引比较**（`parent + '.'`/`parent + '['` 字符串拼接 → `child[parent.length]` 字符判断，每次比较省 2 次分配）——**332 → 109ms**。★语义保真：删除键补 undefined、祖先覆盖子孙、同路径后者胜全部保留（runtime.test 15/15 + 全量 1105/122 全绿）；benchmark 基线已更新（332 → 113ms）——**基准工具闭环：建门禁 → 暴露热点 → 优化 → 验证**；下一候选：devtools B3 时间轴面板 / G-22 App Renderer
221. **devtools B3 时间轴数据层落地（★createTimelineCollector，devtools-plan M3 首块）**：`@proteus-vue/devtools-runtime` 新增 `timeline.ts`——TraceBus 事件流 → 泳道时间轴模型（**UI 无关纯逻辑**，对齐 03-m3-timeline.md 的 Span 数据结构）：① start/end 配对构建 Span（同 source 嵌套 LIFO 栈 → children 树，跨 source 独立栈）② 孤儿 end → duration 0、point/error → 竖线（duration 0，★completed 计数含孤儿/竖线）③ `flushOpen()` 未结束 span 标 pending + 补算耗时（面板刷新可见）④ `query()` 过滤（source/name/minDurationMs/traceId，递归含 children）⑤ 缓冲上限（缺省 10000 丢最旧根，对齐 TraceBus 环形缓冲）+ stats（total/open/completed）⑥ 与 TraceBus `on()` 集成（订阅直喂）。★MP 产物安全约束（决策 #32/#36）：无 ?? / ?. / 对象展开 / 数组解构（`for (const entry of stacks)` 取 entry[0]/[1] 避数组解构）；根 span 序 = 完成序（跨 source 平行时非 start 序）。验证：**11 用例全绿**（配对/嵌套/孤儿/竖线/跨源独立栈/flushOpen/白名单/query 递归/缓冲上限/clear/TraceBus 集成）+ 包构建 + check:pkg 23 包 0 error；**B3 数据层就位（UI 渲染层待面板工程 v1.0+）**；下一候选：G-22 App Renderer（解锁 G-34 M3/M4 + cli-plus M3 + app-config M5 + module M6）
222. **devtools M4 状态快照 + 时间旅行数据层落地（★createStateSnapshotter，devtools-plan M4/B4）**：`@proteus-vue/devtools-runtime` 新增 `state-snapshot.ts`——① **export()**：注入 store 读取器收集非 volatile store + redact（复用 TraceBus redactValue，缺省 password/token/authorization/idcard/phone）+ route/meta 上下文；② **serializeState/deserializeState**：type tag 方案（Date→ISO / Map→[[k,v]] / Set→[v] / BigInt→String，全部可还原）+ **循环引用标记降级**（WeakSet 检测 → `{__t:'__proteus_circular'}` → 还原 undefined，不抛错不栈溢出；★踩坑：seen 必须内部初始化——顶层调用不传 seen 时无限递归）；③ **import()**：校验 version（不兼容抛错）+ 按 id 还原；④ **recordPatch()**：mutation 后记录 before/after（完整 state 序列化，before 可由适配层 peekState 传入）+ index 递增 + 环形缓冲（bufferSize 缺省 1000，裁剪时 index 保留原值）；⑤ **timeTravel(i)**：各 store 取 steps[0..i] 最后一条 after → writeState（**快照栈回放而非补丁重放**——更简单可靠，越界抛错）。★MP 产物安全（决策 #32/#36）全文件遵守。验证：**11 用例全绿**（Date/Map/Set/BigInt 往返/循环降级/嵌套递归/export 过滤+redact/import 还原+版本校验/recordPatch/timeTravel 多 store/越界+clear/缓冲裁剪/未知 store 忽略）+ 包构建 + check:pkg 23 包 0 error；**M4 数据层就位（Pinia 适配插件 + UI 待 v1.0+）**；下一候选：devtools M5 路由回溯 / G-22 App Renderer
223. **devtools M5 路由回溯数据层落地（★createRouteBacktracker，devtools-plan M5/B5）**：`@proteus-vue/devtools-runtime` 新增 `route-backtrack.ts`——① **beginNav/guardStart/guardEnd/finishNav**：导航生命周期采集（Router beforeEach/afterEach 钩子适配，注入 now 时钟可测）；② **守卫耗时瀑布**：guardStart/guardEnd 计时（durationMs）+ 结果 next/redirect/cancel/error（与 M6 火焰图同 traceId 可比对）；③ **回溯查询**：pathTo（逆推父链「怎么到这页」）/ redirects（「为什么重定向」）/ cancels（含 error，「为什么没跳转」）；④ 环形缓冲（缺省 500 最旧裁剪）+ onEvent 可观测（begin/guard/finish，联动 TraceBus/时间轴）；未知 navId / 无进行中守卫的 guardEnd 安全忽略。★踩坑：测试文件编辑中括号失衡（esbuild Unexpected end of file）→ 重写测试文件；fake timers 需 beforeEach 统一开启（advanceTimersByTime 在非 mock 环境抛错）。★MP 产物安全（决策 #32/#36）全文件遵守。验证：**9 用例全绿**（生命周期+耗时+traceId/守卫瀑布/孤儿 guardEnd 忽略/pathTo/redirects/cancels/缓冲裁剪/clear/onEvent）+ 全量 1127/122 + check:pkg 23 包 0 error；**M5 数据层就位（installRouter 注入采集 + UI 待 v1.0+）**；下一候选：devtools M6 性能火焰图 / G-22 App Renderer
224. **devtools M6 性能火焰图数据层落地（★createFlamegraphCollector，devtools-plan M6/B6）**：`@proteus-vue/devtools-runtime` 新增 `flamegraph.ts`——① **全局嵌套栈**（★父子关系跨 source：boot(lifecycle) 根下挂 capability/store 子 span——区别于时间轴 B3 的 per-source 泳道；end 从栈顶往下找第一个同 source 弹栈）② **inclusive/exclusive**：duration = inclusive，self = duration - Σchildren.duration（children 先完成）③ 同层按 start 排序 + startMs 相对录制基线（performance.now 注入）④ icicle 倒置（depth 降序）⑤ **compare 对比模式**（±10% 阈值 → regression/improvement，exclusive 聚合，同 source+name 匹配，新增节点 100%/消失 -100%）⑥ 录制 start/stop 门控（stop 后 ingest 忽略 + 未结束 span 丢弃）+ 缓冲上限 20000。★踩坑：① 最初用 per-source 栈（时间轴语义）→ 火焰图需全局嵌套栈；② compare 的 walkB 误传扁平 collectAll 输出 → children 被重复计数（bMs 翻倍）→ 直接遍历根列表；③ boot 的 exclusive 时间随 children 变化传导（测试预期修正）；④ `??` 违反 MP 产物安全 → 三元替代。验证：**9 用例全绿**（树结构+inclusive/exclusive/相对基线/同层排序/point 忽略/stop 门控/icicle/compare 阈值+新增消失/nodes+缓冲/recording）+ 全量 1145/122 + check:pkg 23 包 0 error；**M6 数据层就位（UI 待 v1.0+）**；下一候选：devtools M7 异常根因 / G-22 App Renderer
225. **devtools M7 异常根因分析数据层落地（★createErrorDiagnoser，devtools-plan M7/B7）**：`@proteus-vue/devtools-runtime` 新增 `error-diagnoser.ts`——① **ingest**：error 事件入候选 + 全部事件入活动上下文（traceId 聚合）；② **归因规则**：规则 ① error 为根因候选 / 规则 ② 同 traceId 多 error → **最早 timestamp 为根因**（验收场景：coreReady 的 token 刷新失败 401 先于守卫取消导航，准确指向 api.refreshToken）/ 规则 ③ **模式库**（内置 401→token 失效·ChunkLoadError→分包加载失败·capability.unsupported→缺 fallback，`patterns` 可扩展插件覆盖）；③ **影响范围**：同 traceId 涉及 source 去重（store/页面/组件推导）；④ **复现脚本**：同 traceId router 导航事件 → 步骤序列（无导航降级为「触发 source.name」）；⑤ 无 error → 空数组不误报 + 缓冲上限（缺省 500 最旧丢弃）。★踩坑：`?.`/`??` 违反 MP 产物安全约束 → 三元替代（第 2 处）。验证：**10 用例全绿**（验收 401 场景/规则 ②/③×2/自定义模式/复现脚本×2/无 error 不误报/多 traceId+clear/缓冲裁剪）+ 全量 1145/122 + check:pkg 23 包 0 error；**devtools 数据层 B3-M7 全部就位（M8 设备面板待 Skyline 能力）**；下一候选：G-22 App Renderer（解锁链核心）或 devtools B8
226. **devtools UI 层落地（★@proteus-vue/devtools 24 包，B3-M7 五视图渲染 + 面板装配 + CDP 通道 E2E 闭环）**：新包 `@proteus-vue/devtools`（浏览器端 dev 工具，依赖 devtools-runtime，esbuild platform=browser）——① **五视图纯渲染函数**（data → DOM，jsdom 可单测）：renderTimeline（按 source 泳道分组 + span 线段宽度∝耗时相对定位 + pending 橙/竖线蓝类名）/ renderFlamegraph（按 depth 分行堆叠 + 宽度∝durationMs + selfMs 标注 + zero 灰）/ renderState（store 列表 + JSON 预览 + 时间旅行滑块 onTimeTravel 回调）/ renderRoute（导航链 from→to + 耗时 traceId + 守卫徽章 next 绿/redirect 橙/cancel·error 红）/ renderErrors（根因卡片 attribution ⚑ + causedBy 调用链 + 影响 chips + 复现步骤，★根因匹配用键值 source+name+timestamp——数据层 chain 与 rootCause 非同一引用）；② **createDevtoolsPanel**：tab 布局 + 数据层收集器装配（timeline/flame/errors 直喂；route 轻量适配 router nav 事件 → NavRecord；state 聚合 store 事件只读展示）+ 16ms 渲染节流 + 火焰图录制按钮 + destroy 清理；③ **createDevtoolsWsSource**：WS 连 dev server → Proteus.enable（CDP 协议）→ Proteus.event 重组 TraceEvent 分发 + 断线 1s 重连；④ **dev-server CDP 桥集成**：per-client createCdpBridge（transport 直发对应 socket + evaluate 注入 + traceSource 注入）+ 自身事件转 Proteus.event（compiler.watch files / incremental compile / hmr.hmr broadcast）+ **E2E 真实验证**：dev-mp 51741 → Proteus.enable 响应 {} → 改页面 → 收到三条 Proteus.event，面板 CDP 通道闭环通过；⑤ panel.html 本地面板入口（?ws= 覆盖地址）。★踩坑：① panel destroy 引用残留 off2（重构未清理）② check-pkg 对 exports 非对象 target 只查对象形态（./panel 改字符串直指 panel.html）③ devtools 包构建依赖 devtools-runtime dist 新鲜（先重建 runtime）④ 测试文件编辑括号失衡（describe 头被吞）→ 谨慎核对。验证：**devtools-panel 9 用例 + dev-server CDP 桥 2 用例** + 全量 1166/122 + check:pkg **24 包** 0 error；**devtools B3-M7 数据层 + UI 层全落地（M8 设备面板待 Skyline；UI 交互深化/插件机制 M9 后续）**；下一候选：G-22 App Renderer（解锁链核心）
227. **接入 Vue 官方 DevTools（★Timeline layer，用户「面板粗糙，能接 Vue 官方 devtools 吗」）**：架构认知——Proteus **Web 端即标准 Vue 应用**，Vue DevTools 扩展**原生**展示组件树/Pinia（零代码）；我们的事件流（编译/路由/API/生命周期——Vue DevTools 看不到的部分）经 `@vue/devtools-api`（8.2.1，devtools-kit 薄壳）推入其 **Timeline** 面板。**① `installProteusTimeline(api, { source })`**（@proteus-vue/devtools 新增 vue-devtools.ts）：注册 `proteus` Timeline layer + 事件映射（time/title=`source.name`/subtitle=phase/data 全量/按 traceId 分组 groupId 回退 source）+ dispose 卸载；结构类型注入（api 形状兼容 DevtoolsApi）可 mock 单测。**② `createTraceBusSource(bus)`**：TraceBus 直连源（Web 端运行时接入）。**③ examples 接入**（main.ts）：TraceBus（enabled）+ defineApp 生命周期四阶段 emit（bootstrap/coreReady/interactive）+ setupDevtoolsPlugin 接线。★机制探索：devtools-kit 8.x **自注入 hook**（覆盖 window.__VUE_DEVTOOLS_GLOBAL_HOOK__，非扩展预注入模型）→ 无扩展时 plugin 进 buffer setupFn 不执行（**安全降级 = 生产零开销，实测无扩展页面零副作用**）+ setupDevToolsPlugin 在 index-node.js 是 no-op（Node 环境解析陷阱）；完整握手模拟过深 → 适配器逻辑单测覆盖（3 用例：layer 注册+事件映射+dispose / traceId 回退 / TraceBus 源）+ examples 编译 + 无扩展降级 E2E（页面正常渲染无错误）。验证：devtools-panel 12/12 + build:web 通过；**Web 端：扩展看组件/Pinia + Timeline 看 Proteus 事件；MP 端：独立面板**；下一候选：G-22 App Renderer（解锁链核心）
228. **devtools 面板 UI 质感升级（★对标 Vue DevTools：用户「UI 能做对标 vue devtools 那种吗」）**：`style.css` 系统主题（VS Code 深色：--pd-* CSS 变量色板 + 等宽 mono + 类型着色 number/string/boolean/null/object + 强调色 #4fc3f7 + 滚动条）+ **布局重构**（panel.ts：顶栏（标题 + 连接状态点——首事件后绿点「已连接」）+ 侧栏导航（pd-nav-item 图标 5 项）+ 内容区）+ 视图增强：① **timeline 加时间刻度尺**（pd-ruler 0/25/50/75/100% 实际时间）+ span hover 高亮 ② **state 改 key-value inspector 树**（renderValue 递归：object/array 可折叠 ▸/▾ + 惰性构建子层 + 根节点默认展开 + 类型着色类 pd-t-*）③ **route 守卫徽章对接**（panel router 适配补 guard 事件：point/error name 含 guard → 附加到最近进行中导航，result 从 name 推断 next/redirect/cancel/error）④ panel.html 改 link style.css。★踩坑：① `built = initiallyOpen` 自相矛盾（build 被 if(built) 跳过）→ built 恒 false + initiallyOpen 时调 build；② dist/panel.js 旧 bundle 未重建导致断言假失败（先 build）；③ kvRows 断言过严（mock store 5 行正确）。验证：**devtools-panel 13/12→13 用例**（state inspector 树 + 嵌套折叠 + 类型着色 + 面板布局/连接状态）+ **UI-ASSERT E2E**（暗色主题 rgb(30,30,30)/侧栏 5/刻度尺 5/4 泳道/state 5 行 4 类型色/route 3 守卫含 cancel 红/根因卡片 2/连接状态点）+ 五视图截图（.e2e-tmp/ui-*.png 验证后清理）+ 全量 1170/1170 + check:pkg 24 包 0 error；**面板质感对标完成（后续可继续：hover tooltip/缩放/虚拟滚动/对比模式）**；下一候选：G-22 App Renderer（解锁链核心）
229. **devtools UI 交互深化 ①：hover tooltip 浮层（★对标 Vue DevTools，M7 交互候选首块）**：新 `src/tooltip.ts`——① **createTooltipLayer**（浮层挂 document.body 避免容器 overflow 裁剪，position:fixed + zIndex:1000 + pointerEvents:none）：show 重建 DOM（title + lines 行）+ 鼠标跟随 + **视口边缘翻转**（右侧/底部放不下 → 左/上翻）+ hide/dispose；**dispose 移除 body 元素**（面板 destroy 时清理，防多面板层残留——测试 querySelector 命中陈旧层的坑）；② **bindTooltip**（150ms 防抖：快速划过不闪烁；resolve(target) → TooltipData | null，null 不显示；mouseover/mousemove/mouseout 三监听，返回解绑函数）；③ **attachTip/resolveTipData**：视图渲染时把 TooltipData 存元素（Symbol 属性）+ data-tip 标记，panel resolve 统一取——三视图接入：timeline span（来源+耗时+阶段 pending/completed）/ flamegraph 块（inclusive/exclusive ms）/ route 守卫徽章（守卫名+结果+耗时），**原生 title 属性替换为浮层**（避免双重 tooltip）；panel.ts createTooltipLayer + bindTooltip 绑根元素 + destroy 时 unbindTip+dispose。★顺手：hmr-dev-server 测试 `new WebSocket()` 依赖 Node ≥21 全局 → 改 import { WebSocket } from 'ws'（Node 18 下也能跑）。验证：**devtools-panel 13→17 用例**（tooltip show/hide/视口翻转/防抖/resolve null/解绑/面板 hover span 贯通）+ 全量 1174/130 全绿（Node 22）+ check:pkg 24 包 0 error + build:web；**hover 浮层就位（后续：timeline 拖拽缩放 / 火焰图对比模式 / 万级 span 虚拟滚动）**；下一候选：G-22 App Renderer（解锁链核心）
230. **devtools UI 交互深化 ②：timeline 时间窗口交互（★对标 Vue DevTools Timeline，M7 交互候选第二块）**：新 `views/timeline-interaction.ts`（`createTimelineZoom`）——① **wheel 缩放**（上滚放大/下滚缩小，以光标为锚点——缩放后锚点时刻保持不动）+ **拖拽平移**（mousedown/move/up 按像素 delta 换算时间位移）+ **双击重置**（回全窗）+ **钳制**（不超数据边界 + 最小窗 MIN_SPAN 防无限放大）；窗口状态模块持有，变更经 `onWindowChange` 回调（panel 统一 16ms 节流 rerender），destroy 解绑全部监听；② **renderTimeline 窗口过滤**（提供 window 时只渲染相交 span——缩放场景避免负坐标溢出 + 万级 span 分块基础）；★踩坑：① **自动窗口循环覆盖权威 window**（`if (s.start < winStart)` 无条件收缩窗口 → 过滤基准错乱，需仅未提供 window 时自动取范围）② happy-dom WheelEvent 继承 UIEvent **无 clientX**（NaN）→ 光标坐标 NaN 回退中心锚点（Number.isFinite 守卫，浏览器侧无影响）③ zoom 块误插在 containers 声明前 → TDZ（`Cannot access 'containers' before initialization`，移到 views 循环后）④ 局部 TimelineWindow 与 import 冲突（vitest 不查类型 esbuild 也不报，tsc build 报 TS2440）。验证：**devtools-panel 17→23 用例**（wheel 锚点保持/全窗钳制/拖拽平移/双击重置/destroy 解绑 + window 过滤）+ 全量 1180/130 全绿（Node 22）+ check:pkg 24 包 0 error + build:web + devtools build（dist/panel.js 43KB）；**时间窗口交互就位（后续：火焰图对比模式 / 万级 span 虚拟滚动）**；下一候选：G-22 App Renderer（解锁链核心）
231. **devtools UI 交互深化 ③：火焰图对比模式（★两录制叠加 ±10% 高低亮，数据层 compare 落地到 UI）**：`renderFlamegraph` 扩展 `compare?: FlameCompareEntry[]`——① **块级高亮**（按 source+name 聚合键与数据层同构：verdict regression → `pd-fg-reg` 红 / improvement → `pd-fg-imp` 绿）+ hover 浮层追加 delta 行；② **对比汇总列表**（`.pd-cmp`：标题计数「N 处回归 · M 处优化」+ top 8 |deltaPct| 排序 + 行内 delta 百分比双色 + 行自身 attachTip）；③ **面板录制流**（recBtn 点击：停止时若有 baseline → `flame.compare(baseline)` 否则置空；**baseline 存 roots()（嵌套树）而非 nodes() 扁平列表**——compare 对 previous 递归遍历，扁平列表 children 重复计数；当前树成为新基线支持连续多次录制两两对比；开始录制时清空 overlay）。★踩坑：① **录制按钮被 replaceChildren 清空的存量 bug**（renderFlamegraph 每次 rerender 清空容器 → 按钮从未存活，E2E 未断言过该元素未被发现）→ fgControls 声明提到 rerender 前 + rerender 渲染后重挂到容器最前；② compare 的 previous 参数必须传嵌套树（roots）否则 selfMs 重复累加。★顺手：hmr-dev-server 真实 fs.watch 测试并行负载下 FSEvents 延迟 → waitFor 默认超时 5s→15s（断言语义不变，消除 2 处 flake）。验证：**devtools-panel 23→25 用例**（renderFlamegraph 对比高亮/汇总列表 + 面板两次录制流程回归高亮块）+ 全量 1182/130 全绿（Node 22，连续两轮稳定）+ check:pkg 24 包 0 error + build:web + devtools build（dist/panel.js 45.5KB）；**火焰图对比就位（后续：万级 span 虚拟滚动）**；下一候选：G-22 App Renderer（解锁链核心）
232. **devtools UI 交互深化 ④：timeline 虚拟滚动（★万级 span 分块渲染，M7 交互候选收官）**：`renderTimeline` 扩展 `virtual?: { scrollTop; viewHeight }`——spacer 占位总高（泳道数 × LANE_H 26px）+ 仅渲染视口内泳道（startIdx/endIdx = 视口 ± OVERSCAN 2 泳道）绝对定位 + 容器 overflow-y 自滚；**panel 接线**：timeline 视图容器 scroll 监听 → scrollTop 跟踪 + 16ms 节流 rerender，viewHeight 每渲染测量（clientHeight 兜底 300），destroy 移除监听；`buildLane` 抽公共（流式/虚拟两布局复用，span 构造/attachTip 单点）。★踩坑：① 两处编辑笔误（new_string→new_text 错键 + 段落替换位置）致 rerender/zoom 块损坏——重构后全文件核对；② 虚拟测试误落入缩放 describe（结构合法留原地）。★环境顺手：**hmr-dev-server 真实 fs.watch（macOS FSEvents）测试在系统高负载下事件延迟 >15s 频繁 flake**（ps 实测 Zed 264% + HBuilderX 71% 占满 8 核——非 vitest 自身）→ ① waitFor 默认超时 5s→15s（#231）；② watch 类用例显式 `{ timeout: 60000 }`；③ **vitest.config.ts 限流 `poolOptions.threads.maxThreads: 4 + minThreads: 2`**（vitest 1.x 键名 maxThreads 非 maxWorkers；不配 minThreads 会与默认 8 冲突 RangeError）——降低 vitest 自身对负载的贡献，实测全绿且 Duration 30s→19s。验证：**devtools-panel 25→27 用例**（spacer 总高/视口分块 14/50/滚动换页 src28）+ 全量 1184/130 全绿（Node 22，多轮含限流前后）+ check:pkg 24 包 0 error + build:web + devtools build（dist/panel.js 47.1KB）；**M7 交互三件套（hover tooltip / 时间窗口缩放平移 / 火焰图对比 / 虚拟滚动）全部就位**；下一候选：G-22 App Renderer（解锁链核心）

## 验证状态（最近一次）

- ✅ `npm run verify` 全绿：**1043 单测 / 120 文件**（e2e 排除）+ **Web E2E 套件 11/11**（npm run test:e2e:web：路由/渲染 8 + 关键路径 3）+ **MP E2E 冒烟 1/1**（proteus test e2e:mp examples 真机跑通 EXIT=0）+ build:web + build:mp（含 project.config.json 生成）+ build --workspaces（2026-08-31，★需 Node ≥22.12——require(ESM) 支持，web/jsdom 测试在 Node 18/20 下加载失败属环境差异非代码问题）
- ✅ 双端构建：web（vue-tsc 零错误 + vite）+ mp（gen-routes → vue-tsc → vite，产出 app.js + 各页四件套 + proteus/ 组件产物 + 共享模块）
- ✅ 各 @proteus-vue/* 包独立构建（**22 个包**，prepare 钩子 + esbuild bundle）+ **npm 已发布**（2026-08-31：22/22 beta 上线，决策 #215）
- ✅ CI 门禁齐备：stores 铁律 / capabilities:check / components:audit / i18n:check / generate types --check / config:check / 模板快照一致性
- ✅ 审计基线：`components:audit` 16 组件零违规；`i18n:check` examples 4/4 引用干净
- ✅ 构建缓存（M8）：编译缓存 + bundle 缓存均 100% 命中、产物 diff 逐字节一致、单文件变更精确失效
- ✅ 组件类型：GlobalComponents 模板标签类型注册（16 组件），examples vue-tsc 0 错误
- ✅ 类型收口：公共类型全部在 @proteus-vue/types，实现包 re-export 兼容层，消费方零改动，type-only 擦除验证
- ✅ 小程序语义 Web 模拟层（@proteus-vue/web）视觉对齐（2026-08-30，方法见 docs/proteus-compiler-plan/17-weui-io-alignment.md）：toast/modal/actionSheet/picker/switch/slider/button 全部 weui.io 官方像素级对齐（CDP 双模式 light/dark 断言 + 动画逐帧采样）

## 待办 / 注意事项

- ✅ **仓库目录重命名为 `proteus`**（用户本地执行）
- ✅ **文档体系**：根 `README.md` + `docs/`（getting-started / configuration / compiler / routing / roadmap / board-inventory）
- ✅ **开源协议**：Apache-2.0（LICENSE + package.json license 字段 + README 章节）
- ✅ **git 仓库**：已关联 https://github.com/proteus-vue/proteus（main 分支）
- **小程序真机/开发者工具实测（唯一剩余验收项）**：`npm run build:mp` 后用微信开发者工具导入 `dist/mp-weixin`（需真实 AppID + 基础库 ≥2.29.2）
- **Skyline iOS 真机白屏关注**：微信平台已知问题，已入 roadmap v0.5；真机实测时记录复现路径（决策 #69）
- **框架本体拆包（C 类）**：✅ 全部完成（决策 #98-#105，docs/packages.md 8 步）——14 个 @proteus-vue/* 包 + create-proteus 模板 npm 化；**npm 已发布**（2026-08-31：22/22 beta 上线，决策 #215，发布记录见 docs/packages.md）
- **组件库 P0（component-plan B1-B8）**：✅ 2026-08 全批收官——16 组件 + 4 runtime 共享模块 + components:audit + 编译器 4 增强（script/watch-props props 源 watch→observers / ref-write 多行 RHS 修复 / 组件 onUnmounted→detached / 未映射 onXxx 钩子警告）；业务组件（player-bar/payment-sheet/login-gate）标注依赖 appBar/支付（v0.6+）
- **i18n-plan（B1-B3）**：✅ 2026-08——@proteus-vue/i18n（ICU 子集：插值/复数/=N/select/#）+ i18n:check 门禁 + demo；分包加载/完整 ICU/Intl/RTL 标后续
- **devtools-plan（B1-B2）**：✅ 2026-08——@proteus-vue/devtools-runtime（TraceBus 协议/环形缓冲/脱敏/采样/零开销门控）+ lifecycle/component 两源接入（type-only 注入）；面板 B3-B8 标 v1.0+
- **npm 发布**：✅ 2026-08-31 完成——**22 包 beta 全部上线**（granular Automation token；create-proteus 收口 @proteus-vue scope，决策 #215）；正式版流程（pre exit → changeset:version → 同步 examples/templates → publish）见 docs/packages.md
- **security-plan（M1-M3）**：✅ 2026-08——@proteus-vue/security（M1 SecretStorage 加密存储：WebCipher/DemoCipher/volatile/redact/migrate；M3 PermissionRegistry + withPermission + PermissionDenied）+ M3 §3 Router 权限守卫自动生成（RouteMeta.permissions + createRouter options.permissions，与 requiresAuth 守卫同层）；M4-M8 标后续
- **app-plan（B1 核心）**：✅ 2026-08——@proteus-vue/renderer-app（Vue createRenderer + NativeAdapter 抽象 + mock adapter，无需真机验证渲染器接线）；B2-B5（原生视图/样式 rpx→dp/路由桥/能力桥/demo）标 v0.6 正式启动（需 npm 发布 + 原生工程）；Vapor 双模式标后续
- **build-plan M8 缓存**：✅ 2026-08——编译缓存（compileCacheKey 全入参哈希 + 磁盘/内存双层）+ esbuild bundle 缓存（输入快照 mtime+size 指纹）；examples 真实构建 100% 命中 + 产物逐字节一致 + 单文件精确失效；PROTEUS_NO_CACHE 关闭；详见 02-optimize-cache.md
- **types-plan（B3-B7 全批）**：✅ 2026-08——@proteus-vue/types 独立包（Platform/PlatformTarget + JSON Schema + generate types --check 防漂移）+ B4 平台守卫（matchPlatform/assertPlatform/exhaustiveCheck，铁律 #4）+ B5 validateConfig（config:check CLI 错误码）+ B6 加固（品牌类型 Brand/配置迁移 migrateConfig/Schema Registry extendConfigSchema）+ B7 migrate types codemod + CI 门禁；四层测试矩阵随批落地
- **组件类型齐全**：✅ 2026-08——GlobalComponents 模板标签类型注册（16 组件 Pascal+kebab 双名）+ @vue-expect-error 断言 fixture（防退化）；examples vue-tsc 0 错误
- **类型收口（T1-T4）**：✅ 2026-08——公共类型全部统一到 @proteus-vue/types（compiler-types/capabilities/router-types/api-types/config/index-shared），各实现包 types.ts 为 re-export 兼容层（消费方零改动）；runtime 值（ApiError/CapabilityError class、createTrace）留实现包；CapabilityPlatform = Platform alias；type-only 擦除验证 + 无环依赖；方案见 docs/proteus-types-plan/10-type-consolidation.md
- ⚠ **根 vue-tsc 零错误**（2026-08-31 清理 web-button multiSelector 既有错误后清零）
- 文档版本号已到 v2.53（git 仓库关联）
- ⚠ **新规划文档批次未提交**：12 个新 docs 目录（architecture-facade/css-compat/app-renderer/safe-area/memory-plan/memorial-skeleton/app-capabilities/test-framework/types-plus/design-principle/glass/performance）+ `slider-check.png` 均为 untracked；一致性已修（决策 #157），待用户确认后 git add + 提交

## 后续规划（2026-08-30 更新）

- **18-picker-swiper（规划 18）**：B1 selector ✅ + B2 multiSelector ✅（749 测试全绿时）；B2 余 **time / date / region**（date 年/月/日三列 fields 裁剪、region 省市区需内置精简行政区划数据标注 partial）；B3-B4 **swiper**（基础：指示点/手势/current 受控 → 进阶：autoplay/circular/vertical/display-multiple-items）；B5 双端实测 + 能力矩阵转 full
- **14 批次 4 收尾**：picker/swiper 完成后能力矩阵转 full + examples 双端实测
- **规划 17 方法论复用**：新组件视觉对齐（swiper/checkbox/radio/loading）按 weui-io-alignment 五步走 + --pwu-* 变量组（暗黑适配是默认要求）
- **已知限制（用户验收时发现，未修）**：tabBar 100vh 遮挡 / 半屏页 / 长列表 list-view 摊平 / 下拉刷新 Web 端未桥接——用户没提先不做
- 其余 roadmap 大项（v0.5 多端 / v0.6 App+Vapor / v1.0 生产可用）见 docs/roadmap.md，待 npm 发布 + 真实项目验证启动

## 会话恢复指引（新 LLM 按此顺序阅读）

1. `PROJECT_MEMORY.md`（本文件）—— 进度 / 决策 / 偏差
2. `LLM_IMPLEMENTATION_GUIDE.md` —— §0 痛点对照 + 当前阶段任务指令（§0.5 定位宪章）
3. `proteus.config.ts` —— 当前配置
4. `src/router/types.ts` + `src/platform/adapter.ts` —— 接口契约
5. `packages/compiler/src/transforms/registry.ts` + `README.md` —— 编译规则注册表（AI 说明书，改动编译器前先查）
