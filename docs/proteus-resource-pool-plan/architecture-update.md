# G-46 架构规约增量

## 原则新增

```
原则 #13.31  内（数据）一致性与外（渲染）一致性同等重要 — G-27 与 G-46 形成闭环
原则 #13.32  登录态归宿主所有，页面借用 — 所有权模型（G-43）应用于资源层
原则 #13.33  双轨（Cookie ⇄ Token）降级不崩溃 — 同根域优先 Cookie，跨域降级 Token
```

## 铁律注册

```
G-46.1 ~ G-46.8   （见 rules.md）
CMP089 ~ CMP096    （RSC-01 ~ RSC-05）
```

## 体系全景增补（L0-L6）

```
L0 语义原语         ... 现有 ...
L3 宿主运行时       + ResourcePool（宿主单例）
L4 资源层（新增）   ResourcePool / 双轨 / 跨页所有权 / RSC
L5 测试             + G-46 conformance（CMP089-096）
L6 工具链           ... 现有 ...
```

## 与既有 G 表互校

| 本套件 | 全局编号（定案） | 说明 |
|--------|---------------|------|
| G-46 | **G-46** | 本批 G-46~G-52 统一入库，编号以 facade G 表为准（决策 #385） |
| 方法论泛化 | 第 10 次 | 不绑定资源容器形态 |

## 缺口与后续（已知，诚实边界）

1. **多进程场景**：宿主进程隔离下 Backend 需额外协调（跨进程资源池）—— 未覆盖
2. **组合 conformance**：G-27（切后端）+ G-46（登录态不丢）的集成测试 —— G-44 INT 系列下一靶
3. **Backend 真实实现**：当前为可运行模拟（reference-impl.cjs），Android/iOS/鸿蒙原生桥接待 B3 落地

## 引用

- G-27 渲染后端（外·渲染一致）
- G-39 宿主运行时（host-runtime）（资源生命周期）
- G-42 宿主容器（安全网关）（签名同源）
- G-43 资源所有权（ownership：Rc/Weak/Drop，语义见 docs/proteus-ownership-plan）
- G-44 测试 IR（conformance runner）
- G-45 动态装载（签名 + manifest 哈希）
