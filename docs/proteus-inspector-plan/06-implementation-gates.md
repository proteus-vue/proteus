# G-57 实施门槛

## 阶段 1：本份（已完成）

- [x] 三层数据模型 L0/L1/L2
- [x] 服务扩展注册机制 + 命名规范
- [x] 降级链（L1/L2 失效不影响 L0）
- [x] 覆盖率诚实模型（空样本 = 0）
- [x] Debug-only + token 鉴权（接口层）
- [x] 参考实现 + verify.sh

## 阶段 2：真实宿主接入（G-58 候选）

- [ ] Flutter/Dart 路径：`registerExtension` 真实接入 + DevTools 扩展包
- [ ] Flipper 路径：Desktop + Client 双插件
- [ ] 自起服务路径：HTTP + WebSocket 真实实现
- [ ] **Release 包扫描断言**（`ext.proteus.*` 零命中）

## 阶段 3：拓扑自动化

- [ ] 从 SPI 注册表**自动生成**拓扑映射（避免手工维护漂移）
- [ ] 拓扑覆盖率纳入 CI 门槛（低于阈值告警）
- [ ] L1 语义标注与 G-19 运行时面板打通

## 阶段 4：Companion 集成

- [ ] Agent 模式分块续跑协议
- [ ] 真机节点可信度分级
- [ ] 与 G-53 共享模拟器池 / 云真机的三层成本模型

## 关键风险

| 风险 | 缓解 |
|------|------|
| 拓扑手工维护会漂移 | 阶段 3 自动生成 |
| Release 未真正剔除 | 阶段 2 扫描断言 |
| 局域网暴露被未授权访问 | localhost 优先 + token |
| iOS 后台服务中断 | 分块续跑 + SKIP 语义 |

## 诚实边界

- 阶段 2~4 **均未实现**，本份只完成阶段 1
- 参考实现的 L0 是**模拟数据**，真实探针需按 04 章对接
- 拓扑覆盖率的**合理阈值**未知，需真实项目数据校准
