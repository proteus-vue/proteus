# 架构更新（G-54）

## 新增原则（接续 G-53 编号：#13.57-59）

- **#13.60** 编码期辅助优先：框架知识应在开发者敲键时生效，而非等到 CI
- **#13.61** 不绑定 IDE 形态：能力内核唯一，适配层可换
- **#13.62** 知识垄断即护城河：框架独占知识（IR/分层/断言/拓扑/等价类/渲染语义）
  应主动工具化，否则锁在文档里等于没有

## 第 18 次泛化

```
不绑定渲染后端   G-27
不绑定数据池     G-46
不绑定组合       G-47
不绑定运行时     G-48
不绑定隔离层级   G-49
不绑平台/生态    G-50（第 14 次）
不绑定验证环境   G-51（第 15 次）
不绑定设备形态   G-52（第 16 次）
不绑定设备供给   G-53（第 17 次）
★ 不绑定 IDE     G-54（第 18 次，本份）
```

**模式稳定复现**：定义语义 → 内核唯一 → 适配可换 → conformance 验证。

> **编号避让登记（决策 #391）**：原稿自编 G-55 / CMP-163~170 / 第 15 次 → 定案 **G-54 / CMP-155~162 / 第 18 次**（G-53=mobile-verification 已占用 147~154，CI 门禁要求 G 序连续）；假想兄弟 plan「DevTools 加固」未入库，降级为未编号后续规划，消费面前指 G-51/G-52。详见 rules.md。

## 成熟度模型更新

```
L-1 编码期辅助   ← ★ G-54（新增，反馈毫秒级）
L0  文档自检      ← G-50
L1  IR 模拟       ← G-51
L2  真运行时      ← G-51 + G-52
L3  跨设备云端    ← G-53
L4  开放生态      ← G-50（依赖 G-49 L3）
```

**G-54 新增了 L-1 层**——把质量保障从"事后检查"前移到"事中提示"。

## 与既有互校

- **G-19**：互补不重叠（运行时 vs 编码期）
- **G-50**：复用其 CLI 流水线，不重写
- **G-51/52/53/54**：消费其断言/等价类/检查项，不生产
- **G-18**：CLI 基础复用

## 已知缺口

- 能力 ⑤⑥ 依赖 G-53/G-27 数据完备，当前 Mock
- 仅 VSCode 适配有参考实现，其余 IDE 依赖 LSP 标准但**未实测**（G-54.8）
- 自研 RPC 面板未实现，MVP 走 CLI
- 六项能力的真实效能收益（省多少 review 往返）**未量化**，需后续实测
