feat(compiler): 消除 unsupported 第三批——watchEffect 族改写为 watch(immediate) 转 aligned

- rewriteWatchEffectToWatch：watchEffect/watchPostEffect/watchSyncEffect(cb) 前置改写为
  watch([deps], cb, { immediate: true })——deps = cb 内访问的 ref（x.value → x），走既有 extractWatch 管线
  （proteusWatchX + onLoad 初始化）。★修 deps 正则（正则字面量误用 \\b → 改 \\b 单反斜杠）致匹配失败。
- 矩阵 watchEffect 族 → aligned（gate 放行）。
- 黄金断言 tests/vue-compat-aligned.test.ts（watchEffect → proteusWatchX + 不再裸 watchEffect）;
  矩阵测试：watchEffect 族从 unsupported/决策1b 名单挪到 aligned。

验证：全量 2593/2593 绿；build:mp / build:web 通过；产物 onLoad this.proteusWatchX(this.data.x, undefined)。
README 状态总览更新 + 下一批候选 toRef/toRefs。
