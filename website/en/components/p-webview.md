---
title: p-webview
group: 页面外壳
order: 2017
---

# p-webview

Embedded web page

> Semantic component (Layer 0) · domain **Page Shell** · compiled to each target's native controls at build time — zero platform branches in business code.

| Semantic | Domain | Mini Program equivalent |
|---|---|---|
| shell.webview | Page Shell | `<web-view>` (L1 primitive) |

## Compat rollout

| Target | Status | Notes |
|---|---|---|
| Web SPA | ✅ | vue-dom · dual-source compile target for both targets (compile-time mapping + event normalization) |
| WeChat Mini Program | ✅ | skyline (WebView fallback) · native control mapping → `<web-view>` (L1 primitive) |
| Headless (SSR / testing) | ✅ | headless · IR render test tier (tooling target) |
| iOS native | 🟡 | native-ios (UIKit) · prototype mapping — component-level wiring not started |
| Android native | 🟡 | native-android (Jetpack) · prototype mapping — component-level wiring not started |
| HarmonyOS | 🟡 | native-harmony (ArkUI) · prototype mapping — component-level wiring not started |
| Flutter hybrid | 🟡 | flutter · widget-level mapping — component-level not yet verified |
| Quick App | ⬜ | Quick App engine (TBD) · target not started |

> Status scale: ✅ shipped & this component usable · 🟡 prototype mapping — component-level wiring not started · ⬜ target not started. Target architecture matrix (engine / runtime / persistence) → [Ends & maturity](/docs/framework/ends-matrix).

## Props

| Prop | Doc | Type | Default | Required |
|---|---|---|---|---|
| `src` | Page URL (aligned with src) | `String` | `''` | No |
| `height` | Height in px (web container; fills the parent by default) | `Number` | `0` | No |
| `sandbox` | Web iframe sandbox policy | `String` | `'allow-scripts allow-forms allow-same-origin'` | No |

### Prop details

#### `src`

- **Type**: `String`　**Default**: `''`　**Required**: No
- **Doc**: Page URL (aligned with src)

#### `height`

- **Type**: `Number`　**Default**: `0`　**Required**: No
- **Doc**: Height in px (web container; fills the parent by default)

#### `sandbox`

- **Type**: `String`　**Default**: `'allow-scripts allow-forms allow-same-origin'`　**Required**: No
- **Doc**: Web iframe sandbox policy

## Events

| Event | Doc | Payload |
|---|---|---|
| `message` | — | `e` |
| `error` | — | `e` |

### Event details

#### `message`

- **Doc**: —
- **Payload**: `e`

#### `error`

- **Doc**: —
- **Payload**: `e`

## Implementation notes

- Aligned with the Mini Program <web-view>: embeds a host web page (the Mini Program requires a registered business domain)
- MP uses the native <web-view>; the web injects an <iframe> at runtime (the literal iframe never enters the MP artifact — same approach as p-camera's getUserMedia)
- MP-safe: the template only contains tags valid on both ends (<web-view> / <view>)

## Usage

```vue
<p-webview :src="…">
  <p-text>content</p-text>
</p-webview>
```

<!-- generated by website/scripts/gen-content.mjs (en overlay) · source SSOT: src/components/p-webview/index.vue -->