---
title: p-page-container
group: 页面外壳
order: 2008
---

# p-page-container

Page container / bottom sheet

> Semantic component (Layer 0) · domain **Page Shell** · compiled to each target's native controls at build time — zero platform branches in business code.

| Semantic | Domain | Mini Program equivalent |
|---|---|---|
| shell.page-container | Page Shell | `<page-container>` (L1 primitive) |

## Compat rollout

| Target | Status | Notes |
|---|---|---|
| Web SPA | ✅ | vue-dom · dual-source compile target for both targets (compile-time mapping + event normalization) |
| WeChat Mini Program | ✅ | skyline (WebView fallback) · native control mapping → `<page-container>` (L1 primitive) |
| Headless (SSR / testing) | ✅ | headless · IR render test tier (tooling target) |
| iOS native | 🟡 | native-ios (UIKit) · prototype mapping — component-level wiring not started |
| Android native | 🟡 | native-android (Jetpack) · prototype mapping — component-level wiring not started |
| HarmonyOS | 🟡 | native-harmony (ArkUI) · prototype mapping — component-level wiring not started |
| Flutter hybrid | 🟡 | flutter · widget-level mapping — component-level not yet verified |
| Quick App | ⬜ | Quick App engine (TBD) · target not started |

> Status scale: ✅ shipped & this component usable · 🟡 prototype mapping — component-level wiring not started · ⬜ target not started. Target architecture matrix (engine / runtime / persistence) → [Ends & maturity](/docs/framework/ends-matrix).

## Props

| Prop | Doc | Type | Default | Required |
|---|---|---|---|---|
| `show` | Whether shown (v-model:show) | `Boolean` | `false` | No |
| `position` | Position: bottom (default) / top / center | `String` | `'bottom'` | No |
| `overlay` | Whether to show the mask | `Boolean` | `true` | No |
| `closeOnClickOverlay` | Close when the mask is tapped | `Boolean` | `true` | No |
| `round` | Rounded corners in px (top two corners) | `Boolean` | `true` | No |

### Prop details

#### `show`

- **Type**: `Boolean`　**Default**: `false`　**Required**: No
- **Doc**: Whether shown (v-model:show)

#### `position`

- **Type**: `String`　**Default**: `'bottom'`　**Required**: No
- **Doc**: Position: bottom (default) / top / center

#### `overlay`

- **Type**: `Boolean`　**Default**: `true`　**Required**: No
- **Doc**: Whether to show the mask

#### `closeOnClickOverlay`

- **Type**: `Boolean`　**Default**: `true`　**Required**: No
- **Doc**: Close when the mask is tapped

#### `round`

- **Type**: `Boolean`　**Default**: `true`　**Required**: No
- **Doc**: Rounded corners in px (top two corners)

## Events

| Event | Doc | Payload |
|---|---|---|
| `update:show` | Two-way binding: fires when `show` changes (syncs the parent binding) | `false` |
| `close` | — | — |

### Event details

#### `update:show`

- **Doc**: Two-way binding: fires when `show` changes (syncs the parent binding)
- **Payload**: `false` (implicit v-model: the value itself)

#### `close`

- **Doc**: —
- **Payload**: none

## Slots

| Slot | Doc |
|---|---|
| default | Default slot (main content) |

## Implementation notes

- Aligned with the Mini Program <page-container>: a half/full-screen container that slides up from the bottom
- Container structure follows p-drawer (teleport → root-portal on Skyline; the mask is visual-only, the root container captures taps)
- MP-safe: CSS transform transitions; no direct wx/document/window calls

## Usage

```vue
<p-page-container :show="…">
  <p-text>content</p-text>
</p-page-container>
```

<!-- generated by website/scripts/gen-content.mjs (en overlay) · source SSOT: src/components/p-page-container/index.vue -->