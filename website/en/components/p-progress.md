---
title: p-progress
group: 内容与表单
order: 1016
---

# p-progress

Progress bar

> Semantic component (Layer 0) · domain **Content & Forms** · compiled to each target's native controls at build time — zero platform branches in business code.

| Semantic | Domain | Mini Program equivalent |
|---|---|---|
| ui.progress | Content & Forms | `<progress>` (L1 primitive) |

## Compat rollout

| Target | Status | Notes |
|---|---|---|
| Web SPA | ✅ | vue-dom · dual-source compile target for both targets (compile-time mapping + event normalization) |
| WeChat Mini Program | ✅ | skyline (WebView fallback) · native control mapping → `<progress>` (L1 primitive) |
| Headless (SSR / testing) | ✅ | headless · IR render test tier (tooling target) |
| iOS native | 🟡 | native-ios (UIKit) · prototype mapping — component-level wiring not started |
| Android native | 🟡 | native-android (Jetpack) · prototype mapping — component-level wiring not started |
| HarmonyOS | 🟡 | native-harmony (ArkUI) · prototype mapping — component-level wiring not started |
| Flutter hybrid | 🟡 | flutter · widget-level mapping — component-level not yet verified |
| Quick App | ⬜ | Quick App engine (TBD) · target not started |

> Status scale: ✅ shipped & this component usable · 🟡 prototype mapping — component-level wiring not started · ⬜ target not started. Target architecture matrix (engine / runtime / persistence) → [Ends & maturity](/docs/framework/ends-matrix).

## Props

| Prop | Doc | Type | Default | Required |
|---|---|---|---|---|
| `percent` | Current progress 0-100 (clamped automatically) | `Number` | `0` | No |
| `showInfo` | Whether to show the percentage text on the right | `Boolean` | `true` | No |
| `status` | Status: active / success / exception | `String` | `'active'` | No |
| `strokeWidth` | Line width in px (ring = ring thickness, line = bar height) | `Number` | `6` | No |
| `type` | Type: line / circle | `String` | `'line'` | No |
| `rounded` | Whether the ends are rounded | `Boolean` | `true` | No |
| `color` | Bar color (overrides the status default) | `String` | `''` | No |
| `trackColor` | Track background color | `String` | `''` | No |

### Prop details

#### `percent`

- **Type**: `Number`　**Default**: `0`　**Required**: No
- **Doc**: Current progress 0-100 (clamped automatically)

#### `showInfo`

- **Type**: `Boolean`　**Default**: `true`　**Required**: No
- **Doc**: Whether to show the percentage text on the right

#### `status`

- **Type**: `String`　**Default**: `'active'`　**Required**: No
- **Doc**: Status: active / success / exception

#### `strokeWidth`

- **Type**: `Number`　**Default**: `6`　**Required**: No
- **Doc**: Line width in px (ring = ring thickness, line = bar height)

#### `type`

- **Type**: `String`　**Default**: `'line'`　**Required**: No
- **Doc**: Type: line / circle

#### `rounded`

- **Type**: `Boolean`　**Default**: `true`　**Required**: No
- **Doc**: Whether the ends are rounded

#### `color`

- **Type**: `String`　**Default**: `''`　**Required**: No
- **Doc**: Bar color (overrides the status default)

#### `trackColor`

- **Type**: `String`　**Default**: `''`　**Required**: No
- **Doc**: Track background color

## Implementation notes

- Aligned with the Mini Program <progress>: percent 0-100 (out-of-range is clamped), status active/success/exception
- type=line linear / type=circle ring (drawn with pure CSS conic-gradient — no SVG dependency, both ends work)
- Both ends share one source: div → view; pure style computation (MP-safe)

## Usage

```vue
<p-progress :percent="…">
  <p-text>content</p-text>
</p-progress>
```

<!-- generated by website/scripts/gen-content.mjs (en overlay) · source SSOT: src/components/p-progress/index.vue -->