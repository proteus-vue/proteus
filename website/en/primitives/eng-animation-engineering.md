---
title: Animation engineering primitives (E19-E23: useAnimation/useScrollAnimation…)
order: 80
group: 工程原语
---

# Animation engineering primitives (E19-E23: useAnimation/useScrollAnimation…)

Animation engineering primitives E19-E23: useAnimation/useGestureAnimation/useScrollAnimation + p-animate

> Source module `@proteus-vue/api` (engineering primitive factories — **injection-based**: the consumer injects reactivity/driver/routerLike etc., the api package has zero vue dependency; MP artifact-safe subset: no `?.`/`??`/array destructuring).

**★G-32 B5 continued (2) (proteus-semantic-primitives-plus-plan §8 ③): E21-E23 animation semantics surface — injectable design**
E21 useAnimation (wx.createAnimation semantics — declarative keyframe builder) / E22 useGestureAnimation (gesture-driven: incremental accumulation)
E23 useScrollAnimation (scroll-driven: progress → interpolated keyframes); component forms E19 p-transition / E20 p-animate live in src/components
Injectable: reactivity (vue or mock) + driver (Web WAAPI / MP host wx.createAnimation / test recording) — the same
zero-runtime-vue-dependency family as createEngineering / createRouterEngineering; without a driver, play degrades to a safe no-op
MP artifact-safe (decisions #32/#36): no ?. / ??; no array destructuring

## Compat rollout

| Target | Status | Notes |
|---|---|---|
| Web SPA | ✅ | Official demo wiring (examples/platform-api-demo — all factories called) |
| WeChat Mini Program | 🟡 | Injectable subset runs on the logic layer (MP artifact-safe); component-form wiring partially rolled out first |
| Headless (SSR/testing) | ✅ | Inject reactivity etc. on Node to run (tooling/testing tier) |
| iOS native | 🟡 | Native validation not started (E-series injection surface follows host batches) |
| Android native | 🟡 | Native validation not started |
| HarmonyOS | 🟡 | Native validation not started |
| Flutter hybrid | 🟡 | same JS logic layer — wiring not started |
| Quick App | ⬜ | target not started |

> Status scale: ✅ target shipped & this primitive usable · 🟡 prototype mapping — wiring not started · ⬜ target not started. Family-level mechanism coverage (not a per-target on-device verification matrix); target architecture matrix (engine / runtime / persistence) → [Ends & maturity](/docs/framework/ends-matrix).

## Core exports (SSOT: `packages/api/src/animation-engineering.ts`)

| Export | Kind | One-liner (source comment) |
|---|---|---|
| `AnimationProps` | interface | Animation property subset (transform/opacity semantics — the wx.createAnimation compat surface) |
| `AnimationKeyframe` | interface | A single keyframe step (offset 0..1) |
| `AnimationDescriptor` | interface | Exported animation descriptor (the consumer hands it to the host driver: Web WAAPI / MP wx.createAnimation / test assertions) |
| `AnimationStepOptions` | interface | Step-level options (wx.step semantics: each keyframe may override duration/easing/delay) |
| `AnimationRun` | interface | Run handle (E21 play — driver-injected; no-op-safe when the driver is absent) |
| `AnimationDriver` | interface | Animation driver (injected — Web: Element.animate; MP: host bridge wx.createAnimation; tests: call recording) |
| `AnimationState` | type | Playback state (reactivity-injected → reactive) |
| `AnimationController` | interface | E21 useAnimation controller (declarative builder — wx.createAnimation semantics) |
| `GestureAnimationHandle` | interface | E22 useGestureAnimation handle (gesture-driven: incremental accumulation → committed frames) |
| `ScrollAnimationRange` | interface | E23 useScrollAnimation handle (scroll-driven: progress → interpolated keyframes) |
| `ScrollAnimationHandle` | interface | — |
| `AnimationEngineeringOptions` | interface | createAnimationEngineering injection options |
| `AnimationEngineering` | interface | G-32 §8 ③ animation semantics (E21-E23 in Hook form; E19/E20 component forms in src/components) |
| `interpolateAnimationProps` | function | / |
| `createAnimationEngineering` | function | / |

## Real usage (dogfooding provenance — the official site itself / example projects run it live, not illustrative)

```ts
const anim = createAnimationEngineering({ … })
const gestureAnim = anim.useGestureAnimation()
```
> Origin: `examples/pages/platform-api-demo.vue:540·547`

## Usage & degradation

- **factory injection**: `createXxxEngineering({ reactivity, driver, routerLike… }) → instance` — the consumer injects reactivity (api package has zero vue dependency); instance methods are the E-series primitives
- component forms (e.g. E20 p-animate / E18 p-router-link) enter the C-IR at compile time; injectable Hooks (E1-E28/R1-R4) wire up at runtime per the injected surface
- real example: `examples/pages/platform-api-demo.vue` (E/R factory calls — see the "Real usage" origins)

<!-- generated by website/scripts/gen-primitives.mjs (en overlay) · SSOT：packages/api/src -->