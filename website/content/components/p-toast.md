---
title: p-toast
---

# p-toast

轻提示

> 语义组件（Layer 0）· 域 **页面外壳** · 编译期映射到各端原生控件，业务零平台分支。

| 语义 | 域 | 小程序等价 |
|---|---|---|
| shell.toast | 页面外壳 | — |

## Props

| Prop | 说明 | 类型 | 默认值 |
|---|---|---|---|
| `pid` | — | `String` | `''` |
| `disabled` | — | `Boolean` | `false` |
| `ariaLabel` | — | `String` | `''` |
| `visible` | — | `Boolean` | `false` |
| `text` | — | `String` | `''` |
| `duration` | — | `Number` | `2000 }, // ms，0 = 不自动关` |
| `position` | — | `String` | `'center' }, // center / top / bottom` |

## Events

`close`

## 实现要点

- 矩阵 01 §8：text + duration 自动关闭（0 = 不自动关）+ enter 淡入；visible 驱动（B3 原语）
- 转场：CSS animation；Worklet 自定义组件（避开原生 showToast 限制）标注 v0.6

## 用法

```vue
<p-toast :pid="…">
  <p-text>内容</p-text>
</p-toast>
```

<!-- generated by website/scripts/gen-content.mjs · 源码 SSOT：src/components/p-toast/index.vue -->