---
title: p-router-link
group: 工程
order: 4003
---

# p-router-link

声明式导航

> 语义组件（Layer 0）· 域 **工程** · 编译期映射到各端原生控件，业务零平台分支。

| 语义 | 域 | 小程序等价 |
|---|---|---|
| engineering.router-link | 工程 | `<navigator>`（L1 原语） |

## 兼容进度

| 端 | 兼容 | 说明 |
|---|---|---|
| Web SPA | ✅ | vue-dom · 双端同源码编译目标（编译期映射 + 事件归一） |
| 微信小程序 | ✅ | skyline（WebView 降级） · 原生控件映射 → `<navigator>`（L1 原语） |
| Headless（SSR / 测试） | ✅ | headless · IR 渲染测试档（工具端） |
| iOS 原生 | 🟡 | native-ios（UIKit） · 端原型映射——组件级接线未开始 |
| Android 原生 | 🟡 | native-android（Jetpack） · 端原型映射——组件级接线未开始 |
| 鸿蒙 | 🟡 | native-harmony（ArkUI） · 端原型映射——组件级接线未开始 |
| Flutter 混合 | 🟡 | flutter · widget 级映射——组件级未验证 |
| 快应用 | ⬜ | 快应用引擎（待定） · 端未开始 |

> 状态口径：✅ 端已落地·本组件可用；🟡 端原型映射·组件级接线未开始；⬜ 端未开始。端架构对照（引擎 / 运行时 / 持久化）见 [端与成熟度](/docs/framework/ends-matrix)。

## Props

| 属性 | 说明 | 类型 | 默认值 | 必填 |
|---|---|---|---|---|
| `to` | 导航目标（路由名或路径）——createRouterEngineering.push({ name: to \| path: to }) | `String` | `''` | 否 |
| `replace` | 替换当前页（E12 语义——push({...to, replace:true})） | `Boolean` | `false` | 否 |
| `switchTab` | 切 Tab 页（E14 语义——push({...to, switchTab:true})） | `Boolean` | `false` | 否 |
| `target` | 跳转目标：self（当前小程序，默认）/ miniProgram（其它小程序） | `String` | `'self'` | 否 |
| `url` | 跳转链接（url） | `String` | `''` | 否 |
| `openType` | 跳转方式：navigate/redirect/switchTab/reLaunch/navigateBack/exit | `String` | `'navigate'` | 否 |
| `delta` | open-type=navigateBack 时回退层数 | `Number` | `1` | 否 |
| `appId` | target=miniProgram 时的目标 appId | `String` | `''` | 否 |
| `path` | target=miniProgram 时的目标路径 | `String` | `''` | 否 |
| `extraData` | target=miniProgram 时传递给目标小程序的参数（★default 不用函数——微信 properties.value 仅支持字面量） | `Object` | `null` | 否 |
| `version` | target=miniProgram 时目标小程序版本：release/trial/develop | `String` | `'release'` | 否 |
| `shortLink` | 目标小程序短链（可不传 appId） | `String` | `''` | 否 |
| `hoverClass` | 按下样式类（'none' 关闭按压态；缺省用框架默认类） | `String` | `''` | 否 |
| `hoverStopPropagation` | 是否阻止祖先节点出现按压态 | `Boolean` | `false` | 否 |
| `hoverStartTime` | 按住多久出现按压态（ms） | `Number` | `50` | 否 |
| `hoverStayTime` | 松开后按压态保留时间（ms） | `Number` | `400` | 否 |

### 属性详解

#### `to`

- **类型**：`String`　**默认值**：`''`　**必填**：否
- **说明**：导航目标（路由名或路径）——createRouterEngineering.push({ name: to \| path: to })

#### `replace`

- **类型**：`Boolean`　**默认值**：`false`　**必填**：否
- **说明**：替换当前页（E12 语义——push({...to, replace:true})）

#### `switchTab`

- **类型**：`Boolean`　**默认值**：`false`　**必填**：否
- **说明**：切 Tab 页（E14 语义——push({...to, switchTab:true})）

#### `target`

- **类型**：`String`　**默认值**：`'self'`　**必填**：否
- **说明**：跳转目标：self（当前小程序，默认）/ miniProgram（其它小程序）

#### `url`

- **类型**：`String`　**默认值**：`''`　**必填**：否
- **说明**：跳转链接（url）

#### `openType`

- **类型**：`String`　**默认值**：`'navigate'`　**必填**：否
- **说明**：跳转方式：navigate/redirect/switchTab/reLaunch/navigateBack/exit

#### `delta`

- **类型**：`Number`　**默认值**：`1`　**必填**：否
- **说明**：open-type=navigateBack 时回退层数

#### `appId`

- **类型**：`String`　**默认值**：`''`　**必填**：否
- **说明**：target=miniProgram 时的目标 appId

#### `path`

- **类型**：`String`　**默认值**：`''`　**必填**：否
- **说明**：target=miniProgram 时的目标路径

#### `extraData`

- **类型**：`Object`　**默认值**：`null`　**必填**：否
- **说明**：target=miniProgram 时传递给目标小程序的参数（★default 不用函数——微信 properties.value 仅支持字面量）

#### `version`

- **类型**：`String`　**默认值**：`'release'`　**必填**：否
- **说明**：target=miniProgram 时目标小程序版本：release/trial/develop

#### `shortLink`

- **类型**：`String`　**默认值**：`''`　**必填**：否
- **说明**：目标小程序短链（可不传 appId）

#### `hoverClass`

- **类型**：`String`　**默认值**：`''`　**必填**：否
- **说明**：按下样式类（'none' 关闭按压态；缺省用框架默认类）

#### `hoverStopPropagation`

- **类型**：`Boolean`　**默认值**：`false`　**必填**：否
- **说明**：是否阻止祖先节点出现按压态

#### `hoverStartTime`

- **类型**：`Number`　**默认值**：`50`　**必填**：否
- **说明**：按住多久出现按压态（ms）

#### `hoverStayTime`

- **类型**：`Number`　**默认值**：`400`　**必填**：否
- **说明**：松开后按压态保留时间（ms）

## Events

| 事件 | 说明 | 载荷 |
|---|---|---|
| `navigate` | — | — |

### 事件详解

#### `navigate`

- **说明**：—
- **载荷**：无

## 插槽

| 插槽 | 说明 |
|---|---|
| default | 默认插槽（组件主内容） |

## 实现要点

- to：导航目标（路由名或路径——createRouterEngineering.push({ name|path }) 语义，E11）
- replace：替换当前页（E12 语义）
- switchTab：切 Tab 页（E14 语义）
- 行为：点击 emit('navigate', { to, replace, switchTab })——父级用 createRouterEngineering（#320）响应；
- 组件零平台依赖（不 import router、不碰 wx/document——审计合规）；web role="link" 可访问性
- MP：@click → bindtap；对齐 p-radio defineEmits + emit 既有链路
- ★2026-09-14 Web 修复：根节点由 `<div>` 改 **`<view>`**（Web 插件改写 proteus-view——hover-* 按压反馈
- 经模拟层生效；MP 端同为 view，产物不变）。

## 用法

```vue
<p-router-link :to="'…'" :replace="true" :switchTab="true">
  <p-text>内容</p-text>
</p-router-link>
```

<!-- generated by website/scripts/gen-content.mjs · 源码 SSOT：packages/components/p-router-link/index.vue -->