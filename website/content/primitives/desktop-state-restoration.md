---
title: p-state-restoration
order: 29
group: 桌面原语
---

# p-state-restoration

> 来源模块 `@proteus-vue/desktop`（Pure logic + Web 接线——env 注入可单测，缺省回落真实全局）。平台映射 / 降级链见模块头原文。

**★G-24 B4（proteus-semantic-primitives-plan 01 §8 p-state-restoration）：状态恢复纯逻辑（iOS UIStateRestoration / SavedStateHandle 语义）**
竞品几乎无人映射的 iOS 核心能力——Proteus 语义化（01 §8）
· buildRestoreToken(state)：JSON 稳定序列化（恢复令牌）
· captureState / restoreState：注入 storage 读写（默认 localStorage——typeof 守卫；测试注入 Map）
· filterRestorable(state, allowKeys)：白名单过滤（敏感字段不入恢复态——安全默认）
纯函数 + storage 注入可单测；Web 载体 localStorage/sessionStorage（桌面/刷新恢复）

## 核心导出（SSOT：`packages/desktop/src/state-restoration.ts`）

| 导出 | 形态 | 一句话（源码注释） |
|---|---|---|
| `RestoreStorage` | interface | 纯函数 + storage 注入可单测；Web 载体 localStorage/sessionStorage（桌面/刷新恢复） |
| `restoreKey` | function | 命名空间化存储键：'proteus-restore:page:scroll' |
| `buildRestoreToken` | function | ★buildRestoreToken：状态 → 恢复令牌（JSON 序列化；undefined/函数字段剔除——可序列化白名单） |
| `filterRestorable` | function | ★filterRestorable：白名单过滤（敏感字段不入恢复态——安全默认） |
| `captureState` | function | ★captureState：序列化 + 持久化（storage 缺（非浏览器）→ 仅返回 token——诚实降级） |
| `restoreState` | function | ★restoreState：读取 + 反序列化（无记录/解析失败 → null——不抛） |
| `clearRestoreState` | function | 清除恢复态 |

## 真实用法（dogfooding 出处——官网自身/示例工程在跑，非示意图）

```ts
const token = captureState('demo', 'view', { path, ts: Date.now() })
```
> 出处：`examples/pages/semantic-primitives-demo.vue:495`

```ts
restoreState('demo', 'view')
```
> 出处：`examples/pages/semantic-primitives-demo.vue:499`

## 用法与降级

- 纯逻辑函数：env 注入测试、浏览器缺省回落（`typeof` 守卫——封装只在框架包内，页面零裸平台 API）
- 指令/组件形态：经 `createDesktopDirectives()` 注册的 `v-p-*`（MP 端不注册天然降级）
- 官网 dogfooding 用法见[质量门禁](/docs/29-quality-gates) 违规速查与[桌面端原语](/docs/30-desktop-primitives)；G-24 系列示例见 `examples/pages/semantic-primitives-demo.vue`

<!-- generated by website/scripts/gen-primitives.mjs · SSOT：packages/desktop/src -->