---
title: 所有权工程原语（PSS：useOwned/useBorrow…）
order: 82
group: 工程原语
---

# 所有权工程原语（PSS：useOwned/useBorrow…）

> 来源模块 `@proteus-vue/api`（工程原语工厂——**注入式**：消费方注入 reactivity/driver/routerLike 等，api 包零 vue 依赖；MP 产物安全子集：无 `?.`/`??`/数组解构）。

**★G-43 B5（proteus-ownership-plan batches B5）：Vue 响应式集成——useOwned / useBorrow（borrow-checker.md §6）**
· CMP071：Owned<T> 默认禁止 ref/reactive 包装（Proxy 破坏所有权语义）——useOwned 提供响应式**元信息**替代
· useOwned(owned)：{ state: Ref<OwnedState>, byteSize: Ref<number>, borrow() }——不暴露实际资源引用
· useBorrow(owned)：Ref<BorrowLike | undefined>——所有者释放时自动变为 undefined（Owned.subscribe 驱动）
injectable 设计族（api 包零运行时依赖 vue，零硬依赖 render-backend——结构类型 OwnedLike/BorrowLike，
对齐 createEngineering/RouterLike 先例：消费方注入 reactivity，实例直接可传）
MP 产物安全（决策 #32/#36）：无 ?. / ??；无数组解构
* 响应式底座（结构复用 @proteus-vue/api Reactivity——只依赖 ref） */

## 核心导出（SSOT：`packages/api/src/ownership-engineering.ts`）

| 导出 | 形态 | 一句话（源码注释） |
|---|---|---|
| `OwnershipReactivity` | interface | 响应式底座（结构复用 @proteus-vue/api Reactivity——只依赖 ref） |
| `OwnedStateLike` | type | Owned 状态（与 render-backend OwnedState 同构） |
| `OwnedLike` | interface | Owned 结构类型（render-backend Owned 实例直接可传——零硬依赖） |
| `BorrowLike` | interface | Borrow 结构类型（render-backend Borrow 实例直接可传） |
| `OwnedView` | interface | 响应式所有权视图（只读元信息——borrow-checker.md §6.3 接口） |
| `OwnershipEngineering` | interface | — |
| `OwnershipEngineeringOptions` | interface | — |
| `createOwnershipEngineering` | function | ★G-43 B5：创建所有权响应式 Hook（useOwned/useBorrow——注入式零 vue 依赖） |

## 用法与降级

- **工厂注入式**：`createXxxEngineering({ reactivity, driver, routerLike… }) → 实例`——消费方注入 reactivity（api 包零 vue 依赖），实例方法即 E 系原语
- 组件形态（如 E20 p-animate / E18 p-router-link）编译期进 C-IR；注入式 Hook（E1-E28/R1-R4）运行时按注入面接线
- 真实示例：`examples/pages/platform-api-demo.vue`（E 系/R 系工厂全调用，见「真实用法」出处）

<!-- generated by website/scripts/gen-primitives.mjs · SSOT：packages/api/src -->