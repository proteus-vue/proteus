---
title: 工具工程原语（E24-E28：useDevTools/defineComponent…）
order: 85
group: 工程原语
---

# 工具工程原语（E24-E28：useDevTools/defineComponent…）

> 来源模块 `@proteus-vue/api`（工程原语工厂——**注入式**：消费方注入 reactivity/driver/routerLike 等，api 包零 vue 依赖；MP 产物安全子集：无 `?.`/`??`/数组解构）。

**★G-32 B5 续三（proteus-semantic-primitives-plus-plan §8 ④）：E24-E28 工程化语义面——injectable 设计**
E24 useDevTools（开发工具接入——dev 事件面）/ E25 useInspector（元素审查——组件树快照）
E26 usePerformance（性能埋点——wx.reportPerformance 语义）/ E27 defineComponent（类型化组件定义含 C-IR 元信息）
E28 defineCapability（能力降级声明——G-30 降级链解析）
注入式：reactivity（vue 或 mock）+ 各句柄可注入时间源/上报器/探测函数——同 createEngineering 族零运行时依赖 vue
MP 产物安全（决策 #32/#36）：无 ?. / ??；无数组解构

## 核心导出（SSOT：`packages/api/src/tooling-engineering.ts`）

| 导出 | 形态 | 一句话（源码注释） |
|---|---|---|
| `DevToolsEvent` | interface | dev 事件（devtools 面板消费；enabled 关闭时不发射） |
| `DevToolsHandle` | interface | E24 useDevTools 句柄 |
| `UseDevToolsOptions` | interface | — |
| `InspectorNode` | interface | 组件审查节点（snapshot 产物） |
| `InspectorHandle` | interface | E25 useInspector 句柄 |
| `PerformanceMetricRecord` | interface | 上报的指标记录（响应式队列——埋点面板消费） |
| `PerformanceHandle` | interface | E26 usePerformance 句柄 |
| `UsePerformanceOptions` | interface | — |
| `ComponentPropDef` | interface | 组件 prop 描述（类型化声明——编译器静态提取 + MP properties 对齐） |
| `ComponentMeta` | interface | 组件定义（C-IR 元信息：semantic 供 toComponentIR/渲染后端消费） |
| `validateComponentMeta` | function | 纯函数：校验组件定义（返回错误列表；空 = 合法）——E27 声明期验证，编译器/开发期消费 |
| `defineComponent` | function | / |
| `CapabilityContract` | interface | 能力合同（G-30 降级链声明） |
| `CapabilityAvailability` | interface | 实现可用性表（resolveCapabilityChain 入参） |
| `CapabilityDefinition` | interface | E28 defineCapability 句柄 |
| `validateCapabilityContract` | function | 纯函数：校验能力合同（返回错误列表；空 = 合法） |
| `resolveCapabilityChain` | function | / |
| `DefineCapabilityOptions` | interface | — |
| `defineCapability` | function | / |
| `ToolingEngineeringOptions` | interface | createToolingEngineering 注入项 |
| `ToolingEngineering` | interface | G-32 §8 ④ 工程化语义（E24-E28） |
| `createToolingEngineering` | function | / |

## 真实用法（dogfooding 出处——官网自身/示例工程在跑，非示意图）

```ts
const tool = createToolingEngineering({ reactivity: { ref, computed, watch } })
```
> 出处：`examples/pages/platform-api-demo.vue:631`

## 用法与降级

- **工厂注入式**：`createXxxEngineering({ reactivity, driver, routerLike… }) → 实例`——消费方注入 reactivity（api 包零 vue 依赖），实例方法即 E 系原语
- 组件形态（如 E20 p-animate / E18 p-router-link）编译期进 C-IR；注入式 Hook（E1-E28/R1-R4）运行时按注入面接线
- 真实示例：`examples/pages/platform-api-demo.vue`（E 系/R 系工厂全调用，见「真实用法」出处）

<!-- generated by website/scripts/gen-primitives.mjs · SSOT：packages/api/src -->