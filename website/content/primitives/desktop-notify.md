---
title: p-notify
order: 24
group: 桌面原语
---

# p-notify

> 来源模块 `@proteus-vue/desktop`（Pure logic + Web 接线——env 注入可单测，缺省回落真实全局）。平台映射 / 降级链见模块头原文。

**★G-24 B2（proteus-semantic-primitives-plan 04-system-integration §1 p-notify）：通知纯逻辑**
· notifySupported / getNotifyPermission / requestNotifyPermission：Notification API 探测与授权归一
· sendNotification(payload, env)：发送（env.Notification 注入——真实 web Notification 构造 / 测试 mock）
映射：iOS UNUserNotificationCenter / Android NotificationManager / 鸿蒙 notificationManager / Web Notification API（04 §1）
纯逻辑零 DOM 直调（env 注入——审计 no-platform-api 安全）；缺 Notification → Err（G-32.3 非抛同步异常风格——返回结果对象）

## 核心导出（SSOT：`packages/desktop/src/notify.ts`）

| 导出 | 形态 | 一句话（源码注释） |
|---|---|---|
| `NotifyPayload` | interface | 纯逻辑零 DOM 直调（env 注入——审计 no-platform-api 安全）；缺 Notification → Err（G-32.3 非抛同步异常风格——返回结果对象） |
| `NotifyResult` | interface | — |
| `NotificationLike` | interface | web Notification 形态（注入面——真实实现为全局 Notification） |
| `NotificationCtor` | interface | — |
| `NotifyEnv` | interface | — |
| `notifySupported` | function | Notification API 可用性（含权限字段——浏览器不支持 → undefined） |
| `getNotifyPermission` | function | 当前通知权限（granted/denied/default/unsupported——PermissionState 语义兼容） |
| `requestNotifyPermission` | function | ★requestNotifyPermission：请求通知授权（Notification.requestPermission 归一） |
| `sendNotification` | function | ★sendNotification：发送系统通知（new Notification——web 真实呈现） |

## 真实用法（dogfooding 出处——官网自身/示例工程在跑，非示意图）

```ts
const r = sendNotification({ title: 'Proteus 演示', body: '系统通知' })
```
> 出处：`examples/pages/semantic-primitives-demo.vue:371`

## 用法与降级

- 纯逻辑函数：env 注入测试、浏览器缺省回落（`typeof` 守卫——封装只在框架包内，页面零裸平台 API）
- 指令/组件形态：经 `createDesktopDirectives()` 注册的 `v-p-*`（MP 端不注册天然降级）
- 官网 dogfooding 用法见[质量门禁](/docs/29-quality-gates) 违规速查与[桌面端原语](/docs/30-desktop-primitives)；G-24 系列示例见 `examples/pages/semantic-primitives-demo.vue`

<!-- generated by website/scripts/gen-primitives.mjs · SSOT：packages/desktop/src -->