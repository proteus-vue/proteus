---
title: 基础工程原语（E1-E9：useState/useComputed/useWatch/useLifecycle…）
order: 81
group: 工程原语
---

# 基础工程原语（E1-E9：useState/useComputed/useWatch/useLifecycle…）

> 来源模块 `@proteus-vue/api`（工程原语工厂——**注入式**：消费方注入 reactivity/driver/routerLike 等，api 包零 vue 依赖；MP 产物安全子集：无 `?.`/`??`/数组解构）。

**★G-32 B5（proteus-semantic-primitives-plus-plan §8）：工程原语 28——injectable 设计（api 包零运行时依赖 vue）**
E1 useState / E2 useComputed / E3 useWatch / E6 useLifecycle / E7 useReady / E9 usePageParam
注入式：消费方注入 reactivity（vue reactive 工厂或自定义）——同 createReactiveStorage 零依赖先例（决策 #310）
MP 产物安全（决策 #32/#36）：无 ?. / ??；无数组解构
* 注入式响应式（vue composables 语义面——useState/useComputed/useWatch 的底座） */

## 核心导出（SSOT：`packages/api/src/engineering.ts`）

| 导出 | 形态 | 一句话（源码注释） |
|---|---|---|
| `Reactivity` | interface | 注入式响应式（vue composables 语义面——useState/useComputed/useWatch 的底座） |
| `LifecycleEvent` | type | 生命周期事件（页面/组件） |
| `LifecycleHandle` | interface | 生命周期钩子句柄（useLifecycle / useReady） |
| `ParamSource` | type | 页面参数源（wx onLoad(options) / web URL query——代码侧注入） |
| `EngineeringOptions` | interface | 工程原语注入项 |
| `Engineering` | interface | G-32 §8 ⑥ 工程原语语义面（E1-E9 首期） |
| `createEngineering` | function | / |

## 真实用法（dogfooding 出处——官网自身/示例工程在跑，非示意图）

```ts
const eng = createEngineering({ reactivity: { ref, computed, watch } })
const engCount = eng.useState(0)
```
> 出处：`examples/pages/platform-api-demo.vue:483·487`

## 用法与降级

- **工厂注入式**：`createXxxEngineering({ reactivity, driver, routerLike… }) → 实例`——消费方注入 reactivity（api 包零 vue 依赖），实例方法即 E 系原语
- 组件形态（如 E20 p-animate / E18 p-router-link）编译期进 C-IR；注入式 Hook（E1-E28/R1-R4）运行时按注入面接线
- 真实示例：`examples/pages/platform-api-demo.vue`（E 系/R 系工厂全调用，见「真实用法」出处）

<!-- generated by website/scripts/gen-primitives.mjs · SSOT：packages/api/src -->