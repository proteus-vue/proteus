---
title: useWifi（capability.wifi）
group: 其他
order: 9005
---

# useWifi

★C55 useWifi：WiFi 句柄（wx.getConnectedWifi/getWifiList/connectWifi；web 无标准 → Err）

> 能力原语 C55 · `capability.wifi` · 返回 `WifiAPI` · **Hook 已实现**（API 就绪，双端桥见下表）

## 签名

```ts
useWifi(): CapResult<WifiAPI>
```

## 返回值

`Promise<CapResult<T>>`——铁律：无回调、无 try/catch 义务，`res.ok` 分支处理：

| 属性 | 类型 | 说明 |
|---|---|---|
| `ok` | `boolean` | 成功 `true` / 失败 `false` |
| `data` | `WifiAPI` | 成功载荷（方法结构见下） |
| `error` | `CapError` | 失败时存在：`code`（机器码）/ `message`（人读原因）/ `cause`（原始异常） |

## 方法

| 方法 | 签名 | 说明 |
|---|---|---|
| [`getConnected`](#getconnected) | `getConnected(): Promise<CapResult<WifiInfo>>` | 当前连接的 WiFi（wx.getConnectedWifi） |
| [`list`](#list) | `list(): Promise<CapResult<WifiInfo[]>>` | 已扫描到的 WiFi 列表（需 wx.startWifi + onGetWifiList 授权；wx.getWifiList → onGetWifiList） |
| [`connect`](#connect) | `connect(SSID: string, password?: string): Promise<CapResult<void>>` | 连接指定 WiFi（wx.connectWifi——Android 支持；iOS/部分基础库 → Err） |

### `getConnected`

```ts
getConnected(): Promise<CapResult<WifiInfo>>
```

**说明**：当前连接的 WiFi（wx.getConnectedWifi）

**返回值**：`Promise<CapResult<WifiInfo>>`

### `list`

```ts
list(): Promise<CapResult<WifiInfo[]>>
```

**说明**：已扫描到的 WiFi 列表（需 wx.startWifi + onGetWifiList 授权；wx.getWifiList → onGetWifiList）

**返回值**：`Promise<CapResult<WifiInfo[]>>`

### `connect`

```ts
connect(SSID: string, password?: string): Promise<CapResult<void>>
```

**说明**：连接指定 WiFi（wx.connectWifi——Android 支持；iOS/部分基础库 → Err）

| 参数 | 类型 | 必填 | 说明 |
|---|---|---|---|
| `SSID` | `string` | 是 | 字符串参数 |
| `password` | `string` | 否 | 字符串参数 |

**返回值**：`Promise<CapResult<void>>`

## 类型引用

### `WifiInfo`

★颗粒度对齐 C3：C55 WiFi（wx.getConnectedWifi / getWifiList / connectWifi——Android 连接）

| 属性 | 类型 | 默认值 | 说明 |
|---|---|---|---|
| `SSID` | `string` | — | — |
| `BSSID` | `string` | — | — |
| `secure` | `boolean` | — | — |
| `signalStrength` | `number` | — | — |
| `frequency` | `number` | — | 频率 MHz（5G/2.4G 区分；部分平台缺失） |

## 错误码

| code | 说明 |
|---|---|
| `wifi.unsupported` | 桥未提供 getWifi（useWifi 不可用） |

> 平台不支持 → `*.unsupported` 族；业务按 code 分支处理，无需 try/catch。

## 兼容进度

| 端 | 兼容 | 说明 |
|---|---|---|
| Web SPA | ⚠️ | vue-dom · webBridge 未提供 getWifi → Err 显式降级（平台无直通 API） |
| 微信小程序 | ✅ | skyline（WebView 降级） · wx 桥 → wx.getConnectedWifi/getWifiList/connectWifi |
| Headless（SSR / 测试） | ✅ | headless · mock 桥注入（测试 / SSR 档） |
| iOS 原生 | 🟡 | native-ios（UIKit） · 端原型映射——能力桥未接线 |
| Android 原生 | 🟡 | native-android（Jetpack） · 端原型映射——能力桥未接线 |
| 鸿蒙 | 🟡 | native-harmony（ArkUI） · 端原型映射——能力桥未接线 |
| Flutter 混合 | 🟡 | flutter · 同一 JS 逻辑层——能力桥未接线 |
| 快应用 | ⬜ | 快应用引擎（待定） · 端未开始 |

> 状态口径：✅ 端已落地·本能力可用；⚠️ 端已落地·桥未提供→Err 显式降级；🟡 端原型映射·能力桥未接线；⬜ 端未开始。端架构对照见 [端与成熟度](/docs/framework/ends-matrix)。

> 铁律：能力原语全部返回 `Result<T>`（无回调 / 无全局对象）；平台不支持 → `Err` 显式降级，业务零平台分支。

## 用法

```ts
const res = await useWifi()

if (res.ok) {
  console.log(res.data)
} else if (res.error.code.endsWith('.unsupported')) {
  // 平台不支持 → 降级路径
}
```

<!-- generated by website/scripts/gen-content.mjs · 源码 SSOT：packages/component-ir/src/primitives.ts + packages/api/src/capability.ts -->