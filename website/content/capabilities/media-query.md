---
title: useMediaQuery（capability.media-query）
group: 设备与系统
order: 1013
---

# useMediaQuery

★C60 useMediaQuery：媒体查询句柄（wx.createMediaQueryObserver / web matchMedia）

> 能力原语 C60 · `capability.media-query` · 返回 `MediaQueryObserver` · **Hook 已实现**（API 就绪，双端桥见下表）

## 签名

```ts
useMediaQuery(): CapResult<MediaQueryObserver>
```

## 返回值

`Promise<CapResult<T>>`——铁律：无回调、无 try/catch 义务，`res.ok` 分支处理：

| 属性 | 类型 | 说明 |
|---|---|---|
| `ok` | `boolean` | 成功 `true` / 失败 `false` |
| `data` | `MediaQueryObserver` | 成功载荷（方法结构见下） |
| `error` | `CapError` | 失败时存在：`code`（机器码）/ `message`（人读原因）/ `cause`（原始异常） |

## 方法

| 方法 | 签名 | 说明 |
|---|---|---|
| [`observe`](#observe) | `observe(condition: MediaQueryCondition, cb: (result: MediaQueryResult) => void): void` | 开始观察媒体查询条件。 |
| [`disconnect`](#disconnect) | `disconnect(): void` | 停止观察（释放） |

### `observe`

```ts
observe(condition: MediaQueryCondition, cb: (result: MediaQueryResult) => void): void
```

**说明**：开始观察媒体查询条件。

| 参数 | 类型 | 必填 | 说明 |
|---|---|---|---|
| `condition` | `MediaQueryCondition` | 是 | 条件（宽高范围 / 方向） |
| `cb` | `(result: MediaQueryResult) => void` | 是 | 匹配状态变化回调 |

**返回值**：`void`

### `disconnect`

```ts
disconnect(): void
```

**说明**：停止观察（释放）

**返回值**：`void`

## 类型引用

### `MediaQueryCondition`

媒体查询条件（wx MediaQueryObserver.observe options——对齐官方字段）

| 属性 | 类型 | 默认值 | 说明 |
|---|---|---|---|
| `minWidth` | `number` | — | 最小宽度（px） |
| `maxWidth` | `number` | — | 最大宽度（px） |
| `width` | `number` | — | 宽度（px） |
| `minHeight` | `number` | — | 最小高度（px） |
| `maxHeight` | `number` | — | 最大高度（px） |
| `height` | `number` | — | 高度（px） |
| `orientation` | `'landscape' \| 'portrait'` | — | 屏幕方向 |

### `MediaQueryResult`

媒体查询结果

| 属性 | 类型 | 默认值 | 说明 |
|---|---|---|---|
| `matches` | `boolean` | — | 是否匹配当前条件 |

## 错误码

| code | 说明 |
|---|---|
| `element.unsupported` | 桥未提供 createMediaQuery（useMediaQuery 不可用） |

> 平台不支持 → `*.unsupported` 族；业务按 code 分支处理，无需 try/catch。

## 兼容进度

| 端 | 兼容 | 说明 |
|---|---|---|
| Web SPA | ✅ | vue-dom · webBridge 实现（平台 API 直连） |
| 微信小程序 | ✅ | skyline（WebView 降级） · wx 桥 → wx.createMediaQueryObserver |
| Headless（SSR / 测试） | ✅ | headless · mock 桥注入（测试 / SSR 档） |
| iOS 原生 | 🟡 | native-ios（UIKit） · 端原型映射——能力桥未接线 |
| Android 原生 | 🟡 | native-android（Jetpack） · 端原型映射——能力桥未接线 |
| 鸿蒙 | 🟡 | native-harmony（ArkUI） · 端原型映射——能力桥未接线 |
| Flutter 混合 | 🟡 | flutter · 同一 JS 逻辑层——能力桥未接线 |
| 快应用 | ⬜ | 快应用引擎（待定） · 端未开始 |

> 状态口径：✅ 端已落地·本能力可用；⚠️ 端已落地·桥未提供→Err 显式降级；🟡 端原型映射·能力桥未接线；⬜ 端未开始。端架构对照见 [端与成熟度](/docs/framework/ends-matrix)。

> 铁律：能力原语全部返回 `Result<T>`（无回调 / 无全局对象）；平台不支持 → `Err` 显式降级，业务零平台分支。

## 用法

```ts
const mq = useMediaQuery() // 同步句柄——无 await、无 res.ok

if (mq.ok) {
  mq.data.observe({ minWidth: 600, orientation: 'landscape' }, (res) => {
    console.log('宽屏:', res.matches)
  })
  // mq.data.disconnect() 停止观察
} else if (mq.error.code === 'element.unsupported') {
  // 桥未提供媒体查询 API → 降级路径
}
```

<!-- generated by website/scripts/gen-content.mjs · 源码 SSOT：packages/component-ir/src/primitives.ts + packages/api/src/capability.ts -->