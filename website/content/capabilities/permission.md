---
title: usePermission（capability.permission）
---

# usePermission

useFetch：网络请求（成功 .data = 载荷——migration.md `const { data } = await useFetch(url)` 解构兼容） */ useFetch<T = unknown>(url: string, config?: FetchConfig): Promise<CapResult<T>> /** C16 usePermission：权限状态（web Permissions API）

> 能力原语 C16 · `capability.permission` · 返回 `Result<PermissionStatus>` · **Hook 已实现**（API 就绪，双端桥见下表）

## 签名

```ts
usePermission(name: string): Promise<CapResult<PermissionState>>
```

## 平台等价

| 端 | 等价物 |
|---|---|
| 小程序 | wx.authorize |
| Web | 平台桥（wx 缺席时 webBridge 实现；不支持 → `Err('capability.permission.unsupported')`） |

> 铁律：能力原语全部返回 `Result<T>`（无回调 / 无全局对象）；平台不支持 → `Err` 显式降级，业务零平台分支。

## 用法

```ts
const res = await usePermission()

if (res.ok) {
  console.log(res.data)
} else if (res.error.code.endsWith('.unsupported')) {
  // 平台不支持 → 降级路径
}
```

<!-- generated by website/scripts/gen-content.mjs · 源码 SSOT：packages/component-ir/src/primitives.ts + packages/api/src/capability.ts -->