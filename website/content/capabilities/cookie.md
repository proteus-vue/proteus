---
title: useCookie（capability.cookie）
---

# useCookie

useCookie：Cookie 罐（web document.cookie / wx storage 兜底）

> 能力原语 C32 · `capability.cookie` · 返回 `CookieJar` · **Hook 已实现**（API 就绪，双端桥见下表）

## 签名

```ts
useCookie(): Promise<CapResult<CookieJar>>
```

## 平台等价

| 端 | 等价物 |
|---|---|
| 小程序 | 无 |
| Web | 平台桥（wx 缺席时 webBridge 实现；不支持 → `Err('capability.cookie.unsupported')`） |

> 铁律：能力原语全部返回 `Result<T>`（无回调 / 无全局对象）；平台不支持 → `Err` 显式降级，业务零平台分支。

## 用法

```ts
const res = await useCookie()

if (res.ok) {
  console.log(res.data)
} else if (res.error.code.endsWith('.unsupported')) {
  // 平台不支持 → 降级路径
}
```

<!-- generated by website/scripts/gen-content.mjs · 源码 SSOT：packages/component-ir/src/primitives.ts + packages/api/src/capability.ts -->