---
title: usePreload（capability.preload）
group: 应用与生命周期
order: 7007
---

# usePreload

★C67 usePreload：预加载句柄（wx.preloadAssets/preloadSkylineView/preloadWebview/preDownloadSubpackage；web 缺省 → Err）

> 能力原语 C67 · `capability.preload` · 返回 `PreloadAPI` · **Hook 已实现**（API 就绪，双端桥见下表）

## 签名

```ts
usePreload(): CapResult<PreloadAPI>
```

## 返回值

`Promise<CapResult<T>>`——铁律：无回调、无 try/catch 义务，`res.ok` 分支处理：

| 属性 | 类型 | 说明 |
|---|---|---|
| `ok` | `boolean` | 成功 `true` / 失败 `false` |
| `data` | `PreloadAPI` | 成功载荷（方法结构见下） |
| `error` | `CapError` | 失败时存在：`code`（机器码）/ `message`（人读原因）/ `cause`（原始异常） |

## 方法

| 方法 | 签名 | 说明 |
|---|---|---|
| [`assets`](#assets) | `assets(data: PreloadAsset[]): Promise<CapResult<void>>` | 预加载资源（字体 / 图片）。 |
| [`skylineView`](#skylineview) | `skylineView(): Promise<CapResult<void>>` | 预加载 Skyline 视图（配合 Skyline 渲染器的页面预载） |
| [`webview`](#webview) | `webview(): Promise<CapResult<void>>` | 预加载 WebView 页面 |
| [`subpackage`](#subpackage) | `subpackage(packageType: string): Promise<CapResult<PreDownloadTask>>` | 预下载分包（目前仅 workers 分包）。 |

### `assets`

```ts
assets(data: PreloadAsset[]): Promise<CapResult<void>>
```

**说明**：预加载资源（字体 / 图片）。

| 参数 | 类型 | 必填 | 说明 |
|---|---|---|---|
| `data` | `PreloadAsset[]` | 是 | 资源列表 |

**返回值**：`Promise<CapResult<void>>`

### `skylineView`

```ts
skylineView(): Promise<CapResult<void>>
```

**说明**：预加载 Skyline 视图（配合 Skyline 渲染器的页面预载）

**返回值**：`Promise<CapResult<void>>`

### `webview`

```ts
webview(): Promise<CapResult<void>>
```

**说明**：预加载 WebView 页面

**返回值**：`Promise<CapResult<void>>`

### `subpackage`

```ts
subpackage(packageType: string): Promise<CapResult<PreDownloadTask>>
```

**说明**：预下载分包（目前仅 workers 分包）。

| 参数 | 类型 | 必填 | 说明 |
|---|---|---|---|
| `packageType` | `string` | 是 | 分包类型（'workers'） |

**返回值**：`Promise<CapResult<PreDownloadTask>>`

## 类型引用

### `PreloadAsset`

预加载资源项（wx.preloadAssets——字体/图片）

| 属性 | 类型 | 默认值 | 说明 |
|---|---|---|---|
| `src` | `string` | — | 资源地址 |
| `type` | `'font' \| 'image'` | — | 资源类型（font 字体 / image 图片） |

### `PreDownloadTask`

分包预下载任务句柄（wx.preDownloadSubpackage 返回）

| 方法 | 签名 | 说明 |
|---|---|---|
| `onProgressUpdate` | `onProgressUpdate(cb: (res: { progress: number; totalBytesWritten: number; totalBytesExpectedToWrite: number }) => void): () => void` | 订阅下载进度。 |

## 错误码

| code | 说明 |
|---|---|
| `preload.unsupported` | 桥未提供 getPreload（usePreload 不可用） |

> 平台不支持 → `*.unsupported` 族；业务按 code 分支处理，无需 try/catch。

## 兼容进度

| 端 | 兼容 | 说明 |
|---|---|---|
| Web SPA | ✅ | vue-dom · webBridge 实现（平台 API 直连） |
| 微信小程序 | ✅ | skyline（WebView 降级） · wx 桥 → wx.preloadAssets/preloadSkylineView/preloadWebview/preDownloadSubpackage |
| Headless（SSR / 测试） | ✅ | headless · mock 桥注入（测试 / SSR 档） |
| iOS 原生 | 🟡 | native-ios（UIKit） · 端原型映射——能力桥未接线 |
| Android 原生 | 🟡 | native-android（Jetpack） · 端原型映射——能力桥未接线 |
| 鸿蒙 | 🟡 | native-harmony（ArkUI） · 端原型映射——能力桥未接线 |
| Flutter 混合 | 🟡 | flutter · 同一 JS 逻辑层——能力桥未接线 |
| 快应用 | ⬜ | 快应用引擎（待定） · 端未开始 |

> 状态口径：✅ 端已落地·本能力可用；⚠️ 端已落地·桥未提供→Err 显式降级；🟡 端原型映射·能力桥未接线；⬜ 端未开始。端架构对照见 [端与成熟度](/docs/framework/ends-matrix)。

> 铁律：能力原语全部返回 `Result<T>`（无回调 / 无全局对象）；平台不支持 → `Err` 显式降级，业务零平台分支。

## 用法

```ts
const pre = usePreload() // 同步句柄——无 await、无 res.ok

if (pre.ok) {
  await pre.data.assets([{ src: "/assets/title.woff", type: "font" }])
  await pre.data.subpackage("workers") // 预下载 workers 分包
  // await pre.data.skylineView() / await pre.data.webview()
} else if (pre.error.code === 'preload.unsupported') {
  // Web 无标准预加载 → 降级路径（<link rel=preload>）
}
```

<!-- generated by website/scripts/gen-content.mjs · 源码 SSOT：packages/component-ir/src/primitives.ts + packages/api/src/capability.ts -->