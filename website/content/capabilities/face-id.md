---
title: useFaceID（capability.face-id）
---

# useFaceID

> 能力原语 C39 · `capability.face-id` · 返回 `Result<boolean>` · **Hook 已实现**（API 就绪，双端桥见下表）

C39 useFaceID：人脸识别认证（wx startSoterAuthentication facial / web WebAuthn）

## 签名

```ts
useFaceID(prompt?: string): Promise<CapResult<boolean>>
```

## 平台等价

| 端 | 等价物 |
|---|---|
| 小程序 | 组合 |
| Web | 平台桥（wx 缺席时 webBridge 实现；不支持 → `Err('capability.face-id.unsupported')`） |

> 铁律：能力原语全部返回 `Result<T>`（无回调 / 无全局对象）；平台不支持 → `Err` 显式降级，业务零平台分支。

## 用法

```ts
const res = await useFaceID()

if (res.ok) {
  console.log(res.data)
} else if (res.error.code.endsWith('.unsupported')) {
  // 平台不支持 → 降级路径
}
```

## 用法

```ts
import { useAppConfig } from '@proteus-vue/api'

const res = await use(FaceID()
// 平台不支持 → Err 显式降级（不用 try/catch 也能处理）
```

<!-- generated by website/scripts/gen-content.mjs · 源码 SSOT：packages/component-ir/src/primitives.ts + packages/api/src/capability.ts -->