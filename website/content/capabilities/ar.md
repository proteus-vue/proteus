---
title: useAR（capability.ar）
group: 设备与系统
order: 1016
---

# useAR

★C76 useAR：AR/XR 视觉算法（wx.createVKSession/isVKSupport；web 无对等 → Err）

> 能力原语 C76 · `capability.ar` · 返回 `ARAPI` · **Hook 已实现**（API 就绪，双端桥见下表）

## 签名

```ts
useAR(): CapResult<ARAPI>
```

## 返回值

`Promise<CapResult<T>>`——铁律：无回调、无 try/catch 义务，`res.ok` 分支处理：

| 属性 | 类型 | 说明 |
|---|---|---|
| `ok` | `boolean` | 成功 `true` / 失败 `false` |
| `data` | `ARAPI` | 成功载荷（方法结构见下） |
| `error` | `CapError` | 失败时存在：`code`（机器码）/ `message`（人读原因）/ `cause`（原始异常） |

## 方法

| 方法 | 签名 | 说明 |
|---|---|---|
| [`createSession`](#createsession) | `createSession(options?: AROptions): ARSessionHandle` | 创建视觉算法会话 |
| [`isSupported`](#issupported) | `isSupported(version?: 'v1' \| 'v2'): Promise<CapResult<boolean>>` | 查询设备是否支持视觉算法。 |

### `createSession`

```ts
createSession(options?: AROptions): ARSessionHandle
```

**说明**：创建视觉算法会话

| 参数 | 类型 | 必填 | 说明 |
|---|---|---|---|
| `options` | `AROptions` | 否 | 配置选项对象 |

**返回值**：`ARSessionHandle`

### `isSupported`

```ts
isSupported(version?: 'v1' | 'v2'): Promise<CapResult<boolean>>
```

**说明**：查询设备是否支持视觉算法。

| 参数 | 类型 | 必填 | 说明 |
|---|---|---|---|
| `version` | `'v1' \| 'v2'` | 否 | 算法版本（v1 旧版 / v2 iOS 2.22.0+） |

**返回值**：`Promise<CapResult<boolean>>`

## 类型引用

### `AROptions`

| 属性 | 类型 | 默认值 | 说明 |
|---|---|---|---|
| `track` | `Record<string, unknown>` | — | 追踪模式（'plane' 平面 / 'marker' 标记 / 'OSD' 图片识别 / 'face' 人脸 / 'hand' 手势 / 'body' 人体 / 'depth' 深度） |
| `version` | `'v1' \| 'v2'` | `v2` | 算法版本（缺省 v2） |
| `gl` | `unknown` | — | WebGL 上下文（渲染锚点用） |

### `ARSessionHandle`

★权威标尺缺口 C76：AR/XR 视觉算法（`useAR()`）。 wx：`wx.createVKSession`（v1/v2 视觉算法会话——平面检测/人脸/手势/深度/OCR/标记）； web：`WebXR` 无对等视觉算法 → Err 诚实降级（isVKSupport 恒 false）。

| 方法 | 签名 | 说明 |
|---|---|---|
| `state` | `state(): VKSessionState` | 会话状态（0 未初始化 / 1 就绪 / 2 运行中 / 3 暂停） |
| `start` | `start(): Promise<CapResult<void>>` | 启动会话 |
| `stop` | `stop(): Promise<CapResult<void>>` | 停止会话 |
| `destroy` | `destroy(): void` | 销毁会话（释放相机/算法资源） |
| `on` | `on(event: string, cb: (payload: unknown) => void): () => void` | 订阅会话事件。 |
| `requestAnimationFrame` | `requestAnimationFrame(cb: (timestamp: number, frame: unknown) => void): number` | 请求下一帧 VK 数据（驱动算法） |
| `cancelAnimationFrame` | `cancelAnimationFrame(handle: number): void` | 取消动画帧 |
| `getVKFrame` | `getVKFrame(width?: number, height?: number): unknown` | 取当前 VK 帧 |

## 错误码

| code | 说明 |
|---|---|
| `ar.unsupported` | 桥未提供 getAR（useAR 不可用） |

> 平台不支持 → `*.unsupported` 族；业务按 code 分支处理，无需 try/catch。

## 兼容进度

| 端 | 兼容 | 说明 |
|---|---|---|
| Web SPA | ✅ | vue-dom · webBridge 实现（平台 API 直连） |
| 微信小程序 | ✅ | skyline（WebView 降级） · wx 桥 → wx.createVKSession/isVKSupport |
| Headless（SSR / 测试） | ✅ | headless · mock 桥注入（测试 / SSR 档） |
| iOS 原生 | 🟡 | native-ios（UIKit） · 端原型映射——能力桥未接线 |
| Android 原生 | 🟡 | native-android（Jetpack） · 端原型映射——能力桥未接线 |
| 鸿蒙 | 🟡 | native-harmony（ArkUI） · 端原型映射——能力桥未接线 |
| Flutter 混合 | 🟡 | flutter · 同一 JS 逻辑层——能力桥未接线 |
| 快应用 | ⬜ | 快应用引擎（待定） · 端未开始 |

> 状态口径：✅ 端已落地·本能力可用；⚠️ 端已落地·桥未提供→Err 显式降级；🟡 端原型映射·能力桥未接线；⬜ 端未开始。端架构对照见 [端与成熟度](/docs/framework/ends-matrix)。

> 铁律：能力原语全部返回 `Result<T>`（无回调 / 无全局对象）；平台不支持 → `Err` 显式降级，业务零平台分支。

## 用法

```ts
const ar = useAR() // 同步句柄——无 await、无 res.ok

if (ar.ok) {
  const supported = await ar.data.isSupported('v2')
  if (supported.ok && supported.data) {
    const session = ar.data.createSession({ track: { plane: {} }, version: 'v2' })
    await session.start()
    session.on('update', (frame) => console.log('AR 帧:', frame))
  }
} else if (ar.error.code === 'ar.unsupported') {
  // 平台不支持视觉算法 → 降级路径
}
```

<!-- generated by website/scripts/gen-content.mjs · 源码 SSOT：packages/component-ir/src/primitives.ts + packages/api/src/capability.ts -->