---
title: useAlbum（capability.album）
group: 其他
order: 9002
---

# useAlbum

★C52 useAlbum：相册句柄（wx.chooseMedia/saveImageToPhotosAlbum/previewMedia；web <input type=file>）

> 能力原语 C52 · `capability.album` · 返回 `AlbumAPI` · **Hook 已实现**（API 就绪，双端桥见下表）

## 签名

```ts
useAlbum(): CapResult<AlbumAPI>
```

## 返回值

`Promise<CapResult<T>>`——铁律：无回调、无 try/catch 义务，`res.ok` 分支处理：

| 属性 | 类型 | 说明 |
|---|---|---|
| `ok` | `boolean` | 成功 `true` / 失败 `false` |
| `data` | `AlbumAPI` | 成功载荷（方法结构见下） |
| `error` | `CapError` | 失败时存在：`code`（机器码）/ `message`（人读原因）/ `cause`（原始异常） |

## 方法

| 方法 | 签名 | 说明 |
|---|---|---|
| [`pick`](#pick) | `pick(options?: AlbumPickOptions): Promise<CapResult<MediaFile[]>>` | 选择图片/视频（wx.chooseMedia；web <input type=file>） |
| [`saveImage`](#saveimage) | `saveImage(filePath: string): Promise<CapResult<void>>` | 保存图片到系统相册（wx.saveImageToPhotosAlbum——需 scope.writePhotosAlbum 授权；web 无标准 → Err） |
| [`saveVideo`](#savevideo) | `saveVideo(filePath: string): Promise<CapResult<void>>` | 保存视频到系统相册（wx.saveVideoToPhotosAlbum；web 无标准 → Err） |
| [`preview`](#preview) | `preview(urls: string[], current?: string): Promise<CapResult<void>>` | 预览媒体（wx.previewImage / previewMedia；web 宿主视图） |

### `pick`

```ts
pick(options?: AlbumPickOptions): Promise<CapResult<MediaFile[]>>
```

**说明**：选择图片/视频（wx.chooseMedia；web <input type=file>）

| 参数 | 类型 | 必填 | 说明 |
|---|---|---|---|
| `options` | `AlbumPickOptions` | 否 | 配置选项对象 |

**返回值**：`Promise<CapResult<MediaFile[]>>`

### `saveImage`

```ts
saveImage(filePath: string): Promise<CapResult<void>>
```

**说明**：保存图片到系统相册（wx.saveImageToPhotosAlbum——需 scope.writePhotosAlbum 授权；web 无标准 → Err）

| 参数 | 类型 | 必填 | 说明 |
|---|---|---|---|
| `filePath` | `string` | 是 | 字符串参数 |

**返回值**：`Promise<CapResult<void>>`

### `saveVideo`

```ts
saveVideo(filePath: string): Promise<CapResult<void>>
```

**说明**：保存视频到系统相册（wx.saveVideoToPhotosAlbum；web 无标准 → Err）

| 参数 | 类型 | 必填 | 说明 |
|---|---|---|---|
| `filePath` | `string` | 是 | 字符串参数 |

**返回值**：`Promise<CapResult<void>>`

### `preview`

```ts
preview(urls: string[], current?: string): Promise<CapResult<void>>
```

**说明**：预览媒体（wx.previewImage / previewMedia；web 宿主视图）

| 参数 | 类型 | 必填 | 说明 |
|---|---|---|---|
| `urls` | `string[]` | 是 | 数组参数 |
| `current` | `string` | 否 | 字符串参数 |

**返回值**：`Promise<CapResult<void>>`

## 类型引用

### `AlbumPickOptions`

| 属性 | 类型 | 默认值 | 说明 |
|---|---|---|---|
| `count` | `number` | `1` | 选择数量上限（默认 1；小程序上限 9） |
| `mediaType` | `'image' \| 'video' \| 'all'` | — | 媒体类型：image（默认）/ video / all |
| `source` | `'album' \| 'camera'` | — | 来源：album 相册（默认）/ camera 拍照 |

### `MediaFile`

★颗粒度对齐 C3：C52 相册（wx.chooseMedia / saveImageToPhotosAlbum / previewImage） 选择媒体 + 保存到系统相册 + 预览——对齐小程序媒体类 API 组。 Web 端：pick 走 <input type=file>（需宿主/用户手势），save 无标准（下载替代）∪ 缺省 Err（诚实降级）。

| 属性 | 类型 | 默认值 | 说明 |
|---|---|---|---|
| `tempFilePath` | `string` | — | 临时文件路径（wx 临时文件 / web Blob URL） |
| `type` | `'image' \| 'video'` | — | 媒体类型 |
| `size` | `number` | — | 字节大小 |
| `duration` | `number` | — | 时长 ms（视频） |
| `width` | `number` | — | — |
| `height` | `number` | — | — |

## 错误码

| code | 说明 |
|---|---|
| `album.unsupported` | 桥未提供 getAlbum（useAlbum 不可用） |

> 平台不支持 → `*.unsupported` 族；业务按 code 分支处理，无需 try/catch。

## 兼容进度

| 端 | 兼容 | 说明 |
|---|---|---|
| Web SPA | ✅ | vue-dom · webBridge 实现（平台 API 直连） |
| 微信小程序 | ✅ | skyline（WebView 降级） · wx 桥 → wx.chooseMedia/saveImageToPhotosAlbum/previewMedia |
| Headless（SSR / 测试） | ✅ | headless · mock 桥注入（测试 / SSR 档） |
| iOS 原生 | 🟡 | native-ios（UIKit） · 端原型映射——能力桥未接线 |
| Android 原生 | 🟡 | native-android（Jetpack） · 端原型映射——能力桥未接线 |
| 鸿蒙 | 🟡 | native-harmony（ArkUI） · 端原型映射——能力桥未接线 |
| Flutter 混合 | 🟡 | flutter · 同一 JS 逻辑层——能力桥未接线 |
| 快应用 | ⬜ | 快应用引擎（待定） · 端未开始 |

> 状态口径：✅ 端已落地·本能力可用；⚠️ 端已落地·桥未提供→Err 显式降级；🟡 端原型映射·能力桥未接线；⬜ 端未开始。端架构对照见 [端与成熟度](/docs/framework/ends-matrix)。

> 铁律：能力原语全部返回 `Result<T>`（无回调 / 无全局对象）；平台不支持 → `Err` 显式降级，业务零平台分支。

## 用法

```ts
const res = await useAlbum()

if (res.ok) {
  console.log(res.data)
} else if (res.error.code.endsWith('.unsupported')) {
  // 平台不支持 → 降级路径
}
```

<!-- generated by website/scripts/gen-content.mjs · 源码 SSOT：packages/component-ir/src/primitives.ts + packages/api/src/capability.ts -->