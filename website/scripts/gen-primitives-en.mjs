// website/scripts/gen-primitives-en.mjs —— 原语分区英文字段（★#482：生成器双语输出，与 zh 同源推导）
//   PRIM_EN[模块名] = { title?, summary, notes[], exports{…}, usage[{code,src}] }——未登记模块 zh-only（EN 态 #noEn 回退）
export const FAMILY_EN = {
  desktop: [
    ['Web SPA', '✅', 'Official wiring: pure logic + env fallback to globals; v-p-* directives (registered via createDesktopDirectives)'],
    ['WeChat Mini Program', '🟡', 'Pure logic unit-testable; directives not registered (desktop interactions have no counterpart — stripped at compile time); page wiring is up to the host'],
    ['Headless (SSR/testing)', '✅', 'Pure logic runs on Node (tooling/testing tier)'],
    ['iOS native', '🟡', 'Mapping planned — official wiring not started (native recognition / system API per the G-24 plan)'],
    ['Android native', '🟡', 'Mapping planned — official wiring not started'],
    ['HarmonyOS', '🟡', 'Mapping planned — official wiring not started'],
    ['Flutter hybrid', '🟡', 'Widget/system mapping not started'],
    ['Quick App', '⬜', 'target not started'],
  ],
  gesture: [
    ['Web SPA', '✅', 'Official wiring: v-gesture directives / useGesture Hook (Pointer Events)'],
    ['WeChat Mini Program', '🟡', 'Recognizer mapping is carried by each target\'s Backend (planned) — pure recognizers unit-testable on the logic layer'],
    ['Headless (SSR/testing)', '✅', 'Recognizer pure logic runs on Node (tooling/testing tier)'],
    ['iOS native', '🟡', 'UIGestureRecognizer mapping planned'],
    ['Android native', '🟡', 'GestureDetector mapping planned'],
    ['HarmonyOS', '🟡', 'Gesture system mapping planned'],
    ['Flutter hybrid', '🟡', 'Gesture mapping not started'],
    ['Quick App', '⬜', 'target not started'],
  ],
  api: [
    ['Web SPA', '✅', 'Official demo wiring (examples/platform-api-demo — all factories called)'],
    ['WeChat Mini Program', '🟡', 'Injectable subset runs on the logic layer (MP artifact-safe); component-form wiring partially rolled out first'],
    ['Headless (SSR/testing)', '✅', 'Inject reactivity etc. on Node to run (tooling/testing tier)'],
    ['iOS native', '🟡', 'Native validation not started (E-series injection surface follows host batches)'],
    ['Android native', '🟡', 'Native validation not started'],
    ['HarmonyOS', '🟡', 'Native validation not started'],
    ['Flutter hybrid', '🟡', 'same JS logic layer — wiring not started'],
    ['Quick App', '⬜', 'target not started'],
  ],
}

export const SHARED_PRIM_EN = {
  sourceCallout: (pkg) =>
    `> Source module \`@proteus-vue/${pkg}\` (pure logic + Web wiring — env-injected for testing, falls back to real globals when absent). Platform mapping / degradation chain → see the module header source.`,
  apiCallout:
    '> Source module `@proteus-vue/api` (engineering primitive factories — **injection-based**: the consumer injects reactivity/driver/routerLike etc., the api package has zero vue dependency; MP artifact-safe subset: no `?.`/`??`/array destructuring).',
  legend:
    '> Status scale: ✅ target shipped & this primitive usable · 🟡 prototype mapping — wiring not started · ⬜ target not started. Family-level mechanism coverage (not a per-target on-device verification matrix); target architecture matrix (engine / runtime / persistence) → [Ends & maturity](/docs/framework/ends-matrix).',
  familyNotes: {
    desktop: [
      '- pure-logic functions: env-injected for testing; browser defaults fall back (`typeof` guards — wrapping lives only inside framework packages, pages keep zero raw platform APIs)',
      '- directive/component forms: `v-p-*` registered via `createDesktopDirectives()` (not registered on MP → degrades naturally)',
      '- official-site dogfooding usage: violation quick-fix table in [Quality gates](/docs/29-quality-gates) and [Desktop primitives](/docs/30-desktop-primitives); G-24 series examples in `examples/pages/semantic-primitives-demo.vue`',
    ],
    gesture: [
      '- recognizers are pure logic with zero dependencies: Web Pointer / MP touch normalized into `GestureInput` → semantic gesture events (tap/pan/swipe/pinch/rotate/longpress…) — unit-testable',
      '- official Web wiring: `useGesture()` Hook and `v-gesture:<kind>="onX"` directives; MP/native mapping carried by each target\'s Backend — "events are a Backend implementation detail"',
      '- real example: `examples/pages/semantic-primitives-demo.vue` (v-gesture:tap)',
    ],
    api: [
      '- **factory injection**: `createXxxEngineering({ reactivity, driver, routerLike… }) → instance` — the consumer injects reactivity (api package has zero vue dependency); instance methods are the E-series primitives',
      '- component forms (e.g. E20 p-animate / E18 p-router-link) enter the C-IR at compile time; injectable Hooks (E1-E28/R1-R4) wire up at runtime per the injected surface',
      '- real example: `examples/pages/platform-api-demo.vue` (E/R factory calls — see the "Real usage" origins)',
    ],
  },
}

/**
 * 模块级 EN 字段：键 = packages/desktop|gesture|api src 的模块 basename（如 hover/shortcut/scroll/window-message/anchor/cursor-glow/engineering…）。
 * title = 页面标题（zh humanName 的英文对应，标识符类照抄）；summary = 页头一句话；notes = 模块头注释块英文逐行；
 * exports = { 导出名: 一句话英文 }（readExports 提取的 doc 对应）；usage = 真实用法 EN（仅当 zh USAGE_MAP 该模块有条目；code 行内注释已译英）。
 */
export const PRIM_EN = {
  hover: {
    title: "p-hover",
    summary: "Hover-state semantics (brighten/lift/underline) — touch auto-degrades to tap highlight",
    notes: [
      "★G-24 B1 (proteus-semantic-primitives-plan 03 §1 p-hover): pointer-hover pure logic",
      "· resolveHoverClass(preset) → 'p-hover-brighten', etc. (CSS class — the render layer defines the transition)",
      "· isHoverPointer(pointerType) → mouse/pen → true; touch → false (p-hover degrades to tap highlight — plan §1)",
      "Pure logic with zero DOM dependency; MP artifact safe: no ?. / ??; no array destructuring",
    ],
    exports: {
      HoverPreset: "Pure logic with zero DOM dependency; MP artifact safe: no ?. / ??; no array destructuring",
      PointerKind: '—',
      resolveHoverClass: "Preset → CSS class name (the render layer's `<style>` defines the transition animation)",
      isHoverPointer: "Pointer classification: mouse/pen support hover (the cursor can hover); touch/remote degrade (plan: iOS compile-time stripping → tap highlight)",
      normalizePointerType: "Event pointerType normalization (Web PointerEvent.pointerType / test injection)",
      canHover: "Hover-state toggle decision (mouseenter/mouseleave semantics — whether the injected pointer can hover)",
    },
    usage: [{ code: '<p-view v-for="p in pillars" v-p-hover class="pillar-card">…</p-view>', src: 'website/src/pages/Home.vue:244' }],
  },
  scroll: {
    title: "Scroll observation primitive (page scroll progress / scroll state)",
    summary: "Page scroll observation: rAF throttle + y/max/progress (the App top progress bar and the Home linkage run on it)",
    notes: [
      "★#449 G-24 B5 (proteus-semantic-primitives-plan follow-up batch): page scroll observation primitive — converging \"scroll progress / scroll state\"",
      "Semantics: subscribes to page scroll → rAF throttle → state callbacks (y / viewport / docHeight / max / progress)",
      "Consumers: App top progress bar (progress) + nav scrolled state, Home hero scroll linkage (y) — pages keep zero raw window/document",
      "Layering: pure logic + Web wiring (env-injected for unit testing; defaults to falling back to real globals — the same convention as the network/lifecycle family)",
    ],
    exports: {
      ScrollState: "Layering: pure logic + Web wiring (env-injected for unit testing; defaults to falling back to real globals — the same convention as the network/lifecycle family)",
      ScrollObserverEnv: '—',
      ScrollObserver: '—',
      readPageScroll: "Real window/document geometry (SSR / no DOM → an all-zero honest state)",
      createScrollObserver: "★createScrollObserver: subscribes to page scroll (rAF-throttled — at most one callback per frame; immediate fires the callback on the first frame)",
    },
    usage: [{ code: 'const scrollObs = createScrollObserver({ immediate: true, onChange: (s) => { progress.value = s.progress } })', src: 'website/src/App.vue:44' }],
  },
  shortcut: {
    title: "p-shortcut",
    summary: "Keyboard-shortcut semantics: mod+s → ⌘S / Ctrl+S (the PRIM005 platform convention); bound actions fire on match",
    notes: [
      "★G-24 B1 (proteus-semantic-primitives-plan 03 §3 p-shortcut): keyboard-shortcut pure logic",
      '· parseShortcutExpr("mod+s:save") → { keys: [\'mod\',\'s\'], id: \'save\' } (the p-shortcut="mod+s:save" semantics)',
      '· normalizeMod("mod", platform) → \'meta\' (darwin) / \'ctrl\' (elsewhere) — follows PRIM005 platform conventions automatically',
      "· matchShortcut(e, keys) → keyboard event match test (mod/s/alt/shift + key/code normalization)",
      "· shortcutLabel(binding, platform) → '⌘S' (Mac) / 'Ctrl+S' (Win/Linux) — acceptance: \"mod+s → Mac ⌘S / Win Ctrl+S\"",
      "Pure logic with zero DOM dependency (event-shape injection is unit-testable); MP artifact safe: no ?. / ??; no array destructuring",
    ],
    exports: {
      ShortcutMod: "Pure logic with zero DOM dependency (event-shape injection is unit-testable); MP artifact safe: no ?. / ??; no array destructuring",
      ShortcutKey: '—',
      ShortcutBinding: "Parse result: key sequence + semantic id",
      KeyEventLike: "Keyboard-event shape (an injectable subset of the Web KeyboardEvent — mockable in tests)",
      detectShortcutPlatform: "★#445 platform-detection primitive (pure, injectable function; callers never touch navigator — for the short label: Darwin/mac/iPhone/iPad → 'Mac', everything else 'web')",
      parseShortcutExpr: "Parses \"mod+s:save\" / \"mod+shift+a\" / \"escape\" → binding (case/whitespace tolerant; invalid segments skipped)",
      normalizeMod: "Platform mod normalization: darwin/mac → 'meta' (⌘); the rest → 'ctrl' (PRIM005 automatically follows platform conventions)",
      matchShortcut: '/',
      shortcutLabel: "Shortcut label (menu-bar display — the PRIM005 acceptance): mod+s → '⌘S' (darwin) / 'Ctrl+S'",
    },
    usage: [
      { code: '<button v-p-shortcut="{ expr: \'mod+k:open\', handler: () => toggle(true) }">', src: 'website/src/DocSearch.vue:133' },
      { code: "const kbd = shortcutLabel('mod+k', detectShortcutPlatform())", src: 'website/src/DocSearch.vue:47' },
    ],
  },
  'window-message': {
    title: "Cross-window message primitive (iframe postMessage funnel)",
    summary: "Cross-window message subscription: origin allowlist + type filtering + destroy (used by the spirit iframe bubble)",
    notes: [
      "★#449 G-24 B5 (proteus-semantic-primitives-plan continuation batch): cross-window message primitive — postMessage funnel (source validation + type filtering)",
      "Semantics: same-origin / designated-origin iframe message subscription (origin validation happens inside the framework package — pages keep zero raw window.addEventListener('message'))",
      "Consumption: App-shell spirit iframe (same-origin morph message → form bubble) — reclaiming the cross-window message gap",
      "Layering: pure logic + Web wiring — env-injected for unit testing, falls back to real globals by default (the network/lifecycle family convention)",
    ],
    exports: {
      WindowMessage: "Layering: pure logic + Web wiring — env-injected for unit testing, falls back to real globals by default (the network/lifecycle family convention)",
      WindowMessageEnv: '—',
      WindowMessageOptions: '—',
      WindowMessageHandle: '—',
      subscribeWindowMessage: "★subscribeWindowMessage: cross-window message subscription (origin allowlist validation + type filtering; destroy cleanup)",
    },
    usage: [{ code: "subscribeWindowMessage({ types: ['proteus-spirit-morph'], onMessage })", src: 'website/src/App.vue:30' }],
  },
  anchor: {
    title: "Anchor positioning primitive (scrollToId)",
    summary: "Smooth-scroll to an id anchor (jump within v-html docs on a new SPA page; delayable)",
    notes: [
      "★#449 G-24 B5 (proteus-semantic-primitives-plan continuation batch): anchor positioning primitive — scrollIntoView funnel (element lookup inside the framework package)",
      "Semantics: locate the element by id and smooth-scroll (optional delay — for v-html content newly rendered after an SPA route change)",
      "Consumption: DocSearch result anchor jumps (v-html doc heading) — reclaiming the element-query gap",
      "Layering: pure logic + Web wiring — env-injected for unit testing, falls back to the real document by default (the network/lifecycle family convention)",
    ],
    exports: {
      AnchorScrollEnv: "Layering: pure logic + Web wiring — env-injected for unit testing, falls back to the real document by default (the network/lifecycle family convention)",
      scrollToId: '/',
    },
    usage: [{ code: "scrollToId(hit.anchor, { behavior: 'smooth', delayMs: 60 }) // scroll only after the new page's v-html has rendered", src: 'website/src/DocSearch.vue:106' }],
  },
  'cursor-glow': {
    title: "p-cursor-glow",
    summary: "Pointer-following ambient glow (brand purple/cyan dual blooms, lerp-interpolated trail)",
    notes: [
      "★G-24 B5 (proteus-semantic-primitives-plan continuation batch): pointer-following glow — desktop interaction semantics \"ambient light follows the pointer\"",
      "Semantics: a glow layer (primary purple + secondary cyan dual blooms) follows the pointer with lerp interpolation — AI-tech pointer ambient feedback;",
      "Degradation chain: prefers-reduced-motion → not enabled; touch (pointer: coarse) → not enabled; MP logic layer has no DOM → not enabled",
      "Layering: pure logic (this module, unit-testable) + thin directive (directives.ts v-p-cursor-glow)",
    ],
    exports: {
      CursorGlowOptions: '—',
      CursorGlowHandle: '—',
      CURSOR_GLOW_DEFAULTS: "Defaults (Proteus design-tokens: brand purple / brand2 cyan)",
      prefersReducedMotion: '—',
      hasFinePointer: "Whether the environment has a fine pointer (mouse/pen — not enabled on touch; no matchMedia environment = non-Web, disabled)",
      createCursorGlow: '/',
    },
    usage: [{ code: '<p-page v-p-cursor-glow="cursorGlowOptions" …> // size/color/accent brand glow', src: 'website/src/App.vue:67' }],
  },
  // —— 桌面批次（#482 续）——
  breadcrumb: {
    title: 'p-breadcrumb',
    summary: 'Breadcrumb trail derived from the route stack (kebab → readable label)',
    notes: [
      '★G-24 B3 (proteus-semantic-primitives-plan 01 §7 Navigation p-breadcrumb): breadcrumb pure logic (derived from the route stack)',
      "· deriveBreadcrumb(segments): path segments → breadcrumb (the last segment is current — home merged / camelized display name, aligned with the deriveNameFromFile convention)",
      'Pure function (data comes from the route stack — the host passes the current route path in as segments)',
    ],
    exports: {
      Crumb: 'Pure function (data comes from the route stack — the host passes the current route path in as segments)',
      crumbLabel: "Segment → display name: kebab/underscores → space-joined capitalized words ('user-profile' → 'User Profile'; 'index' folds to empty — handled by the host)",
      deriveBreadcrumb: "★deriveBreadcrumb: route path segments (root 'index' excluded) → breadcrumb trail (last segment has current=true)",
    },
    usage: [{ code: "deriveBreadcrumb(['home', 'user', 'profile'])", src: 'examples/pages/semantic-primitives-demo.vue:468' }],
  },
  clipboard: {
    title: 'p-clipboard',
    summary: 'Clipboard read/write: Clipboard API → execCommand fallback → honest Err',
    notes: [
      '★G-24 B2 (proteus-semantic-primitives-plan 04-system-integration §1 p-clipboard): clipboard pure logic',
      "· clipboardSupported / copyText / pasteText: Web Clipboard API (secure context) → execCommand('copy') fallback → Err",
      'Mapping: iOS UIPasteboard / Android ClipboardManager / HarmonyOS pasteboard / Web Clipboard API (04 §1)',
      'env injection is unit-testable; env omitted → falls back to the real browser globals (typeof guards — the wrapping lives only inside framework packages, pages keep zero raw platform APIs)',
    ],
    exports: {
      ClipboardResult: 'env injection is unit-testable; env omitted → falls back to the real browser globals (typeof guards — the wrapping lives only inside framework packages, pages keep zero raw platform APIs)',
      copyText: "★copyText: writes the clipboard — Clipboard API (async Promise) → execCommand('copy') fallback (temporary textarea selection) → unsupported",
      pasteText: "★pasteText: reads the clipboard — Clipboard API readText (execCommand('paste') has no reliable fallback — honest Err)",
    },
    usage: [{ code: 'void copyText(url) // Clipboard API + fallback', src: 'website/src/components/TransformDemo.vue:179' }],
  },
  command: {
    title: 'p-command',
    summary: '⌘K command palette data layer: filtering/ordering + moving the index up/down',
    notes: [
      '★G-24 B3 (proteus-semantic-primitives-plan 01 §7 Navigation p-command): command palette ⌘K pure logic',
      '· filterCommands(items, query): fuzzy filter over title/keywords/group (stable order)',
      '· moveCommandIndex(list, active, dir): ↑↓ keyboard navigation (wraps around; empty list → -1) — the ⌘K palette data layer',
      'Pure functions with zero dependencies (palette UI / shortcut wiring is up to the host — global ⌘K listening follows the p-shortcut B1 pattern)',
    ],
    exports: {
      CommandItem: 'Pure functions with zero dependencies (palette UI / shortcut wiring is up to the host — global ⌘K listening follows the p-shortcut B1 pattern)',
      filterCommands: '★filterCommands: substring filter (title + keywords + group; case-insensitive; stable order)',
      moveCommandIndex: '★moveCommandIndex: keyboard ↑↓ movement (wraps around; empty/out-of-range resets to 0; no results → -1)',
    },
    usage: [
      { code: 'const list = filterCommands(cmdItems, cmdQuery.value)', src: 'examples/pages/semantic-primitives-demo.vue:456' },
      { code: 'cmdIdx.value = moveCommandIndex(cmdIdx.value, dir, list.items.length)', src: 'examples/pages/semantic-primitives-demo.vue:457' },
    ],
  },
  'context-menu': {
    title: 'p-context-menu',
    summary: 'Right-click / long-press menu: overflow-safe flip positioning + one-step construction',
    notes: [
      '★G-24 B1 (proteus-semantic-primitives-plan 03 §2 p-context-menu): right-click context-menu pure logic (touch long-press / mouse right-click normalized)',
      '· buildMenuPosition(point, size, viewport) → overflow-safe flip positioning (the menu never leaves the viewport)',
      '· parseMenuItems: validation + danger-item normalization',
      'Pure logic with zero DOM dependency; MP artifact safe: no ?. / ??; no array destructuring',
    ],
    exports: {
      MenuItem: 'Pure logic with zero DOM dependency; MP artifact safe: no ?. / ??; no array destructuring',
      MenuPoint: 'Trigger point (mouse right-click coordinates / touch long-press coordinates — normalized)',
      MenuSize: 'Menu popup size (used by overflow-safe positioning)',
      ViewportSize: 'Viewport size (used by overflow-safe positioning; 1024×768 default fallback)',
      buildMenuPosition: '/',
      menuPointFrom: 'Normalized long-press/right-click trigger point (coordinates injected — Web contextmenu e.clientX/Y / composed from touch long-press)',
      buildContextMenu: 'Menu construction (validation + danger-item normalization + positioning merged in one step — a thin wrapper for the directive layer)',
    },
    usage: [{ code: '<div v-p-context-menu="cardMenu" class="ctx-card">Right-click me</div>', src: 'examples/pages/semantic-primitives-demo.vue:214' }],
  },
  deeplink: {
    title: 'p-deeplink',
    summary: 'Deep-link parsing and parameterized matching (scheme://host/path?query + :param)',
    notes: [
      '★G-24 B2 (proteus-semantic-primitives-plan 04-system-integration §deep links): p-deeplink pure logic',
      '· parseDeepLink(url): normalizes scheme://host/path?query (relative /path also supported — deep links within routes)',
      "· matchDeepLink(pattern, url): parameterized match of 'proteus://user/:id' or '/user/:id' → { matched, params }",
      'Compiler side: iOS Associated Domains + apple-app-site-association / Android intent-filter / HarmonyOS Want declarations (04 §4)',
      'Runtime: natively supported by the router configuration (G-17 router.onDeepLink) — this module supplies the matching pure logic for routers/hosts to wire up',
      'Pure functions with zero dependencies; invalid URL → null (no throw)',
    ],
    exports: {
      DeepLink: 'Pure functions with zero dependencies; invalid URL → null (no throw)',
      parseDeepLink: "Parses a deep-link URL: 'proteus://user/profile?id=1' → { scheme, host, path, query }; relative '/user/1' → empty scheme/host",
      matchDeepLink: "Parameterized matching: ':name' segments are captured; literal segments must be equal; absent scheme/host are unconstrained (relative-path patterns wildcard any scheme)",
    },
    usage: [
      { code: "const dl = parseDeepLink('proteus://order/42?tab=detail')", src: 'examples/pages/semantic-primitives-demo.vue:385' },
      { code: "matchDeepLink('proteus://order/:id', 'proteus://order/42')", src: 'examples/pages/semantic-primitives-demo.vue:386' },
    ],
  },
  directives: {
    title: 'Desktop directives factory (createDesktopDirectives: v-p-* registration)',
    summary: 'Registers the B1-B4 primitives as v-p-* Vue directives (a thin Web-wiring layer; not registered on MP → degrades naturally)',
    notes: [
      '★G-24 B1 (proteus-semantic-primitives-plan 03): Vue directive factory — v-p-hover / v-p-context-menu / v-p-shortcut / v-p-focus-trap',
      'Official Web wiring (same pattern as the gesture package createGestureDirective); MP does not register the directives (desktop interactions have no counterpart — degrades naturally)',
      'Thin directive layer (DOM bindings) + pure logic lives in the shortcut/focus-trap/context-menu/hover modules (unit-testable)',
    ],
    exports: {
      createHoverDirective: '/',
      createShortcutDirective: '/',
      createFocusTrapDirective: '/',
      createContextMenuDirective: '/',
      createPermissionDirective: '/',
      createCursorGlowDirective: '/',
      createDesktopDirectives: '★G-24 B1+B2+B5: the directive-factory set (main.ts registers v-p-* via Object.entries)',
    },
    usage: [{ code: '<button v-p-shortcut="{ expr: \'mod+k:open\', handler: () => toggle(true) }">⌘K Search</button>', src: 'website/src/DocSearch.vue:133 (createDesktopDirectives registered in website/src/main.ts)' }],
  },
  'focus-trap': {
    title: 'p-focus-trap',
    summary: 'Overlay focus containment: Tab cycles / Shift+Tab reverses / focus restored on close',
    notes: [
      '★G-24 B1 (proteus-semantic-primitives-plan 03 §4 p-focus-trap): focus-trap pure logic (an accessibility necessity)',
      '· Tab cycles within the container, Shift+Tab reverses (acceptance checklist)',
      '· Focuses the first element when opened, restores the previously focused element when closed (modal accessibility standard)',
      "· Injectable: queryFocusable (Web can use el.querySelectorAll('a[href],button,...'); tests can inject a fake list)",
      'Pure logic with zero DOM dependency (element-shape injection); MP artifact safe: no ?. / ??; no array destructuring',
    ],
    exports: {
      FocusableElement: 'Pure logic with zero DOM dependency (element-shape injection); MP artifact safe: no ?. / ??; no array destructuring',
      createFocusTrap: '/',
    },
    usage: [
      { code: 'trap = modalEl.value ? createFocusTrap(modalEl.value) : null', src: 'website/src/DocSearch.vue:64' },
      { code: '<div v-p-focus-trap class="trap-dialog">…</div>', src: 'examples/pages/semantic-primitives-demo.vue:218' },
    ],
  },
  lifecycle: {
    title: 'p-lifecycle',
    summary: 'Foreground/background lifecycle observation (visibilitychange/focus — app phase)',
    notes: [
      '★G-24 B4 (proteus-semantic-primitives-plan 01 §8 Lifecycle p-lifecycle): app foreground/background/activation pure logic',
      'Mapping: UIApplicationDelegate didEnterBackground/willEnterForeground / onPause·onResume (01 §8)',
      "· phaseOf: normalizes the hidden check to 'foreground' | 'background'",
      '· createLifecycleTracker: Web visibilitychange/focus subscription → onPhase (env-injectable & unit-testable; default env talks directly to document — typeof guard)',
    ],
    exports: {
      AppPhase: '· createLifecycleTracker: Web visibilitychange/focus subscription → onPhase (env-injectable & unit-testable; default env talks directly to document — typeof guard)',
      phaseOf: 'Normalizes the foreground/background phase (hidden → background; otherwise foreground)',
      createLifecycleTracker: '★createLifecycleTracker: subscribes to visibilitychange/focus → onPhase (instant notification when the page switches to background or returns to foreground)',
    },
    usage: [{ code: 'lifeTracker = createLifecycleTracker({ onChange: (phase) => … })', src: 'examples/pages/semantic-primitives-demo.vue:477' }],
  },
  'low-power': {
    title: 'p-low-power',
    summary: 'Low battery / power-saving-mode detection (Battery API)',
    notes: [
      '★G-24 B4 (proteus-semantic-primitives-plan 01 §8 p-low-power): low-power / low-data pure logic (NSProcessInfo lowPowerMode / BatteryManager semantics)',
      '· detectLowPower: Battery API normalization (supported/charging/level/lowPower — level ≤20% and not charging)',
      '· createLowPowerTracker: subscribes to chargingchange/levelchange → onChange (env-injected for unit testing)',
      'Honest degradation: no Battery API (most desktop browsers do not support navigator.getBattery) → supported:false (the host bridges native battery status later)',
    ],
    exports: {
      PowerInfo: 'Honest degradation: no Battery API (most desktop browsers do not support navigator.getBattery) → supported:false (the host bridges native battery status later)',
      detectLowPower: '★detectLowPower: instant battery status (getBattery injected; no API → honest supported:false degradation)',
      createLowPowerTracker: '★createLowPowerTracker: subscribes to chargingchange/levelchange → onChange(PowerInfo)',
    },
    usage: [{ code: 'const p = await detectLowPower()', src: 'examples/pages/semantic-primitives-demo.vue:490' }],
  },
  'master-detail': {
    title: 'p-master-detail',
    summary: 'Large-screen three-column / narrow-screen exclusive layout state machine (UISplitViewController semantics)',
    notes: [
      '★G-24 B3 (proteus-semantic-primitives-plan 01 §7 Navigation + 06 B3): p-master-detail pure logic',
      'Three columns (master + detail + inspector) → the UISplitViewController primary/supplementary/secondary three-column mode',
      '(HarmonyOS SideBarContainer / Android SlidingPaneLayout likewise — the same semantics, implemented per target)',
      '· computeSplitLayout: viewport width → column layout (narrow: master/detail exclusive switching (iOS collapse); medium: two columns; wide: three columns incl. inspector)',
      'Pure function, zero dependencies; consumed jointly by the Web demo and native hosts',
    ],
    exports: {
      SplitColumn: 'Pure function, zero dependencies; consumed jointly by the Web demo and native hosts',
      computeSplitLayout: '★computeSplitLayout: width → column layout (maps UISplitViewController collapse / two-column / three-column)',
      SplitNavAction: '★Narrow-screen navigation-action normalization (iOS collapse semantics: select enters detail / back returns to master)',
      applySplitNav: '★applySplitNav: the pure state machine for narrow-screen list/detail switching and inspector toggling (demo/host wiring)',
    },
    usage: [
      { code: 'computeSplitLayout({ width: viewW.value, detailOpen, inspector: inspectorOn })', src: 'examples/pages/semantic-primitives-demo.vue:406' },
      { code: "applySplitNav({ type: 'select' }, { layout: l, inspectorOn })", src: 'examples/pages/semantic-primitives-demo.vue:411' },
    ],
  },
  network: {
    title: 'p-network-status',
    summary: 'Network status: online + connection-type normalization + change subscription',
    notes: [
      '★G-24 B4 (proteus-semantic-primitives-plan 01 §8 p-network-status): network-status pure logic (NWPathMonitor / ConnectivityManager semantics)',
      '· detectNetwork: online + connection-type normalization (navigator.onLine + connection.type/effectiveType injected — typeof guards)',
      '· createNetworkTracker: subscribes to online/offline/connection changes → onChange (env-injected for unit testing)',
      'Complements @proteus-vue/api useNetwork (CapResult dual bridge): this module is the injectable tracker for desktop/all targets (no vue dependency)',
    ],
    exports: {
      NetworkKind: 'Complements @proteus-vue/api useNetwork (CapResult dual bridge): this module is the injectable tracker for desktop/all targets (no vue dependency)',
      detectNetwork: '★detectNetwork: instant network status (online + kind + effectiveType)',
      createNetworkTracker: '★createNetworkTracker: subscribes to online/offline/connection changes → onChange(NetworkInfo)',
    },
    usage: [{ code: 'const i = detectNetwork() // { online, kind, effectiveType }', src: 'examples/pages/semantic-primitives-demo.vue:485' }],
  },
  notify: {
    title: 'p-notify',
    summary: 'System notifications: detection / permission / send normalization (Notification API; honest Err when there is no implementation)',
    notes: [
      '★G-24 B2 (proteus-semantic-primitives-plan 04-system-integration §1 p-notify): notification pure logic',
      '· notifySupported / getNotifyPermission / requestNotifyPermission: Notification API detection and authorization normalization',
      '· sendNotification(payload, env): sending (env.Notification injected — real web Notification construction / test mock)',
      'Mapping: iOS UNUserNotificationCenter / Android NotificationManager / HarmonyOS notificationManager / Web Notification API (04 §1)',
      'Pure logic with zero direct DOM calls (env injection — safe under the no-platform-api audit); missing Notification → Err (the G-32.3 non-throwing synchronous-error style — returns a result object)',
    ],
    exports: {
      NotifyPayload: 'Pure logic with zero direct DOM calls (env injection — safe under the no-platform-api audit); missing Notification → Err (the G-32.3 non-throwing synchronous-error style — returns a result object)',
      NotificationLike: 'Web Notification shape (injection surface — the real implementation is the global Notification)',
      notifySupported: 'Notification API availability (includes the permission field — no browser support → undefined)',
      getNotifyPermission: 'Current notification permission (granted/denied/default/unsupported — PermissionState-semantics compatible)',
      requestNotifyPermission: '★requestNotifyPermission: requests notification authorization (Notification.requestPermission normalization)',
      sendNotification: '★sendNotification: sends a system notification (new Notification — real web presentation)',
    },
    usage: [{ code: "const r = sendNotification({ title: 'Proteus Demo', body: 'System notification' })", src: 'examples/pages/semantic-primitives-demo.vue:371' }],
  },
  'page-url': {
    title: 'Page URL read/write primitive (location / history chokepoint)',
    summary: 'Page address read/write: origin / pathname + replaceState chokepoint (in use for share-link syncing)',
    notes: [
      '★#449 G-24 B5 (proteus-semantic-primitives-plan continuation batch, p-deeplink address-bar companion): page URL read/write primitive',
      'Semantics: read the current origin/pathname + same-URL replacement (history.replaceState—no history entry is created)',
      'Consumers: TransformDemo share link (builds the Playground URL from the current address + syncs the address bar as you edit)—recovers the location/history gap',
      'Layering: pure logic + Web wiring (env injection is unit-testable; absent env falls back to the real location/history—same convention as the network/lifecycle family)',
    ],
    exports: {
      PageUrlEnv: 'Layering: pure logic + Web wiring (env injection is unit-testable; absent env falls back to the real location/history—same convention as the network/lifecycle family)',
      currentPageOrigin: "Current page origin (no location environment → '')",
      currentPagePathname: "Current page pathname (no location environment → '')",
      replacePageUrl: '★replacePageUrl: same-URL address-bar replacement (history.replaceState—no history entry is created; silent when there is no history environment)',
    },
    usage: [{ code: 'replacePageUrl(playgroundUrl(currentPageOrigin(), currentPagePathname(), src))', src: 'website/src/components/TransformDemo.vue:112' }],
  },
  permission: {
    title: 'p-permission',
    summary: 'Permission gate: six-semantic catalog + check/request normalization + v-p-permission intercept-and-replay',
    notes: [
      '★G-24 B2 (proteus-semantic-primitives-plan 04-system-integration §2 Permission gating): p-permission pure logic',
      '· PERMISSION_CATALOG: semantics → web permission channel (principle #10.8—only listed when there is a clear native system counterpart)',
      '· buildPermissionManifest(semantics): compiler-phase permission manifest (04 §2 "p-permission compile-time validation checklist," auto-generating each platform permission declarations"—a pure function hooking G-21: iOS Info.plist / Android Manifest / HarmonyOS module.json5 are each derived from the manifest)',
      '· checkPermission / requestPermission: unified granted/denied/prompt/unsupported normalization (query and request are injectable and unit-testable)',
      'Pure logic with zero direct DOM calls; default wiring goes through injection (document/navigator are passed as env by the caller/directive factory—audited no-platform-api safety)',
    ],
    exports: {
      PermissionState: 'Pure logic with zero direct DOM calls; default wiring goes through injection (document/navigator are passed as env by the caller/directive factory—audited no-platform-api safety)',
      PERMISSION_CATALOG: '★semantics → web permission channel (04 §1 p-permission rows + additions: entries backed by a web standard are listed)',
      PermissionEnv: 'Permission-query injection surface (default wiring lives on the directive factory/host side—zero direct calls inside the package)',
      permissionEntry: 'Semantics → catalog entry (unknown semantics → undefined—honest, never invents permissions)',
      buildPermissionManifest: '★Compiler-phase permission manifest (04 §2, hooks G-21): the p-permission semantic set found in the input templates → manifest (deduped + unknown filtered + stable order)',
      defaultPermissionQuery: 'Default query normalization: navigator.permissions.query (only for entries declaring webQueryName in the catalog); missing API / thrown error → null (caller falls back to prompt)',
      checkPermission: '★checkPermission: unified state query (unknown to the catalog → unsupported; no web query channel → prompt—honest "may be available; a request is needed to confirm")',
      requestPermission: '★requestPermission: authorization request (env.request injects the real implementation—notification dialogs / camera streams; missing implementation → unsupported, honest degradation)',
      defaultPermissionRequest: 'Common web default request implementation (for host/directive factory wiring): notification → Notification.requestPermission; the rest have no unified channel → false',
    },
    usage: [
      { code: "const manifest = buildPermissionManifest(['notification', 'camera'])", src: 'examples/pages/semantic-primitives-demo.vue:360' },
      { code: '<p-button v-p-permission="{ semantic: \'notification\' }" @click="onSendNotify">Send notification</p-button>', src: 'examples/pages/semantic-primitives-demo.vue:232' },
    ],
  },
  'state-restoration': {
    title: 'p-state-restoration',
    summary: 'UI state-restoration tokens: capture / restore / restorable-state filtering (state-survival scenarios)',
    notes: [
      '★G-24 B4 (proteus-semantic-primitives-plan 01 §8 p-state-restoration): state-restoration pure logic (iOS UIStateRestoration / SavedStateHandle semantics)',
      'An iOS core capability that almost no competitor maps—semanticized by Proteus (01 §8)',
      '· buildRestoreToken(state): stable JSON serialization (the restoration token)',
      '· captureState / restoreState: injected storage read/write (defaults to localStorage—typeof guard; tests inject a Map)',
      '· filterRestorable(state, allowKeys): allowlist filtering (sensitive fields never enter restored state—secure by default)',
      'Pure functions + injectable storage, unit-testable; on the web it rides on localStorage/sessionStorage (desktop / refresh restoration)',
    ],
    exports: {
      RestoreStorage: 'Pure functions + injectable storage, unit-testable; on the web it rides on localStorage/sessionStorage (desktop / refresh restoration)',
      restoreKey: "Namespaced storage key: 'proteus-restore:page:scroll'",
      buildRestoreToken: '★buildRestoreToken: state → restoration token (JSON serialization; undefined/function fields are dropped—a serializable allowlist)',
      filterRestorable: '★filterRestorable: allowlist filtering (sensitive fields never enter restored state—secure by default)',
      captureState: '★captureState: serialize + persist (no storage available (non-browser) → returns only the token—honest degradation)',
      restoreState: '★restoreState: read + deserialize (no record / parse failure → null—never throws)',
      clearRestoreState: 'Clears the restored state',
    },
    usage: [
      { code: "const token = captureState('demo', 'view', { path, ts: Date.now() })", src: 'examples/pages/semantic-primitives-demo.vue:495' },
      { code: "restoreState('demo', 'view')", src: 'examples/pages/semantic-primitives-demo.vue:499' },
    ],
  },
  tabs: {
    title: 'p-tabs',
    summary: 'Desktop tab-close migration: activation prefers the right neighbor; last tab falls back to its left neighbor',
    notes: [
      '★G-24 B3 (proteus-semantic-primitives-plan 01 §7 Navigation p-tabs): desktop tabs pure logic',
      '· resolveTabAfterClose: the id to activate after a tab is closed (closing an inactive tab → unchanged; closing the active tab → right neighbor first, trailing tab falls back to the left neighbor)',
      '· normalizeTabs: active-id validity normalization (missing/empty active id → first tab; empty list → none)',
      'Mapping: UISegmentedControl / TabLayout (01 §7); distinct from mobile p-tabbar (S3 bottom bar)—desktop window tabs (closable; multi-row/scroll handled by the host)',
    ],
    exports: {
      DesktopTab: 'Mapping: UISegmentedControl / TabLayout (01 §7); distinct from mobile p-tabbar (S3 bottom bar)—desktop window tabs (closable; multi-row/scroll handled by the host)',
      resolveTabAfterClose: '★resolveTabAfterClose: active-tab transition after a tab is closed',
      normalizeTabs: '★normalizeTabs: active-id validity normalization (missing/unknown active id → first tab; empty → null)',
    },
    usage: [{ code: 'resolveTabAfterClose(demoTabs.value, demoActive.value, id)', src: 'examples/pages/semantic-primitives-demo.vue:436' }],
  },
  // —— 手势（#482 续）——
  recognizers: {
    title: 'Gesture recognizers (tap/pan/swipe/pinch/rotate)',
    summary: 'Gesture recognizers: Web Pointer / MP touch normalized into GestureInput → semantic events such as tap/pan/swipe/pinch/rotate',
    notes: [
      '★G-32 B4 ④ Gesture (proteus-semantic-primitives-plus-plan §6): gesture recognizers (pure logic, zero dependencies — unit-testable)',
      'Gesture = a declarative constraint (the G-32 principle): platform events (Web Pointer / MP touch) are normalized into GestureInput and fed in,',
      'the recognizer emits semantic gesture events (tap/longpress/pan/swipe/pinch/rotate/press) — "events are a Backend implementation detail"',
      'Backend mapping: Web uses Pointer Events; iOS UIGestureRecognizer / Android GestureDetector / HarmonyOS gesture system comes in later batches',
    ],
    exports: {
      GesturePoint: 'Backend mapping: Web uses Pointer Events; iOS UIGestureRecognizer / Android GestureDetector / HarmonyOS gesture system comes in later batches',
      GestureEvent: 'Semantic gesture event (output — the declarative-constraint result developers consume)',
      createGestureRecognizer: '/',
    },
  },
  'use-gesture': {
    title: 'useGesture Hook + v-gesture directive (Web wiring)',
    summary: 'Official Web wiring for gestures: useGesture Hook + v-gesture:<kind>="onX" directive',
    notes: [
      '★G-32 B4 ④ Gesture: useGesture() Hook (G10) + v-gesture directive (G1-G7, Web)',
      'Platform wiring layer: Web Pointer Events → GestureInput → recognizer → semantic gesture events',
      '"events are a Backend implementation detail": developers write v-gesture:tap="onTap" (declarative constraint) and never touch touchstart/bindtap',
      '★MP/native: recognizer mapping is carried by each target\'s Backend (iOS UIGestureRecognizer, etc.) — this file is the official Web wiring',
    ],
    exports: {
      useGesture: '/',
      VGestureBindingValue: '★G1-G7 v-gesture directive factory: v-gesture:tap="onTap" / v-gesture:pan="onPan" (value = a callback or { handler, config })',
      createGestureDirective: "Directive factory (call app.directive('gesture', createGestureDirective()) before createApp)",
    },
    usage: [{ code: '<div v-gesture:tap="onTapG" class="gesture-demo">{{ tapMsg }}</div>', src: 'examples/pages/semantic-primitives-demo.vue:197' }],
  },
  // —— 工程原语（#482 续）——
  engineering: {
    title: 'Basic engineering primitives (E1-E9: useState/useComputed/useWatch/useLifecycle…)',
    summary: 'Engineering primitives E1-E9: injectable useState/useComputed/useWatch/useLifecycle… (the api package has zero vue dependency)',
    notes: [
      '★G-32 B5 (proteus-semantic-primitives-plus-plan §8): engineering primitives 28 — injectable design (the api package has zero runtime dependency on vue)',
      'E1 useState / E2 useComputed / E3 useWatch / E6 useLifecycle / E7 useReady / E9 usePageParam',
      'Injectable: the consumer injects reactivity (the vue reactive factory or a custom one) — the same zero-dependency precedent as createReactiveStorage (decision #310)',
      'MP artifact-safe (decisions #32/#36): no ?. / ??; no array destructuring',
      '* Injectable reactivity (vue composables semantics surface — the substrate for useState/useComputed/useWatch) */',
    ],
    exports: {
      Reactivity: 'Injectable reactivity (vue composables semantics surface — the substrate for useState/useComputed/useWatch)',
      LifecycleEvent: 'Lifecycle events (page/component)',
      LifecycleHandle: 'Lifecycle hook handle (useLifecycle / useReady)',
      ParamSource: 'Page parameter source (wx onLoad(options) / web URL query — injected on the consumer side)',
      EngineeringOptions: 'Engineering primitives injection options',
      Engineering: 'G-32 §8 ⑥ engineering primitives semantics surface (first phase: E1-E9)',
      createEngineering: '/',
    },
    usage: [{ code: 'const eng = createEngineering({ reactivity: { ref, computed, watch } })\nconst engCount = eng.useState(0)', src: 'examples/pages/platform-api-demo.vue:483·487' }],
  },
  'router-engineering': {
    title: 'Routing engineering primitives (E10-E18: useRoute/push/back/guards…)',
    summary: 'Routing engineering primitives E10-E18: useRoute/push/back/guards + p-router-link',
    notes: [
      '★G-32 B5 continued (proteus-semantic-primitives-plus-plan §8 ⑥): E10-E17 route semantics — interfacing with the existing `@proteus-vue/router`',
      'Design: engineering primitives = a semantics surface (not bound to an implementation) — the consumer injects a compatible router instance (a createRouter product of @proteus-vue/router or a mock)',
      'E10 useRoute (reactive current route) / E11 push / E12 replace / E13 back / E14 switchTab / E15 reLaunch / E16 beforeEach / E17 afterEach',
      'MP artifact-safe (decisions #32/#36): no ?. / ??; no array destructuring',
    ],
    exports: {
      RouterTargetOptions: 'Route target (E11-E15 navigation input)',
      RouterLike: 'Router-compatible interface (a structural subset of the @proteus-vue/router RouterInstance; mock injection is unit-testable)',
      CurrentRoute: 'Current-route read result (E10 useRoute)',
      RouterEngineeringOptions: 'createRouterEngineering injection options',
      RouterEngineering: 'G-32 §8 ⑥ route semantics (E10-E17)',
      createRouterEngineering: '/',
    },
    usage: [{ code: 'const rx = createRouterEngineering({ routerLike: { … } })', src: 'examples/pages/platform-api-demo.vue:505' }],
  },
  'animation-engineering': {
    title: 'Animation engineering primitives (E19-E23: useAnimation/useScrollAnimation…)',
    summary: 'Animation engineering primitives E19-E23: useAnimation/useGestureAnimation/useScrollAnimation + p-animate',
    notes: [
      '★G-32 B5 continued (2) (proteus-semantic-primitives-plus-plan §8 ③): E21-E23 animation semantics surface — injectable design',
      'E21 useAnimation (wx.createAnimation semantics — declarative keyframe builder) / E22 useGestureAnimation (gesture-driven: incremental accumulation)',
      'E23 useScrollAnimation (scroll-driven: progress → interpolated keyframes); component forms E19 p-transition / E20 p-animate live in src/components',
      'Injectable: reactivity (vue or mock) + driver (Web WAAPI / MP host wx.createAnimation / test recording) — the same',
      'zero-runtime-vue-dependency family as createEngineering / createRouterEngineering; without a driver, play degrades to a safe no-op',
      'MP artifact-safe (decisions #32/#36): no ?. / ??; no array destructuring',
    ],
    exports: {
      AnimationProps: 'Animation property subset (transform/opacity semantics — the wx.createAnimation compat surface)',
      AnimationKeyframe: 'A single keyframe step (offset 0..1)',
      AnimationDescriptor: 'Exported animation descriptor (the consumer hands it to the host driver: Web WAAPI / MP wx.createAnimation / test assertions)',
      AnimationStepOptions: 'Step-level options (wx.step semantics: each keyframe may override duration/easing/delay)',
      AnimationRun: 'Run handle (E21 play — driver-injected; no-op-safe when the driver is absent)',
      AnimationDriver: 'Animation driver (injected — Web: Element.animate; MP: host bridge wx.createAnimation; tests: call recording)',
      AnimationState: 'Playback state (reactivity-injected → reactive)',
      AnimationController: 'E21 useAnimation controller (declarative builder — wx.createAnimation semantics)',
      GestureAnimationHandle: 'E22 useGestureAnimation handle (gesture-driven: incremental accumulation → committed frames)',
      ScrollAnimationRange: 'E23 useScrollAnimation handle (scroll-driven: progress → interpolated keyframes)',
      AnimationEngineeringOptions: 'createAnimationEngineering injection options',
      AnimationEngineering: 'G-32 §8 ③ animation semantics (E21-E23 in Hook form; E19/E20 component forms in src/components)',
      interpolateAnimationProps: '/',
      createAnimationEngineering: '/',
    },
    usage: [{ code: 'const anim = createAnimationEngineering({ … })\nconst gestureAnim = anim.useGestureAnimation()', src: 'examples/pages/platform-api-demo.vue:540·547' }],
  },
  'tooling-engineering': {
    title: 'Tooling engineering primitives (E24-E28: useDevTools/defineComponent…)',
    summary: 'Tooling engineering primitives E24-E28: useDevTools/useInspector/defineComponent/defineCapability',
    notes: [
      '★G-32 B5 continued (3) (proteus-semantic-primitives-plus-plan §8 ④): E24-E28 tooling semantics surface — injectable design',
      'E24 useDevTools (dev-tooling integration — dev event surface) / E25 useInspector (element inspection — component-tree snapshot)',
      'E26 usePerformance (performance instrumentation — wx.reportPerformance semantics) / E27 defineComponent (typed component definition carrying C-IR metadata)',
      'E28 defineCapability (capability degradation declaration — G-30 degradation-chain resolution)',
      'Injectable: reactivity (vue or mock) + each handle may inject a time source / reporter / probe functions — the createEngineering family with zero runtime dependency on vue',
      'MP artifact-safe (decisions #32/#36): no ?. / ??; no array destructuring',
    ],
    exports: {
      DevToolsEvent: 'dev event (consumed by the devtools panel; not emitted while enabled is off)',
      DevToolsHandle: 'E24 useDevTools handle',
      InspectorNode: 'Component inspection node (snapshot output)',
      InspectorHandle: 'E25 useInspector handle',
      PerformanceMetricRecord: 'Reported metric record (reactive queue — consumed by the instrumentation panel)',
      PerformanceHandle: 'E26 usePerformance handle',
      ComponentPropDef: 'Component prop description (typed declaration — statically extracted by the compiler + aligned with MP properties)',
      ComponentMeta: 'Component definition (C-IR metadata: semantic — consumed by toComponentIR / the rendering backends)',
      validateComponentMeta: 'Pure function: validates a component definition (returns the error list; empty = valid) — E27 declaration-time validation, consumed by the compiler / at dev time',
      defineComponent: '/',
      CapabilityContract: 'Capability contract (G-30 degradation-chain declaration)',
      CapabilityAvailability: 'Implementation-availability table (resolveCapabilityChain input)',
      CapabilityDefinition: 'E28 defineCapability handle',
      validateCapabilityContract: 'Pure function: validates a capability contract (returns the error list; empty = valid)',
      resolveCapabilityChain: '/',
      defineCapability: '/',
      ToolingEngineeringOptions: 'createToolingEngineering injection options',
      ToolingEngineering: 'G-32 §8 ④ tooling semantics (E24-E28)',
      createToolingEngineering: '/',
    },
    usage: [{ code: 'const tool = createToolingEngineering({ reactivity: { ref, computed, watch } })', src: 'examples/pages/platform-api-demo.vue:631' }],
  },
  'request-engineering': {
    title: 'Request engineering (R1-R4: request/useQuery/enqueue/runOnce)',
    summary: 'Request engineering R1-R4: request strategy / useQuery / enqueue / runOnce',
    notes: [
      '★G-32 B6 precursor + G-31 B7 closure: request data-layer semantics surface (api layer — not in the catalog, per the same E21-E28 discipline that "API-layer semantics produce no C-IR nodes")',
      'Same family as the route semantics: "bare requests" are converged into "request semantics with strategies" — business code never hand-writes cache/dedupe/queueing logic',
      'R1 request (strategized request: cache + dedupe + optional queueing) · R2 useQuery (SWR reactive data fetching)',
      'R3 enqueue (concurrency queue) · R4 dedupe (concurrent coalescing — shared internally by R1/R2)',
      'Injectable: client (a createApi product / adapter / ★capability bridge adapter — the request(config) execution surface) + reactivity (vue or mock)',
      '+ cache? (CompatStorage substrate — in-memory/persistent injection) + concurrency? (queue concurrency cap)',
      '★G-31 B7 closure: createCapabilityRequestClient — the underlying capability bridge of useFetch (wx.request/fetch on both targets)',
      'upgraded into a strategized request executor (useFetch semantics = the bare version of req.request; enhancements go through cache/dedupe/queue)',
      'Zero runtime dependency on vue; MP artifact-safe (decisions #32/#36): no ?. / ??; no array destructuring',
    ],
    exports: {
      RequestExecutor: 'Request execution surface (both createApi products and IRequestAdapter satisfy it — structurally compatible)',
      RequestCacheEntry: 'Cache entry (TTL semantics: staleAt = createdAt + ttl; 0 = never expires)',
      QueryState: 'Query state (reactive — the state of a useQuery handle)',
      QueryHandle: 'R2 useQuery handle',
      QueryOptions: 'useQuery options',
      RequestStrategyOptions: 'request strategy options',
      RequestEngineeringOptions: 'createRequestEngineering injection options',
      RequestEngineering: 'Request data-layer semantics surface (R1-R4)',
      defaultCacheKey: 'Default cache key: method + url + serialized params (GET semantics — the same url with different query strings counts as different resources)',
      createRequestEngineering: '/',
      createCapabilityRequestClient: '/',
    },
    usage: [{ code: 'const req = createRequestEngineering({ … })', src: 'examples/pages/platform-api-demo.vue:736' }],
  },
  'ownership-engineering': {
    title: 'Ownership engineering primitives (PSS: useOwned/useBorrow…)',
    summary: 'Ownership engineering primitives (PSS): useOwned / useBorrow / automatic drop',
    notes: [
      '★G-43 B5 (proteus-ownership-plan batches B5): Vue reactivity integration — useOwned / useBorrow (borrow-checker.md §6)',
      '· CMP071: Owned<T> forbids ref/reactive wrapping by default (a Proxy breaks ownership semantics) — useOwned offers reactive **metadata** as the substitute',
      '· useOwned(owned): { state: Ref<OwnedState>, byteSize: Ref<number>, borrow() } — never exposes the actual resource reference',
      '· useBorrow(owned): Ref<BorrowLike | undefined> — automatically becomes undefined when the owner releases it (driven by Owned.subscribe)',
      'Injectable design family (the api package has zero runtime dependency on vue and zero hard dependency on render-backend — structural types OwnedLike/BorrowLike,',
      'aligned with the createEngineering/RouterLike precedent: the consumer injects reactivity and instances can be passed directly)',
      'MP artifact-safe (decisions #32/#36): no ?. / ??; no array destructuring',
      '* Reactive substrate (structurally reuses the @proteus-vue/api Reactivity — depends on ref only) */',
    ],
    exports: {
      OwnershipReactivity: 'Reactive substrate (structurally reuses the @proteus-vue/api Reactivity — depends on ref only)',
      OwnedStateLike: 'Owned state (isomorphic with render-backend OwnedState)',
      OwnedLike: 'Owned structural type (render-backend Owned instances can be passed directly — zero hard dependency)',
      BorrowLike: 'Borrow structural type (render-backend Borrow instances can be passed directly)',
      OwnedView: 'Reactive ownership view (read-only metadata — the borrow-checker.md §6.3 interface)',
      createOwnershipEngineering: '★G-43 B5: creates the ownership reactive Hooks (useOwned/useBorrow — injectable, zero vue dependency)',
    },
  },
}
